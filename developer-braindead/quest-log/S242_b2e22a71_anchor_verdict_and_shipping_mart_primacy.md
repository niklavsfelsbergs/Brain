# S242 — 2026-06-15 — Anchor: continuation verdict + shipping-mart source-#1 primacy

Dev-brain via "lets develop gielinor" (sid b2e22a71). Two related cue-forcing builds, both landing the *output/routing* half of knowledge the brain already held. No live Braindead siblings (comms CLOSED through [[S237_a7ea5300_scope-block-deletes-to-brain-paths|S237]]). SNNN: asked-S241 was taken by a live Jebrim session → S242.

- **Task 1 — the `Anchor:` continuation verdict.** Niklavs: force a first-turn move where the player states whether the opening prompt *continues prior work* (name the referent) or is a *new topic*. Mapped it onto what already stood — the internal Recall-shaped lens, the `Reading:` line, the anchor-to-continuation keepsake reflex — and found the gap: the verdict was a silent thinking-check, never stated. Built the **visible verdict**: new *The `Anchor:` line* section in [[communication-protocol]] (always-on; session-open + genuinely-new-topics, `continues <X>` vs `new topic` after a check, trivial-turn omission) + an `ANCHOR_DIRECTIVE` riding **both** arms of `keepsake-forced-read.py` (SessionStart guaranteed + first-player-prompt salient) + a one-line reinforcement in `grounding-cue-reminder.py`'s continuation nudge. Honest seam carried in the prose: a hook injects the demand, it can't force the model to emit the line — the always-on rule is the rule, the hooks are the reliable reminder.
- **Task 2 — shipping-mart is SOURCE #1.** Niklavs: Jebrim sometimes routes shipping-data questions to non-mart sources; the mart-first rule + knowledge only surface when he opens the shipping-agent. **Diagnosis (corrected a wrong first guess):** the `cue_registry.py` shipping **patterns already fire broadly** (shipping/shipment/carrier/parcel/freight/carrier-names) — the trigger wasn't the gap. The gap was the **directive content**: everywhere (`cue_registry.py`, [[shipping-mart]] digest, jebrim/CLAUDE.md) it said "don't reason about the mart from memory / load the reference", which *presupposes he already chose the mart*. None stated **mart-primacy**. Niklavs picked **strong-default + justify-off-mart**. Threaded primacy through the three homes: mart = #1 source for any shipping-data question, start there; off-mart (NFE ad-hoc, raw invoice tables, silver/bronze, CSV exports, direct Redshift) needs an explicit stated reason the mart can't serve (legit cases documented: `fact_truck_charges` linehaul, silver/bronze raw vocab). Jebrim CLAUDE.md reframed so primacy is precondition #1, "don't-reason-from-memory" demoted to #2.
- **Verification.** Full cue/forced-read suite green after the changes: `test_keepsake_forced_read` 27/27 (+2 Anchor asserts both arms), `test_grounding_cue` 17/17 (+1), `test_domain_cue_registry` 40/40 (+1 mart-primacy assert), `test_domain_digest_discovery` 23/23 (repointed case-8's pinned-wording assert to the new `SOURCE #1` signal), `test_domain_cue_inline` 26/26, `test_lorebook_cue` 22/22 all unregressed. `py_compile` clean on all three touched hooks.
- **Build-lesson harvested:** corrected my own diagnosis mid-task (assumed narrow cue patterns; they were already broad — the gap was directive *framing*, not the trigger). Reinforces the verify-the-thing reflex on one's own hypotheses. Captured in `bank/build-lessons.md`.

**Cascade.** `developer-braindead/`: NEW `quest-log/S242_anchor_verdict_and_shipping_mart_primacy.md`; test edits in `verification/{test_keepsake_forced_read,test_grounding_cue,test_domain_cue_registry,test_domain_digest_discovery}.py`; `bank/build-lessons.md` (+1); `respawn.md`; `comms/active.md` (OPEN+UPDATE+CLOSING).

**Main-brain changes.** Into `gielinor/`: `meta/communication-protocol.md` (new Anchor: section, always-on); `.claude/hooks/keepsake-forced-read.py` (ANCHOR_DIRECTIVE on both arms); `.claude/hooks/grounding-cue-reminder.py` (Anchor reinforcement line); `.claude/hooks/cue_registry.py` (shipping entry mart-primacy message + read_before); `players/jebrim/CLAUDE.md` (Shipping/mart section reframed, primacy = precondition #1); `players/jebrim/bank/domains/shipping-mart.md` (source-#1 lead line).
