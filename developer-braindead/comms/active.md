# active.md — dev-to-dev comms

> Append-only log. Each Braindead session reads at respawn, posts an `OPEN` declaration, dialogues as needed, posts a `CLOSING` entry at session-close.
>
> See `_about.md` for the protocol and entry kinds.

> **Rotated through 2026-06-09** — older entries bulk-moved to `comms/archive/active-*.md` (2026-05-27 … 2026-06-09). Most recent entries kept below; seek to EOF for the live tail. Nothing deleted (per `_about.md` → Rotation). Prior per-rotation banners collapsed S165.

> **Rotated 2026-06-09 — 3 older entries bulk-moved to `active-2026-06-09.md`** (auto-rotation, comms_append.py). Kept the most recent 50 below; seek to EOF for the live tail. Nothing deleted (per `_about.md` -> Rotation).

> **Rotated 2026-06-09 — 2 older entries bulk-moved to `active-2026-06-09.md`** (auto-rotation, comms_append.py). Kept the most recent 50 below; seek to EOF for the live tail. Nothing deleted (per `_about.md` -> Rotation).

> **Rotated 2026-06-09 — 2 older entries bulk-moved to `active-2026-06-09.md`** (auto-rotation, comms_append.py). Kept the most recent 50 below; seek to EOF for the live tail. Nothing deleted (per `_about.md` -> Rotation).

> **Rotated 2026-06-09 — 3 older entries bulk-moved to `active-2026-06-09.md`** (auto-rotation, comms_append.py). Kept the most recent 50 below; seek to EOF for the live tail. Nothing deleted (per `_about.md` -> Rotation).

> **Rotated 2026-06-09 — 1 older entries bulk-moved to `active-2026-06-09.md`** (auto-rotation, comms_append.py). Kept the most recent 50 below; seek to EOF for the live tail. Nothing deleted (per `_about.md` -> Rotation).

> **Rotated 2026-06-09 — 1 older entries bulk-moved to `active-2026-06-09.md`** (auto-rotation, comms_append.py). Kept the most recent 50 below; seek to EOF for the live tail. Nothing deleted (per `_about.md` -> Rotation).

> **Rotated 2026-06-09 — 1 older entries bulk-moved to `active-2026-06-09.md`** (auto-rotation, comms_append.py). Kept the most recent 50 below; seek to EOF for the live tail. Nothing deleted (per `_about.md` -> Rotation).

> **Rotated 2026-06-09 — 1 older entries bulk-moved to `active-2026-06-09.md`** (auto-rotation, comms_append.py). Kept the most recent 50 below; seek to EOF for the live tail. Nothing deleted (per `_about.md` -> Rotation).

> **Rotated 2026-06-09 — 1 older entries bulk-moved to `active-2026-06-09.md`** (auto-rotation, comms_append.py). Kept the most recent 50 below; seek to EOF for the live tail. Nothing deleted (per `_about.md` -> Rotation).

> **Rotated 2026-06-11 — 1 older entries bulk-moved to `active-2026-06-11.md`** (auto-rotation, comms_append.py). Kept the most recent 50 below; seek to EOF for the live tail. Nothing deleted (per `_about.md` -> Rotation).

> **Rotated 2026-06-11 — 1 older entries bulk-moved to `active-2026-06-11.md`** (auto-rotation, comms_append.py). Kept the most recent 50 below; seek to EOF for the live tail. Nothing deleted (per `_about.md` -> Rotation).

> **Rotated 2026-06-18 — 9 older entries bulk-moved to `active-2026-06-18.md`** (auto-rotation, comms_append.py). Kept the most recent 40 below; seek to EOF for the live tail. Nothing deleted (per `_about.md` -> Rotation).

> **Rotated 2026-06-19 — 6 older entries bulk-moved to `active-2026-06-19.md`** (auto-rotation, comms_append.py). Kept the most recent 40 below; seek to EOF for the live tail. Nothing deleted (per `_about.md` -> Rotation).

[2026-06-11 18:32] braindead-9fca792a OPEN (post-close follow-up — live WRAPPING UP state)
  S188 closed but Niklavs added a follow-up: the board's WRAPPING UP (closing) state never surfaces — a clean one-turn close writes the closing marker then overwrites it with wrapped_up before any hook fires, so the board jumps straight to WRAPPED UP. Fix: backend reads the <sid8>.mode marker LIVE at poll time (same pattern as the action-heartbeat re-read) so closing shows within one 2s poll.
  Targets: cockpit/backend.py (live mode-marker read in build_session_model), cockpit/test_backend.py (case); respawn/quest-log/comms at close.
  Steering clear of: all player-namespace writes; the live Jebrim session + e6e7b78d residue staged in the shared tree.
  No live Braindead siblings.

[2026-06-11 18:46] braindead-9fca792a CLOSING (S189 — live WRAPPING UP state during session-close)
  Completed: backend now reads the <sid8>.mode marker LIVE per poll so the board shows WRAPPING UP for the DURATION of a close (was: only WRAPPED UP at the end — a one-turn close never fires a hook while the closing marker is set). Live-verified end-to-end (a17168ec walked closing→WRAPPING UP→wrapped_up→WRAPPED UP). Added a staleness guard (mtime ≥ last_event_ts) after the live read exposed 13 lingering crash-exit markers that false-flipped live rows to DONE. Gate: test_backend.py 79/79. backend.py only; cockpit relaunch needed.
  Leaving open: ★NEXT cockpit RELAUNCH verifies the 4 repros (S188 ×3 + this WRAPPING UP). Lingering-.mode-marker GC is a hygiene candidate (guard makes them harmless for display). Standing: e6e7b78d to commit/close; Zezima bootstrap; CORE-thinning; Khaan item 9; 2 S128 one-liners; Jebrim cleanup.
  No live Braindead siblings. Committed by pathspec only; sibling staged files untouched. No push.

[2026-06-11 22:14] braindead-977f8615 OPEN (lesson-store consolidation — design pass)
  Quest: collapse/reconcile the 3 overlapping lesson stores (global keepsake reflexes + jebrim examine/confirmed ~80 + harness MEMORY.md ~80 feedback) into a coherent model with merge-on-write + retire-on-stale. Design-FIRST: ground & quantify the real overlap before any structural change (verify-the-thing — the review was from sampling; contrarian lens says they may serve different read-paths, not be truly redundant).
  Targets (design pass, read-heavy): READ gielinor/keepsake/, players/*/examine/confirmed/, ~/.claude/.../memory/MEMORY.md; WRITE a findings/design note under developer-braindead/bank/ + a tracked task list. Identity-shaped writes (examine/, keepsake/, any new ritual) come AFTER Niklavs sees the design.
  Steering clear of: the e6e7b78d uncommitted residue (adherence-rates.py, hygiene-check.py, knowledge-miss-regression-set.md, domain-cue-reminder.py, grounding-cue-reminder.py, lorebook/_about.md, brain-health-check.md) + the live Jebrim alching pass (players/jebrim/ draft promotions). Both left untouched; commit by pathspec only. Overlap to watch: a retire-on-stale arm may want firing telemetry (adherence/ritual-stats), which e6e7b78d touched — coordinate if so.
  Open to handoff: Zezima bootstrap; ~6.6k CORE-thinning; Khaan item 9; 2 S128 one-liners; Jebrim cleanup hand-off; Health-Index re-score.
  No live Braindead siblings (braindead-a0b39f49 intent is 06-09, CLOSED x4 in comms).

[2026-06-11 22:36] braindead-977f8615 CLOSING (S190 — lesson-store two-funnel bridge: cap + reconcile)
  Completed: reframed the outside review's "3 stores -> collapse to 1" against B-015 (keep both; collapse already rejected) and built the never-built BRIDGE instead. (1) Acute: harness MEMORY.md recompressed 28.3KB->22.3KB under the load-truncation cap, 104 entries preserved (bijection). (2) NEW verification/lesson-store-check.py + test 14/14 (cap/long-line/integrity/examine<->MEMORY drift); live = 28 dupes (7 exact/21 likely, 23 unlinked). (3) Rulebook: bankstanding.md step 8 (reconcile = canonical examine anchor + cross-linked warm MEMORY copy, never merge; retire on graduation/supersession, move->memory/archive/) + alching pointer + layer-routing row (AGENTS.md auto-synced); D-035 recorded. (4) Verified loop: one pair reconciled -> detector flipped UNLINKED->linked (23->22). Build committed 1e2c888; close commits D-035 + quest-log + respawn + build-lessons, by pathspec.
  Leaving open: ~27 remaining dupes backfill at future bankstandings (step 8, not a hand-pass). Review remainder: #4 root-clutter cleanup, #2 accumulation-rate (extend detector to examine pile + in-progress quests), #5 respawn-dialect legibility. Standing: e6e7b78d retrieval-part-2 to commit/close; Zezima bootstrap; ~6.6k CORE-thinning; Khaan item 9; 2 S128 one-liners; Jebrim cleanup; Health-Index re-score; cockpit relaunch verify.
  No live Braindead siblings. Committed by pathspec only; e6e7b78d + the dead Jebrim alching staged residue left untouched. No push.

[2026-06-11] braindead-a248fdd2 OPEN (push brain to a second GitHub remote — ops/advice)
  Entered dev-brain via "lets develop gielinor". Niklavs asks to push the brain to a SECOND repo: https://github.com/nfelsbergs/Brain.git (distinct from origin niklavsfelsbergs/Brain.git). DISCOVERY/ADVICE first — no push without an explicit separate go (commit policy: push is always a separate principal action). Findings so far: target repo returns 'Repository not found' (doesn't exist or not visible to current creds); local main is 194 ahead of origin/main; no secret leak in tracked files (gielinor/.mcp.json empty {}, real .mcp.json + telemetry gitignored). Blocked on Niklavs: create+private the target, confirm intent (mirror vs add-remote).
  Targets IF greenlit: git remote add + git push only — NO file writes to brain content. comms/quest-log/respawn at close.
  Steering clear of: all player WIP, the e6e7b78d retrieval residue + dead Jebrim alching staged in the shared tree (committed by others, not mine to sweep), cockpit/*, experiments/*.
  No live Braindead siblings (comms tail all CLOSED; S190 977f8615 CLOSED 22:36).

[2026-06-11 23:30] braindead-a248fdd2 CLOSING (S191 — push brain to a second GitHub remote)
  Completed: pushed the brain to a second repo nfelsbergs/Brain.git (besides origin niklavsfelsbergs). Pre-flight: no secret leak in tracked files (gielinor/.mcp.json empty {}, real .mcp.json + telemetry gitignored); local main was 194 ahead of stale origin/main. The 404-not-401 catch: first push failed instantly no-popup — GitHub returns 404 (not 401) for a private repo you cannot see, git only prompts on 401, so the cached niklavsfelsbergs identity authed silently → 404; Niklavs confirmed the repo exists in-browser → wrong-account auth not a missing repo. Fixed via username-in-URL (nfelsbergs@github.com) → fresh GCM browser login → push OK (194 commits). Fan-out wired: origin carries two push URLs so one git push origin updates BOTH; main upstream back to origin/main; verified both remotes at 5ba1a65. Build-lesson + a git-ops memory entry captured. Committed by pathspec.
  Leaving open: standing backlog unchanged — e6e7b78d retrieval-part-2 to commit/close; Zezima bootstrap; ~6.6k CORE-thinning; Khaan item 9; 2 S128 one-liners; Jebrim cleanup hand-off; Health-Index re-score; cockpit relaunch verify; ~27 lesson-store dupes backfill at future bankstandings.
  No live Braindead siblings.

[2026-06-11 09:17] braindead-e6e7b78d CLOSING (S192 — retrieval part 2: lorebook cue arm + artifact verify + obedience measured)
  Completed: scorecard 6/2/3 → 8/2/1 (mechanical ceiling realized). (1) [LOR] case 10 — NEW gielinor/lorebook/_index.md (18 decisions mapped; 4 cue-armed) + lorebook arm in domain-cue-reminder.py (rule inlined once/session then silent); hygiene-check Axis C drift guard; drafts-triage/bankstanding add-the-row step; test_lorebook_cue 22/22. (2) [GND] case 5 — ARTIFACT_KEYS proven dead code vs the shipped binary's payload constructor; grounding-cue rebuilt on the real upload placeholders ([Image #N]/[Pasted text #N]); test_grounding_cue 16/16. (3) adherence-rates --obedience: tier-3 obedience = 50% either-signal all-time (shipping 47% n=268, grounding-cue 65% n=68, Reading:-line 15%); snapshot appended; health-check dim 5 rewired. Verified: S187 Jebrim hand-off (a) ran fully (Axis A clean, research 17/18 cited). Close-time catch: SNNN collision (S188 taken; prefix-glob trap) → S192, 30 refs fixed, 2 build-lessons.
  Leaving open: ★NEXT handoff = close-gate continuation false-FAIL fix + MEMORY.md over-limit trim + shipping-digest decision (data: 47% @ n=268). Watch: lorebook-cue live-fire from a real player session; first Fable-5 digest inline (still none). Standing: Zezima bootstrap; ~6.6k CORE-thinning; Khaan item 9; 2 S128 one-liners.
  No live Braindead siblings. Committed by per-file pathspec only; sibling Jebrim working-tree deletions untouched. No push.

[2026-06-12 19:17] braindead-a7ea5300 OPEN (scope block-deletes hook to brain paths only)
  Entered dev-brain via "lets develop gielinor" mid-session, pivoted from the Jebrim EU-tender cleanup pass (same sid a7ea5300, which committed + pushed the tender cleanup to bi-analytics origin). Task: scope gielinor/.claude/hooks/block-deletes.py so it only blocks deletes that target BRAIN paths. Right now it pattern-matches the delete verb regardless of cwd and blocked a legitimate delete in the bi-analytics work repo (a throwaway probe). Fix = a path-scope check (effective dir outside the brain AND no absolute brain path in the command -> allow), preserving the braindead bypass + the full brain floor for every other actor. Braindead D-032 = edit directly; showing Niklavs the diff; verify both ways (brain-path delete still BLOCKED, work-repo delete ALLOWED).
  Targets: gielinor/.claude/hooks/block-deletes.py, NEW developer-braindead/verification/test_block_deletes_scope.py; gielinor/players/jebrim/quest-log/in-progress/S236 hand-off note (mini-respawn for the outgoing Jebrim work); developer-braindead/{quest-log, respawn.md, comms} at close.
  Steering clear of: all player WIP (31 Jebrim in-progress quests + drafts), cockpit/* runtime, switchboard/*.log, state.ndjson, experiments/*, other gielinor content.
  Open to handoff: standing backlog — close-gate continuation false-FAIL fix, MEMORY.md trim, shipping-digest decision, Zezima bootstrap, ~6.6k CORE-thinning, Khaan item 9, 2 S128 one-liners, Jebrim quest graduation (31 files).
  No live Braindead siblings (comms tail all CLOSED through S192; no fresh braindead-* intent besides my own a7ea5300).

[2026-06-12 19:24] braindead-a7ea5300 UPDATE — block-deletes scope guard built + verified; PAUSING for a Jebrim discussion
  block-deletes.py scope guard landed: allow a delete whose effective dir is an absolute path OUTSIDE the brain AND no absolute brain path appears in the command; braindead bypass + the full brain floor for every other actor untouched; git-bash /c/ vs Windows C:\ drive forms normalized. NEW developer-braindead/verification/test_block_deletes_scope.py 9/9 from the real stdin entry point (brain-path deletes BLOCK incl the /c/ form; work-repo deletes ALLOW). Fix is LIVE in the working tree but UNCOMMITTED — Niklavs hasn't given the commit go (brain always-ask).
  Niklavs pivoted via "Hey Jebrim" to DISCUSS the EU-tender project folder architecture (no execution this session). Flipping intent back to jebrim; dev session PAUSED, not closed. Open hand-off: commit block-deletes.py + the new test when greenlit, then a standard dev close.
  No live Braindead siblings.

[2026-06-12 19:40] braindead-a7ea5300 CLOSING (S237 — scope block-deletes.py to brain paths only)
  Completed: scoped block-deletes.py so it only blocks deletes that target BRAIN paths — it had matched DELETE_PATTERNS against the command string regardless of target, blocking a legitimate git rm in the bi-analytics WORK repo (hit live during the Jebrim tender cleanup). NEW scope guard after the braindead bypass: allow a delete whose effective dir is an absolute path OUTSIDE the brain AND no absolute brain path in the command (closes the cd /tmp && rm <brain-path> leak); /c/ vs C:\ drive forms normalized; braindead bypass + full floor for every other actor untouched. NEW developer-braindead/verification/test_block_deletes_scope.py 9/9 from the real stdin entry point (work-repo deletes ALLOW; brain-path deletes incl the /c/ form BLOCK) — the hook's first suite. Committed by pathspec.
  Session shape: one session / two actors (a7ea5300). Jebrim did the EU-tender post-presentation cleanup (pushed c10818a..b92a110; +NFE standing commit-as-you-go rule; +REPORTS_STATUS.md markers; +doc CURRENT-STATE banners) + drafted docs/ARCHITECTURE_DISCOVERY_PLAN.md (committed in bi-analytics, UNPUSHED), pivoted here for the hook fix.
  Leaving open: ★NEXT = EU-tender architecture-discovery EXECUTION (next Jebrim session: trace pipeline.py + import graph, mine quest-log/bank record, ask Niklavs only residue -> ARCHITECTURE_INVENTORY.md -> REFACTOR_PLAN.md). Standing: close-gate continuation false-FAIL fix; MEMORY.md trim; shipping-digest decision; Zezima bootstrap; ~6.6k CORE-thinning; Khaan item 9; 2 S128 one-liners; Jebrim quest graduation (31). active-mode -> unscoped.
  No live Braindead siblings.

[2026-06-15 ig] braindead-b2e22a71 OPEN (forced continuation-vs-new "Anchor:" verdict line)
  Entered dev-brain via "lets develop gielinor". Niklavs: add a FORCED first-turn move where the player classifies the opening prompt against prior work -- "continues [X]" or "new topic" -- and states it explicitly before answering. Maps onto the existing internal Recall-shaped lens + Reading: line + anchor-to-continuation keepsake reflex; the gap is making the verdict VISIBLE/forced, not a silent thinking check. Direction nodded: prose rule + hook arm (defer detector); fires session-open + genuinely-new topics. DISCUSSION done; building this turn after showing the always-on prose diff first (high-blast-radius).
  Targets: gielinor/meta/communication-protocol.md (new Anchor: preamble line, always-on), gielinor/.claude/hooks/ (cue-arm in an existing first-prompt hook, TBD after reading them), NEW developer-braindead/verification/ test; developer-braindead/{quest-log, respawn.md, comms} at close.
  Steering clear of: all player WIP (Jebrim 31 in-progress + drafts), cockpit/* runtime, switchboard/*.log, state.ndjson, experiments/*, other gielinor content, examine/confirmed + keepsake (no identity writes this session).
  Open to handoff: standing backlog -- close-gate continuation false-FAIL fix, MEMORY.md trim, shipping-digest decision, Zezima bootstrap, ~6.6k CORE-thinning, Khaan item 9, 2 S128 one-liners, Jebrim quest graduation (31).
  No live Braindead siblings (comms tail CLOSED through S237; no fresh braindead-* intent).

[2026-06-15 ig] braindead-b2e22a71 UPDATE — second task: shipping-mart-FIRST routing primacy (Jebrim)
  Anchor: line set DONE (5 files, tests green, UNCOMMITTED awaiting go). Niklavs raised a 2nd, related task: Jebrim sometimes routes shipping-data questions to non-mart sources; the mart-first rule + knowledge only surface when he opens the shipping-agent. Diagnosis: the cue PATTERNS already fire broadly (shipping/shipment/carrier/parcel/freight/carrier-names) — the gap is the DIRECTIVE CONTENT: everywhere it says "don't reason about the mart from memory / load the reference", which presupposes he already chose the mart. None states MART-PRIMACY (mart = source #1 for any shipping-data question, start there). Fix = content/framing in 3 homes, no new mechanism. Niklavs picked: strong default + justify off-mart.
  Added targets: gielinor/.claude/hooks/cue_registry.py (shipping entry message + read_before), gielinor/players/jebrim/CLAUDE.md (reframe Shipping/mart section, primacy = precondition #1), gielinor/players/jebrim/bank/domains/shipping-mart.md (one-line primacy lead); test_domain_cue_registry.py if it asserts message text.
  No live Braindead siblings.

[2026-06-15 ig] braindead-b2e22a71 CLOSING (S242 — Anchor: continuation verdict + shipping-mart source-#1 primacy)
  Completed: two related cue-forcing builds. (1) Anchor: line — new always-on section in gielinor/meta/communication-protocol.md (state continues-<X> vs new-topic on session-open + new topics) + ANCHOR_DIRECTIVE on both arms of keepsake-forced-read.py + reinforcement in grounding-cue-reminder.py. (2) Shipping-mart = SOURCE #1 — corrected a wrong first guess (cue patterns were already broad; gap was directive CONTENT) and threaded mart-primacy (strong-default + justify-off-mart) through cue_registry.py + players/jebrim/CLAUDE.md (primacy = precondition #1) + bank/domains/shipping-mart.md. Full cue/forced-read suite green (keepsake 27/27, grounding 17/17, registry 40/40, digest-discovery 23/23, inline 26/26, lorebook 22/22); py_compile clean. Build-lesson + memory captured (diagnose trigger-vs-content; verify your own diagnosis). SNNN: asked-S241 taken by a live Jebrim session -> S242. Committed by pathspec.
  Leaving open: deferred adherence arm (Anchor:-presence + mart-first compliance) if proof-of-fire wanted. Standing: close-gate continuation false-FAIL fix; MEMORY.md trim; shipping-digest decision; Zezima bootstrap; ~6.6k CORE-thinning; Khaan item 9; 2 S128 one-liners; Jebrim quest graduation. active-mode -> unscoped.
  No live Braindead siblings (a live Jebrim S241 session shares the tree — committing by pathspec only, nothing of theirs swept).

[2026-06-17 12:46] braindead-c8a3bd38 OPEN (diagnose: gnome spawn never shows "awaiting crew")
  Entered dev-brain via "lets develop gielinor". Niklavs: a live session spawned a gnome but the board never showed "awaiting crew". DIAGNOSIS done (read-only): the gnome (G12, sid 5974b4ee) was launched via the modern Agent tool, which returns a handle in ~253ms and runs the sub-agent ASYNC (2 task-notification re-invokes in the transcript), with NO run_in_background flag set. emit-event.py background-detection only checks run_in_background/runInBackground, so it treats the Agent spawn as a blocking foreground Task: records the pending gnome at PreToolUse, pops it at PostToolUse 253ms later (despawn-gnome event). Cockpit polls ~2s and derives crew from the live pending list, which empties in 253ms -> no crew tag, no subtitle, sprite flickers (same class as the S028 background-task invisibility). Root = Agent-tool async semantics unrecognized by the spawn hook.
  Targets IF fix greenlit (no writes yet): developer-braindead/.claude/hooks/emit-event.py (treat Agent-tool / task-notification-style spawns like background: skip Post despawn, clear on the real completion signal or GC), maybe cockpit despawn-on-task-notification; + a verification/ test. respawn/quest-log/comms at close.
  Steering clear of: all player WIP (the live 5974b4ee Jebrim alching session + its tree), cockpit/* runtime beyond read, switchboard/*.log, state.ndjson, experiments/*.
  No live Braindead siblings (freshest braindead intent 06-12; comms tail CLOSED through S242).

[2026-06-17 13:50] braindead-c8a3bd38 CLOSING (S255 — async Agent spawns now show "awaiting crew")
  Completed: diagnosed + fixed why a session that spawned a gnome never showed "awaiting crew" on the cockpit board. Root = the modern Agent tool launches sub-agents ASYNC (PostToolUse fires ~230ms at LAUNCH, result via a later <task-notification>), but emit-event.py only knew run_in_background as async, so it despawned the role-state entry at launch — before the ~2s board poll. Reproduced LIVE twice (G12 gnome, D264 dwarf, ~230ms spawn->despawn while the agent ran on). Fix: emit-event.py flags a foreground Agent spawn async_agent + skips the launch-despawn; status-sidecar.py _reconcile_async_crew despawns it when the <task-notification> for its tool-use-id lands in the transcript (GC 1h backstop). NEW test_async_crew_lifecycle.py 14/14; test_actor_resolution 5/5; cockpit/test_backend 79/79; py_compile clean. Live end-to-end verified (D265 persisted then cleared on notification). Build-lesson + cross-conv memory captured. Committed by pathspec (Jebrim alching 5974b4ee shares the tree).
  Leaving open: cockpit RELAUNCH to visually confirm the awaiting-crew chip renders; optional SubagentStop registration as a cleaner completion signal (needs settings + restart). Standing: close-gate continuation false-FAIL fix; MEMORY.md trim; shipping-digest decision; Zezima bootstrap; ~6.6k CORE-thinning; Khaan item 9; 2 S128 one-liners; Jebrim quest graduation. active-mode -> unscoped.
  No live Braindead siblings (freshest braindead intent 06-12; comms tail CLOSED through S242).

[2026-06-18 ig] braindead-f14690e1 OPEN (DISCUSSION — visualize Jebrim's usage / utilization dashboard)
  Entered dev-brain via "lets develop gielinor". Niklavs: "if you had to visually represent the usage of Jebrim, how would you do it?" — a snapshot dashboard of how Jebrim has been utilized + what's been done. DISCUSSION/DESIGN-FIRST per act-only-when-asked — survey what data is on disk (quest-log 71 in-progress / 274 completed, 94 bank notes, 119 examine/confirmed, 9 domain digests, 21 research, 43 inventory, 310 commits 05-20..06-18) + propose a dashboard shape with a recommendation. No build until Niklavs steers.
  Targets IF greenlit later: likely a NEW generator (tools/jebrim_usage.py or similar) walking the jebrim namespace + git log -> a static HTML/SVG snapshot; reuses cockpit palette. Scope TBD on his pick.
  Steering clear of: all player WIP (read-only tree/git walk for counts), cockpit/* runtime, .claude/hooks runtime, switchboard/*.log, state.ndjson, experiments/*, gielinor/ writes.
  Open to handoff: standing backlog (close-gate continuation false-FAIL fix; MEMORY.md trim; shipping-digest decision; Zezima bootstrap; ~6.6k CORE-thinning; Khaan item 9; 2 S128 one-liners; Jebrim quest graduation; cockpit relaunch verify).
  No live Braindead siblings (comms tail CLOSED through S255; no fresh braindead-* intent).

[2026-06-18 ig] braindead-f14690e1 UPDATE — v1 card built + iterated 5x; now deep-reading the brain for a grounded redesign
  Built tools/jebrim_usage.py -> tools/jebrim-card.html (premium dark+gold; Cinzel/Inter/donut/heatmap/throughput; delegations regex fixed _[dgp]\d{1,2}_ excl sid8; peak-ring date bug fixed). Niklavs likes the look but wants the CONTENT rethought — activity heatmap is decorative, memory-donut hues don't separate. Now: 3 read-only Jebrim dwarves mining (1) work-types/outcomes, (2) knowledge/identity state, (3) operational/temporal health, to ground a panel-by-panel redesign PROPOSAL (no rebuild until he picks).
  Targets unchanged (tools/ generator + card; no gielinor writes beyond dwarf quest-log traces). Still clear of cockpit/*, hooks runtime, player WIP writes, experiments/*.
  No live Braindead siblings.

[2026-06-18] braindead-9d93fcd1 OPEN (build the quest-graduation fix — Parts 1-2; continues Guthix B-020)
  Entered dev-brain via "lets develop gielinor, pick this up" (same sid as the just-closed Guthix bankstanding 9d93fcd1). Building the principal-approved fix from gielinor/deities/guthix/proposals/2026-06-18-quest-graduation-detector-and-trace-home.md:
    P1 (keystone): NEW developer-braindead/verification/quest-graduation-check.py — read-only detector; per player scans quest-log/in-progress/, reads each quest's inventory resume open_dep header + cross-checks comms CLOSING -> flags GRADUATABLE / NO-RESUME / TRACE (sub-agent prefixes); counts; --graduatable list; cap flag. Mirror of lesson-store-check.py. + a test suite.
    P2 (structural): sub-agent traces get a non-quest home (quest-log/traces/ or archive-on-return) so they stop inflating in-progress; touches the 4 *-write-boundary.py allow-lists + gielinor/meta/modes.md + the spawn skills.
  Targets: developer-braindead/verification/* (new detector + test), gielinor/.claude/hooks/{dwarf,gnome,penguin,shipping-agent}-write-boundary.py (widen allow-list only, non-breaking), gielinor/meta/modes.md, gielinor/spellbook/skills/spawning-*.md. respawn/quest-log/comms at close.
  P3 (the cleanup that MOVES Jebrim quest files) DEFERRED — live Jebrim S265 (5cbb1d00) + sibling braindead-f14690e1 is actively analyzing Jebrim quest-log COUNTS for the usage card; moving files now corrupts their analysis. Hands off Jebrim namespace entirely.
  Live sibling: braindead-f14690e1 (busy ~150s) on tools/jebrim_usage.py + tools/jebrim-card.html + read-only Jebrim dwarves. NO overlap: I stay off tools/ and off all Jebrim quest-file moves; they steer clear of hooks/gielinor writes. Pathspec-scoped commit to my footprint only; never push.

[2026-06-18] braindead-9d93fcd1 UPDATE — Parts 1-2 BUILT + verified (Part 3 deferred)
  P1 done: NEW developer-braindead/verification/quest-graduation-check.py (read-only) + test_quest_graduation.py (37/37). Live run: jebrim 73 in-progress = 45 quests + 28 traces; 10 graduatable (open_dep none), 3 no-resume. Reproduces the B-020 picture; sid8 disambiguation splits the two S250s.
  P2 done (trace-home): widened the 4 *-write-boundary.py allow-lists to /quest-log/traces/ (permissive, non-breaking; floor + bank/confirmed blocks regression-tested green); redirected the run-log trace in spawning-{dwarves,gnomes,penguins}.md + .claude/agents/shipping-agent.md + respawn.md step 7; new convention section in meta/modes.md (*Sub-agent traces*); traces/ added to both players' quest-log/_about.md structure. py_compile clean; hook smoke tests ALLOW traces / BLOCK meta+bank+confirmed.
  P3 (move the 73 Jebrim files) STILL DEFERRED — live Jebrim S265 (5cbb1d00) + sibling f14690e1 analyzing Jebrim counts. Hands off Jebrim namespace.
  Uncommitted: my P1-2 footprint + the carried Guthix B-020 gielinor footprint. Commit pending principal go (not session-close yet). Never push. No overlap with f14690e1 (tools/ + jebrim-card) — confirmed.

[2026-06-18 ig] braindead-f14690e1 CLOSING (S269 — Jebrim usage dashboard)
  Completed: built tools/jebrim_usage.py -> tools/jebrim-card.html, a self-contained premium HTML/SVG usage snapshot of how Jebrim is used (regenerable, stdlib-only). Iterated heavily with Niklavs: v0 generic-stats -> 5 premium polish passes -> recon-grounded redesign (3 read-only Jebrim dwarves) -> value-first panels (project arcs w/ flagships, current-focus, job-to-be-done, work-rhythm, knowledge-moat, domains). Load-bearing counting fixes (both caught by Niklavs disbelief): sessions = wrap-ups = distinct SNNN = 173 (the 226 was junk — phantom sessions from a fallback key); quests de-inflated to 187 parent (88 sub-agent traces were padding it). UI fixes: aligned HTML project bars (was a ragged SVG Gantt); fixed scrolling-background colour drift (fixed viewport-anchored glow, green removed); fixed chart-label clipping. 2 build-lessons + 1 memory captured (name-the-count-unit; backdrop-on-fixed-layer). Committed by pathspec.
  Leaving open: dead-code cleanup in jebrim_usage.py (unused recurrence/job constants); optional cockpit fold; possible value/impact strip (Niklavs declined the curated one — prefers computed). Standing backlog: close-gate continuation false-FAIL; MEMORY.md trim; shipping-digest decision; Zezima bootstrap; ~6.6k CORE-thinning; Khaan item 9; 2 S128 one-liners; Jebrim quest graduation.
  Note: a live Jebrim alching session shares the tree — its draft promotions/deletions + 2 dwarf recon traces left UNCOMMITTED, not mine to sweep.
  No live Braindead siblings.

[2026-06-18 ig] braindead-07bbe4c6 OPEN (cockpit click-to-open reliability — diagnose first)
  Entered dev-brain via "lets develop gielinor". Niklavs: clicking output-path links in the cockpit terminal sometimes opens (HTML->browser, SQL->vscode) and sometimes does nothing. DIAGNOSIS-first (read-only so far): the click-to-open stack is cockpit/web/links.js (PATH_RE + xterm registerLinkProvider + transcript linkifyPaths + global delegate) -> /api/open-path in backend.py (os.startfile / explorer-reveal, workspace-scoped). Ranked failure modes: (1) xterm line-WRAPPING — provideLinks scans ONE visual row via getLine().translateToString, so a long path wrapped across 2 rows yields only a fragment -> no/partial link (short paths fit one row -> work; explains the 'sometimes'); (2) bare filename no dir-segment (PATH_RE requires >=1 seg/) not linkified; (3) backend 403 outside-workspace / 404 not-found fail SILENTLY (click handler is best-effort, no user feedback) so a dead click looks identical to no-link. Confirming the dominant symptom with Niklavs before touching code.
  Targets IF fix greenlit: cockpit/web/links.js (wrapped-line reconstruction in the xterm provider + maybe relax PATH_RE for bare filenames), possibly cockpit/web/term.js (provider wiring), cockpit/backend.py (open-path), cockpit/web/styles.css (failure affordance); cockpit/test_backend.py + node --check gates; respawn/quest-log/comms at close.
  Steering clear of: all player WIP (read-only), .claude/hooks runtime, switchboard/*.log, state.ndjson, experiments/*, gielinor/ writes, the verification/ + write-boundary-hook + meta/modes.md footprint of sibling 9d93fcd1.
  Live sibling: braindead-9d93fcd1 (quest-graduation detector P1-2, UPDATE no CLOSING, intent ~18min stale — paused awaiting commit go). Footprint = verification/ + *-write-boundary.py + meta/modes.md + spawning skills; FULLY non-overlapping with cockpit/. Treating as parked; not synthesizing its CLOSING. Pathspec-scoped commit to my cockpit footprint only; never push.
  Open to handoff: standing backlog (close-gate continuation false-FAIL; MEMORY.md trim; shipping-digest decision; Zezima bootstrap; ~6.6k CORE-thinning; Khaan item 9; 2 S128 one-liners; Jebrim quest graduation).

[2026-06-18 ig] braindead-07bbe4c6 CLOSING (S270 — cockpit click-to-open link reliability)
  Completed: fixed why clicking a file-path link in the cockpit terminal SOMETIMES opened (HTML->browser, SQL->vscode) and sometimes did nothing. Read-only trace found 3 gaps in cockpit/web/links.js: (1) LINE-WRAPPING — the xterm provider scanned one visual row, so a path wrapped across 2 grid rows was a fragment -> no link (dominant 'sometimes' cause); (2) BARE filenames never matched; (3) backend 403/404 failed SILENTLY. Fix (links.js + styles.css ONLY, backend untouched): logicalLine() rebuilds the wrapped line via isWrapped continuations + multi-row range mapping; terminal-only TERM_PATH_RE adds whitelisted bare-names (transcript stays strict slash-only); openPath flashes a #pathlink-flash toast on rejection. Verified node --check + test_backend 79/79 + css braces 365/365 + a throwaway regex/wrap fixture; RUNTIME-UNVERIFIED pending relaunch. Build-lesson + memory captured (a surprising synthetic FAIL was a fixture artifact — a phantom row-padding space — not a code bug; suspect the fixture first). Committed by pathspec (sibling 9d93fcd1 quest-graduation P1-2 + Jebrim WIP share the tree).
  Leaving open: cockpit RELAUNCH eyeball (running window holds stale JS) — hover a long WRAPPED path, a bare something.sql, a moved path (flashes). Standing backlog: close-gate continuation false-FAIL fix; MEMORY.md trim; shipping-digest decision; Zezima bootstrap; ~6.6k CORE-thinning; Khaan item 9; 2 S128 one-liners; Jebrim quest graduation (sibling 9d93fcd1 built the detector P1-2, awaiting its own commit/close).
  Live sibling: braindead-9d93fcd1 (quest-graduation detector, UPDATE no CLOSING, intent stale) — footprint verification/ + write-boundary hooks + meta/modes.md, fully non-overlapping with my cockpit/ footprint; left untouched.

[2026-06-18] braindead-9d93fcd1 CLOSING (S271 — quest-graduation detector + sub-agent trace-home; B-020 Parts 1-3)
  Completed: built + verified + committed the full B-020 fix. (1) quest-graduation-check.py (read-only, 37/37) wired into respawn + bankstanding step 6. (2) sub-agent trace-home — traces route to quest-log/traces/, 4 write-boundary hooks widened + modes.md + spawn skills + shipping-agent + respawn step7 + _about.md. (3) Jebrim in-progress drained 73->36 (9 graduated, 28 traces archived, 8 resumes archived; S245 held). Committed 6993ee0 (65 files, pathspec-scoped) + born-link follow-up + this close. Promoted D-035 drafts->confirmed (+_index) on principal cue. Carried the Guthix B-020 footprint (MEMORY 26.3->23.7KB on disk). Harvest: 2 build-lessons + 1 cross-conv memory.
  Leaving open: nothing blocking. Optional next: SubagentStop-driven trace auto-archive; MEMORY working-cap (20KB) retirement campaign. Standing backlog unchanged minus the two retired items (Jebrim quest graduation + MEMORY trim — handled). active-mode -> unscoped.
  Parallel ORWO session (64902bef) committed interleaved, no collision; foreign churn (B-018/S069/orphan S224) left untouched per S144. Never pushed.

[2026-06-18 ig] braindead-07bbe4c6 NOTE (close-gate respawn FAIL = sibling-prepend artifact)
  close_check --sid8 07bbe4c6 reports 5/6 PASS; the lone FAIL is 'respawn most-recent block does not mention 07bbe4c6'. Cause: sibling 9d93fcd1 (S271 quest-graduation) ran its close CONCURRENTLY and respawn_update --prepend'd its block ABOVE mine (committed 220ac1a), demoting my S270 'Last updated' to a Prior <details> block. My block is present + intact (grep S270_07bbe4c6 = 1). NOT re-prepending: it would (a) invert true close order (S271 committed after me), (b) duplicate my block (new top copy + the already-demoted Prior copy), (c) churn a prepend-war on a finished sibling. This is the standing-backlog 'close-gate continuation/parallel false-FAIL' class. Taking the Stop-gate's sanctioned escape (note + second stop releases the loop-guard). Substantive close is complete: links.js fix committed e737372, quest-log rename 6a7a9b2, CLOSING posted, active-mode unscoped, core artifacts committed.
  No live Braindead siblings (9d93fcd1 closed S271).

[2026-06-18 ig] braindead-c3eb261e OPEN (full brain-system audit — fresh sweep)
  Entered dev-brain via "lets develop gielinor". Niklavs: perform a full audit on how well the brain system is working. READ-ONLY sweep — run the verification harness + every detector (check.py, brain-weight, hygiene-check, lesson-store-check, quest-graduation-check, domain-coverage, adherence-rates, ritual-stats) + the test suites as ground truth, cross-read meta/rituals/hooks/players/globals, and deliver a graded health verdict with prioritized findings. NOT leaning on prior audit verdicts (S110/S125/S186) — re-measuring fresh. No fixes this turn unless Niklavs greenlights after the verdict.
  Targets: READ-ONLY across gielinor/ + developer-braindead/ + globals; WRITES limited to developer-braindead/{quest-log, respawn.md, comms} at close + this OPEN. No player-namespace writes, no gielinor writes, no hook/runtime edits.
  Steering clear of: all player WIP, cockpit/* runtime, switchboard/*.log, state.ndjson, experiments/*, the cockpit relaunch-verify backlog.
  Open to handoff: standing backlog (close-gate continuation false-FAIL fix; shipping-digest decision; Zezima bootstrap; ~6.6k CORE-thinning; Khaan item 9; 2 S128 one-liners; optional SubagentStop trace auto-archive; MEMORY 20KB working-cap campaign).
  No live Braindead siblings (comms tail CLOSED through S271; no fresh braindead-* intent files; my sid c3eb261e).

[2026-06-18 ig] braindead-c3eb261e UPDATE — audit fixes landed; scope now includes a gnome (jebrim graduation) + MEMORY trim
  Verification trio DONE + verified (full suite 19/19 via NEW run_tests.py): (1) test_git_index_guard.py HOOK path repaired (was pointing at a missing file -> BLOCK-by-accident; live hook verified correct); (2) adherence-rates.py keepsake display showed injected(113) not covered(89) -> fixed to match the 79% rate; (3) test_close_gate_stop real-subprocess fn decoupled from live b82b0b90 tree state (plumbing-only). Docs trued up: ~72% OPEN-posting -> ~56% (92/164) in root+dev CLAUDE.md + respawn-ritual.md + AGENTS.md resync. Audit snapshot appended to brain-health-snapshots.ndjson (index 80/B).
  SCOPE CHANGE (Niklavs go on "do it all"): now spawning ONE gnome into the jebrim namespace for a quest-graduation pass (graduate the 1 open_dep:none, classify 3 no-resume, archive stale inventory resumes; PROPOSES/moves in working tree, I review before commit) + doing a MEMORY.md conservative recompression (24.3KB -> <20KB working cap, bijection-preserving) in ~/.claude (outside the repo). zezima domains bootstrap DEFERRED (only 4 notes, no cluster yet).
  Steering clear of: cockpit/* runtime, switchboard/*.log beyond the snapshot append, experiments/*, other players. Pathspec-scoped commit to my footprint only; never push.
  No live Braindead siblings (comms tail CLOSED through S271; my sid c3eb261e).

[2026-06-18 ig] braindead-c3eb261e CLOSING (S272 — full brain audit (80/B) + verification-layer fixes + jebrim graduation + MEMORY cliff catch)
  Completed: full 7-dimension health audit (whole instrument harness + 6 guarantees live-fired) -> Health Index 80/B (was 83/B 06-09), snapshotted; bones green, decline = OPEN-posting 56% + jebrim active-state. On "do it all", 6 fixes all verified: (1) test_git_index_guard.py was DEAD (HOOK at a missing path; Python exit-2 == BLOCK code, so BLOCK cases passed by accident for weeks) -> live hook verified correct, path repaired 19/19; (2) NEW run_tests.py batch-gate over all 19 suites (the rot root cause — nothing ran them); (3) adherence-rates.py keepsake line printed injected(113) not covered(89) -> matches 79%; (4) test_close_gate_stop real-subprocess decoupled from live b82b0b90 tree state (standing close-gate false-FAIL class); (5) docs ~72%->~56% OPEN (root+dev CLAUDE.md, respawn-ritual.md, both AGENTS.md); (6) audit snapshot. Gnome (g1, my sid): jebrim graduated S245 + 3 no-resume (S224/S236/S246) + 2 stale inventory archived, in-progress 37->33. MEMORY.md was 100 B under the 24,400 hard truncation cap -> conservative recompression (131 lessons preserved, bijection-verified) to 23.3 KB, headroom restored. Full suite 19/19. Committed by pathspec.
  Leaving open: MEMORY 20 KB working-cap retire/merge at next bankstanding (34 examine<->MEMORY dupes the lever); optional run_tests.py pre-commit/respawn wiring; jebrim 32 still over soft-cap (real open work); 3 orphan jebrim inventory resumes (principal call). Standing: close-gate continuation false-FAIL (test half decoupled); shipping-digest decision; Zezima bootstrap; ~6.6k CORE-thinning; Khaan item 9; 2 S128 one-liners; cockpit relaunch verify.
  No live Braindead siblings.

[2026-06-19 09:39] braindead-9dd296c7 OPEN (continue confirmed-scan — real-use eval + bankstanding-wire + body-sim decision)
  Entered dev-brain via "lets develop gielinor, continue with this". Continuing the ae6ca4c confirmed-scan arc (tool committed last session but NEVER given a dev close — no quest-log/respawn/comms; orphan in the dev record, will reconcile at close). Point 3 of that hand-off: (1) run confirmed-scan.py a few more times in REAL use + judge output quality (HARD flags + RELATED queue); (2) decide whether to wire it into bankstanding (as a REVIEW QUEUE you approve, never auto-fix — the whole lesson of the DPD false-positive); (3) decide the known limitation — RELATED pairs key on title tokens only, so cross-title body contradictions slip through (widen to body-similarity or accept+document). EVIDENCE-FIRST: run live, show Niklavs, build only on his nod.
  Targets IF greenlit: developer-braindead/verification/confirmed-scan.py + test_confirmed_scan.py (body-sim widening), gielinor/spellbook/rituals/bankstanding.md (review-queue step). developer-braindead/{quest-log, respawn.md, comms} at close (incl. reconciling the orphan ae6ca4c into the record).
  Steering clear of: all player WIP (read-only scan over gated layers), cockpit/* runtime, switchboard/*.log, state.ndjson, experiments/*, other gielinor content.
  Open to handoff: standing backlog (close-gate continuation false-FAIL fix; shipping-digest decision; Zezima bootstrap; ~6.6k CORE-thinning; Khaan item 9; 2 S128 one-liners; cockpit relaunch verify; MEMORY 20KB working-cap campaign).
  No live Braindead siblings (comms tail CLOSED through S272; no fresh braindead-* intent; my sid 9dd296c7).

[2026-06-19 10:03] braindead-9dd296c7 CLOSING (S275 — confirmed-scan DANGLING calibration + bankstanding step-9 wire-in)
  Completed: continued the confirmed-scan arc (tool built in a prior conversation, committed ae6ca4c but never dev-closed — orphan now reconciled). Ran it live -> the HARD/DANGLING tier was ~80% FALSE-POSITIVE (28 flags, ~22 concept-slug links resolving to real files under feedback_/date-prefixed stems), contradicting its 'near-zero FP' claim, so 'wire once trusted' became 'fix the resolver first'. Calibrated the resolver (prefix-strip symmetric on target+corpus covering feedback_/reference_ + D-NNN_/S018 IDs; multi-word concept-slug misses -> soft LOOSE; only bare-IDs/placeholders stay HARD) -> 28->8 genuine. Wired into bankstanding step 9 (HARD=worklist; RELATED/COLLISION=review queue the principal approves, NEVER auto-fix, DPD-PL as the cautionary case); body-sim widening deferred+documented. test 16/16, full batch 20/20. Ran the live semantic pass over the 4 candidate pairs -> 0 real contradictions (all supersession/complementary, cross-linked) — review-queue flow validated. Committed 6757a54 + close commit, by pathspec.
  Leaving open: step 9's first live exercise at the next REAL bankstand (best once the live Jebrim sibling S274 a1f05d25 closes, so Phase 0 isn't colliding); standing worklist = 8 bare-ID DANGLING (full-stem rewrites, most in lorebook/confirmed/) + 7 UNDATED bank notes. Standing backlog: close-gate continuation false-FAIL; shipping-digest decision; Zezima bootstrap; ~6.6k CORE-thinning; Khaan item 9; 2 S128 one-liners; cockpit relaunch verify; MEMORY 20KB working-cap campaign.
  Live sibling: braindead/jebrim S274 (a1f05d25, shipping-agent teaching pass) shares the tree — staged drafts + promotion pass left untouched, pathspec-scoped commit. active-mode -> unscoped.

[2026-06-19 11:02] braindead-f5f676e6 OPEN (cockpit BRAIN side-panel broken — feed missing on open, panel gone after feed-toggle)
  Entered dev-brain via "lets develop gielinor". Niklavs: the right BRAIN side panel (Obsidian graph dock + activity feed) is broken — on first cockpit open the feed list does not render (only the graph/map), and after clicking the board feed-toggle (hide) then reopening, the whole panel is gone. DIAGNOSIS-FIRST (read-only): suspect the feed.js brain-dock mount lifecycle (mountBrain once-effect + host._brainMounted guard + brain.js module-global cnv/ctx) and/or the feed-col flex layout (brain-dock height var vs feed-items flex:1). Confirming repro by observation before touching code.
  Targets IF fix greenlit: cockpit/web/feed.js (FeedPanel mount/remount of the brain dock), cockpit/web/brain.js (mountBrain teardown/guard, module-global canvas state), cockpit/web/styles.css (.feed-col / .brain-dock / .feed-items layout); node --check + cockpit/test_backend.py gates; respawn/quest-log/comms at close.
  Steering clear of: all player WIP (live Jebrim sessions ORWO/Maersk/Teaching share the tree — read-only), .claude/hooks runtime, switchboard/*.log, state.ndjson, experiments/*, gielinor/ content writes.
  Open to handoff: standing backlog (close-gate continuation false-FAIL; shipping-digest decision; Zezima bootstrap; ~6.6k CORE-thinning; Khaan item 9; 2 S128 one-liners; MEMORY 20KB working-cap campaign).
  No live Braindead siblings (comms tail CLOSED through S275 at 10:03; no fresh braindead-* intent besides mine).

[2026-06-19 11:20] braindead-f5f676e6 CLOSING (S276 — cockpit BRAIN side-panel: feed visibility + canvas remount fix)
  Completed: diagnosed + fixed the broken right BRAIN side panel (Obsidian graph dock + feed) evidence-first via a headless-Chrome websockets-CDP driver (default localStorage rendered fine → state-dependent bug). TWO compounding bugs, both code-only: (A) feed-below-fold — `.brain-dock` fixed-height flex:none + an oversized saved --brain-h (toward the 820 clamp) overflowed the column at zoom 1.35; fixed with `max-height: calc(100% - 170px)` (reserves feed floor, makes the bad saved value harmless). (B) blank-canvas-after-feed-toggle — brain.js draw() skipped applySize() on remount because module-global view.w/h still matched the fresh canvas size → default 300×150 backing store → nodes off-screen; fixed by sizing the backing store on every (re)mount. Verified headless at worst-case brain-h=820 (feed visible + graph renders after toggle); node --check clean, CSS 365/365. Committed by pathspec (live Jebrim siblings share the tree).
  Leaving open: cockpit RELAUNCH to eyeball the live fix (running window holds stale JS — RUNTIME-UNVERIFIED). ⚠ CAUGHT: MEMORY.md is 24,461 B, OVER the 24,400 hard cap (regressed past the S272 cliff — tail truncated each load); first item for the MEMORY 20KB retire/merge campaign, deferred to bankstanding step-8. Standing: close-gate continuation false-FAIL; shipping-digest decision; Zezima bootstrap; ~6.6k CORE-thinning; Khaan item 9; 2 S128 one-liners.
  No live Braindead siblings (comms tail CLOSED through S275; the live ORWO/Maersk/Teaching rows are Jebrim player sessions, not dev).

[2026-06-19 13:15] braindead-d371d189 OPEN (Guthix floor-bypass during bankstanding — can the principal authorize confirmed/ closes?)
  Entered dev-brain via "lets develop gielinor" mid-conversation, pivoting from a live Guthix bankstanding (B-021, same sid d371d189; the bankstanding itself is committed/complete). Principal's question: why can't Guthix promote drafts->confirmed/ + graduate quests + write domain digests DURING bankstanding with explicit permission? He wants bankstanding to be a one-stop "close everything." Current block = the confirmed/ floor hook (block-confirmed-writes.py) is braindead-only ([[D-032_braindead_full_access|D-032]]/[[D-034_guthix_executes_on_explicit_authorization|D-034]]). Investigating a scoped, audited floor-bypass for Guthix-in-bankstanding-with-explicit-auth.
  Targets IF greenlit: .claude/hooks/block-confirmed-writes.py (+ maybe block-deletes.py), gielinor/meta/write-rules.md, gielinor/spellbook/rituals/bankstanding.md, a new gielinor/lorebook/ decision + dev bank/decisions/D-NNN. DESIGN-FIRST; no code until the principal nods.
  Steering clear of: live Jebrim player sessions sharing the tree (ORWO 6fbdcee1 etc. — [[D-024_parallel_player_coordination|D-024]] hazard; hit an index collision earlier this same session). Pathspec-scoped commits only; never push.
  No live Braindead siblings (comms CLOSED through S276; stale 0-byte respawn.md.lock from f5f676e6, closed 11:20).

[2026-06-19 13:30] braindead-d371d189 CLOSING (S277 — Guthix floor-unlock for confirmed/ writes in bankstanding)
  Completed: built the second confirmed/-floor bypass ([[D-036_guthix-floor-unlock-in-bankstanding|D-036]]) — block-confirmed-writes.py allows Guthix to write confirmed/ under actor==guthix x .mode in {bankstanding,alching} x a non-empty .floor-unlock marker (the principal's explicit per-session grant); logged bypass-guthix-authorized. confirmed/ writes ONLY; block-deletes.py untouched. New test_block_confirmed_unlock.py 9/9 live from the stdin entry point; run_tests.py 21/21; hygiene Axis C clean. Wired bankstanding.md (marker lifecycle) + write-rules.md + guthix.md (carve-outs) + lorebook D-036/_index + D-034 amendment + dev bank/decisions D-036 + S277 quest-log + respawn.
  Leaving open: first LIVE exercise by a real guthix-actor bankstanding (this session was Braindead — path test-verified, not yet run by a guthix session); offered the principal a live test (re-enter Guthix, grant, close B-021's pending examine promotions). Standing backlog unchanged.
  Live siblings: Jebrim ORWO (6fbdcee1) + others share the tree — pathspec-scoped commit to my footprint ONLY; hit a shared-index sweep earlier this same session ([[D-024_parallel_player_coordination|D-024]]). Never pushed. active-mode -> unscoped.

[2026-06-19 17:24] braindead-3c1fd4d3 OPEN (cockpit terminal copy — only part of a selection copies)
  Entered dev-brain via "lets develop gielinor". Niklavs: in the cockpit he cannot copy from the terminal — only PART of the selected text copies. DIAGNOSIS-FIRST (read-only): backend _write_clipboard_text handles arbitrary-length UTF-16 (no truncation), and the .term-host subtree has carried no CSS transform since S073 (clean 1:1 mouse->cell), so the partial copy is most likely selection-side (live TUI repaint mid-select, viewport-vs-scrollback, or wrap/altscreen) rather than the write bridge. Pinning the actual complaint (how he selects + copies, what "part" means) before touching code.
  Targets IF fix greenlit: cockpit/web/term.js (selection/copy path: _copyToClipboard, Ctrl+C handler, getSelection), possibly cockpit/web/styles.css; node --check + cockpit/test_backend.py gates; respawn/quest-log/comms at close.
  Steering clear of: all player WIP (live Jebrim sessions share the tree — read-only), .claude/hooks runtime, switchboard/*.log, state.ndjson, experiments/*, gielinor/ content writes.
  Open to handoff: standing backlog (close-gate continuation false-FAIL; shipping-digest decision; Zezima bootstrap; ~6.6k CORE-thinning; Khaan item 9; 2 S128 one-liners; MEMORY 20KB working-cap campaign; cockpit relaunch verify).
  No live Braindead siblings (comms CLOSED through S277 at 13:30; no fresh braindead-* intent besides mine).

[2026-06-19 17:42] braindead-3c1fd4d3 CLOSING (S278 — cockpit terminal copy: selection-snapshot fix)
  Completed: diagnosed + fixed the cockpit terminal copy bug (only part copies; Ctrl+C erased typed input). Evidence-first read ruled out the backend write (arbitrary-length UTF-16, no truncation) + the old CSS-transform selection drift (gone since S073). Root cause: claude's live TUI repaints clear xterm's live selection, so by Ctrl+C-time hasSelection() is false -> copy skipped -> \x03 SIGINT wipes the input ("two Ctrl+C's erased it" was the tell). Fix (cockpit/web/term.js, code-only): _selCache snapshot via a new onSelectionChange handler captures the full selection at mouse-up; Ctrl+C copies live-or-recent-snapshot (<=5s) so it never falls through to SIGINT after a select; consumed on copy, cleared on typing so a real interrupt still works. node --check clean; test_backend.py 79/79; onSelectionChange+clearSelection confirmed in vendored xterm. Committed by pathspec.
  Leaving open: cockpit RELAUNCH to verify LIVE (RUNTIME-UNVERIFIED — running window holds stale JS); the live test handed to Niklavs. Fallback if first Ctrl+C still erases -> copy-on-selection. Standing backlog: close-gate continuation false-FAIL; shipping-digest decision; Zezima bootstrap; ~6.6k CORE-thinning; Khaan item 9; 2 S128 one-liners; MEMORY 20KB working-cap; S277 Guthix floor-unlock first-live-exercise.
  Live siblings: Jebrim player sessions share the tree (ORWO etc.) — pathspec-scoped commit to cockpit/web/term.js + my dev footprint only; never push. active-mode -> unscoped.
