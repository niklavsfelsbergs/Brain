# S136 — 2026-05-31 — Cockpit polish pass (feed/header/board), live-iterated

Dev-brain via "lets develop gielinor", mid-conversation; OPEN posted via `comms_append`. Principal drove an eyeball-iterated cockpit polish across ~6 rounds, relaunching between each. All work in `cockpit/web/` (frontend infra — not `gielinor/`).

- **Round 1 — feed/header polish** (principal named 4 + "what else"; two taste calls answered via multiple-choice). `feed.js` + `styles.css`: the four green native filter checkboxes → a **`filters ▾` dropdown** (themed menu, gold-accent boxes, outside-click close); `main.js` dropped the redundant **"terminal · on subscription"** console-head subtitle + grouped the `tv-toggle`/`release` into a `.console-head-right` cluster (fixed two competing `margin-left:auto`); feed cards rounded `0 6px 6px 0` → **8px** to match the switchboard rows; comms purple calmed to a desaturated **`--comms-calm` lavender** so `DONE`/`NEEDS-YOU` lead the eye (hierarchy was backwards).
- **Round 2 — header height + BRAIN↔FEED resizer.** Slimmed all four header bars **50 → 34px** (shared rule). Added a draggable vertical split between the brain map and the feed list: `--brain-h` CSS var (persisted) + a `.brain-gutter` (row-resize) in `feed.js`; **overrides brain.js's inline `aspect-ratio:1` from CSS** (`aspect-ratio:auto !important`) so I never edited the live map-session's file — the canvas re-fits each frame off `getBoundingClientRect()` (brain.js L237), so a live drag reflows automatically.
- **Round 3 — topbar cleanup + button spill.** `board.js`: removed the `b91.0` version stamp + the `N live · N need you` count (+ the now-dead `waiting` const); `styles.css` `.newbtn { white-space:nowrap }` fixed the `+ new` wrapping two lines and spilling below the slim bar.
- **Round 4 — corner rivets removed.** Both top-left + top-right gold studs gone from all four headers (read as stray bullets against the titles); deleted the whole `::before`/`::after` rivet block rather than leaving overrides.
- **Round 5 — feed stick-to-bottom ([[S133_233cf954_cockpit-feed-prose-density-and-autoscroll|S133]] fix proved incomplete).** Root cause: [[S133_233cf954_cockpit-feed-prose-density-and-autoscroll|S133]]'s render-effect snaps once after commit, but content height isn't always settled then (tall comms block / late font swap / wrapped prose reflows a frame later) → snap lands short. Fix: a **`ResizeObserver`** on a new `.feed-items-inner` wrapper re-snaps on real content-height change, gated on `pinned`. Build-lesson logged.

**Shared-index hazard (audit beam #1) live again:** three concurrent Braindead sessions all stamped `(S134)` and entangled `styles.css`/`board.js`/`main.js`. **It resolved cleanly without `-p`:** braindead-7311cd20 closed first and its `bac936c` (S134 board UX) **swept my `board.js` topbar-removal + `main.js` console-head hunks into its commit** (exactly the co-edit hand-off I flagged) — so at my close those two are already in HEAD. My remaining commit is `feed.js` (exclusively mine) + `styles.css` (mine, plus 7311cd20's **3 dormant `.flavor-*` lines** it deliberately left uncommitted as "durability deferred" — committing styles.css gives them durability; their consumers in `board.js`/`status-sidecar.py` already landed in S134, so it's coherent). `brain.js` (braindead-304c77fd's map WIP) left untouched. My session is **S136**; comment stamps renumbered from the wrongly-guessed S134 (protecting the sibling's line 294). Lesson reinforced: [[S135_304c77fd_brain-map-improvement-options|S135]]'s "diff HEAD not just status" — the `M board.js`/`M main.js` from my session-start snapshot read as "mine to commit," but a `git diff HEAD --stat` at close showed them already clean (sibling had committed them), redirecting my scope to feed.js+styles.css only.

**Status:** every change RUNTIME-VERIFIED by the principal through round 4 (relaunched each round — rounded cards / filters menu / calm purple / slim headers / button fix all eyeballed); round 5 autoscroll is node-checked but **RUNTIME-UNVERIFIED** (the relaunch after it hadn't happened at close).

**Cascade.** `cockpit/web/feed.js` + `cockpit/web/styles.css` (committed this session); `cockpit/web/main.js` + `cockpit/web/board.js` (my hunks already in HEAD via braindead-7311cd20's `bac936c`); dev `quest-log/S136`, `respawn.md` prepend, `bank/build-lessons.md` (stick-to-bottom lesson), `comms/active.md` (OPEN + 3 co-edit flags + CLOSING).
**Main-brain changes.** none (no `gielinor/` writes — `cockpit/` is frontend infra).
