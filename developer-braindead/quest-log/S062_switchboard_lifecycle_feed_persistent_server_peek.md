# S062 — 2026-05-23 — Switchboard: lifecycle feed, persistent server, read-only peek

Principal opened in dev-brain: the switchboard app isn't usable — *"the only thing I want to fix now."* Three axes named: switchboard, chat (the COMMS feed), terminal, plus their interactions. Ran as a discussion that turned into targeted builds.

- **Stabilized the tree first.** Six parallel sessions ([[S056_e433ac17_switchboard-osrs-chatbox|S056]]–[[S061_fd0e0707_switchboard-row-info-and-zombie-gate|S061]]) had left ~1100 lines of client-file edits riding uncommitted in the shared tree (the recurring [[D-024_parallel_player_coordination|D-024]] deferral). Landed them as checkpoint commit `7d2b0c8` before touching anything — including the never-committed [[S056_e433ac17_switchboard-osrs-chatbox|S056]] `settings.js`.
- **Rebuilt the COMMS feed as a per-session lifecycle ticker** ([[status-sidecar.py]] + [[chat.js]] + `styles.css`). It was two wrong altitudes — raw-action firehose vs. sparse comms posts — with nothing between. Now five checkpoints: **PICKED UP** (the principal's prompt, rendered as `NIKLAVS:`), **PLAN/PROGRESS** (in-voice intent narration, already emitted), **NEEDS YOU** (AskUserQuestion/ExitPlanMode + the question, styled loudest with an amber pulse), **DONE** (Stop). status-sidecar.py gained `_emit_chat_checkpoint` + `_waiting_question` + three emissions on events it already handles; chat.js renders the kinds, **silences raw actions** (still feeds the row sparklines), and **pulls comms posts out** (`initCommsFeed` left defined but unwired). Panel still labelled "COMMS" — rename pending.
- **Diagnosed "empty convo on VS Code click" + "message won't send" as a stale server, not code.** A bare `python -m http.server` was squatting `:8765` (no `/history`, no `/chat`) after the real aiohttp `server.py` died with its parent session (`7c9033f4`). Proved fresh in-app chat works (drove `/chat` headless → `PONG`, result success) and `/history` resolves a VS Code session to its on-disk transcript once the right server runs (19 turns).
- **Cured the server-dying disease.** New [[start-switchboard.vbs]] launches `server.py` via the venv `pythonw` (no console), detached from any session; a Startup-folder shortcut (`Switchboard Server.lnk`) auto-starts it at logon. Verified detached: listener PID's parent chain roots at an orphaned `pythonw`, not the session.
- **Switched VS Code clicks from takeover to read-only peek** ([[terminal.js]]). One session can have only one live owner, so resuming a session VS Code is running conflicted — that was the silent dropped-message bug. `focusBySid8` now routes app-owned pills → resume (`wakeConvo`), external sessions → `peekConvo` (loads transcript read-only, input disabled, "running in VS Code" footer, steel-blue dot). New agents are launched in-app (the fresh path), which is the road off VS Code.

Verification: `py_compile` (both hooks) + `node --check` (chat.js, terminal.js) + isolated unit test of the new emitters + `/history` (19 turns) + fresh-chat `PONG` — all green. **The live browser render of the new feed and the read-only peek were NOT yet eyeballed by the principal** — that's the first task next session.

Decisions surfaced (not yet a D-NNN): the switchboard's model is now *navigator/observer of everything + launcher of new in-app agents*; VS Code sessions are observed (read-only), not absorbed. Worth a `D-NNN` if the direction holds.

**Cascade.** `developer-braindead/.claude/hooks/status-sidecar.py`; `switchboard/{chat.js,terminal.js,styles.css,index.html}`; new `switchboard/start-switchboard.vbs`; this quest-log entry; `respawn.md`; `comms/active.md`. Checkpoint commit `7d2b0c8` + the S062 close commit. A Windows Startup shortcut was created outside the repo (`%APPDATA%\…\Startup\Switchboard Server.lnk`).
**Main-brain changes.** none.
