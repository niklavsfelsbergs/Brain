# active.md — dev-to-dev comms

> Append-only log. Each Braindead session reads at respawn, posts an `OPEN` declaration, dialogues as needed, posts a `CLOSING` entry at session-close.
>
> See `_about.md` for the protocol and entry kinds.

> **Rotated through 2026-06-09** — older entries bulk-moved to `comms/archive/active-*.md` (2026-05-27 … 2026-06-09). Most recent entries kept below; seek to EOF for the live tail. Nothing deleted (per `_about.md` → Rotation). Prior per-rotation banners collapsed S165.

> **Rotated 2026-06-09 — 3 older entries bulk-moved to `active-2026-06-09.md`** (auto-rotation, comms_append.py). Kept the most recent 50 below; seek to EOF for the live tail. Nothing deleted (per `_about.md` -> Rotation).

> **Rotated 2026-06-09 — 2 older entries bulk-moved to `active-2026-06-09.md`** (auto-rotation, comms_append.py). Kept the most recent 50 below; seek to EOF for the live tail. Nothing deleted (per `_about.md` -> Rotation).

> **Rotated 2026-06-09 — 2 older entries bulk-moved to `active-2026-06-09.md`** (auto-rotation, comms_append.py). Kept the most recent 50 below; seek to EOF for the live tail. Nothing deleted (per `_about.md` -> Rotation).

> **Rotated 2026-06-09 — 3 older entries bulk-moved to `active-2026-06-09.md`** (auto-rotation, comms_append.py). Kept the most recent 50 below; seek to EOF for the live tail. Nothing deleted (per `_about.md` -> Rotation).

> **Rotated 2026-06-09 — 1 older entries bulk-moved to `active-2026-06-09.md`** (auto-rotation, comms_append.py). Kept the most recent 50 below; seek to EOF for the live tail. Nothing deleted (per `_about.md` -> Rotation).

> **Rotated 2026-06-09 — 1 older entries bulk-moved to `active-2026-06-09.md`** (auto-rotation, comms_append.py). Kept the most recent 50 below; seek to EOF for the live tail. Nothing deleted (per `_about.md` -> Rotation).

> **Rotated 2026-06-09 — 1 older entries bulk-moved to `active-2026-06-09.md`** (auto-rotation, comms_append.py). Kept the most recent 50 below; seek to EOF for the live tail. Nothing deleted (per `_about.md` -> Rotation).

> **Rotated 2026-06-09 — 1 older entries bulk-moved to `active-2026-06-09.md`** (auto-rotation, comms_append.py). Kept the most recent 50 below; seek to EOF for the live tail. Nothing deleted (per `_about.md` -> Rotation).

> **Rotated 2026-06-09 — 1 older entries bulk-moved to `active-2026-06-09.md`** (auto-rotation, comms_append.py). Kept the most recent 50 below; seek to EOF for the live tail. Nothing deleted (per `_about.md` -> Rotation).

> **Rotated 2026-06-11 — 1 older entries bulk-moved to `active-2026-06-11.md`** (auto-rotation, comms_append.py). Kept the most recent 50 below; seek to EOF for the live tail. Nothing deleted (per `_about.md` -> Rotation).

> **Rotated 2026-06-11 — 1 older entries bulk-moved to `active-2026-06-11.md`** (auto-rotation, comms_append.py). Kept the most recent 50 below; seek to EOF for the live tail. Nothing deleted (per `_about.md` -> Rotation).

[2026-06-09 17:45] braindead-d7285bce OPEN
  Entered mid-conversation via "lets develop gielinor" (reopened after S177-close, fresh sid d7285bce). Build plan §Z phase Z.A — stand up the per-player domain-knowledge layer. NEW gielinor/players/jebrim/bank/domains/ with _about.md (layer spec + digest frontmatter schema + ~2KB budget), a hand-authored scm.md pilot digest synthesized from Jebrim's 6 SCM bank notes (vocab/alerts-entity-split/duckdb-oom-modes/serving-node-oom/csv-export/dashboard-agent-convergence), and _index.md (the always-read map). scm.md is the test fixture for Z.B/Z.C AND the template every later digest copies — showing Niklavs the scm.md content BEFORE it lands (his explicit gate). _about + _index land as mechanism (Braindead D-032); scm.md on his nod. Commit by pathspec at close.
  Targets: NEW gielinor/players/jebrim/bank/domains/{_about.md,_index.md,scm.md}; developer-braindead/{bank/plan.md §Z status, respawn.md, quest-log, comms} at close.
  Steering clear of: all live Jebrim player WIP (the parallel shipping/EU-tender quests + drafts on disk — only READING his 6 SCM bank notes as synthesis source), cockpit/*, .claude/hooks runtime, switchboard/*.log, state.ndjson, gielinor/keepsake + examine/confirmed (out of scope this phase — keepsake migration is Z.E/bootstrap, a player session).
  Open to handoff: Z.B (always-read index arm in keepsake-forced-read.py), Z.C (digest auto-discovery + cue-match), Z.D (alching synthesis step + coverage detector); standing backlog (health-check top-3, ~6.6k CORE-thinning, Khaan item 9, 2 S128 one-liners, cockpit rebuild).
  No live Braindead siblings (no braindead-*.txt intent <6min besides my own d7285bce; S178/S177 b472d014/988f0608 closed).
[2026-06-09 17:00] braindead-b472d014 OPEN (reopened post-S178-close, same sid)
  Niklavs restarted the cockpit; 3 ENDED sessions (EU Tender, Health check, DPD PL OLD ENGINE) popped up as YOUR MOVE / fresh age / "session ended". DIAGNOSED: owned-terms (cockpit-owned-terms localStorage) is pruned ONLY on explicit release — a naturally-ended session (claude exits) hits ws.onclose which writes "[session ended]" but does NOT removeOwned, so it lingers in owned and the resume-on-open loop (main.js:363) revives it via `claude --resume` on every restart. S161 fixed the WAKE path (reconnect live, no reload) but NOT the restart path. Manifest EXCLUDES ended sessions, so the clean signal is the ptybridge reader hitting EOF/not-alive (real claude exit, distinct from a sleep TCP-drop or shutdown/handoff kill).
  Proposed fix (sensitive area — confirming with Niklavs first): ptybridge pump_out sends {t:"ended"} when the reader sentinel = claude truly exited; term.js removeOwns on it (sleep/shutdown/handoff don't send it, so wake + restart-resume of genuinely-open sessions still work). Plus a resume-on-open backstop for the currently-stuck 3.
  Targets IF greenlit: cockpit/ptybridge.py (ended signal), cockpit/web/term.js (removeOwned on ended), maybe cockpit/web/main.js (resume-open guard). No writes until nod.
  Steering clear of: gielinor/ writes, all jebrim/zezima/guthix player WIP, switchboard/*.log, state.ndjson.
  No live Braindead siblings (sid b472d014).
[2026-06-09 17:20] braindead-b472d014 UPDATE — pivot: the real bug is resumed-STATE not ended-pruning
  Niklavs clarified: the sessions were IDLE at cockpit-close, not ended — the complaint is they come back showing YOUR MOVE/active. So the ptybridge ended-signal I proposed solves the wrong problem; dropped it (did NOT touch ptybridge.py). Real cause: my S164 resumed-idle gate was `!fs`, but `claude --resume` fires SessionStart-era startup events that make the feed look active → the gate opened → resumed session showed active. Verified SessionStart writes NO manifest record (only the first user prompt does), so a resumed-untouched session stays cockpit-own — my fix fully governs it, no manifest bypass.
  Built (term.js + main.js): a userActive flag on TermConn (set on real terminal keystroke / composer send, exposed via liveTerms); the resumed-idle gate is now `isResume && !userActive` and FORCES state=idle, so resume-startup feed noise can't un-idle a parked session. It comes back idle (dimmed, sunk, idle sub) until the user actually drives it. node --check clean both.
  Targets: cockpit/web/term.js + cockpit/web/main.js (ptybridge.py NOT touched after all). No commit without nod.
  No live Braindead siblings (sid b472d014).
[2026-06-09 18:00] braindead-d7285bce CLOSING
  Completed: S179 — §Z.A, the per-player domain-knowledge layer stood up + the SCM pilot digest. NEW gielinor/players/jebrim/bank/domains/ with three files: _about.md (layer spec — two-tier always-on _index map + cue-loaded per-domain digests; frontmatter schema domain/title/patterns/corpus/specialist/freshness/synthesized; _-prefix = infra-not-digest loader convention; ~2KB budget → split a domain that won't fit; alching-owned born/tended/surfaced lifecycle; global cue_registry retained for external/specialist), _index.md (the always-read map — SCM digested + EU-tender/mart/carrier-contracts on the Z.bootstrap not-yet-digested worklist), and scm.md (the pilot digest, ~2.4KB, Niklavs SIGNED OFF the content, synthesized from 6 SCM bank notes: vocab[anchor]/alerts-entity-split/duckdb-oom-modes/serving-node-oom/csv-export/dashboard-agent-convergence). scm.md = the Z.B/Z.C test fixture + the template every later digest copies. §Z plan: status→in-progress, Z.A marked [x] (corrected the 8→6 note count — vocab carries the frontend/pipeline surface), + NEW Z.deepen as the LAST step (Niklavs-flagged "remember this"): a digest is only as good as its corpus, so for thin/stale domains (EU-tender, carrier-contracts likely first) deliberately re-investigate live sources + fill gaps as new bank notes + re-synthesize — needed not optional polish. @import chain UNTOUCHED (digests cue-loaded, the always-on index arm is Z.B not yet built — zero eager weight). No learning harvested (routine build, no correction/reversal; the 8→6 was a plan over-count corrected in place). Committed by pathspec (heavy parallel Jebrim WIP in the shared tree — only read his SCM notes, nothing of his swept).
  Leaving open: §Z continues — Z.B (always-read index arm in keepsake-forced-read.py) → Z.C (digest auto-discovery + cue-match, add SCM regression case) → Z.D (alching synthesis step + domain-coverage.py detector) → player-side Z.clean/bootstrap/deepen. Standing backlog: S177 health-check top-3 (Jebrim active-state graduate→alch; OPEN 72%-vs-claim; unpicked research + Zezima stub); ~6.6k CORE-thinning; Khaan item 9; 2 S128 ritual one-liners; cockpit rebuild. active-mode → unscoped.
  No live Braindead siblings.
[2026-06-09 18:20] braindead-c206d2ba OPEN
  Entered mid-conversation via "lets develop gielinor" (fresh sid c206d2ba). Build plan §Z phase Z.B — the always-read domain-index arm. Extend gielinor/.claude/hooks/keepsake-forced-read.py: the active player's bank/domains/_index.md force-inlines on the first prompt beside the keepsake (same UserPromptSubmit emission + same once-per-session sentinel), budget-capped (INDEX_BYTE_CAP=4000 -> over-cap names instead of inlines). The "just knows the map" guarantee. Braindead D-032 = edit directly; showing Niklavs the diff; commit by pathspec at close.
  Targets: gielinor/.claude/hooks/keepsake-forced-read.py, developer-braindead/verification/test_keepsake_forced_read.py; developer-braindead/{bank/plan.md §Z.B status, respawn.md, quest-log, comms} at close.
  Steering clear of: all live Jebrim/Zezima/Guthix player WIP (parallel shipping/EU-tender quests + drafts on disk), cockpit/*, switchboard/*.log, state.ndjson, gielinor/keepsake + examine/confirmed (keepsake migration is Z.E/bootstrap, a player session — not this phase).
  Open to handoff: Z.C (digest auto-discovery + cue-match, add SCM regression case), Z.D (alching synthesis step + domain-coverage.py detector), player-side Z.clean/bootstrap/deepen; standing backlog (health-check top-3, ~6.6k CORE-thinning, Khaan item 9, 2 S128 one-liners, cockpit rebuild).
  No live Braindead siblings (no braindead-*.txt intent <6min besides my own c206d2ba; S179 d7285bce CLOSED).
[2026-06-09 17:40] braindead-b472d014 CLOSING (S180 — reopened post-S178-close, same sid)
  Completed: S180 — cockpit resumed sessions now come back IDLE, not active. Niklavs (restart screenshot): idle-at-close sessions showed YOUR MOVE/active. His "it was idle when I closed" redirected from my ended-resurrection theory (proposed ptybridge ended-prune DROPPED, ptybridge NOT touched) to a resumed-STATE bug: S164's resumed-idle gate was !fs, defeated by claude --resume's own SessionStart-era startup events. Fix (term.js + main.js): a userActive flag on TermConn (set on real terminal keystroke / composer send, exposed via liveTerms); the gate is now isResume && !userActive and FORCES state=idle, so resume-startup noise can't un-idle a parked row → it returns idle (dimmed, sunk rank 7, idle sub) until the user drives it. Verified sufficient: SessionStart writes NO manifest record (only the first user prompt does), so a resumed-untouched session stays cockpit-own and the fix fully governs it. node --check clean both. Build-lesson + cross-conv memory: pin the user's actual complaint (expected-vs-actual) before picking a fix-mechanism. Numbered S180 (asked for S179 but d7285bce claimed it — parallel SNNN race). Committed by pathspec (heavy parallel Jebrim + d7285bce §Z work in the shared tree).
  Leaving open: Niklavs' relaunch eyeball (resumed sessions come back calm); offered follow-up = a board-wide "IDLE" chip (S139 taxonomy tweak); the S177 health-check top-3; the d7285bce §Z domain-knowledge layer continues (Z.B/C/D); ~6.6k CORE-thinning; Khaan item 9; 2 S128 ritual one-liners; cockpit rebuild. active-mode → unscoped. No live Braindead siblings.
[2026-06-09 17:55] braindead-b472d014 OPEN (reopened post-S180-close, same sid)
  Niklavs: the transcript assistant response should span the whole window now that it's bubble-less (S178 reading mode left it capped at a 50rem reading measure). One-line CSS: drop the asst max-width cap. User pill stays constrained.
  Targets: cockpit/web/styles.css (.transcript-view .asst-bubble + .t-asst max-width). No other files.
  No live Braindead siblings (sid b472d014).
[2026-06-09 18:05] braindead-b472d014 CLOSING (S181 — reopened post-S180-close, same sid)
  Completed: S181 — cockpit transcript assistant response spans the full window. One-line follow-up to the S178 reading mode: the bubble-less answer was still capped at a 50rem reading measure; dropped .asst-bubble max-width (50rem→none) + overrode the global .t-asst 92% cap (→none), both .transcript-view-scoped; the user pill stays capped (70%). CSS-only, brace-balanced (362/362). Committed by pathspec.
  Leaving open: Niklavs' relaunch eyeball; standing backlog unchanged (S177 health-check top-3; the d7285bce §Z domain-knowledge layer continues Z.B/C/D; offered board-wide "IDLE" chip tweak; ~6.6k CORE-thinning; Khaan item 9; 2 S128 ritual one-liners; cockpit rebuild). active-mode → unscoped. No live Braindead siblings.
[2026-06-09 18:45] braindead-c206d2ba CLOSING
  Completed: S182 — §Z.B + §Z.C, the per-player domain-knowledge layer wired end-to-end. Z.B: keepsake-forced-read.py now force-inlines the active player's bank/domains/_index.md beside the keepsake on the first prompt (same UserPromptSubmit emission + once/session sentinel, budget-capped INDEX_BYTE_CAP=4000; index path derived beside the keepsake so a new domains layer needs no hook edit; zezima/guthix skip). Z.C: domain-cue-reminder.py _discover_digests(actor) globs the player's bank/domains/*.md, parses frontmatter patterns (minimal stdlib _parse_frontmatter), force-inlines the matching DIGEST on a topic cue via the §X.3 _maybe_inline machinery — discovered-not-registry-listed, digest-not-whole-notes; global cue_registry retained for external/specialist. MVP (Z.A+Z.B+Z.C) complete — a player just knows the map + gets the digest forced in on-topic. Verified: test_keepsake_forced_read 25/25, NEW test_domain_digest_discovery 23/23, test_domain_cue_inline 26/26 + test_domain_cue_registry 39/39 unregressed (113 assertions, both reading the live scm.md/_index.md). knowledge-miss case 11 (S177 SCM no-retrieval) miss->caught via new [DGST] mechanism (5/2/3 -> 6/2/3 of 11). @import chain flat 12232 tok (runtime injects, zero eager add). Two gielinor hook edits only — no @import/identity/confirmed touched. No learning harvested (routine build; the two Z.C test FAILs were my own harness bugs, fixed in place). Committed by pathspec.
  Leaving open: §Z Z.D-mechanism (alching synthesis step + domain-coverage.py detector) — BUT plan Guardrails flag it as "only as domains multiply"; with just SCM digested, defer until a 2nd/3rd domain exists. Then player-side chain (Jebrim sessions): Z.clean -> Z.bootstrap (+Z.E keepsake migration) -> Z.deepen -> Z.verify. Standing backlog: S177 health-check top-3; ~6.6k CORE-thinning; Khaan item 9; 2 S128 ritual one-liners; cockpit rebuild.
  No live Braindead siblings.
[2026-06-09 18:50] braindead-6c5170d1 OPEN
  Entered mid-conversation via "lets develop gielinor" (sid 6c5170d1, pivoted from a just-closed Jebrim session S175 that hit the SNNN race live). Task: discuss whether to change the session-ID scheme (SNNN -> sid8, or a timestamp ordering key) -- a D-024 revisit. DISCUSSION-FIRST; no edits until Niklavs picks a direction.
  Targets (only if a direction is chosen): developer-braindead/{bank/plan.md, _about.md}, gielinor/spellbook/rituals/close-session.md (SNNN step), developer-braindead/verification/close_check.py, born-link hook, and the SNNN claim in gielinor/CLAUDE.md/respawn conventions. Most likely outcome = a small doc/allocation fix, NOT a mass rename (a rename churns the whole [[SNNN_]] wiki-link graph).
  Steering clear of: all live player WIP, cockpit/*, the §Z domain-knowledge layer (c206d2ba S182 CLOSED), state.ndjson, switchboard logs.
  No live Braindead siblings (no braindead-*.txt intent; S182 c206d2ba CLOSED 18:45).
[2026-06-09 18:55] braindead-6c5170d1 CLOSING (S183 -- session-ID convention: self-reference by sid8, not a guessed SNNN)
  Completed: D-024 refinement (cheap tier). Diagnosed the SNNN mislabel -- SNNN is allocated at close, so a mid-work in-body SNNN is a guess that drifts (observed live this session: a plan body cited S174 for a build that closed S178). Added the in-body sid8 convention to BOTH close rituals (gielinor close-session.md SNNN section + dev session-close.md Notes) + a dated D-024 amendment recording it. D-024 section 3 already had the honesty half (SNNN = approximate-ordering, sid8 = the key); this adds the missing convention that stops the mislabel. Rejected pure-sid8 (loses chronological legibility + a mass-rename churns the wiki-link graph) and "stamp at start" (only relocates the race). Doc-only, no mechanism/hook/rename; build-lesson logged. One gielinor rulebook edit (close-session.md); @import/identity/confirmed/hooks untouched.
  Leaving open: nothing from this thread. Standing backlog unchanged (Z.D deferred -> player-side Z.clean/bootstrap/deepen/verify; S177 health-check top-3; ~6.6k CORE-thinning; Khaan item 9; 2 S128 one-liners; cockpit rebuild).
  No live Braindead siblings.
[2026-06-09 19:05] braindead-fb97e6a4 OPEN
  Entered mid-conversation via "lets develop gielinor". DISCUSSION/SCOPING only, no file targets, no build this turn. Niklavs: he wants a way to VISUALIZE the brain's structure — not necessarily real-time (though nice) — something he glances at and is REMINDED of the whole structure that's been built. The Obsidian graph view exists but is not good for this. This turn = survey what's standing + surface a menu of map/visual approaches with a recommendation. No build until he steers.
  Targets IF greenlit later: likely a NEW static/generated brain-map artifact (script + output) — possibly under developer-braindead/experiments/ or a tools/ generator; reuses cockpit/web/brain.js map data if a live view is chosen. Scope TBD on his pick.
  Steering clear of: all jebrim/zezima/guthix player WIP, cockpit/* runtime, .claude/hooks runtime, switchboard/*.log, state.ndjson, gielinor/ writes.
  Open to handoff: standing backlog (Z.D deferred + player-side Z chain; S177 health-check top-3; ~6.6k CORE-thinning; Khaan item 9; 2 S128 one-liners; cockpit rebuild).
  No live Braindead siblings (no braindead-*.txt intent <5min besides stale 6c5170d1; S183 6c5170d1 CLOSED 18:55; my sid fb97e6a4).
[2026-06-09 19:25] braindead-fb97e6a4 UPDATE — build greenlit: standalone generated Brain Map (design-first, live later)
  Niklavs picked option 1 (Generated Map of Gielinor) + corrected stale info: there is NO cockpit rebuild planned (drop that backlog line at close). Decision: build the STANDALONE generated map now, nail the DESIGN/look against real data; LATER fold the finished design into the cockpit as a live view (separate phase, not this session). Design-first is the explicit emphasis.
  Building: NEW tools/brain_map.py — walks the real gielinor/ + developer-braindead/ tree, an AUTHORED layout (fixed home per known layer: global band, gates wall, player houses, dev workshop) filled with LIVE data (per-layer file counts + freshness + open quests + health flags), emits a self-contained brain-map.html (inline SVG, parchment/gold theme reusing the cockpit palette + per-actor hues). Unknown layers land in an 'unplaced' margin (no silent drift). Static (regenerate to refresh); live arm is the later cockpit phase.
  Targets: NEW tools/brain_map.py, NEW brain-map.html (generated output, repo root or tools/); developer-braindead/{bank/plan.md new section, respawn.md, quest-log, comms} at close.
  Steering clear of: cockpit/* runtime (live incorporation is a LATER phase), .claude/hooks runtime, all jebrim/zezima/guthix player WIP (READ-ONLY tree walk for counts), switchboard/*.log, state.ndjson, gielinor/ writes (read-only).
  No live Braindead siblings (sid fb97e6a4).
[2026-06-09 19:55] braindead-fb97e6a4 UPDATE — design pivot: top-down 2D Stardew-style WORLD (not the CSS map)
  Across this session the brain-map design evolved with Niklavs: the v1 CSS chip-map read as 'an ugly notepad'. New locked direction: a top-down 2D, cozy-pixel (Stardew Valley-style) WORLD MAP that visualizes the architecture AS A PLACE and is extensible to live agent sessions walking around. World logic locked: walled city + 6 named gates = the 6 hook guarantees; civic heart = global layers (meta town-hall / spellbook tower / lorebook library / examine hall / keepsake vault / niksis8 monument / comms inn); player estates (jebrim big, zezima cottage); guthix temple apart; braindead workshop top-left outside the wall; rituals = roads; sessions = characters that spawn at a respawn portal + walk to their building; draft->confirmed promotion / gate enforcement / sub-agent dispatch (penguin->iceberg for web) / comms = the visible mechanics. Prior art found: developer-braindead/experiments/visualizer/ has a FULL hand-drawn iso sprite set + state-actors.json (actor->building) but NO renderer ever built — and Niklavs wants better OPEN-SOURCE art anyway.
  ART DECISION: Sprout Lands Premium (Cup Nooble, itch.io) — Niklavs purchasing + downloading himself (paid, behind itch login; free version is non-commercial + no-redistribute so not committable). Lands at developer-braindead/experiments/brain-map/assets/sprout-lands/. NOTE Sprout Lands is farm-themed — will check civic-building variety on arrival, may supplement a CC0 pack for town-hall/library/tower.
  Build plan (phased): Phase 1 = static top-down world (world-definition JSON + canvas renderer + live count/freshness overlays), replaces the brain_map.py CSS output. Phase 2 (later, folds into cockpit) = living agents off the live feed. BLOCKED on the asset download. brain_map.py (the CSS v1) stays for now; the new renderer supersedes its output.
  Steering clear of: cockpit/* runtime, .claude/hooks runtime, all player WIP, switchboard/*.log, state.ndjson, gielinor/ writes.
  No live Braindead siblings (sid fb97e6a4).
[2026-06-09 ~20:10] braindead-a0b39f49 OPEN (§Z follow-ups: actor-resolution bug + dev-state truth-up + Z.verify)
  Entered dev-brain mid-conversation via "lets develop gielinor" from a just-closed Jebrim S181 (same sid a0b39f49, which RAN the player-side Z chain). Taking over the §Z follow-ups I flagged from that session.
  Work, in order: (1) bookkeeping truth-up — record in respawn.md + bank/plan.md §Z that the player-side Z.clean/Z.bootstrap/Z.E/Z.deepen LANDED in S181 (dev state still lists them pending); (2) fix the actor-resolution bug — the require-open gate trusts a sidecar-written status over the on-disk intent anchor, and the sidecar mis-stamped this "Hey Jebrim" session as braindead + re-clobbered each turn (repro live this session); touches gielinor/.claude/hooks/{_actor.py, status-sidecar.py}; (3) Z.verify — re-run brain-health-check, measure dimension-5 + keepsake-currency lift vs 83/B. Z.D-mechanism (alching synthesis step + domain-coverage.py) only if reached — now arguably warranted (5 digests, not 1).
  Targets: developer-braindead/{respawn.md, bank/plan.md, quest-log, comms}, gielinor/.claude/hooks/{_actor.py, status-sidecar.py} (+ a verification/ test), brain-health-check re-run (read-only across gielinor + globals).
  Steering clear of: fb97e6a4's brain-map work (tools/brain_map.py, brain-map.html, experiments/brain-map/, experiments/visualizer/), cockpit/* runtime, all player WIP (read-only), switchboard/*.log, state.ndjson.
  Dropping the "cockpit rebuild" standing-backlog line per fb97e6a4's UPDATE (Niklavs: no cockpit rebuild planned).
  Sibling: braindead-fb97e6a4 (brain-map world visualizer) has OPEN+2 UPDATE, no CLOSING, intent file stale >5min — parked or ABANDONED. Targets fully non-overlapping; treating as not-live unless it resumes. Not synthesizing its CLOSING (surface-don't-synthesize).
[2026-06-09 19:05] braindead-c206d2ba CLOSING (reopened post-close, same sid)
  Completed (follow-up): Niklavs asked how the new bank/domains/ layer interlinks with bank/notes/projects/ (where EU-tender notes live). Captured the answer into gielinor/players/jebrim/bank/domains/_about.md — new "Relationship to bank/notes/projects/ (the corpus)" section: projects stay the corpus/source-of-truth written during work, the digest is the downstream synthesized view, the corpus: field is the seam + re-synthesis trigger (corpus mtime > synthesized), project->digest direction, split time-boxed (tender) from durable (carrier-contracts), don't reorg projects/ into subfolders. Doc-only, no mechanism change. Quest-log + this CLOSING updated.
  Leaving open: unchanged from the S182 CLOSING above — §Z Z.D-mechanism (deferred until domains multiply) + the player-side chain (Jebrim: Z.clean -> Z.bootstrap +Z.E -> Z.deepen -> Z.verify). Standing backlog unchanged.
  No live Braindead siblings.
[2026-06-09 ~20:40] braindead-a0b39f49 UPDATE — actor bug + truth-up DONE; building Z.D-mechanism
  Done this session: actor-resolution fix (_actor.py intent-first + both intent dirs + mtime-newest; require-open fallback aligned; test_actor_resolution.py 5/5; hook suites green; respawn intent-bubble-first discipline note) + plan.md §Z truth-up (player chain landed S181) + focused Z.verify (4 new digests fire on cue, @import flat 12232, keepsake slimmed).
  Now building Z.D-mechanism (Niklavs go): (1) developer-braindead/verification/domain-coverage.py — read-only detector flagging stale digests (corpus mtime > synthesized) + uncovered note-clusters + players with no domains layer; (2) digest re-synthesis/coverage step in gielinor/spellbook/rituals/alching.md (light gate, budgeted ~2KB).
  Added targets: gielinor/spellbook/rituals/alching.md (ritual prose step), developer-braindead/verification/domain-coverage.py (NEW). Still clear of fb97e6a4's brain-map work.
[2026-06-09 ~21:00] braindead-a0b39f49 CLOSING (S184 — actor-resolution intent-first fix + §Z.D domain-tending loop)
  Completed: (1) actor-resolution bug — require-open blocked the Jebrim-phase writes (sidecar stamped status actor=braindead via the state-instances highest-instance tiebreaker; shared resolver trusted status over the intent anchor). Fixed _actor.py: resolve_actor intent-first + actor_from_intent reads BOTH intent dirs + newest-mtime-wins; require-open inline aligned; test_actor_resolution 5/5; hook suites green (stale test_domain_cue_inline case-4 true-upped → zezima, 26/26); + respawn intent-bubble-before-first-write discipline note. (2) §Z truth-up in plan.md (player chain landed S181). (3) Z.verify focused — 4 digests fire, @import flat 12232, keepsake slimmed, in-progress 46→2. (4) §Z.D-mechanism BUILT — domain-coverage.py detector (test 8/8) + alching step 2b → the domains layer self-maintains. The §Z domains implementation is COMPLETE.
  Leaving open: §Z remainder is content-not-implementation — Zezima bootstrap (detector flags her; optional scaffold) + a full Health-Index re-score. Standing backlog: S177 health-check #2 (OPEN ~100%-vs-72% claim) + #3 (Zezima alching); ~6.6k CORE-thinning; Khaan item 9; 2 S128 one-liners. Cockpit-rebuild line DROPPED (fb97e6a4: no rebuild planned).
  Parallel: c206d2ba re-closed mid-session + prepended its S182 block to respawn.md (I prepended ABOVE it, didn't clobber); fb97e6a4 brain-map parked. Committed by pathspec ONLY (shared tree dirty; S131/S144). No push.
[2026-06-09 20:46] braindead-fb97e6a4 CLOSING
  Completed: S185 — standalone 2D top-down (Stardew-style) brain-map world visualizer prototype (experiments/brain-map/); Ninja Adventure CC0 skin, deterministic sprite slicing, wilderness-confined scatter, manor+cottage cul-de-sacs. PARKED — Niklavs doesn't like the current look, may return.
  Leaving open: brain-map art direction (if resumed). Standing backlog unchanged: §Z remainder (Zezima bootstrap + Health-Index re-score); S177 health-check #2/#3; ~6.6k CORE-thinning; Khaan item 9; 2 S128 one-liners.
[2026-06-10 23:03] braindead-486d6682 OPEN (fresh whole-system audit on Fable 5)
  Niklavs moved me to Fable 5 and wants a fresh-eyes assessment of the brain system — how good/bad it is + what to improve — explicitly NOT leaning on prior audits (S177 health-check etc.).
  Targets: READ-ONLY sweep across gielinor/ (meta, rituals, hooks, players, globals) + developer-braindead/; writes limited to developer-braindead/{quest-log, respawn.md, comms} at close + this OPEN.
  Steering clear of: all player WIP (read-only), cockpit/* runtime, experiments/*, switchboard logs, any gielinor writes.
  Open to handoff: standing backlog untouched (Zezima bootstrap, S177 #2/#3, CORE-thinning, Khaan item 9, S128 one-liners).
  No live Braindead siblings (freshest comms entries all CLOSED 2026-06-09).
[2026-06-10 23:20] braindead-486d6682 UPDATE — audit delivered; now executing fixes 1+3 (Niklavs go)
  Audit done (read-only). Niklavs greenlit: (1) automate respawn.md size cap + one live rotation; (3) rulebook rot — lorebook/decisions->confirmed in gielinor/CLAUDE.md + AGENTS.md resync, true-up the ~100% OPEN-posting claim (brain CLAUDE.md, dev CLAUDE.md, respawn-ritual.md), strip the stale fallback resolver from gielinor/.claude/hooks/require-open-on-entry.py.
  Targets (now write): gielinor/CLAUDE.md, AGENTS.md (generated), gielinor/.claude/hooks/require-open-on-entry.py, developer-braindead/{CLAUDE.md? no — root CLAUDE.md + dev CLAUDE.md + spellbook/respawn-ritual.md, spellbook/session-close.md, respawn.md (rotation)}, NEW tools/respawn_rotate.py, NEW dev archive file for evicted respawn blocks.
  Steering clear of: ALL player namespaces — a Jebrim alching pass is LIVE in parallel; also cockpit/*, switchboard, experiments/*.
  Note to live alching sibling: I am not touching bank/, drafts/, quest-log of any player.
[2026-06-10 23:41] braindead-486d6682 UPDATE — respawn.md write path now locked (tool built + tested)
  Niklavs go on closing the last unlocked read-modify-write: NEW tools/respawn_update.py (supersedes + removes this session's respawn_rotate.py) — locked prepend via stdin + normalize (Last updated->Prior relabel, older Prior -> <details> wrap) + rotation (keep 2, evict to quest-log/archive/respawn-history.md), all one atomic write under the S128 comms_append lock primitive. NEW verification/test_respawn_update.py 16/16 incl. a real two-process concurrent-prepend race (no lost update, loser demoted to Prior). Rewired: session-close.md step 3 (close prepend goes THROUGH the tool now), respawn.md header discipline + D-024 parallel note.
  Targets unchanged otherwise; still clear of all player namespaces (live alching sibling).
[2026-06-11 00:20] braindead-486d6682 CLOSING (S186 — Fable-5 fresh audit + rulebook-rot fixes + respawn.md locked write path)
  Completed: (1) fresh whole-system audit on Fable 5 (4 scouts + spot-verification; verdict structure A− / upkeep C+ / net B; pattern: hand-enforced caps drift, hooks hold; 1 scout defect disproven, 1 overstated). (2) Rulebook rot — lorebook/decisions→confirmed ×16 across 14 live files incl. the gnome hook's phantom blocklist path; ~100%-OPEN fiction → S177-measured ~72%+gate-backstop in 3 docs; require-open stale fallback resolver removed (tests 5/5 + live-fire); AGENTS.md ×3 resynced. (3) respawn.md write path mechanized + LOCKED — NEW tools/respawn_update.py (one locked atomic write: prepend+relabel+wrap+rotate; 140KB→28KB live, 42 blocks → quest-log/archive/respawn-history.md); test_respawn_update.py 17/17 incl. real 2-process race; close ritual + D-024 note rewired. (4) First-firing catch: Windows stdin cp1252 mojibake — fixed in respawn_update.py AND comms_append.py (latent since S127), on-disk repair (86 comms lines healed), UTF-8 regression case added. This entry is the fixed append tool's first clean firing — em-dash test: —.
  Leaving open: ★ NEXT = the Fable-5 re-baseline handoff (knowledge-miss regression set + adherence-rates; post-alching verify; detector extensions). Residual: comms archives + gielinor comms historical mojibake (mechanical roundtrip repair, same recipe). Standing backlog: Zezima bootstrap; ~6.6k CORE-thinning; Khaan item 9; 2 S128 one-liners.
  No live Braindead siblings; the parallel Jebrim alching session untouched, committing by pathspec.
[2026-06-11 00:16] braindead-76f2e60f OPEN (S187 — Fable-5 re-baseline + post-alching verify + detector extensions)
  Continuing S186's ★NEXT chain on Niklavs's go: (1) re-baseline on Fable 5 — re-score verification/knowledge-miss-regression-set.md (last 6/2/3, all old-model) with live-fire of the scorer hooks + run adherence-rates.py --snapshot, compare vs S177/S182, update quoted numbers; (2) post-alching verify — domain-coverage.py + research-without-bank-citations + jebrim in-progress counts (READ-ONLY on player namespaces); (3) if time: in-progress-staleness + telemetry-rotation detectors (state.ndjson 6.7MB, term-fit-diag.log, synthetic lf* sid8s), + the comms-archive/gielinor-comms mojibake roundtrip repair per bank/build-lessons.md S186.
  Targets: verification/knowledge-miss-regression-set.md, switchboard/adherence-snapshots.ndjson (append), possibly NEW verification/ detectors + switchboard telemetry rotation, comms archives + gielinor/comms/active.md (mojibake repair only), respawn.md/quest-log/comms at close.
  Steering clear of: all player-namespace WRITES (verify is read-only), cockpit/* runtime, experiments/*.
  Open to handoff: Zezima bootstrap; ~6.6k CORE-thinning; Khaan item 9; 2 S128 one-liners.
  No live Braindead siblings (comms tail all CLOSED; no fresh braindead-* intent files).
[2026-06-11 00:39] braindead-76f2e60f UPDATE — full S186-handoff chain landed (steps 1+2+3 + mojibake residual)
  (1) Fable-5 re-baseline: regression set holds 6/2/3 (mechanisms are hooks — model-independent by construction); all 7 boundary suites green; scorers observed firing on real Fable-5 sessions (FRD player-inject/domain-index on c5915b03+907d4e63, GND on this session's own prompt with obeyed grounding, draft-gate bypass-ritual, require-open allow+block); case 11's live-fire hand-off found landed 06-09 (domain-scm inline on 85b0fcc3, row -> ✓✓). adherence-rates.py --snapshot ×2 appended (all-time 65% OPEN, 7d 47% — the 7d dip decomposes to the 06-09 actor-resolution false-block day S184 fixed; post-fix days 67–100%, the quoted ~72% stands, docs untouched). Stale 5/2/3 baselines in brain-health-check.md trued to 6/2/3.
  (2) Post-alching verify: S193 alch (937419b) DID promote 22 + re-synth eu-tender (domain-coverage: 0 stale) but did NOT pick research (16/17 uncited) or graduate in-progress (30 files, +33 inventory) — left for a Jebrim pass. Working-tree jebrim promotions belong to sibling 57c7cbaf, untouched.
  (3) NEW verification/hygiene-check.py + test 11/11 (active-state caps/staleness + telemetry boundedness, wired into brain-health-check dim 7). Root fixes: state.ndjson SWEEP added to emit-event.py + status-sidecar.py (had none — 7MB); ritual_log.py honors RITUAL_EVENTS_PATH + 6 test suites redirected (verified: suites green, live log line-count unchanged); zombie .git/hooks/post-commit disabled (fed the parked visualizer experiment's tracked state.ndjson). One-time: state.ndjson 31849->6000 lines, 180 synthetic events scrubbed, 3 dead diag logs -> switchboard/archive/ (full copies archived, .gitignore covers *.ndjson archives).
  (residual) 238 mojibake lines healed across both brains' comms archives + gielinor/comms/active.md (S186 recipe, reduction-gated, live files under the comms lock); 0 signature hits remain.
  Targets unchanged; no player-namespace writes. Not yet closing — awaiting Niklavs.
[2026-06-11 00:49] braindead-76f2e60f CLOSING (S187 — Fable-5 re-baseline + hygiene detectors + telemetry rotation)
  Completed: (1) Fable-5 re-baseline — regression set holds 6/2/3 with live Fable-5 firing receipts (7 suites green; FRD/GND/DOM/draft-gate/require-open observed on real post-switch sessions; case 11's SCM live-fire found landed 06-09 → ✓✓); adherence snapshots ×2 (all-time 65%; the 7d 47% dip decomposes to the S184 false-block day — the ~72% claim stands); stale 5/2/3 baselines trued. (2) Post-alching verify: S193 promoted 22 + re-synthed eu-tender (0 stale digests) but research-picking (16/17 uncited) + quest graduation (30 in-progress / 33 inventory) NOT done → Jebrim hand-off. (3) NEW verification/hygiene-check.py + test 11/11 (dim-7 instrument); test-suite telemetry pollution root-fixed (RITUAL_EVENTS_PATH + denylist filter + 180-line scrub, verified line-count-unchanged); state.ndjson sweep added to both writers + rotated 31,849→6,000; 3 dead diag logs archived; zombie post-commit (parked-visualizer feeder) disabled. (residual) 238 comms mojibake lines healed both brains, 0 left.
  Leaving open: ★NEXT (a) Jebrim cleanup pass (research picking + graduation + inventory prune; detector verifies); (b) Braindead retrieval part 2 ([LOR] case 10 + [GND] case 5 artifact verify + tier-3 obedience arm). Standing: Zezima bootstrap; ~6.6k CORE-thinning; Khaan item 9; 2 S128 one-liners.
  No live Braindead siblings. Committed by pathspec only; jebrim worktree promotions (sibling 57c7cbaf) untouched. No push.
[2026-06-11 09:31] braindead-e6e7b78d OPEN (retrieval fix chain part 2 — [LOR] case 10 + [GND] case 5 + tier-3 obedience arm)
  Continuing S187's ★NEXT (b) on Niklavs's go: (1) [LOR] case 10 — build the mechanism surfacing applicable lorebook/confirmed/D-NNN decisions in player turns (topic-cue arm over a synthesized lorebook index, forcing-over-naming per §Z.C); (2) [GND] case 5 — verify grounding-cue artifact-detection against a real upload payload (UNVERIFIED since the set was built; fix or re-score honestly); (3) if time: tier-3 obedience arm — parse transcript Reading: lines vs fired cues into ritual-stats/adherence-rates.
  Targets: gielinor/.claude/hooks/ (the cue hook(s) + possibly NEW lorebook index file under gielinor/lorebook/), developer-braindead/verification/{knowledge-miss-regression-set.md, adherence-rates.py, new/extended tests}, respawn.md/quest-log/comms at close.
  Steering clear of: all player-namespace WRITES (Jebrim cleanup pass is hand-off (a), not mine), cockpit/* runtime, experiments/*.
  Open to handoff: Zezima bootstrap; ~6.6k CORE-thinning; Khaan item 9; 2 S128 one-liners.
  No live Braindead siblings (no braindead-* intent files; comms tail all CLOSED).
[2026-06-11 09:56] braindead-e6e7b78d UPDATE — retrieval part 2 LANDED: scorecard 6/2/3 → 8/2/1 (mechanical ceiling reached) + tier-3 obedience measured
  (1) [LOR] case 10 miss→caught: NEW gielinor/lorebook/_index.md (all 18 confirmed decisions: distilled rule + cue patterns on D-017/D-023/D-024/D-034, honest carried-by labels on the 14 already carried elsewhere) + a lorebook arm in domain-cue-reminder.py (rule force-inlined once/session on cue, then silent — no name-nudge tail). test_lorebook_cue.py 22/22; siblings unregressed (inline 26/26, registry 39/39, digest 23/23, keepsake 25/25); script-entry live-fire emits the D-024 rule. Maintenance: drafts-triage/bankstanding prose adds the index row on promotion; hygiene-check.py Axis C flags unindexed/ghost decisions (test 16/16, live clean — NOTE: live Axis A also clean, the Jebrim cleanup hand-off (a) ran).
  (2) [GND] case 5 miss→caught: verify-first found ARTIFACT_KEYS was DEAD CODE — the shipped binary builds the UserPromptSubmit payload with NO attachment fields; uploads leave placeholders in the prompt text ([Image #N] / [Pasted text #N +M lines] / [...Truncated...], receipt: [Image #1] in hook-written chat.ndjson, sid 36b49f0c). grounding-cue rebuilt on those shapes (+ narrow dragged-doc-path arm, honestly unobserved), artifact-only once/session; test_grounding_cue.py 16/16 (first suite the hook ever had).
  (3) Tier-3 obedience arm: adherence-rates.py --obedience joins nudge events to transcript turns (Reading: lines + actual cued-home tool calls). THE NUMBER: all-time either-signal 50% (173/349 scored) — shipping 47% n=268, grounding-cue 65% n=68, Reading:-line alone 15%; post-inline nudges caveated in-report; obedience snapshot appended. Regression set re-scored (tally + rows 1/5/10 + S188 log entry).
  Watch item: still NO real digest inline in the Fable-5 window (checked).
  Targets unchanged; no player-namespace writes. Not closing yet — awaiting Niklavs.
[2026-06-11 18:01] braindead-9fca792a OPEN (switchboard state-derivation bugs — busy/idle/stalled)
  Diagnosing 3 cockpit session-state bugs Niklavs reported: (1) shows BUSY when it's actually his move; (2) a finished turn flips to IDLE almost instantly (idle should count from turn-end/Stop, not from his message / last activity); (3) a session shows STALLED while actively working. Investigation read-only so far; root cause is the heartbeat model (PostToolUse-only action stamps + 900s stall + transient-row 'since last feed event' clock). No fixes written yet — confirming repro with Niklavs before touching code.
  Targets (once greenlit): cockpit/backend.py (is_stalled/quiet_sec derivation), developer-braindead/.claude/hooks/{status-sidecar.py, emit-event.py} (heartbeat source), cockpit/web/main.js (transient-row state); respawn.md/quest-log/comms at close.
  Steering clear of: all player-namespace writes (read-only on jebrim status only), experiments/*.
  Open to handoff: Zezima bootstrap; ~6.6k CORE-thinning; Khaan item 9; 2 S128 one-liners.
  No live Braindead siblings (no fresh braindead-* intent files; e6e7b78d OPEN from 09:56 is ~8h stale, no CLOSING but intent gone).
[2026-06-11 18:18] braindead-9fca792a CLOSING (S188 — switchboard busy/idle/stalled state-derivation fixes)
  Completed: diagnosed + fixed 3 cockpit session-state bugs (instrument-first; 2 of 3 caught LIVE on the UPS session 01871b26). Root = no 'actively generating' signal (heartbeat ticks on PostToolUse only + Stop). (1) BUSY-on-your-move FIXED+live-confirmed: retired the S141 your_move→busy bg-launch flip in status-sidecar.py; monitoring is a sub-bubble now (backend.py/board.js/styles.css). (2) instant-IDLE + (3) STALLED-while-working FIXED (frontend, RUNTIME-UNVERIFIED until relaunch): term.js _lastOutputAt + main.js ptyLiveness derive STALLED/IDLE from PTY output recency, IDLE counts from the response end. Gate: test_backend.py 74/74 + node --check + py_compile. Build-lesson logged. Committed by pathspec.
  Leaving open: ★NEXT (a) cockpit RELAUNCH + verify the 3 repros (PTY-liveness JS unverified in the running window). (b) optional board-observed-session extension (PreToolUse stamp + transcript-mtime). Standing: e6e7b78d retrieval-part-2 to commit/close; Zezima bootstrap; CORE-thinning; Khaan item 9; 2 S128 one-liners; Jebrim cleanup hand-off.
  No live Braindead siblings. Committed by pathspec only; e6e7b78d's + the live Jebrim session's uncommitted tree changes left untouched. No push.

[2026-06-11 18:32] braindead-9fca792a OPEN (post-close follow-up — live WRAPPING UP state)
  S188 closed but Niklavs added a follow-up: the board's WRAPPING UP (closing) state never surfaces — a clean one-turn close writes the closing marker then overwrites it with wrapped_up before any hook fires, so the board jumps straight to WRAPPED UP. Fix: backend reads the <sid8>.mode marker LIVE at poll time (same pattern as the action-heartbeat re-read) so closing shows within one 2s poll.
  Targets: cockpit/backend.py (live mode-marker read in build_session_model), cockpit/test_backend.py (case); respawn/quest-log/comms at close.
  Steering clear of: all player-namespace writes; the live Jebrim session + e6e7b78d residue staged in the shared tree.
  No live Braindead siblings.

[2026-06-11 18:46] braindead-9fca792a CLOSING (S189 — live WRAPPING UP state during session-close)
  Completed: backend now reads the <sid8>.mode marker LIVE per poll so the board shows WRAPPING UP for the DURATION of a close (was: only WRAPPED UP at the end — a one-turn close never fires a hook while the closing marker is set). Live-verified end-to-end (a17168ec walked closing→WRAPPING UP→wrapped_up→WRAPPED UP). Added a staleness guard (mtime ≥ last_event_ts) after the live read exposed 13 lingering crash-exit markers that false-flipped live rows to DONE. Gate: test_backend.py 79/79. backend.py only; cockpit relaunch needed.
  Leaving open: ★NEXT cockpit RELAUNCH verifies the 4 repros (S188 ×3 + this WRAPPING UP). Lingering-.mode-marker GC is a hygiene candidate (guard makes them harmless for display). Standing: e6e7b78d to commit/close; Zezima bootstrap; CORE-thinning; Khaan item 9; 2 S128 one-liners; Jebrim cleanup.
  No live Braindead siblings. Committed by pathspec only; sibling staged files untouched. No push.

[2026-06-11 22:14] braindead-977f8615 OPEN (lesson-store consolidation — design pass)
  Quest: collapse/reconcile the 3 overlapping lesson stores (global keepsake reflexes + jebrim examine/confirmed ~80 + harness MEMORY.md ~80 feedback) into a coherent model with merge-on-write + retire-on-stale. Design-FIRST: ground & quantify the real overlap before any structural change (verify-the-thing — the review was from sampling; contrarian lens says they may serve different read-paths, not be truly redundant).
  Targets (design pass, read-heavy): READ gielinor/keepsake/, players/*/examine/confirmed/, ~/.claude/.../memory/MEMORY.md; WRITE a findings/design note under developer-braindead/bank/ + a tracked task list. Identity-shaped writes (examine/, keepsake/, any new ritual) come AFTER Niklavs sees the design.
  Steering clear of: the e6e7b78d uncommitted residue (adherence-rates.py, hygiene-check.py, knowledge-miss-regression-set.md, domain-cue-reminder.py, grounding-cue-reminder.py, lorebook/_about.md, brain-health-check.md) + the live Jebrim alching pass (players/jebrim/ draft promotions). Both left untouched; commit by pathspec only. Overlap to watch: a retire-on-stale arm may want firing telemetry (adherence/ritual-stats), which e6e7b78d touched — coordinate if so.
  Open to handoff: Zezima bootstrap; ~6.6k CORE-thinning; Khaan item 9; 2 S128 one-liners; Jebrim cleanup hand-off; Health-Index re-score.
  No live Braindead siblings (braindead-a0b39f49 intent is 06-09, CLOSED x4 in comms).

[2026-06-11 22:36] braindead-977f8615 CLOSING (S190 — lesson-store two-funnel bridge: cap + reconcile)
  Completed: reframed the outside review's "3 stores -> collapse to 1" against B-015 (keep both; collapse already rejected) and built the never-built BRIDGE instead. (1) Acute: harness MEMORY.md recompressed 28.3KB->22.3KB under the load-truncation cap, 104 entries preserved (bijection). (2) NEW verification/lesson-store-check.py + test 14/14 (cap/long-line/integrity/examine<->MEMORY drift); live = 28 dupes (7 exact/21 likely, 23 unlinked). (3) Rulebook: bankstanding.md step 8 (reconcile = canonical examine anchor + cross-linked warm MEMORY copy, never merge; retire on graduation/supersession, move->memory/archive/) + alching pointer + layer-routing row (AGENTS.md auto-synced); D-035 recorded. (4) Verified loop: one pair reconciled -> detector flipped UNLINKED->linked (23->22). Build committed 1e2c888; close commits D-035 + quest-log + respawn + build-lessons, by pathspec.
  Leaving open: ~27 remaining dupes backfill at future bankstandings (step 8, not a hand-pass). Review remainder: #4 root-clutter cleanup, #2 accumulation-rate (extend detector to examine pile + in-progress quests), #5 respawn-dialect legibility. Standing: e6e7b78d retrieval-part-2 to commit/close; Zezima bootstrap; ~6.6k CORE-thinning; Khaan item 9; 2 S128 one-liners; Jebrim cleanup; Health-Index re-score; cockpit relaunch verify.
  No live Braindead siblings. Committed by pathspec only; e6e7b78d + the dead Jebrim alching staged residue left untouched. No push.

[2026-06-11] braindead-a248fdd2 OPEN (push brain to a second GitHub remote — ops/advice)
  Entered dev-brain via "lets develop gielinor". Niklavs asks to push the brain to a SECOND repo: https://github.com/nfelsbergs/Brain.git (distinct from origin niklavsfelsbergs/Brain.git). DISCOVERY/ADVICE first — no push without an explicit separate go (commit policy: push is always a separate principal action). Findings so far: target repo returns 'Repository not found' (doesn't exist or not visible to current creds); local main is 194 ahead of origin/main; no secret leak in tracked files (gielinor/.mcp.json empty {}, real .mcp.json + telemetry gitignored). Blocked on Niklavs: create+private the target, confirm intent (mirror vs add-remote).
  Targets IF greenlit: git remote add + git push only — NO file writes to brain content. comms/quest-log/respawn at close.
  Steering clear of: all player WIP, the e6e7b78d retrieval residue + dead Jebrim alching staged in the shared tree (committed by others, not mine to sweep), cockpit/*, experiments/*.
  No live Braindead siblings (comms tail all CLOSED; S190 977f8615 CLOSED 22:36).

[2026-06-11 23:30] braindead-a248fdd2 CLOSING (S191 — push brain to a second GitHub remote)
  Completed: pushed the brain to a second repo nfelsbergs/Brain.git (besides origin niklavsfelsbergs). Pre-flight: no secret leak in tracked files (gielinor/.mcp.json empty {}, real .mcp.json + telemetry gitignored); local main was 194 ahead of stale origin/main. The 404-not-401 catch: first push failed instantly no-popup — GitHub returns 404 (not 401) for a private repo you cannot see, git only prompts on 401, so the cached niklavsfelsbergs identity authed silently → 404; Niklavs confirmed the repo exists in-browser → wrong-account auth not a missing repo. Fixed via username-in-URL (nfelsbergs@github.com) → fresh GCM browser login → push OK (194 commits). Fan-out wired: origin carries two push URLs so one git push origin updates BOTH; main upstream back to origin/main; verified both remotes at 5ba1a65. Build-lesson + a git-ops memory entry captured. Committed by pathspec.
  Leaving open: standing backlog unchanged — e6e7b78d retrieval-part-2 to commit/close; Zezima bootstrap; ~6.6k CORE-thinning; Khaan item 9; 2 S128 one-liners; Jebrim cleanup hand-off; Health-Index re-score; cockpit relaunch verify; ~27 lesson-store dupes backfill at future bankstandings.
  No live Braindead siblings.

[2026-06-11 09:17] braindead-e6e7b78d CLOSING (S192 — retrieval part 2: lorebook cue arm + artifact verify + obedience measured)
  Completed: scorecard 6/2/3 → 8/2/1 (mechanical ceiling realized). (1) [LOR] case 10 — NEW gielinor/lorebook/_index.md (18 decisions mapped; 4 cue-armed) + lorebook arm in domain-cue-reminder.py (rule inlined once/session then silent); hygiene-check Axis C drift guard; drafts-triage/bankstanding add-the-row step; test_lorebook_cue 22/22. (2) [GND] case 5 — ARTIFACT_KEYS proven dead code vs the shipped binary's payload constructor; grounding-cue rebuilt on the real upload placeholders ([Image #N]/[Pasted text #N]); test_grounding_cue 16/16. (3) adherence-rates --obedience: tier-3 obedience = 50% either-signal all-time (shipping 47% n=268, grounding-cue 65% n=68, Reading:-line 15%); snapshot appended; health-check dim 5 rewired. Verified: S187 Jebrim hand-off (a) ran fully (Axis A clean, research 17/18 cited). Close-time catch: SNNN collision (S188 taken; prefix-glob trap) → S192, 30 refs fixed, 2 build-lessons.
  Leaving open: ★NEXT handoff = close-gate continuation false-FAIL fix + MEMORY.md over-limit trim + shipping-digest decision (data: 47% @ n=268). Watch: lorebook-cue live-fire from a real player session; first Fable-5 digest inline (still none). Standing: Zezima bootstrap; ~6.6k CORE-thinning; Khaan item 9; 2 S128 one-liners.
  No live Braindead siblings. Committed by per-file pathspec only; sibling Jebrim working-tree deletions untouched. No push.
