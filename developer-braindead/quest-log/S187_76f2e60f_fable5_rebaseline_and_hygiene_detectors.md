# S187 — 2026-06-11 — Fable-5 re-baseline + hygiene detectors + telemetry rotation

Continued [[S186_486d6682_fresh_audit_fixes_respawn_lock|S186]]'s ★NEXT handoff chain end-to-end on Niklavs's go: (1) re-baseline the retrieval/adherence numbers on Fable 5, (2) verify the parallel Jebrim alching pass, (3) extend the §Z.D detector pattern to the two drift axes the audit flagged, plus the comms-mojibake residual. No live Braindead siblings; no player-namespace writes (sibling `57c7cbaf`'s uncommitted Jebrim promotions left strictly alone).

- **Fable-5 re-baseline — scorecard holds 6/2/3, with receipts.** [[knowledge-miss-regression-set|verification/knowledge-miss-regression-set.md]] re-scored: tally unchanged *by construction* (the scorers are Python hooks outside the model), so the re-baseline's real content is mechanism re-verification — all 7 boundary suites green under the current harness, and the scorers observed firing on **real Fable-5 sessions** in `ritual-events.ndjson` (`[FRD]` player-inject + §Z.B domain-index on the parallel alching session `c5915b03` and `907d4e63`; `[GND]` on S187's own opening prompt, obeyed n=1; draft-gate `bypass-ritual` mid-alching; require-open allow+block). **Case 11's "live-fire from a real SCM prompt" hand-off found already landed** (2026-06-09, `domain-cue:domain-scm inline` on `85b0fcc3`) — row upgraded ✓✓. Re-score log entry appended; honest residuals named (no digest *inline* inside the Fable-5 window yet; tier-3 obedience still unmeasured beyond n=1).
- **Adherence snapshot — the ~72% OPEN claim survives a scare.** `adherence-rates.py --snapshot` ×2 (all-time + 7d). Raw 7d read **47%** vs the quoted ~72% — decomposed by day before calling it drift: the dip is the 06-09 cluster (9 allow / 18 block), the exact day the [[S184_a0b39f49_actor_resolution_intent_first_and_zd_domain_tending|S184]] actor-resolution bug was live false-blocking Jebrim sessions; post-fix days run 67–100%. Docs' dated ~72% claims left intact; genuinely stale numbers trued ([[brain-health-check|spellbook/brain-health-check.md]] still carried the pre-S182 5/2/3 baseline → 6/2/3).
- **Eval-hygiene defect found + root-fixed: the boundary suites polluted the LIVE analytics stream.** ~180 synthetic test-fixture events (sids `aaaa1111`/`scmone00`/`zv_*`/`deadbe99` — several hex-shaped, no regex separates them) had accumulated since 06-02; **78% of the raw draft-redirect count was test fixtures** (18→4 real). Fixes: [[ritual_log|switchboard/ritual_log.py]] honors `RITUAL_EVENTS_PATH` per call; 6 emitting suites redirected to a temp file (verified the verified-thing way: suites green, live log line-count unchanged); `adherence-rates.py` gained the synthetic-sid denylist filter (default on, `--include-synthetic` for raw); historical pollution scrubbed (180 lines → `switchboard/archive/`).
- **Post-alching verify (read-only): half landed, half didn't.** The S193 alch (`937419b`) DID promote 22 drafts + re-synthesize eu-tender — `domain-coverage.py`: **0 stale digests**. It did NOT pick research (**16/17 research files uncited from bank** — retrieval odds still ~0) and did NOT graduate quests (**30 in-progress**, 11 dwarf traces; **33 inventory resumes** — new finding). Handed off as a Jebrim pass.
- **§Z.D pattern extended — NEW [[hygiene-check|verification/hygiene-check.py]]** (+ `test_hygiene_check.py` **11/11**): Axis A per-player active-state drift (in-progress cap 10 + 14d staleness + inventory cap 8), Axis B telemetry boundedness (sweep-not-holding ×1.25, dead root `*.log`s, synthetic-sid reappearance via the shared denylist, tmp litter). Wired into `brain-health-check.md` dimension 7. Live run: telemetry clean, Jebrim 30/33 flagged.
- **Telemetry rotation + two zombie kills.** `state.ndjson` had **no sweep at all** (7.06 MB) — `sweep_state_ndjson()` added to BOTH writers ([[emit-event|developer-braindead/.claude/hooks/emit-event.py]] SessionEnd + [[status-sidecar|developer-braindead/.claude/hooks/status-sidecar.py]] per-prompt; the [[S178_b472d014_cockpit_wrappedup_interrupt_transcript_pilljump|S178]] fix-the-class discipline), one-time rotation 31,849→6,000 lines; 3 dead diag logs (`term-fit` 1.3 MB, `term-size`, `rename`) moved to `switchboard/archive/` (tracked moves, archive-discipline); `.gitignore` covers archived `*.ndjson`. **Zombie writer shot:** `.git/hooks/post-commit` still fed the PARKED old visualizer experiment's *tracked* `state.ndjson` on every commit (+280 uncommitted lines) — hook disabled (original preserved in-comment), file reverted to HEAD.
- **Mojibake residual closed: 238 lines healed, 0 signatures left.** The [[S186_486d6682_fresh_audit_fixes_respawn_lock|S186]] reversible roundtrip (`encode("cp1252").decode("utf-8")`, signature-gated, reduction-verified) applied across both brains' comms archives + `gielinor/comms/active.md` (live files re-read fresh under the comms lock).

**Cascade.** NEW `verification/hygiene-check.py` + `verification/test_hygiene_check.py`; `verification/knowledge-miss-regression-set.md` (re-score log + case-11 row), `verification/adherence-rates.py` (synthetic filter), 6 test suites (`RITUAL_EVENTS_PATH` redirect), `spellbook/brain-health-check.md` (baselines + instruments + dim 7), `.claude/hooks/emit-event.py` + `.claude/hooks/status-sidecar.py` (state sweep), `bank/build-lessons.md` (+1), `comms/active.md` (OPEN/UPDATE/CLOSING) + 4 comms archives healed, this quest entry, `respawn.md` via the tool. Root: `switchboard/ritual_log.py`, `.gitignore`, switchboard diag-log moves, `switchboard/adherence-snapshots.ndjson` (+2, gitignored), local `.git/hooks/post-commit` disabled.

**Main-brain changes.** `gielinor/comms/active.md` + 3 `gielinor/comms/archive/` files (mojibake heal only — no content change beyond byte repair). No rulebook, hook, identity, or player-namespace writes.
