Server-dying disease cured: server.py now runs detached via a hidden VBS launcher + a Startup-folder shortcut — survives every terminal close, auto-starts at logon (PID 46312, orphaned tree). And clicking a live VS Code session is now a read-only peek, not a hijack — fixes the silent dropped message. terminal.js + CSS green. New agents: start them in-app, where the fresh path works.