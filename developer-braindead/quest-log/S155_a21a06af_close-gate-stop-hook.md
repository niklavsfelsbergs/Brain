# S155 — 2026-06-02 — §X.2 Stop-hook close-ritual gate

- Built plan [[plan|§X.2]] — the **Stop-event close gate**, layer 2 of the [[S115_a9fe4749_khaan-failure-receipts-and-hygiene|S115]] close-enforcement model (layer 1 = in-session quest-log+OPEN; layer 3 = the existing [[session-end-safety-net|gielinor/.claude/hooks/session-end-safety-net.py]] SessionEnd auto-CLOSING stub). It turns the discipline-run [[close_check|developer-braindead/verification/close_check.py]] (a markdown ritual step the agent CAN skip — the [[S115_a9fe4749_khaan-failure-receipts-and-hygiene|S115]] premature-done-claim) into a mechanical gate: a **claimed** close can't pass while gaps remain. Precondition was the [[S154_0526dd9d_close-check-continuation-lineage-fix|S154]] continuation-lineage fix, now cleared.
- **The design seam (Niklavs-flagged):** a Stop fires on **every** turn-end, not just close — so the gate engages **only** on the `wrapped_up` close-claim marker (NOT every Stop, NOT mid-wrap `closing`, NOT a flavor tag). This makes false-blocking of in-flight work **structurally impossible** — every ordinary turn-end passes straight through. On a `wrapped_up` claim it runs `close_check --ritual {dev,player}`; PASS → allow, FAIL → block.
- **Severity (Niklavs multiple-choice = "block w/ loop-guard"):** a `wrapped_up`-claim FAIL **blocks** the Stop (exit 2, gaps on stderr) so the agent fixes + re-claims; **degrades to warn** (exit 0) when `stop_hook_active` is set, so it can never trap a session in an infinite continue-loop on a gap it can't clear (e.g. a sibling's uncommitted shared-lineage file in this parallel tree — observed **live** on `b82b0b90`, whose close FAILs on an uncommitted `S124` file owned by another live session).
- **Two decisions caught BY reproducing firing** (exactly the "reproduce before wiring hard" intent): (1) **ritual selection** uses **per-session** signals only (`braindead-<sid8>.txt` intent anchor / `braindead-<sid8>` in dev comms → dev, else player) — I **dropped** an `active-mode.txt` check after realizing it's **global**, so a player session closing in parallel with a live dev session would misclassify as dev and spuriously block. (2) **marker read from BOTH intent dirs** (root + `gielinor/.claude/intent`) since the `.mode` location scatters with CWD. `resolve_actor` was unusable as the discriminator (returns `"unknown"` for dev sessions — the [[S149_c6985ff9_reading-preamble-line-and-import-trim-stage-d|S149]] truthy-short-circuit bug).
- **Verified:** `py_compile` clean; boundary harness [[test_close_gate_stop|developer-braindead/verification/test_close_gate_stop.py]] **26/26** (the seam — most Stops invisible / no close_check run; block·pass·degrade severity; per-session ritual + parallel-safety; marker scatter+freshest-wins; fail-open on unparseable/wrong-event/empty-sid/missing-close_check; real `close_check` subprocess — PASS-direction proven by the monkeypatched case, FAIL-direction by the live `b82b0b90` exit-1). **Live-fired through the real hook binary:** mid-work (no marker) → exit 0 silent; `wrapped_up`+FAIL → exit 2 BLOCKED; +`stop_hook_active` → exit 0 warn. Printed stderr kept ASCII (the Windows-console lesson `close_check` already follows). **Scope held:** `close_check.py` consumed unchanged; verification/ + hook + settings.json only; no player WIP, no cockpit.
- **Open / hand-off:** the standing **live-fire from a real close** — a genuine `wrapped_up` write confirming the root Stop hook engages in-anger + board behavior on a block (the harness + binary reproduction prove logic and wiring; the in-anger fire is the verify item). The §X retrieval/write-discipline/trim/telemetry/Stop-gate half is now all landed; remaining §X items (X.6 critic gnome, X.8 mem0-reconcile, X.9 two-bucket audit) lean on principal/ritual surfaces. Standing live-fire debt X.3/X.4/domain-cue still needs a real player session.

**Cascade.** New `gielinor/.claude/hooks/close-gate-stop.py`; new `developer-braindead/verification/test_close_gate_stop.py`; `.claude/settings.json` (root `Stop` array += close-gate-stop, first in group; `_comment_close_gate` doc); `developer-braindead/bank/plan.md` §X.2 → `[x]`; `respawn.md`; `bank/build-lessons.md`; this quest-log entry.

**Main-brain changes.** `gielinor/.claude/hooks/close-gate-stop.py` (new hook) + `.claude/settings.json` registration — the gate is a gielinor hook fired at brain root. No `gielinor/` content/identity/ritual-prose writes (the gate consumes `close_check` and the `.mode` lifecycle unchanged).
