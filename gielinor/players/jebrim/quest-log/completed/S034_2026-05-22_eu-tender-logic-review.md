# S034 — EU tender logic review

**Opened:** 2026-05-22
**Player:** Jebrim
**Trigger:** "hey jebrim. Lets review the eu tender" → followed by "Logic review. Do the actual calculations make sense. And then what assumptions are wrong or missed and not considered."
**Scope:** `Documents/GitHub/bi-analytics-main/NFE/projects/2_EU_tender_2026/2_analysis/`

## Goal

Surface logic errors and assumption gaps in the EU tender Phase 2 analysis. Two passes:
1. **Calculations** — do the cost math, capability rules, scoring add up correctly.
2. **Assumptions** — what's treated as given that isn't, or isn't even acknowledged.

Findings group by severity: calc bug → wrong assumption → missing assumption → soft.

## Plan

- Read tender docs (DECISIONS, ASSUMPTIONS, DATA_NOTES, SANITY_CHECK_2026-05-13, NEXT, PLAN, REPORT_NOTES).
- Read shared infra: `pipeline.py`, `capability.py`, `cost_matrix.py`, `_base/`, `decision_scorer.py`, `scenarios.py`, `invoice_adjustments.py`.
- Per-carrier rate engine pass: maersk, austrian_post, gls, guell, hermes, dhl_express, dhl_paket, fedex, dpd_pl. (UPS pending offer; Hermes pending too per bank note — check.)
- Audit implicit assumptions against what's been written down.
- Write findings here, group by severity, cite file:line.

## Turn log

- 2026-05-22 — opened quest; tasks created.
- 2026-05-22 — read NEXT, DECISIONS (truncated at 474), ASSUMPTIONS, DATA_NOTES, SANITY_CHECK_2026-05-13, PLAN (truncated at 220), OPEN_QUESTIONS (truncated at 220). Confirmed: prior 8-agent sanity check on 2026-05-13 covered the original 6 engines; since then DPD PL + FedEx engines shipped 2026-05-20, 7 carriers got Round 1 dispatches, AP multi-service reversal locked, Maersk reply set landed 2026-05-18 closing 5 items.
- 2026-05-22 — user picked parallel-dwarves; 7 dwarves spawned in flight:
  - D1 Maersk engine logic audit
  - D2 DHL Paket + DHL Express audit
  - D3 GLS + Güll engine audit
  - D4 Austrian Post + Hermes audit
  - D5 DPD PL + FedEx audit (newest, high priority)
  - D6 shared infra audit (pipeline / capability / cost_matrix / _base / invoice_adjustments / SQL)
  - D7 decision scorer + cross-cutting implicit-assumption audit
- 2026-05-22 — awaiting dwarf returns; synthesis pending.
- 2026-05-22 — **D1 Maersk back.** Headline findings: (1) IT oversize trigger encoded as `max_height=170` but compared against `d_min` (unreachable) + null surcharge → silent over-reject; (2) overpack 0.40 always-on assumption depends on Maersk doing sorting — no signal in code for the in-house cutover; (3) engine still at fuel 10% / no AT-DE-DK Tolls despite 2026-05-18 decision — net ~EUR 175k Q1 stale; (4) latent SE/UK duplicate rows in eu_oversize.parquet, no dedupe guard; (5) last-mile vendor mapping load-bearing but undocumented in ASSUMPTIONS.md.
- 2026-05-22 — **D4 Austrian Post + Hermes back.** Major theme: doc drift. Three HIGH severity: (1) AP CH customs 1.00 EUR `ch_customs.py` module **doesn't exist** despite 2026-05-18 DECISIONS entry — ~EUR 33k Q1 under-priced; (2) AP multi-service expansion to all 4 AT services + cheapest-eligible picker **not shipped** — engine still `austrian_post-1.0.0` with HD-only; AT lane over-priced ~3%; (3) AP line-haul module **not implemented** — constants don't exist in code; ~EUR 187k / 6-mo unmodelled. PLAN §B.7.d marks open but DECISIONS+ASSUMPTIONS contradict PLAN. MED: rate lookup keys on `weight_kg` not `billable_weight_kg` in BOTH AP and Hermes — latent until vol-weight flip then would silently negate the effect. Hermes engine otherwise faithful to its provisional v1 framing.
- 2026-05-22 — User said "tackle them all". Launched 8 fix dwarves: F1 Maersk oversize, F2 eligible gate, F3 billable fixtures, F4 bias_table refresh, F5 ASSUMPTIONS additions, F6 scorer wiring, F7 doc rollback, F8 freight drop.
- 2026-05-22 — **F5 back.** 6 cross-cutting assumptions appended to ASSUMPTIONS.md. Key finding: population.sql is **trackingnumber-grain** (not shipment-grain), so `decision_scorer.py:58 .unique(subset="shipment_id")` silently drops non-first parcels on multi-parcel shipments. Magnitude unaudited but the scorer-vs-mart inconsistency is real. Picanova mix is mostly single-parcel so tail plausibly small.
- 2026-05-22 — **F8 back.** Parcel+freight dual scoring axis dropped from docs. CLAUDE.md Decision Framework, DECISIONS.md 2026-05-22 entry, PLAN.md annotation. Verified `decision_scorer.py` has no `freight_variant` — premise holds, axis never built.
- 2026-05-22 — **F4 back.** bias_table.md refreshed for 9-engine matrix. Headlines: Hermes 0.588 (unchanged), AP 0.701, Güll 0.750, Maersk 0.849, DHL Paket 1.020, GLS 1.276, DHL Express 1.956 (residual noise on 420 parcels), **DPD PL full-eligibility 2.957** (new offer ~3× invoice; confirms retire-only stance), **FedEx full-eligibility 1.732** (tail bidder). 7-engine winning-slice picture identical to 2026-05-15 (DPD PL + FedEx invisible to scorer per D7 — full-eligibility slice used with caveat). Portfolio bias 0.889.
- 2026-05-22 — **F3 back.** AP+Hermes regression fixtures landed at `tests/test_f3_billable_lookup.py` in each engine. Forced mode="max" tests fail with explicit `S034 F3 REGRESSION` messages — that's the regression signal that flips when vol-weight lands. Comments inserted at calculate.py rate-lookup sites. Surprises: `add_chargeable_weight` only accepts `{"gross", "max"}`; `__init__.py` re-export shadows the submodule (monkeypatch needs importlib). Pre-existing fixtures still pass.
- 2026-05-22 — **F7 back.** Doc rollback complete. Engine state confirmed: `maersk-2.2.0` still on fuel 10%/no tolls; `austrian_post-1.0.0` still HD-only/no CH customs/no line-haul. Additive status headers (no deletes) on 5 DECISIONS entries + 2 ASSUMPTIONS section headers + 3 PLAN.md STATUS NOTE blockquotes. New top entry in DECISIONS summarising the F7 audit. Docs now honest about engine state.
- 2026-05-22 — **F1 back.** Correction to my D1 framing: the IT trigger encoding was actually correct — engine convention treats "Max" column as the surcharge trigger band, not a hard cap (consistent with AT/175→23.20, BG/175→12.77, DE/120→21.00). The bug was purely the null `oversize_surcharge_eur` value. Patched 5 lanes from offer text: BE/LU/FR ColisPrive 6.10, IT GLS 2.00, ES PAACK 1.00. CH stays null (hard reject is contract). UK Evri + IE ANPOST left null (volume-tiered mechanic; out of EU_COUNTRY_NAME anyway). +EUR 37,543 Q1; 3,400 parcels recovered from oversize_no_surcharge reject. 14/14 fixtures pass. New ES fixture added. Cost matrix rebuilt. Three carrier-facing Qs folded into Q-DBS-1: ES tier axis, IT band semantics, CH intent.
- 2026-05-22 — **F2 back.** Eligible gate added to `_base/pipeline.py` (_apply_single + _apply_exclusive_group). Surprise: bug was **latent** — current always-on surcharges already self-gate via `pl.col("base_rate_eur").is_not_null()` which is nulled on rejects, so totals are identical pre/post (within float epsilon). Fix is defensive — moves guard from per-surcharge convention into pipeline contract. 0.0 (not NULL) on ineligible rows; matches existing fixture defaults. 166 carrier fixtures across 9 engines, zero regressions. `sink_parquet` OOM'd on cost_matrix rebuild — totals verified in-memory; on-disk parquet stays as pre-fix artefact (numerically identical to fixed version). Two ad-hoc verify scripts left at `_s034_f2_verify_*.py` — pruning is a principal call.
- 2026-05-22 — **F6 back.** Scorer wired to 9 engines. FedEx added to CARRIERS + NEW_ENTRANTS; DPD PL flipped to full 3-state and joined RENEWABLES; NON_RENEWABLE_INCUMBENTS trimmed to {ups, db_schenker}. cross_carrier_view + report driven by `_decision_sets.CARRIERS`; palette/labels include Hermes/DPD PL/FedEx. 90 decision sets (was 35). **New top-5 cap6 portfolios:** (1) `add_hermes` EUR 397k, (2) **`renew_maersk_plus_fedex` EUR 377k — new lever, didn't exist before**, (3) `renew_maersk_plus_hermes` EUR 370k, (4) `all_renewals_plus_fedex` EUR 340k (new), (5) `all_renewals_plus_hermes` EUR 340k. **`renew_dpd_pl` standalone = -EUR 416k mandatory** — confirms F4 bias finding; do not sign DPD PL new offer alone. FedEx joins GLS as second unlock for DPD PL retirement. Greedy 1→9: hermes → fedex (EUR 739k at 99.84% coverage in 2 picks). Flagged not-done: bias_table.md still 7-engine; CARRIER_NARRATIVE missing FedEx prose; enumeration cartesian-growth open.
- 2026-05-22 — **All 8 fixes shipped. Summary delivered to principal.**
- 2026-05-22 — **Close-session.** No pending external actions. Dwarf + fix children (S034_d1..d7, S034_f1..f8) moved to `quest-log/completed/`. Parent stays in-progress — 5 explicit follow-ups captured in `inventory/eu-tender-logic-review-resume.md`. Harvest: 2 drafts (bank delta on eu_tender_2026; examine on audit-finding-vs-ground-truth). No commit yet — pending pre-commit soft-block triage on other pre-existing in-progress quests ([[S001_2026-05-20_repo-orientation|S001]]/[[S015_2026-05-21_ttyd-review-and-dry-run|S015]]/[[S023_2026-05-21_shipping-mart-coverage-audit|S023]]/[[S024_2026-05-21_shipping-agent-rulebook-revamp|S024]]/[[S026_2026-05-22_shipping-costs-nextjs-architecture-read|S026]]/[[S031_2026-05-22_temp-tracking-missing-orderitems|S031]]/[[S032_2026-05-22_bi-etl-shipping-mart-harvest|S032]]/[[S033_2026-05-22_shipping-agent-audit|S033]]) that lack matching resume files; those are not this session's to fix.
- 2026-05-22 — **Alching pass.** All 8 pending drafts parked (no promotions/rejections). last-alched.md updated.
- 2026-05-22 — **Strategic assessment delivered.** Principal asked "will the project produce a good decision?" — answered with bottom-line: credible path conditional on three items landing before signing (enough Round 1 replies, service-quality sidecar, volume-tier rerate). Biggest risk: provisional numbers getting locked.
- 2026-05-22 — **Keepsake pinned.** Bottom-line assessment pinned to `keepsake/current.md` so it surfaces at every Jebrim respawn until v2 ships. Proposal archived to `keepsake/archive/proposals/`.
- 2026-05-22 — **Second close.** Commit pending; session ends.
- 2026-05-22 — **D6 shared infra back.** Architecture-drift theme: ontrac pattern won; contract-layer didn't. HIGH: (1) Surcharges fire on rejected rows — `_base/pipeline.py:33-44, 67-86` evaluates conditions() without `eligible` gate; always-on surcharges populate `cost_<name>` on rejects too; `cost_total_eur` masked by `_finalize` so headline OK, but any component-level cross-engine aggregation (bias table) over-counts. (2) `capability.evaluate()` is **dead code** across all 9 engines — REGISTRY never consulted; engines re-implement eligibility inline; CLI build_matrix uses divisor-5000 universally (wrong for GLS EBP @ 6000). (3) Forward-asof returns null silently above max band — no validator that `MAX_WEIGHT_KG ≤ largest rate-card band`. MED: cost matrix has no `components_json` despite CLAUDE.md promising it; engines emit their own `cost_<surcharge>` columns; downstream consumers need per-engine knowledge. DEPENDENT-phase confirmed dead on all 9 engines.
- 2026-05-22 — **D2 DHL Paket + DHL Express back.** Both engines clean — no catastrophic calc bugs. Kleinpaket (1.1.0) + fuel-aware service-select (1.1.0) both correct. Findings: (1) MED DHL Paket — `_attach_de_rate` has undocumented re-sort at line 218 (output row order != input order); cost numbers unaffected. (2) MED DHL Express service-selection tie-break uses `<=` (ES wins ties) while DHL Paket Intl uses `<=` (Premium wins ties) — direction inconsistent across engines; no practical impact. (3) MED cross-carrier: neither DHL models €0.49 Routing Code fee; open-question flags it for DHL Paket (worst case EUR 254k Q1 if always-on, realistic <EUR 3k); DHL Express analogous concepts have no module. LOW items: rejection-reason semantics, doc gap on overweight-vs-nonconveyable using different weight bases.
- 2026-05-22 — **D5 DPD PL + FedEx back.** No HIGH severity calc bugs — walkthrough docs `open_questions/{dpd_pl,fedex}.md` already exhaustive on contract-vs-engine drift. Findings: (1) MED doc drift — DPD PL `constants.py:71-72` comment still says NonSortable "two smaller dims" but `non_sortable.py:30-33` correctly uses footprint reading (d_max AND d_mid); one-line comment fix; (2) LOW/MED v2 watch — FedEx service-selection uses gross `weight_kg > 68` not billable; currently fine under WA#1 gross-only but breaks when Q5 vol-weight reply lands. Same posture on freight base + forward-asof. Approach B for AHS-Dim 18 kg lift validates clean; NonSortable footprint reinterpretation mathematically justified (31.5 kg vol cap makes literal reading unreachable). Newest engines are unexpectedly clean.
- 2026-05-22 — **D3 GLS + Güll back.** Lighter — most contract-vs-engine drift already exhausted in 2026-05-18/19 dispatch reviews; this pass focused on engine-code shape. HIGH: GLS airfreight EBP over-rejects on dim weight — `calculate.py:250-256` rejects `billable_weight_kg > 40` but `_supplement` returns uncapped airfreight billable (`max(gross, dim)`), so 5kg / 60kg-dim parcel rejects as over-weight even though rate card has top band that would price it. Sub-EUR 5k Q1 but clean code fix. MED: Güll `AT_MAX_GIRTH_CM` interpretation ambiguous between L+girth and girth-alone; GLS enforces L+girth ≤ 300 universally where GTC scopes it to >30 kg only (coverage-loss). LOW: Güll FX bound at two sites — two-touch change for any per-date implementation.
- 2026-05-22 — **D7 scorer + cross-cutting back.** Structural gaps. HIGH: (1) FedEx + DPD PL **invisible to decision sets** — FedEx absent from `_decision_sets.py CARRIERS` entirely; DPD PL still `{INCUMBENT, OFF}`-only despite 2026-05-20 reopening as normal candidate. Both engines live in cost_matrix but the scorer can't see them. (2) `cross_carrier_view.py:67-76` + `report.py` **hardcoded to original 6 carriers** — Hermes/DPD PL/FedEx absent from greedy path, pair-coverage, bias table; per-carrier narrative cards exist for Hermes but not DPD PL or FedEx. (3) "parcel-only vs parcel+freight" dual-scoring locked 2026-05-12 is **silently unimplemented** — no `freight_variant` in scorer, DB Schenker frozen INCUMBENT. MED: per-carrier `report.py` doesn't apply `invoice_adjustments` — numbers don't reconcile across artefacts on UPS-overlap cells. Cross-cutting: 18-country `IN(...)` filter in population.sql is invisible to ASSUMPTIONS.md (Hermes/DPD PL/FedEx have wider coverage); cost-only-scoring + qualitative-exclusion not in ASSUMPTIONS; 2026 Q1 replay is transition-state not steady-state; bias_table.md 7 days + 4 engine changes stale.
