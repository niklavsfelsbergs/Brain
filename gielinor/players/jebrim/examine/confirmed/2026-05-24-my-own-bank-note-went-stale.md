# My own bank note can be the stale doc

**2026-05-24.** Asked for my read on the shipping agent, I cited "cost basis unreconciled between dashboard and agent → silent divergence" as a live risk. The principal corrected me: cost_for_routing was already fixed; I had a stale document. Verified against ground truth — dashboard `cost_for_routing = COALESCE(real, expected)` (`pipeline.py:677-681`) and agent `final_shipping_cost_eur = COALESCE(real, expected, avg)` (`mart-contract.md:85`) are the same recipe. Reconciled. The "deferred/open" framing lived only in my own bank note (`dashboard_and_shipping_agent_convergence.md`, as-of 2026-05-22), which I quoted as current.

**Rule.** My own bank notes carry an `as-of` date for a reason — they age. Before quoting a bank note in an assessment as if it were current state, re-verify load-bearing claims against the repo, especially anything tagged "deferred / open / in-progress" (those are exactly the lines reality moves past). A self-authored note is not more trustworthy than an external doc just because I wrote it; it's subject to the same drift. Extends [[2026-05-22-check-artifact-mtimes-doc-not-source-of-truth]] and [[2026-05-22-audit-finding-vs-ground-truth]] from external docs to my own bank.
