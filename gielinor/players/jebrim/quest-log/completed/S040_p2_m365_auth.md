# [[S040_1cf1eb75_outlook-mcp-research|S040]] / p2 — M365 corp-tenant auth for MCP

**Role:** penguin (research).
**Topic:** Microsoft 365 corporate-tenant authentication substrate for an Outlook MCP server (mail + calendar, full read+send+search scope). Account is tcgroup.com (corporate work account).
**Sibling deliverable:** `players/jebrim/research/2026-05-23-m365-corp-tenant-auth-for-mcp.md`.

## Turn log

- T1 — Read research-skill spec (`spellbook/skills/research.md`) and penguin write boundary (`meta/modes.md`). Confirmed scope: write to `research/` and this quest-log file only.
- T2 — Broad scout planned across five clusters: (a) MS Graph auth-flow taxonomy, (b) Outlook-MCP scope inventory, (c) admin-consent requirements per scope, (d) corporate CA / token-lifetime gotchas, (e) Azure AD app-registration recipe. Running parallel WebSearch.
- T3 — Source map built; pulled primaries: Microsoft Learn `graph/auth/auth-concepts`, `entra/identity-platform/msal-authentication-flows`, `graph/permissions-reference`, `entra/identity/enterprise-apps/user-admin-consent-overview`, `entra/identity-platform/configurable-token-lifetimes`, `graph/auth-register-app-v2`. Corroborated `Mail.Send` no-admin-consent via merill.net. Surfaced MC1097272 (user-consent default flipped to disabled, mid-Jul → Aug 2025) via AppGov Score. Surfaced Microsoft-managed CA policy blocking device-code flow (Feb-May 2025 rollout, triggered by STORM-2372).
- T4 — Confirmed: `Users can register applications` defaults to Yes (Entra default user permissions doc); confirmed device-code-block CA policy is the Microsoft-managed flavor and trends widely-deployed at corporate tenants. Picked auth-code+PKCE over device-code as the recommended shape.
- T5 — Drafted `research/2026-05-23-m365-corp-tenant-auth-for-mcp.md` with sections Question, Date, Confidence (high on flow+scopes, medium on tcgroup-specific outcomes), Sources (11 anchors), Findings (5 sub-questions), Gaps (5 open items — tcgroup-specific tenant settings, CA policy specifics, MFA cadence, app-management policy block possibility, device-code-block deployment at tcgroup).
- T6 — Returning short report to principal: recommended auth-code+PKCE on `http://localhost`, scope list, "no admin consent required" per-scope but MC1097272 likely means user-consent is disabled tenant-wide so admin-consent-workflow ticket is the realistic path, no blocker that should change the P1 recommendation. Quest closes here.
