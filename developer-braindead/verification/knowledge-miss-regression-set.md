# Knowledge-miss regression set — Phase 0 eval

**Purpose.** A fixed set of *real* cases where the brain HAD relevant knowledge (or a rule) on disk and failed to load/apply it. This is the pass/fail bar for the anti-deterioration plan ([[S145_543c6caf_knowledge_loading_and_rule_adherence_audit|S145]], `plan.md` §X): every later fix must move coverage here, and no fix may regress a case already covered. Built 2026-06-02; cases drawn from the S125/[[S145_543c6caf_knowledge_loading_and_rule_adherence_audit|S145]] audits, the quest-log, and confirmed memory.

**How to score a case.** For each, ask: *given the current live mechanisms, would the right knowledge have been forced into view before the agent acted?* `caught` = a live hook/gate fires on it; `partial` = fires only under a narrower condition (e.g. a continuation cue that may be absent); `miss` = nothing fires.

**Live mechanisms today (the scorers):**
- `grounding-cue` — UserPromptSubmit nudge on continuation cues / artifacts (own past work). Artifact-detection is UNVERIFIED.
- `domain-cue` — UserPromptSubmit nudge on a registered domain's topic cues (external knowledge home). Two rows live: **shipping** + **deploy-schema** (the latter added §X.3 session — closes cases 8/9; EU-tender/FIF/SCM beyond it remain a commented stub).
- `forced-read` — **NEW ([[S150_26ef6430_session-start-forced-read|S150]], §X.4): `keepsake-forced-read.py`.** SessionStart inlines the GLOBAL keepsake + a forcing directive (read the active keepsake + relevant resume; treat "layer empty" as a hypothesis to verify); the first UserPromptSubmit **hard-inlines the active player's `keepsake/current.md` contents** into context (a guarantee, not a name — the enforced pairing for the `Reading:` line). This is the first mechanism that *forces knowledge into view*, not just nudges toward its home. Boundary-verified 19/19; live-fire (a real player session start) is the hand-off.
- `draft-gate` — **NEW (§X.3, this session): `draft-gate-rewrite.py`.** PreToolUse input-rewrite that auto-redirects a main-agent `bank/notes/`→`bank/drafts/notes/` and `spellbook/skills/`→`spellbook/drafts/skills/` write (turning the two guided-only draft gates into guarantees). **Note its axis:** this is a *write-discipline* guarantee (rule-adherence), **not** a knowledge-*retrieval* mechanism — so it does **not** appear in the "Caught by" column of this scorecard, which measures retrieval misses. It belongs to [[S145_543c6caf_knowledge_loading_and_rule_adherence_audit|S145]]'s *other* diagnostic half (decided-rules-drift → hook the bright lines); its payoff shows on the adherence axis, not here. Listed for completeness, scored nowhere.
- *(no Stop-gate yet; lorebook decisions still not surfaced in player turns — `[LOR]`.)*

**Plan mechanisms that would catch the misses (the targets):**
- `[DOM]` domain-cue registry row (Phase 2, cheap — one dict)
- `[GND]` grounding-cue (exists; widen/verify artifact path)
- `[FRD]` SessionStart forced-read of keepsake + active resume (Phase 2)
- `[LOR]` load applicable lorebook decisions in player sessions (Phase 1/2)
- `[CRIT]` critic/verify step — judgment rule, NOT hookable (Phase 2 #6 / discipline)

---

## Coverage scorecard

| # | Case (session) | Knowledge that existed | Today | Caught by |
|---|---|---|---|---|
| 1 | Shipping report from memory (S124) | `shipping-agent/reference/{mart-contract,tables}.md` | **caught** ([[S144_9b67aceb_domain_grounding_cue_registry|S144]] domain-cue; fired 14× in 30d — obedience unmeasured) | `[DOM]` ✓ |
| 2 | "cost NULL = load in flight" misread as quality gap ([[S091_remove_leftover_windows_toast_notifier|S091]]) | jebrim `bank/notes/...quality-assessment` | **caught** ([[S150_26ef6430_session-start-forced-read|S150]] `[FRD]` hard-inlines the jebrim keepsake, which lists `cost_source` NULL/uncosted 8% as a normal category; + live `[DOM]` shipping) | `[FRD]`+`[DOM]` ✓ |
| 3 | Property analyzed as "new/third" ([[S066_7f5db8c5_cockpit-sweep|S066]]) | zezima `research/`+`bank` on [[S056_e433ac17_switchboard-osrs-chatbox|S056]] apartments | partial (needs a continuation cue) | `[GND]` |
| 4 | "the message I prepared" grepped in repo, was in bank ([[S076_322cb5c3_cockpit-vscode-rename-and-focus|S076]]) | jebrim `bank/notes/` onboarding msg | **caught** (grounding-cue "prepared") | `[GND]` ✓ |
| 5 | Uploaded folio re-derived cold ([[S095_f9310a45_cockpit-transcript-full-width-speech-bubble|S095]]) | zezima `research/` [[S066_7f5db8c5_cockpit-sweep|S066]] deep-dive | **miss** (artifact-detect unverified) | `[GND]`(artifact) |
| 6 | Claimed a pipeline behavior that was fiction ([[S113_b072b8df_bankstander-question-and-godly-proposal-flow|S113]]) | the corpus / git record | **miss** | `[CRIT]` |
| 7 | Scout said keepsake empty; repeated unverified ([[S145_543c6caf_knowledge_loading_and_rule_adherence_audit|S145]], this session) | the actual `keepsake/` files | **partial** ([[S150_26ef6430_session-start-forced-read|S150]] `[FRD]` hard-inlines the keepsake into the *main* agent + directs "verify empty as a hypothesis"; the scout sub-agent that *originated* the false claim is unreached — residual judgment) | `[FRD]`+`[CRIT]` |
| 8 | Schema-add filtered views 500'd ([[S098_b53fca39_obsidian_fit_and_dlink_migration|S098]]) | jebrim `bank/notes` schema-add ordering | **caught** (§X.3 session: the `deploy-schema` `[DOM]` row fires on deploy/schema/column cues → points at `scm_alerts_entity_split.md` + the regenerate-before-serve rule) | `[DOM]` ✓ |
| 9 | FIF deploy-critical config under blanket ignore (S143) | jebrim `bank/notes` FIF lookup | **caught** (same `deploy-schema` row: `FIF`/`gitignore` cues → the FIF quirks note + "gitignored config is a defect" rule) | `[DOM]` ✓ |
| 10 | Applicable lorebook decision unread in a player turn (general) | `lorebook/confirmed/D-NNN` (~30, never loaded) | **miss** | `[LOR]` |

**Today: 5 caught / 2 partial / 3 miss (of 10).** (§X.3 session — the `deploy-schema` `[DOM]` row moved **cases 8 + 9 miss→caught**, both deploy-time hazards now pointed at jebrim's deploy bank notes by topic cue. Caught: 1, 2, 4, 8, 9. Partial: 3, 7. Miss: 5, 6, 10.)
**Count-correction (this session):** the prior summary read "3 caught / 3 partial / 4 miss" but the per-row table only ever carried 3/**2**/5 (caught 1/2/4, partial 3/7, miss 5/6/8/9/10) — an off-by-one slip introduced at the [[S150_26ef6430_session-start-forced-read|S150]] re-score (it counted case 5 or 7 as a third partial; the row + its detail both say `miss`/`partial` as tabulated). Corrected here against the per-row truth before applying the §X.3 moves, so the 5/2/3 above is from the real 3/2/5 base, not the mis-stated 3/3/4.
**Plan target:** `[DOM]` rows closed 8/9 + reinforce 2 (**done this session**); `[FRD]` (live) closed the keepsake-carried own-note miss (2) + firmed 7; `[GND]` artifact-verify closes 5; `[LOR]` closes 10; `[CRIT]` (6, + 7's scout-origin residue) is the irreducible judgment a hook can't catch — critic step or stay discipline. **Reachable: ~8/10 mechanically (now 5 realized); 2/10 are judgment.**

---

## Case detail

**1 — S124 shipping report from memory.** Built the automated report as Jebrim-principal without loading the shipping-agent reference; *speculated whether the mart carries dimensions* when `tables.md` answers in one line. Knowledge home: `shipping-agent/reference/`. The guarantee was baked into the shipping-agent SUB-agent config; the principal path had no trigger. **Now caught** by the [[S144_9b67aceb_domain_grounding_cue_registry|S144]] domain-cue (`shipping`/`mart`/carrier cues). Live-fire from a real Jebrim mart prompt still pending.

**2 — [[S091_remove_leftover_windows_toast_notifier|S091]] cost-NULL misread.** Had the signal that a NULL cost = a shipment in flight (transient), but read it as a data-quality gap. **Now caught ([[S150_26ef6430_session-start-forced-read|S150]] `[FRD]`):** the jebrim `keepsake/current.md` pins the `cost_source` breakdown verbatim — `'invoice' 65%, 'expected' 24%, NULL/uncosted 8%, 'avg' 2%` — and `keepsake-forced-read.py` **hard-inlines that keepsake into context on the first prompt**, so "NULL is a normal 8% category (uncosted/in-flight)" is in view *before* the analysis, not a note the agent must decide to open. The live shipping `domain-cue` independently points at the reference. (Honest edge: the keepsake gives the *category*; the full "transient, don't treat as a gap" framing is in the bank note. Keepsake-fact + domain-cue together are enough to stop the misread — caught.)

**3 — [[S066_7f5db8c5_cockpit-sweep|S066]] property "new third".** Analyzed an apartment as a new, third property before recognizing it as [[S056_e433ac17_switchboard-osrs-chatbox|S056]] apartment #1. Prior analysis was in zezima `research/`+`bank`. Grounding-cue catches this *if* the prompt carried a continuation cue ("again", "back to"); a cold fresh framing slips. *Partial.*

**4 — [[S076_322cb5c3_cockpit-vscode-rename-and-focus|S076]] "the message I prepared".** Grepped the working repo for a prepared message; it was in jebrim's own `bank/notes`. Grounding-cue fires on "prepared earlier" → **caught.** (This is the case that motivated grounding-cue; it's the proof the pattern works.)

**5 — [[S095_f9310a45_cockpit-transcript-full-width-speech-bubble|S095]] folio cold.** Opened an uploaded folio and re-derived the whole analysis before recognizing it as Gate 2 of the [[S066_7f5db8c5_cockpit-sweep|S066]] deep-dive. The catch would be grounding-cue's artifact-detection path — but that path is UNVERIFIED against the real payload, so today this is a **miss**. Fixing the artifact path (or a forced "map the upload to prior work" step) closes it.

**6 — [[S113_b072b8df_bankstander-question-and-godly-proposal-flow|S113]] fiction pipeline.** Asserted the system picks up Guthix's suggestions through a pipeline that didn't exist — a design story stated without checking the record. No topic cue; the failure is *asserting without verifying*. **Miss**, and not hookable — this is the `[CRIT]`/judgment residue (the brain's own "check the record before defending a design story" lesson).

**7 — [[S145_543c6caf_knowledge_loading_and_rule_adherence_audit|S145]] keepsake (this session).** A scout reported global+player keepsake "empty"; I repeated it as a cheap-win recommendation without opening the files (they were mostly full). Same class as #6: a claim asserted without verification. **Now partial ([[S150_26ef6430_session-start-forced-read|S150]] `[FRD]`):** `keepsake-forced-read.py` hard-inlines the keepsake into the *main* agent's context + the SessionStart directive says, in as many words, *treat any "layer empty" claim as a hypothesis to verify against the file* — aimed at this exact failure. So the main agent that *echoed* the claim now has the content in front of it. **Not caught**, because the false claim *originated* in a SCOUT sub-agent (skipped by the hook — `agent_type` set), and reconciling a sub-agent's report against context is judgment (`[CRIT]`). The hook closes the echo, not the origin. Still the strongest case that prose rules don't self-enforce — and the most direct argument for *forcing* over *naming*.

**8 — [[S098_b53fca39_obsidian_fit_and_dlink_migration|S098]] schema-add 500.** A serving change filtered on a new column before the data was regenerated; filtered views 500'd while "All" worked. The ordering rule was in jebrim `bank/notes` (`scm_alerts_entity_split.md` + the schema-add-ordering memory). **Still miss after [[S150_26ef6430_session-start-forced-read|S150]]** — and this is the honest boundary of `[FRD]`: the forced-read surfaces *keepsake + the active resume*, not an arbitrary bank note that is neither pinned nor in the current resume. The schema-add-ordering note is neither, so `[FRD]` does not reach it. Closing 8 needs the *topic* trigger — a deploy/schema `[DOM]` registry row (one dict in `cue_registry.py`), the cheap sibling build. `[FRD]` and `[DOM]` are complementary, not interchangeable: `[FRD]` forces *standing* context, `[DOM]` routes to *topical* knowledge homes.

**9 — S143 FIF gitignored config.** A deploy-critical lookup caught by a blanket `*.csv`/data ignore was nearly architected-around instead of fixed. The "gitignored config is a defect" lesson + the FIF note existed. **Miss**; an FIF/deploy domain row catches the topic.

**10 — lorebook decisions unread (general).** ~30 `lorebook/confirmed/D-NNN` decisions are not per-player-scoped and never load at respawn. A player mid-task can miss an applicable system decision made in a prior session. **Miss**; closing it means surfacing applicable decisions in player sessions (`[LOR]` — by topic-cue or a forced index read).

---

## Notes & honesty

- Cases 6/7 are the **irreducible residue**: "verify before asserting" is judgment, not a bright line — a hook can't grep it. They belong to the critic-step / reasoning-prose bucket, and are the cases that prove *not everything can be hooked* (so the plan must not pretend otherwise).
- This set is **deliberately small and fixed.** It is an eval, not a survey — its job is to make each fix's payoff legible (move the scorecard), not to catalogue every miss.
- Re-score this table after each Phase 2/3 build. A fix that doesn't move it (or regresses a `caught`) hasn't earned its place.
- **Re-score log.** §X.5 @import-trim **Stage A** ([[S145_543c6caf_knowledge_loading_and_rule_adherence_audit|S145]]), **Stage B** ([[S147_79b03308_import_chain_trim_stage_b|S147]], `modes`+`write-rules` → JIT), and **Stage C** ([[S148_4d020f31_import_chain_trim_stage_c|S148]], the `communication-protocol` split — operational sidecars → new JIT `meta/intent-narration.md`) all left the scorecard **unchanged at 2/3/5** — expected, since they are load-order changes (what's *eagerly* expanded), not retrieval-mechanism changes (what *pushes* knowledge into view). No case here depends on `modes.md`/`write-rules.md`/intent-narration being eager. The trim's payoff is the cumulative weight cut (chain 23.3k→11.9k, ~49%) proven *not to regress* coverage — exactly the "smaller without getting dumber" bar.
- **Re-score log (§X.10, the `Reading:` preamble line, [[S148_4d020f31_import_chain_trim_stage_c|next session]]).** Scorecard **unchanged at 2/3/5** — and this is the *honest* result, not a disappointment. The `Reading:` line is **R3-tier (visible, not enforced)**: it surfaces the agent's grounding plan at the Plan line but does **not force** the read, so by this table's scoring rule (*would the right knowledge have been forced into view before the agent acted?*) it catches nothing new. Its value is **orthogonal to this scorecard**: it makes cue-obedience *observable* on the cases already marked `caught` (1, 4) — where a hook fires but obedience is unmeasured (#1 domain-cue "fired 14× in 30d — obedience unmeasured"). A future `ritual-stats` arm parsing the `Reading:` line against the hook's nudge is what would turn that observability into a number; the line itself is the precondition. CORE grew 3,722→4,036 tok (chain 11,899→12,213, +314) — the acceptable cost of a per-turn behavioral rule. The actual coverage move is **X.4** (the `SessionStart` forced-read), which this line *complements, not replaces*.
- **Re-score log (§X.4 — `keepsake-forced-read.py`, the SessionStart forced-read, [[S150_26ef6430_session-start-forced-read|S150]]).** **First scorecard move off 2/3/5: now 3 caught / 3 partial / 4 miss.** Unlike every prior §X build (all load-order, scorecard-neutral by design), this one is a *retrieval-mechanism* change — it adds a scorer. **Case 2 partial→caught** and **case 7 miss→partial**, both because the hook **hard-inlines the keepsake CONTENTS** (genuine forcing) rather than naming a read — the distinction that separates `[FRD]` from the `Reading:` line (§X.10) it pairs with. The honest non-moves: **case 8 held at miss** (`[FRD]` forces keepsake+resume, not an unpinned bank note — needs a deploy `[DOM]` row); **case 7 capped at partial** (the hook reaches the main agent's echo, not the scout sub-agent that originated the false "empty"). **Weight:** the `@import` chain is **unchanged at 11,886 tok** — X.4 adds *zero* to the eager load (it's a runtime hook injection, not an `@import`; the Stage-A–D trim is fully preserved). The runtime cost is the SessionStart directive (~130 tok) + the global keepsake (a placeholder today) every session, and the player keepsake re-injected once on the first prompt — but that player keepsake is *already* counted in `brain-weight`'s ritual-read subtotal (respawn nominally reads it), so X.4 doesn't add it as new weight; it **converts a counted-but-drifting ritual read into a guaranteed one**. That is the whole §X thesis in one mechanism: same content, moved from discipline to guarantee. **Live-fire pending** — the SessionStart arm fires only at a real session start and the player arm skips Braindead, so the boundary harness (19/19) is the proof today; first real Jebrim/Zezima session start is the hand-off (look for `forced-read:session-start` + `forced-read:player-inject` rows in `switchboard/ritual-events.ndjson`).
- **Re-score log (§X.5 @import-trim Stage D — `task-lists` split + R3 rule-index, same session).** Scorecard **unchanged at 2/3/5** — load-order again, not retrieval. `task-lists.md`'s *threshold* (the load-bearing per-turn decision + the durable-`inventory`-mirror trigger) stayed in the always-on CORE; only the *mechanics* (two-homes table, lifecycle, anti-patterns, scope → new JIT `meta/task-lists-mechanics.md`) left the eager chain. No regression case depends on task-list mechanics being eager. Net chain effect across X.10 + Stage D: `task-lists.md` 1,415→721 (−694) offset X.10's +314, so the chain landed **11,886 tok — fractionally below the 11,899 session-start** while *adding* the `Reading:` rule + the R3 rule-index. "Smaller without getting dumber" held a fourth time.
- **Re-score log (§X.3 — `draft-gate-rewrite.py` + the `deploy-schema` `[DOM]` row, this session).** Two builds, two axes. **(a) The draft-gate hook (X.3 proper)** is a *write-discipline* guarantee — it redirects a main-agent `bank/notes/`/`spellbook/skills/` write into the `drafts/` home so fresh knowledge goes through the alching gate by guarantee, not discipline. It moves **no** case on this scorecard *by design*: this table measures knowledge-*retrieval* misses (knowledge that existed and wasn't loaded), and X.3 governs *how knowledge is written*, not whether prior knowledge loads. It belongs to [[S145_543c6caf_knowledge_loading_and_rule_adherence_audit|S145]]'s other diagnostic half (decided-rules-drift → hook the bright line), whose eval is the adherence axis (ritual-stats / boundary harness 19/19), not this set. Logged here for the record; correctly scored nowhere. **(b) The `deploy-schema` `[DOM]` row** (the cheap sibling §X.4 left open, built in the same session since X.3 landed clean) IS a retrieval mechanism and **moved the scorecard: cases 8 + 9 miss→caught** (3/2/5 → **5/2/3**). One registry dict, patterns `deploy`/`schema`/`migration`/`DAG`/`gitignore`/`FIF`/column-add, pointing at jebrim's real deploy bank notes (`bi_analytics_deploy_topology.md`, `scm_alerts_entity_split.md`, `2026-05-28-ups-orwo-fif-data-quirks.md`); end-to-end verified (fires on case-8/9 prompts, silent on benign + shipping, shipping row unregressed). **Weight:** `@import` chain **unchanged at 11,886 tok** — both are runtime hooks, zero eager add (the draft-gate injects nothing into context unless a write triggers a redirect; the registry row only fires on a topic-cue match). Same §X thesis as X.4: convert discipline to guarantee at zero always-on cost. Also **corrected an off-by-one** in the [[S150_26ef6430_session-start-forced-read|S150]] summary (3/3/4 → the true 3/2/5 base) before applying the moves. **Live-fire pending** for the draft-gate (it skips Braindead, so a real player `bank/notes/` write is the hand-off — look for `draft-gate:redirect` rows in `switchboard/ritual-events.ndjson`) and the deploy-schema cue (a real Jebrim deploy/schema prompt → `domain-cue:deploy-schema`).
