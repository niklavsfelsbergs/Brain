# Zezima — keepsake/current.md

> Read at respawn (when Zezima is active). Pinned items that must surface every Zezima session. Under size budget (~2k tokens). User-only; the agent proposes via `proposals/`.

*(empty — pins land here as the principal decides what's always-on for Zezima)*
