# S012 — 2026-05-21 — Braindead the construction crew gets a sprite + workshop

Continuation of [[S011_visualizer_intent_narration]]. Niklavs pointed out the dev brain still rendered as the wisp — same sprite as gielinor's own self-tending — and asked for it to have its own character walking around gielinor when it modifies the main brain. Three knobs settled in chat: quirky tinkerer for the sprite (matches the cheeky name), a new building called The Workshop in the top-left corner, and the character is named Braindead. Decision in [[D-013_braindead_character_and_workshop]] (started life as D-011 mid-session; a concurrent harvest-pump decision claimed D-011 and then renumbered to D-012 in parallel, so this one shifted to D-013 to avoid stepping on it).

- **Sprite + workshop.** Added `--braindead-robe`, `--braindead-robe-dk`, `--braindead-bandage`, `--braindead-goggle`, `--braindead-hammer` CSS tokens and a new `<g id="braindead">` def — slate-blue robe, head wrap with dangling end, goggles pushed to forehead, hammer in right hand, rolled blueprint in left, belt with a bandage pouch. New `braindead-workshop` entry in `BUILDINGS` (140, 140), `STAND` (140, 178), `LABEL_Y_OFFSET` (58). New `buildBuildings()` block draws a small shack via `isoBuilding` with scaffolding poles + crossbeams + a propped plank rising above the roof, a sawhorse with half-cut plank on the SW, a toolbox at SE ground, a blueprint pinned to the wall, a lit window, slate-blue roof palette matching the sprite.
- **Lifecycle.** `braindeadActive` + `braindeadNode` state. `spawnBraindead(ev)` / `despawnBraindead()` mirror the wisp pattern. `applyEvent` cases `spawn-braindead` / `despawn-braindead` (clears intent on despawn). `deriveSpeaker` returns 'braindead' when active. `resetWorld` clears him.
- **Path-map reorder.** New buildingRules at the top: `/developer-braindead/experiments/` → spellbook-tower (visualizer work is a spell, per [[S008_iso_visualizer_v0]] / [[S009_visualizer_six_deltas_and_frame]]); `/developer-braindead/` → braindead-workshop (everything else dev-brain). Removed the per-file rules for dev-brain root files (CLAUDE.md, _about.md, respawn.md) — they fall through to the workshop now, which is more correct. New actorRule `/developer-braindead/` → braindead. Default actor remains wisp; the hook layers the mode-marker on top.
- **Hook learns the mode marker.** `emit-event.py`: `read_active_mode()` reads the first line of `brain/.claude/active-mode.txt` (lowercased, stripped). `handle_active_mode_write()` detects writes to the marker, compares to `state-actors.json["_mode"]`, and on transition emits `spawn-braindead` (entering dev-brain) or `despawn-braindead` (leaving) along with a session-start-class log line. After path classification, if the actor would be the default (wisp) and the mode is dev-brain, the hook flips the actor to braindead — so writes to gielinor/ paths in a dev-brain session show Braindead at the appropriate gielinor building.
- **Protocol + rituals.** `developer-braindead/CLAUDE.md` scope section reframed: the agent is **Braindead, the construction crew**. The old "does not modify gielinor/ from this brain" line was already counterfactual (the brain-root CLAUDE.md permits it; recent sessions routinely do it) — corrected. New "The visualizer marker" section names the marker convention. `spellbook/respawn-ritual.md` step 5 writes the marker on dev-brain entry; `spellbook/session-close.md` step 6 clears it on close.
- **Runtime housekeeping + live test.** `.claude/active-mode.txt` added to `.gitignore`. Wrote `dev-brain` to the marker during this session to exercise the loop; `state.ndjson` shows the `spawn-braindead at braindead-workshop` event landing and `state-actors.json` shows `_mode: dev-brain`. Also wrote a test intent to `.claude/intent/braindead.txt` ("Building Braindead's workshop") and confirmed the `intent` event reached the NDJSON.

**Cascade.** Dev-brain files: this quest log entry; [[D-013_braindead_character_and_workshop]] decision; `experiments/visualizer/index.html` (CSS tokens, sprite def, BUILDINGS/STAND/LABEL_Y_OFFSET, buildBuildings block, lifecycle state + helpers, applyEvent cases, deriveSpeaker, resetWorld); `experiments/visualizer/path-map.json` (rule reordering + new rules + updated _notes); `.claude/hooks/emit-event.py` (mode-marker handling + default-actor override); `CLAUDE.md` (scope + marker section); `spellbook/respawn-ritual.md` (step 5); `spellbook/session-close.md` (step 6 + renumber). Root-level: `.gitignore` (active-mode rule), `.claude/active-mode.txt` (gitignored), `.claude/intent/braindead.txt` (gitignored). Main-brain: none.

**Notes for the next iteration.**

- **Live test pending.** Hook-side firing is confirmed via NDJSON inspection; browser-side rendering (the sprite at the workshop, the bubble above his head) hasn't been eyeballed in a tab yet. Next move: `python -m http.server 8765` from the visualizer folder, open `?live=1`, look for Braindead in the top-left.
- **Workshop placement might need nudging.** (140, 140) puts the building well above town. If the roof apex clips the top edge or the label collides with the brand-bar, slide it down/in. Adjustable.
- **The "scope" line in dev-brain CLAUDE.md was stale.** Caught only because the new section forced re-reading the file. Worth a note: stale scope claims rot quietly; periodic re-read of orientation files is cheap.
