# S152 — 2026-06-02 — §X.3 draft-gate PreToolUse input-rewrite + deploy-schema DOM row

Dev-brain via "lets develop gielinor"; OPEN→CLOSING via `comms_append`; no live Braindead siblings ([[S150_26ef6430_session-start-forced-read|S150]] `26ef6430` CLOSED 14:55; intent dir held only gielinor-player files). Continued the [[S145_543c6caf_knowledge_loading_and_rule_adherence_audit|S145]] anti-deterioration work — plan [[plan|§X.3]], the **second** of the X.2–X.4 hooks that move the brain off discipline-only (after [[S150_26ef6430_session-start-forced-read|S150]]'s §X.4 forced-read). All built directly under [[D-032_braindead_full_access|D-032]]; diff shown before commit.

- **Built `gielinor/.claude/hooks/draft-gate-rewrite.py`** — a `PreToolUse` input-rewrite on `Edit|Write|NotebookEdit|MultiEdit`. When the **main agent** writes a gielinor `bank/notes/` or `spellbook/skills/` path, it returns `updatedInput` (full `tool_input` copied, only the path swapped, `permissionDecision:"allow"`) redirecting into `bank/drafts/notes/` / `spellbook/drafts/skills/` — turning the **two guided-only draft gates** ([[write-rules|gielinor/meta/write-rules.md]] L62-63, the bank + skills gates that were prose where the `confirmed/` gates are hooks) into **guarantees**. Redirect-not-block (the write just lands in the draft home — better ergonomics, can't be argued past), idempotent (a path already under `drafts/` is left alone), exits 0 always (a bug falls through to a plain allow — advisory in spirit, like the cue hooks).
- **The load-bearing design call — don't break promotion.** Alching *legitimately* writes `bank/notes/`/`spellbook/skills/` (promotion IS a canonical write — [[write-rules|write-rules.md]] alching reach). A blind redirect would silently send every promotion back into `drafts/`. Three exemptions guard it: **sub-agents** (`agent_type` set — gnomes RUN alching, their writes are promotions + they carry own boundary hooks; and §X.3 targets the *principal* path, where the documented "knowledge straight to bank/notes/" failure lives), **braindead** ([[D-032_braindead_full_access|D-032]], via `_actor.resolve_actor`), and **any active ritual `.mode` marker** (read from `gielinor/.claude/intent/<sid8>.mode` ∈ alching/drafts/bankstanding/closing/consultation/wrapped_up). Failure calculus is asymmetric — exempt-too-little breaks a live ritual (high blast radius), exempt-too-much == today's guided-only status quo (harmless) — so exemptions bias wide and every redirect logs `draft-gate:redirect` for observability.
- **Registered in BOTH settings.json** (root abs path + `gielinor/.claude/settings.json` `${CLAUDE_PROJECT_DIR}`, on the existing `Edit` matcher; idempotency makes the double-fire safe) + a `_comment_draft_gate` at root. **Confirmed the `updatedInput` contract authoritatively** (full-replace — hence the `dict(tool_input)` copy is load-bearing; `permissionDecision:"allow"` + exit 0). **Boundary harness `verification/test_draft_gate_rewrite.py` 19/19** — redirect fires for main-agent bank/notes + skills (full input preserved: content/edits/notebook_path survive); stands down for sub-agent / braindead / ritual-mode / already-drafts / non-gielinor / non-write; backslash + NotebookEdit + deity paths; malformed→exit 0.
- **Cheap sibling (the [DOM] row [[S150_26ef6430_session-start-forced-read|S150]] left open).** Since X.3 landed clean + there was room, added a `deploy-schema` row to [[cue_registry|gielinor/.claude/hooks/cue_registry.py]] (one dict; patterns `deploy`/`schema`/`migration`/`DAG`/`gitignore`/`FIF`/column-add, pointing at jebrim's real deploy notes `bi_analytics_deploy_topology.md` + `scm_alerts_entity_split.md` + `2026-05-28-ups-orwo-fif-data-quirks.md`). End-to-end verified (fires on case-8/9 prompts, silent on benign + shipping, shipping row unregressed).
- **Re-scored `verification/knowledge-miss-regression-set.md`: 3/2/5 → 5/2/3.** The deploy-schema row moved **cases 8 + 9 miss→caught**. The draft-gate hook moved **no** case *by design* — it's a *write-discipline* guarantee (rule-adherence axis), not a knowledge-*retrieval* mechanism, so it scores nowhere on this knowledge-loading scorecard (logged, not counted; it serves [[S145_543c6caf_knowledge_loading_and_rule_adherence_audit|S145]]'s *other* diagnostic half). Also **caught + corrected an off-by-one** the [[S150_26ef6430_session-start-forced-read|S150]] re-score left — its summary read "3/3/4" but the per-row table only ever carried 3/**2**/5. **Weight: `@import` chain unchanged at 11,886 tok** — both builds are runtime hooks, zero eager add.
- **Honest limit — live-fire pending.** Like §X.4, both hooks skip Braindead, so neither can fire from a dev session. A real Jebrim `bank/notes/` write (→ `draft-gate:redirect`) and a real deploy/schema prompt (→ `domain-cue:deploy-schema`) in `switchboard/ritual-events.ndjson` are the live confirmations; the 19/19 boundary harness + end-to-end registry test are the proof today.

**Cascade.** `developer-braindead/quest-log/S152_78488dca_draft-gate-input-rewrite.md` (this), `verification/test_draft_gate_rewrite.py` (new), `verification/knowledge-miss-regression-set.md` (re-score + count-correction), `bank/plan.md` (§X.3 → done), `bank/build-lessons.md` (2 lessons), `respawn.md`, `comms/active.md` (OPEN+CLOSING).

**Main-brain changes.** `gielinor/.claude/hooks/draft-gate-rewrite.py` (new), `gielinor/.claude/hooks/cue_registry.py` (deploy-schema row), `.claude/settings.json` + `gielinor/.claude/settings.json` (register the hook + `_comment_draft_gate`). No `meta/` / `confirmed/` / player-content writes. Mechanism committed `d1734e9`.
