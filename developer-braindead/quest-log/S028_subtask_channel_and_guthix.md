# S028 — 2026-05-22 — Subtask channel and Guthix

- Picked up [[subtask-channel-design]] (Step 2 from S027 respawn). Goal: a third visualizer channel between **intent** (agent voice, slow, scope) and **action** (hook voice, fast, raw) — a hook-authored natural-language micro-step that updates the bubble at medium cadence so the sprite feels alive while the agent is hammering tool calls under a stale intent. The principal's S027 close complaint — *"sprite gets stuck on one intent for 10 minutes while doing a billion things underneath"* — is the load-bearing motivation.
- **Slice planned.** Ship in four steps: (1) hook synthesis dict + `subtask` event emission, JS dispatch is a no-op pre-step; (2) visualizer reception — `actorSubtask[actorKey]` state + dispatch case; (3) bubble rendering extension (Option A — secondary muted line under intent); (4) debounce + live test. Step 1 first.
- **Divergence from design doc.** Doc puts debouncer in the hook ("in-memory dict per actor"). But the hook is a fresh process per tool call — no persistent in-memory state. Debouncer moves to the visualizer (long-lived JS), where in-memory is real. Hook emits every call; visualizer collapses. Bonus: full-fidelity event log survives for replay; debounce can be tuned without re-running anything.
- **Slice 1 — hook synthesis + emission (shipped).** Added `subtask_for(tool_name, tool_input)`, `bash_subtask(cmd)`, `BASH_SUBTASK_TABLE`, and `emit_subtask(wall, actor, tool_name, tool_input)` to `developer-braindead/.claude/hooks/emit-event.py`. Wired emission into all four sites that already attribute an actor: `handle_bash`, `handle_write_or_read` sub-agent path, `handle_write_or_read` main-thread path, and `handle_task_pre`. Event shape: `{type: "subtask", actor, text, wallTime, source: "hook"}`. SUBTASK_MAX_LEN = 80. Smoke test (`experiments/subtask_smoketest.py`) confirms phrases land cleanly across Read/Edit/Write/Glob/Grep/Bash (with verb-table coverage for git status|diff|log|show|blame / git add|commit|push|pull|fetch / ls family / cat family / python|node|npm|… / echo for sidecar / fallback) / Task / WebFetch / WebSearch.
- **Slice 2+3 — visualizer reception + render extension (shipped).** Added `setSubtask(actor, text)` helper; added `subtask` field on `intents[actorKey]` entry creation; added `case 'subtask'` to `applyEvent`'s switch. Modified `renderIntent` to layer subtask as a styled secondary line under the intent — rendered as `fill="rgba(31,21,12,0.58)" font-style="italic"` to read muted. Subtask occupies line 2 **only** when the intent wraps to a single line (Option A from the design doc; intent wins ties because it carries scope). Subtask is dropped when there's no intent — no bubble-creator role; it's a secondary signal.
- **Deviation from design doc — COMMS append.** Doc recommends subtask events also append to COMMS with a new `.log-entry.subtask` class. Held off: action lines already cover that surface (every tool call already gets a chat line), and adding subtask would near-duplicate. Bubble-only for now; promote to COMMS in slice 4 if the bubble alone doesn't satisfy.
- **Replay demo deferred.** The EVENTS array predates D-014 — no `intent` or `action` events in it yet, only `log`/`move`/`spawn-dwarf`/`commit`/`session-start`. A clean replay demo of the channel would need restructuring a session arc to use intent + subtask + action together. Cheaper to verify live; defer the replay arc to a follow-up.
- **Debounce — slice 4 (open).** Without debounce, every tool call updates the bubble. For a typical turn with ~5–10 calls spread over 30s, that's 3–6s between updates — not strobing. The strobe risk is during fast loops (parallel Read calls, etc). Starting numbers from the design: 500ms min update, 1.5s aggregation, 2s reset gap. Decision deferred to live test — may not be needed at all.
- **Pivot mid-session — bankstanding wisp confusion → Guthix.** Live-testing the subtask channel surfaced a UX issue: principal saw Jebrim's session running bankstanding and asked *"why is my Jebrim walking around as a wisp?"* Tracing it: the session wrote intent to `wisp.txt` (correct per the old `communication-protocol.md` — bankstanding = system voice), and subtask events made the wisp's activity feel intensely Jebrim-shaped ("reading write-rules.md", "reading modes.md", etc) — exposing an architecture/intuition mismatch the old design papered over. S028 expanded scope to fix it.
- **Decision: introduce Guthix as the bankstanding deity.** Wisp was carrying two unrelated meanings (unscoped session + system curation) and the second one didn't fit. Guthix is a new dedicated actor for bankstanding: hooded green-robed sprite (lore-aligned — RuneScape god of balance), floats above buildings instead of standing, glides between them without a walk cycle. Wisp shrinks back to "unscoped session" only. Player sprites stay in place while Guthix works the brain. Principal greenlit "Yes Guthix. Also make him look like Guthix and make him float around instead of sit like the others."
- **Shipped (Guthix end-to-end).**
  - **Visualizer.** New CSS palette (`--guthix-robe` family + `--guthix-text/dot/swatch`), float keyframes (`guthix-float`, 3800ms, ±6px) and glow filter (`guthixGlow`), full pixel-art sprite (hooded green robe, white beard, wooden staff with green gem, soft aura), `ACTOR_Y_OFFSET['guthix'] = -48` so he floats above buildings on every move, `NON_WALKING_ACTORS = {'guthix'}` suppresses `.walking` class + dust trail in `moveActor`, chat lines now say "drifts to" instead of "walks to" for non-walkers. `spawnGuthix` / `despawnGuthix` parallel to wisp/braindead; default spawn location `lorebook-library`. `ensureActorExists` adds the guthix branch (self-heal on first move/intent). `applyEvent` dispatches `spawn-guthix` / `despawn-guthix`. `resetWorld` cleans him up. `deriveSpeaker` puts him at the top of the precedence ladder (above braindead and wisp — bankstanding is the top-level ritual). Legend row + COMMS tab + filter + speaker bullet + chat `.user` color all wired.
  - **Hook.** `GUTHIX_ACTOR = "guthix"`, `GUTHIX_DEFAULT_BUILDING = "lorebook-library"`. New `is_guthix_session()` parallel to `is_dev_brain_session()` — reads `_guthix_session_id` from `state-actors.json` and gates on `_SESSION_ID`. New `_handle_guthix_lifecycle(actor)` called from `handle_intent_write` before the intent event lands: when this session writes to `guthix.txt` for the first time it emits `spawn-guthix` + a log line "Guthix descends to tend the brain" and stamps `_guthix_session_id`; when intent flips to a non-guthix actor it emits `despawn-guthix` + "Guthix recedes — the brain is tended" and clears the marker. `handle_session_end` does the same despawn if the closing session owned guthix. The wisp-fallback in `handle_write_or_read` main-thread path, `current_main_actor`, and `infer_dwarf_parent` all now check `is_guthix_session()` between the dev-brain check and the recency walk so unclassified paths route to Guthix when bankstanding is active.
  - **Meta docs.** New `gielinor/meta/guthix.md` (persona, voice, write reach, visualizer notes). `modes.md` bankstanding section rewritten: voice is now Guthix, intent file is `guthix.txt`, with cross-references to the new file. `communication-protocol.md` active-actor-by-mode bullet adds the bankstanding → `guthix.txt` route. `write-rules.md` adds a "Voice per ritual" paragraph below the Ritual write-reach table — bankstanding = Guthix voice, alching = active player voice, others carry the active actor's voice.
- **Visualizer-side caveats / follow-ups.**
  - Replay-mode demo deferred — same reason as subtask. EVENTS array doesn't have intent/action events; adding a bankstanding arc would mean restructuring.
  - The hook spawns guthix on the *first* `guthix.txt` write. If a session writes to guthix.txt and then never to anything else (and never ends cleanly), the sprite stays until the visualizer's 5-minute idle despawn fires. Acceptable for v1.
  - `is_guthix_session()` is gated on `_SESSION_ID` like `is_dev_brain_session()` — parallel sessions sharing the state file can't fire each other's guthix override.
- **`Hey Guthix` invocation pattern.** Principal asked: *"shouldn't we just have a Hey Guthix option, and it prompts what it could do for me?"* — extended the address-by-invocation router to recognize Guthix alongside players + unscoped. `gielinor/CLAUDE.md` adds `Hey Guthix, ...` row + matching rules updated ("known address" covers players + `unscoped` + `Guthix`). Mid-session switching extends to Guthix (same mini-respawn pattern; Guthix has no `quest-log/` so the bankstanding output is itself the trace). `gielinor/meta/guthix.md` adds a full *Invocation contract* section with a four-item menu — Bankstand / Triage drafts / Survey across players / Audit a global layer — plus a *What he refuses* subsection (redirects per-player work back to alching, dev-brain construction back to dev-brain mode) and a *Returning* section. `modes.md` bankstanding `Set by` line now lists both entry routes (`Hey Guthix` and `let's bankstand`).
- **Live-test debugging (post-ship).** Principal opened the visualizer mid-S028 and reported (a) stale bugged-out sprites cluttering the map, (b) Braindead getting no speech bubbles, (c) a Jebrim session "currently acting but no bubble or movement." Diagnosed and fixed in three small batches:
  - **Map wipe.** Truncated `state.ndjson` + reset `state-actors.json` / `state-dwarves.json` / `state-gnomes.json` / `state-instances.json` via a single Bash python script. Visualizer's pollLive detected truncation (B5 from S027) and `resetWorld()`. Re-stamped `active-mode.txt` so the dev-brain session re-spawned Braindead.
  - **Braindead bubble routing.** Hook's `handle_intent_write` only stripped the `-<sid8>` suffix when the prefix matched `PLAYER_ACTORS`. So `braindead-ed610cbe.txt` emitted `actor="braindead-ed610cbe"` (not `"braindead"`) — visualizer couldn't route to the sprite, bubble never rendered. Added `NON_PLAYER_SUFFIX_ACTORS = {"braindead", "guthix", "wisp"}` and extended the suffix-strip check. Bug has been silently present since D-018 chunk 3 (whenever the agent first wrote per-session braindead files).
  - **Disk-fallback for session→actor recovery.** State wipe surfaced a separate issue: a Jebrim session whose state.ndjson events got wiped attributed subsequent Bash to `wisp` because the recency walk for that session found nothing. Added `_session_actor_from_disk()` that checks `.claude/intent/<actor>-<sid8>.txt` on disk — the intent file outlives state.ndjson, so it survives truncation/reset cycles. Plugged into `current_main_actor()`, `infer_dwarf_parent()`, and the wisp-fallback in `handle_write_or_read` main-thread path. Helps sessions using per-session intent files; sessions on bare files (the in-flight Jebrim session 58f8e88a, writing to bare `jebrim.txt`) recover on their next respawn.
- **Protocol tightened.** `gielinor/meta/communication-protocol.md` intent-narration paragraph rewritten: per-session intent filename (`<actor>-<sid8>.txt`) is now the only sanctioned shape, for every actor (players + Braindead + Guthix + Wisp). The bare-file fallback is removed — if `CLAUDE_CODE_SESSION_ID` is unavailable, surface to the principal rather than silently fall back, because bare files have no session ownership and break the recovery path. Resolves the recurring "in-flight sessions clobber each other" issue ([[S014]], [[S022]], [[S023]], [[S027]] all touched this).

**Cascade.**
- `developer-braindead/.claude/hooks/emit-event.py` — subtask synthesis dict (`subtask_for`, `bash_subtask`, `BASH_SUBTASK_TABLE`, `emit_subtask`) + wiring into all four attribution sites; Guthix lifecycle (`GUTHIX_ACTOR`, `GUTHIX_DEFAULT_BUILDING`, `is_guthix_session`, `_handle_guthix_lifecycle`, session-end despawn); `NON_PLAYER_SUFFIX_ACTORS` + extended suffix-strip; `_session_actor_from_disk` + plumbed into three fallback paths.
- `developer-braindead/experiments/visualizer/index.html` — subtask state + dispatch + bubble layer (Option A muted-italic second line); Guthix CSS palette + chat tints + float keyframes + glow filter + sprite SVG + spawn/despawn + ensureActorExists branch + moveActor `ACTOR_Y_OFFSET` + `NON_WALKING_ACTORS` + "drifts to" verb + legend row + COMMS tab + speaker dot + filter + `.user` color + resetWorld cleanup; instant-move branch now honors `ACTOR_Y_OFFSET` so scrub-seek keeps floaters airborne.
- `developer-braindead/experiments/subtask_smoketest.py` — one-shot smoke test for the synthesis dict.
- `developer-braindead/quest-log/S028_subtask_channel_and_guthix.md` (this entry; renamed from `S028_subtask_channel.md`).
- `developer-braindead/respawn.md` — refreshed for end of S028.
- `developer-braindead/bank/research/subtask-channel-design.md` — promoted from untracked to tracked.

**Main-brain changes.**
- `gielinor/meta/guthix.md` — new persona doc for the bankstanding deity. Includes Invocation contract + What he refuses + Returning sections.
- `gielinor/meta/modes.md` — bankstanding mode section rewritten: voice = Guthix, intent file = `guthix.txt`, two entry routes (`Hey Guthix` and `let's bankstand`).
- `gielinor/meta/communication-protocol.md` — active-actor-by-mode bullet adds bankstanding → `guthix.txt`; per-session intent filenames now mandatory for all actors (bare-file fallback removed).
- `gielinor/meta/write-rules.md` — "Voice per ritual" paragraph added under the Ritual write-reach table; "Guthix's godly proposals" paragraph added explaining the scoped authority extension.
- `gielinor/CLAUDE.md` — `Hey Guthix` row added to "Player invocation by address"; matching rules updated; mid-session switching note extended to Guthix; `deities/` added to the layer index.

---

## S028 continuation — after the first commit (38dbb8e)

The session kept going after the initial S028 close. Five more arcs landed before this second commit; they're S028-flavoured (subtask + Guthix outputs continuing to evolve) so they live in this quest-log rather than spinning off a new SNNN. Some of the work below is hook/visualizer code; some is main-brain scaffolding the principal asked for in conversation.

- **Guthix gets his own home.** Principal asked: *"Guthix needs his own place to live, draft his own overarching quests, notes, etc. Similar setup as for the players but overarching. Where could he live?"* Recommended `gielinor/deities/guthix/` as a sibling to `players/` (not under players/, not in a premature `deities/` abstraction — but on second turn the principal said "already put it in deities folder" so the directory landed as `gielinor/deities/guthix/`). Scaffolded with `_about.md` files at every layer, mirroring the per-player layout but only the layers that fit a system-scope actor with no relationship-self: `bank/` (cross-cutting knowledge), `quest-log/` (per-bankstanding-pass entries, B-NNN counter distinct from player S-NNN), `inventory/` (in-progress ritual state), `keepsake/` (Guthix-specific surface pins). Skipped `examine/` and `niksis8_character/` (no self-to-model). Updated `meta/guthix.md` ("What he is" + "Write reach" reflect new home), `meta/layer-routing.md` (four new rows route Guthix-shape content), `gielinor/CLAUDE.md` layer index adds `deities/`. Committed as `4e6cf7d`.
- **Godly proposals surface.** Principal followed up: *"when bankstanding he should also propose godly updates to himself. He has all the power to suggest crazy things which would improve the system during this process."* Added `deities/guthix/proposals/` with `_about.md` defining authority + scope + shape. Guthix may draft proposed *changes* to surfaces normally marked user-only — `meta/*.md`, `spellbook/rituals/*.md`, hooks, body files, architecture, his own role and existence — during bankstanding. Required proposal shape: observation / proposed change / reasoning / scope of impact / alternatives considered / risk if landed wrong. Hook-enforced architectural guarantees remain non-overridable; he proposes, principal lands. Rejections preserved per archive discipline. Updated `meta/write-rules.md` ("Guthix's godly proposals" paragraph) and `meta/layer-routing.md` (new row) and `deities/guthix/_about.md`. Also committed as `4e6cf7d`.
- **Pre-S028 protocol drift live-debug wave.** Three issues spotted by the principal mid-flight, all fixed same session: (a) Visualizer cluttered with stale sprites from many sessions → truncated `state.ndjson` + reset `state-actors.json`/`state-dwarves.json`/`state-gnomes.json`/`state-instances.json` via a single Bash python script; visualizer's pollLive detected truncation (B5 from S027) and `resetWorld()`. Re-stamped `active-mode.txt` so the dev-brain session re-spawned Braindead. (b) Braindead bubble routing bug — `handle_intent_write` only stripped `-<sid8>` suffix for `PLAYER_ACTORS`, so `braindead-<sid8>.txt` emitted as actor `"braindead-<sid8>"` and the visualizer couldn't route. Bug silently present since D-018 chunk 3; extended with `NON_PLAYER_SUFFIX_ACTORS = {"braindead", "guthix", "wisp"}` and the suffix-strip check now covers both sets. (c) Disk-fallback session→actor recovery — wipe surfaced that a session whose state.ndjson events got cleared falls back to wisp because the recency walk returns nothing. Added `_session_actor_from_disk()` that reads `.claude/intent/<actor>-<sid8>.txt` on disk (intent file outlives state.ndjson). Plumbed into `current_main_actor`, `infer_dwarf_parent`, and the wisp-fallback in `handle_write_or_read` main-thread path. Protocol tightened: per-session intent filenames now mandatory for all actors (bare-file fallback removed).
- **Stale-intent re-emit + idle-despawn for non-player actors.** Principal flagged a wisp bubble that "constantly has this bubble open" — traced to two structural gaps. (i) Wisp had no idle-despawn lifecycle; only player instances 2+ had the 5-min sweep. Extended `despawnIdleInstances` in the visualizer to cover wisp, braindead, and guthix — fade out + clear bubble + drop from tracking when they go idle. (ii) `_reemit_intent_after_move` was re-resurrecting stale intents on every move because any actor's intent file got re-read regardless of mtime. Added a 5-min staleness check: if the intent file is older than `_STALE_INTENT_SEC`, skip the re-emit so abandoned intents age out instead of locking the bubble in.
- **Sprites gather instead of overlap.** Principal asked: *"how could we make the sprites not overlap each other? they need to gather"*. Replaced the deterministic `jitter()` hash with a slot-based gather system — 10 predefined slots per building, each with its own `dx/dy/bubbleY` triple. `assignGatherSlot(actorKey, atKey)` picks the lowest free slot at arrival; existing residents keep their slots so the cluster grows outward as more actors arrive. `freeGatherSlot` releases on despawn. Wired through `moveActor`, `spawnDwarf`, `spawnGnome`, `spawnPlayerInstance`, `spawnBraindead`, `spawnWisp`, `spawnGuthix`, `ensureActorExists`, `despawnDwarf`/`despawnGnome`/`despawnBraindead`/`despawnWisp`/`despawnGuthix`/`despawnPlayerInstance`, and `resetWorld`. Iso-aware spacing: ±34px horizontal × ±8px vertical for front row, ±58px far-side, etc. After first pass the principal said *"more spaced apart, and also the chatboxes need to somehow not fight so hard"* — widened slots a second time (front row ±34, far-side ±58, back-row ±22 dy ±14) and added per-slot `bubbleY` so back-row sprites' bubbles fan up automatically.
- **Bubbles act like sprites — dynamic collision avoidance.** Per-slot `bubbleY` wasn't enough; bubbles still stacked when crowded because bubble widths (60–300px) far exceed sprite spacing. Built a real layout pass — `relayoutBubbles()` walks every visible bubble left-to-right, each starts at its slot's natural Y, and any bubble that would overlap with an already-placed neighbour gets pushed upward above the conflicting bubble's top edge (up to 16 iterations per bubble). Resolved offsets stored on `intents[actor].layoutYOff`; `renderIntent` reads this in preference to the slot's natural `bubbleY`. Triggered from `setIntent`, `setSubtask`, `clearIntent`, and the move case in `applyEvent` (instead of the old single-bubble `renderIntent` call). `bubbleDims(actor)` helper replicates the width/height math without rendering.
- **Background sub-agent lifecycle.** Principal: *"I have tons of dwarves running but theyre not on the map"*. Diagnosed: the agent in their Jebrim session was using `Task(... run_in_background=true ...)` — the tool returns the handle to the parent immediately, so PreToolUse → PostToolUse fires ~300ms apart even though the sub-agent's actual work runs for minutes. The hook was emitting `despawn-dwarf` on every Post — dwarves vanished before the visualizer's 1800ms fade-in even completed. Fix: `handle_task_pre` flags the spawn entry with `background: True` when `tool_input.run_in_background` (or `runInBackground`) is set. `handle_task_post` peeks at the entry first; if background, leaves it in place and returns without emitting despawn. Then for the cleanup signal — added `lastActiveAt` stamping in `attribute_to_subagent` so every sub-agent tool call refreshes the timestamp; extended `gc_stale_subagents` with a 5-min idle threshold (`IDLE_BG_SEC = 300`) for background entries against `max(spawnedAt, lastActiveAt)`. Foreground entries keep the 1h fallback. Despawn log line surfaces the reason — "idle 5m (background)" vs "no PostToolUse, GC after 60m". Then principal: *"the speech bubbles are not despawning"* — gap was that gc was only firing from Task pre/post and `attribute_to_subagent`. Added GC sweep at the top of `main()` for both kinds so it fires on every hook invocation. Cheap: two state-file reads per fire.

**Cascade (continuation).**
- `developer-braindead/.claude/hooks/emit-event.py` — `NON_PLAYER_SUFFIX_ACTORS`; `_session_actor_from_disk()` plus plumbing in three fallback paths; stale-intent check in `_reemit_intent_after_move` with `_STALE_INTENT_SEC = 300`; `background` flag on Task entries; `lastActiveAt` stamping; `gc_stale_subagents` extended with `IDLE_BG_SEC`; top-of-`main()` GC sweep.
- `developer-braindead/experiments/visualizer/index.html` — `GATHER_SLOTS` table with per-slot bubble Y offsets; `assignGatherSlot` / `freeGatherSlot` helpers; wired through every spawn/despawn function; `actorSlot` map; idle-despawn extended to wisp/braindead/guthix in `despawnIdleInstances`; `bubbleDims` helper and `relayoutBubbles` layout pass; `renderIntent` honours `entry.layoutYOff`; trigger points in `setIntent`/`setSubtask`/`clearIntent`/`move` case.
- `developer-braindead/quest-log/S028_subtask_channel_and_guthix.md` — this entry (continuation appended).
- `developer-braindead/respawn.md` — refreshed for end of S028 continuation.

**Main-brain changes (continuation).**
- `gielinor/deities/` — new top-level layer for overarching system-scope actors. `_about.md` defines the category.
- `gielinor/deities/guthix/` — full per-deity scaffold: `_about.md`, `bank/_about.md`, `bank/drafts/notes/`, `bank/notes/`, `quest-log/_about.md` + `in-progress/`/`completed/`/`archive/`, `inventory/_about.md`, `keepsake/_about.md` + `proposals/` + `current.md`, `proposals/_about.md` (godly proposals scope + shape) + `rejected/`.
- `gielinor/meta/guthix.md` — "What he is" rewritten to reference the new home (no longer "no per-character layers — period"; now "no self-relational layers but does carry continuity-of-work"); "Write reach" expanded to include the deity folder and the godly-proposals surface.
- `gielinor/meta/layer-routing.md` — five new rows route Guthix's content shapes (bank notes, quest-log, inventory, keepsake, godly proposals).
- `gielinor/meta/write-rules.md` — "Guthix's godly proposals" paragraph added.
- `gielinor/CLAUDE.md` — `Hey Guthix` row + matching rules + mid-session switching extension; `deities/` added to layer index.





