Distilled S091 into bank/build-lessons.md — 4 lessons: clean-output source is the transcript jsonl not the TUI grid (and reuse the sibling render); a polling view must yield to a live selection; a rotatable identity key needs carry across ALL stores + comment-assumptions rot (--resume doesn't pin the id, --session-id does); under eyeball-blindness, ship parallel correct fixes but flag which you think mattered.