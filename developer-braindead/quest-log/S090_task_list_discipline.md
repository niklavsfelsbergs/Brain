# S090 — 2026-05-25 — Task-list discipline for gielinor

- Principal observed gielinor charges long tasks head-first and wanted it to **decompose into tracked task lists more often**. Entered dev-brain mid-conversation via "lets develop gielinor"; ran the entry ritual (markers set, OPEN posted, no live siblings — S087/S088/S089 all CLOSING'd).
- Mapped the gap: the Understanding/Plan preamble catches *misunderstanding* but is one sentence (no step state); `inventory/` resume-state is written at session *boundaries*, not live; dwarf-spawn covers *parallel* decomposition only; the harness task tools were never referenced by any ritual. So a long *sequential* in-session task had no tracking machinery — the principal's read was accurate.
- Offered the design choice via [[D-025]]-style multiple-choice-with-recommendation; principal picked the recommended **threshold-gated, two-home** shape over lightweight (harness-only, no durability) and heavy (durable checklist for every long task, fights terseness).
- Landed the rule [[D-031]]: new `gielinor/meta/task-lists.md` (threshold = *multi-step AND (multi-file OR spans turns OR irreversible/outward OR order-dependent)*; harness list for live state + `inventory/` mirror for cross-turn/risky work; lifecycle, anti-patterns, scope). The threshold deliberately mirrors the dwarf-spawn/alching heuristics, and the durable mirror reuses the existing `inventory/` resume file — it makes the crash-recovery signal `death-and-spawn.md` already wanted obligatory at task grain, not new structure.
- Wired it in: `@meta/task-lists.md` import in `gielinor/CLAUDE.md`; a *Task-list surfacing* subsection in `communication-protocol.md` (sibling to the dwarf-spawn annotation — how the decomposition appears in the Plan line, surface-once-don't-recite); a routing row in `layer-routing.md`; listed in `meta/_about.md`. Flagged (untouched) that `meta/_about.md`'s file list pre-existingly omits `guthix.md`/`communication-protocol.md`/`layer-routing.md` — bankstanding territory.

**Cascade.** `developer-braindead/bank/decisions/D-031_task_list_discipline.md` (new); this quest entry; `respawn.md` (prepend); `comms/active.md` (OPEN + CLOSING).

**Main-brain changes.** `gielinor/meta/task-lists.md` (new rule, imported every session); `gielinor/CLAUDE.md` (import line); `gielinor/meta/communication-protocol.md` (Task-list surfacing subsection); `gielinor/meta/layer-routing.md` (routing row); `gielinor/meta/_about.md` (file list). Doc-only — no hooks, no code.
