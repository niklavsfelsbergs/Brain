# S164 — 2026-06-09 — Cockpit: chat numbering, session history/restore, + three UX fixes

Dev-brain via "lets develop gielinor"; OPEN→UPDATE×4→CLOSING via `comms_append`; no live Braindead siblings ([[S163_596912a5_cockpit_transcript_markdown_tables|S163]] `596912a5` closed 2026-06-08 11:52). Live parallel Jebrim WIP on disk (UPS/EU-tender, separate namespace — committed by pathspec, no sweep). Five cockpit changes this session, all driven by Niklavs, all `node`/`py_compile`-clean and logic-tested; the live render is his relaunch eyeball (same ceiling as [[S160_fb951652_cockpit-clickable-paths-and-sleep-wake-resume|S160]]/[[S161_fb951652_cockpit-wake-resume-in-place-reconnect|S161]]/[[S163_596912a5_cockpit_transcript_markdown_tables|S163]] web work).

- **Per-chat numbering (replaces the status dot).** Niklavs steered off a color-grading idea to a recycling **number**: the top-left dot becomes a status-colored disc holding the chat's number. NEW [[slots|cockpit/web/slots.js]] (`reconcileSlots`/`slotFor` — frees a number when its chat leaves the board, hands new chats the lowest free slot, oldest-first so a reload rebuilds deterministically); [[board|cockpit/web/board.js]] renders the number in `.dot`; [[styles|cockpit/web/styles.css]] grows `.dot` to a 1–2-digit disc keeping its color + pulse. 10-assertion recycle test green.
- **Session history + restore.** Park/close a chat and reopen it from a list. Decisions (Niklavs): **all** on-disk history (not a curated parked-set), backend-sourced, restore = live `claude --resume`, park=de-own (already free — `release` removeOwns, `resumeTerm` addOwns). NEW read-only `GET /api/history` in [[backend|cockpit/backend.py]] (globs `~/.claude/projects`, mtime-sorted, cap 150, head-scan title via ai-title/first-prompt) + `_safe_mtime`/`_session_summary`; NEW `HistoryModal` + `fmtAgo` + `doReopen` + Ctrl+H in [[main|cockpit/web/main.js]]; history button in [[board|cockpit/web/board.js]]; `.hist-*` CSS. Endpoint live-tested vs the real 559 transcripts.
- **Fix — resumed/idle rows jumping to the top at frozen "0s".** Niklavs caught it (reopening surfaced a long-standing pattern): a cockpit-own row with no feed event fell back to `quiet=0`, so the idle gate (`quiet>300`) could never trip → stuck `your_move` rank 1, pinned to top, frozen 0s. Fix in [[term|cockpit/web/term.js]] (`openedAt`+`isResume` on `TermConn`, exposed via `liveTerms`) + [[main|cockpit/web/main.js]] merge: feed-less rows age from `openedAt` (no frozen 0s); a feed-less **resumed** terminal reads idle → rank 7 → sinks. Fresh placements keep their top slot. Also calms the owned-resume-on-open/wake burst.
- **Fix — compose-bar draft lost on session switch.** [[term|cockpit/web/term.js]] `TermComposer` held the draft in component-local state, discarded on unmount when selecting away. Moved it onto `conn.composeDraft` (survives the switch — sessions run off-screen); seed-on-mount + write-through + clear-on-submit/Esc + a mount effect to re-grow a restored multi-line draft.
- **Fix — clickable paths only opened brain-repo files.** Niklavs: a report path (`bi-analytics-main/NFE/…`) wouldn't open. `api_open_path` ([[S160_fb951652_cockpit-clickable-paths-and-sleep-wake-resume|S160]]) scoped to `BRAIN_ROOT`, but that report is a **sibling** repo. Fix (Niklavs multiple-choice = widen to the GitHub workspace): NEW `WORKSPACE_ROOT = BRAIN_ROOT.parent`; resolve relative paths brain-first then workspace, validate `relative_to(WORKSPACE_ROOT)`. Live-tested: the report → OPEN, brain paths → OPEN, `../../../Windows/System32` → 403 (boundary holds).

**Cascade.** NEW [[slots|cockpit/web/slots.js]]; edits to [[board|cockpit/web/board.js]], [[main|cockpit/web/main.js]], [[term|cockpit/web/term.js]], [[styles|cockpit/web/styles.css]], [[backend|cockpit/backend.py]]; dev-brain [[S164_b472d014_cockpit_chat_numbering_history_restore_and_ux_fixes|this quest-log]], `respawn.md`, `bank/build-lessons.md` (+2), `comms/active.md`.

**Main-brain changes.** none — `cockpit/` is brain-root tooling, not `gielinor/`; no `gielinor/` content, identity, or rulebook touched, `@import` chain untouched.
