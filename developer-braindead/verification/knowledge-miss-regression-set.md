# Knowledge-miss regression set — Phase 0 eval

**Purpose.** A fixed set of *real* cases where the brain HAD relevant knowledge (or a rule) on disk and failed to load/apply it. This is the pass/fail bar for the anti-deterioration plan ([[S145_543c6caf_knowledge_loading_and_rule_adherence_audit|S145]], `plan.md` §X): every later fix must move coverage here, and no fix may regress a case already covered. Built 2026-06-02; cases drawn from the S125/[[S145_543c6caf_knowledge_loading_and_rule_adherence_audit|S145]] audits, the quest-log, and confirmed memory.

**How to score a case.** For each, ask: *given the current live mechanisms, would the right knowledge have been forced into view before the agent acted?* `caught` = a live hook/gate fires on it; `partial` = fires only under a narrower condition (e.g. a continuation cue that may be absent); `miss` = nothing fires.

**Live mechanisms today (the scorers):**
- `grounding-cue` — UserPromptSubmit nudge on continuation cues / artifacts (own past work). **Artifact-detection VERIFIED + rebuilt ([[S192_e6e7b78d_lorebook_cue_artifact_verify_obedience|S192]]):** the original payload-field guess (ARTIFACT_KEYS) was dead code — the shipped binary constructs the UserPromptSubmit input with NO attachment fields; detection now matches the upload placeholders the TUI inserts into the prompt text (`[Image #N]`, `[Pasted text #N +M lines]`, `[...Truncated...]`, dragged absolute doc paths), receipts in the hook header; once-per-session for artifact-only fires. `test_grounding_cue.py` 16/16 (the hook's first suite).
- `lorebook-cue` — **NEW ([[S192_e6e7b78d_lorebook_cue_artifact_verify_obedience|S192]], the `[LOR]` arm):** `domain-cue-reminder.py` parses `gielinor/lorebook/_index.md` (synthesized cue index: per-decision literal patterns + a one-line distilled rule) and force-inlines the matching decision's RULE on a topic cue, once per session per decision, then goes silent (no name-nudge tail — "commit"-shaped cues recur too often). 4 decisions cue-armed ([[D-017_user-only-with-explicit-permission|D-017]]/[[D-023_powershell-utf8-readall-not-getcontent|D-023]]/[[D-024_scope-git-commits-with-pathspecs-parallel-sessions|D-024]]/[[D-034_guthix_executes_on_explicit_authorization|D-034]]); the other 14 carry an honest `carried-by:` label (always-on CORE / ritual read / hook). Index drift detector in `hygiene-check.py` Axis C; promotion rituals add the index row in the same pass. `test_lorebook_cue.py` 22/22.
- `domain-cue` — UserPromptSubmit nudge on a registered domain's topic cues (external knowledge home). Two rows live: **shipping** + **deploy-schema** (the latter added §X.3 session — closes cases 8/9; EU-tender/FIF/SCM beyond it remain a commented stub). **Force-inline upgrade (2026-06-05):** a domain may now carry `inline_homes` — small, in-tree knowledge files whose *contents* are injected (once per session per domain, byte-capped, sub-agents excluded), not merely named. `deploy-schema` uses it (its 3 jebrim notes, ~7.5 KB); shipping deliberately does not (external repo / large reference → stays name-only + routes to the specialist). This is the §X.4 keepsake move applied to domain knowledge: cases 8/9 go from *caught-by-naming* (a nudge the agent can ignore) to *caught-by-inlining* (the contents are in context — a guarantee, like the `[FRD]` keepsake inject). Tally unchanged (no bucket sits above `caught`); the upgrade hardens the *mechanism*, not the count.
- `domain-digest` — **NEW (§Z.B/Z.C, [[S179_d7285bce_domain_knowledge_layer_zA_scm_pilot|S179]]+/this session): the per-player `bank/domains/` layer.** Two arms on top of the §X.4/§X.3 machinery: (Z.B) `keepsake-forced-read.py` hard-inlines the active player's `bank/domains/_index.md` (the always-read domain MAP) beside the keepsake on the first prompt; (Z.C) `domain-cue-reminder.py` **auto-discovers** the active player's `bank/domains/*.md` DIGESTS, reads their frontmatter `patterns`, and force-inlines the matching digest on a topic cue — discovered (not a hand-listed `cue_registry.py` row) and digest-not-whole-notes (synthesized ~2 KB, not the raw corpus). This is the retrieval ladder for *topics* that the fact ladder (draft→bank→keepsake) lacked: a player **just knows the map of their domains** + gets the synthesized digest forced in on-cue. The global registry stays for external/specialist homes (shipping). Adding a domain = alching drops a digest file; no hook edit.
- `forced-read` — **NEW ([[S150_26ef6430_session-start-forced-read|S150]], §X.4): `keepsake-forced-read.py`.** SessionStart inlines the GLOBAL keepsake + a forcing directive (read the active keepsake + relevant resume; treat "layer empty" as a hypothesis to verify); the first UserPromptSubmit **hard-inlines the active player's `keepsake/current.md` contents** into context (a guarantee, not a name — the enforced pairing for the `Reading:` line). This is the first mechanism that *forces knowledge into view*, not just nudges toward its home. Boundary-verified 19/19; live-fire (a real player session start) is the hand-off.
- `draft-gate` — **NEW (§X.3, this session): `draft-gate-rewrite.py`.** PreToolUse input-rewrite that auto-redirects a main-agent `bank/notes/`→`bank/drafts/notes/` and `spellbook/skills/`→`spellbook/drafts/skills/` write (turning the two guided-only draft gates into guarantees). **Note its axis:** this is a *write-discipline* guarantee (rule-adherence), **not** a knowledge-*retrieval* mechanism — so it does **not** appear in the "Caught by" column of this scorecard, which measures retrieval misses. It belongs to [[S145_543c6caf_knowledge_loading_and_rule_adherence_audit|S145]]'s *other* diagnostic half (decided-rules-drift → hook the bright lines); its payoff shows on the adherence axis, not here. Listed for completeness, scored nowhere.
- *(no Stop-gate yet.)*

**Plan mechanisms that would catch the misses (the targets):**
- `[DOM]` domain-cue registry row (Phase 2, cheap — one dict)
- `[GND]` grounding-cue (exists; widen/verify artifact path)
- `[FRD]` SessionStart forced-read of keepsake + active resume (Phase 2)
- `[LOR]` load applicable lorebook decisions in player sessions (Phase 1/2)
- `[DGST]` per-player domain digest — auto-discovered, force-inlined on topic cue (§Z)
- `[CRIT]` critic/verify step — judgment rule, NOT hookable (Phase 2 #6 / discipline)

---

## Coverage scorecard

| # | Case (session) | Knowledge that existed | Today | Caught by |
|---|---|---|---|---|
| 1 | Shipping report from memory (S124) | `shipping-agent/reference/{mart-contract,tables}.md` | **caught** ([[S144_9b67aceb_domain_grounding_cue_registry|S144]] domain-cue; obedience now MEASURED ([[S192_e6e7b78d_lorebook_cue_artifact_verify_obedience|S192]]): 47% either-signal all-time, n=268 scored) | `[DOM]` ✓ |
| 2 | "cost NULL = load in flight" misread as quality gap ([[S091_remove_leftover_windows_toast_notifier|S091]]) | jebrim `bank/notes/...quality-assessment` | **caught** ([[S150_26ef6430_session-start-forced-read|S150]] `[FRD]` hard-inlines the jebrim keepsake, which lists `cost_source` NULL/uncosted 8% as a normal category; + live `[DOM]` shipping) | `[FRD]`+`[DOM]` ✓ |
| 3 | Property analyzed as "new/third" ([[S066_7f5db8c5_cockpit-sweep|S066]]) | zezima `research/`+`bank` on [[S056_e433ac17_switchboard-osrs-chatbox|S056]] apartments | partial (needs a continuation cue) | `[GND]` |
| 4 | "the message I prepared" grepped in repo, was in bank ([[S076_322cb5c3_cockpit-vscode-rename-and-focus|S076]]) | jebrim `bank/notes/` onboarding msg | **caught** (grounding-cue "prepared") | `[GND]` ✓ |
| 5 | Uploaded folio re-derived cold ([[S095_f9310a45_cockpit-transcript-full-width-speech-bubble|S095]]) | zezima `research/` [[S066_7f5db8c5_cockpit-sweep|S066]] deep-dive | **caught** ([[S192_e6e7b78d_lorebook_cue_artifact_verify_obedience|S192]]: artifact path verified-then-rebuilt — placeholder detection on the prompt text, the shapes uploads actually take) | `[GND]`(artifact) ✓ |
| 6 | Claimed a pipeline behavior that was fiction ([[S113_b072b8df_bankstander-question-and-godly-proposal-flow|S113]]) | the corpus / git record | **miss** | `[CRIT]` |
| 7 | Scout said keepsake empty; repeated unverified ([[S145_543c6caf_knowledge_loading_and_rule_adherence_audit|S145]], this session) | the actual `keepsake/` files | **partial** ([[S150_26ef6430_session-start-forced-read|S150]] `[FRD]` hard-inlines the keepsake into the *main* agent + directs "verify empty as a hypothesis"; the scout sub-agent that *originated* the false claim is unreached — residual judgment) | `[FRD]`+`[CRIT]` |
| 8 | Schema-add filtered views 500'd ([[S098_b53fca39_obsidian_fit_and_dlink_migration|S098]]) | jebrim `bank/notes` schema-add ordering | **caught — now force-INLINED** (§X.3: `deploy-schema` `[DOM]` row fires on deploy/schema/column cues; 2026-06-05 inline upgrade injects `scm_alerts_entity_split.md` *contents* + the regenerate-before-serve rule, once/session) | `[DOM]` ✓✓ |
| 9 | FIF deploy-critical config under blanket ignore (S143) | jebrim `bank/notes` FIF lookup | **caught — now force-INLINED** (same `deploy-schema` row: `FIF`/`gitignore` cues; the FIF quirks note *contents* + "gitignored config is a defect" rule are inlined, not just named) | `[DOM]` ✓✓ |
| 10 | Applicable lorebook decision unread in a player turn (general) | `lorebook/confirmed/D-NNN` (18 live, never loaded — the "~30" was stale) | **caught** ([[S192_e6e7b78d_lorebook_cue_artifact_verify_obedience|S192]] `[LOR]`: cue-armed rules force-inline via `lorebook/_index.md`; the un-cueable rest carry verified `carried-by:` tiers) | `[LOR]` ✓ |
| 11 | "Hey Jebrim, let's work on the SCM" fires no retrieval ([[S177_988f0608_brain_health_check_procedure_and_first_rating|S177]] health-check dim-5) | the 6 SCM bank notes → now the `scm.md` digest | **caught — live-fired** (§Z.C: `domain-cue` auto-discovers the `scm.md` digest, matches `scm`/`shipping costs monitoring`/`alert engine`, force-inlines its synthesized contents once/session; §Z.B always-inlines the domain `_index.md` map. The real-session hand-off landed 2026-06-09: `domain-cue:domain-scm inline` on session 85b0fcc3) | `[DGST]` ✓✓ |

**Today: 8 caught / 2 partial / 1 miss (of 11).** ([[S192_e6e7b78d_lorebook_cue_artifact_verify_obedience|S192]] — `[LOR]` moved case **10 miss→caught**, the `[GND]` artifact verify-and-rebuild moved case **5 miss→caught**. Caught: 1, 2, 4, 5, 8, 9, 10, 11. Partial: 3, 7. Miss: 6. The mechanical ceiling named since the set was built — ~8/10 reachable, 2 judgment — is now REALIZED; what remains is the irreducible `[CRIT]` residue.) Prior state: (§X.3 session — the `deploy-schema` `[DOM]` row moved **cases 8 + 9 miss→caught**, both deploy-time hazards now pointed at jebrim's deploy bank notes by topic cue. Caught: 1, 2, 4, 8, 9. Partial: 3, 7. Miss: 5, 6, 10.) **2026-06-05 force-inline upgrade:** cases 8 + 9 hardened *within* `caught` — their deploy notes are now force-**inlined** (contents in context, once/session) rather than named; tally holds at 5/2/3 because the scorecard has no tier above `caught`, but those two catches are now guarantees on par with the `[FRD]` keepsake inject, not ignorable nudges. (Shipping/case 1 stays a named nudge by design — external home; its strengthening path is the specialist-spawn, not inlining.)
**Count-correction (this session):** the prior summary read "3 caught / 3 partial / 4 miss" but the per-row table only ever carried 3/**2**/5 (caught 1/2/4, partial 3/7, miss 5/6/8/9/10) — an off-by-one slip introduced at the [[S150_26ef6430_session-start-forced-read|S150]] re-score (it counted case 5 or 7 as a third partial; the row + its detail both say `miss`/`partial` as tabulated). Corrected here against the per-row truth before applying the §X.3 moves, so the 5/2/3 above is from the real 3/2/5 base, not the mis-stated 3/3/4.
**Plan target:** `[DOM]` rows closed 8/9 + reinforce 2 (**done this session**); `[FRD]` (live) closed the keepsake-carried own-note miss (2) + firmed 7; `[GND]` artifact-verify closes 5; `[LOR]` closes 10; `[CRIT]` (6, + 7's scout-origin residue) is the irreducible judgment a hook can't catch — critic step or stay discipline. **Reachable: ~8/10 mechanically — REALIZED at [[S192_e6e7b78d_lorebook_cue_artifact_verify_obedience|S192]] (8 caught); 2/10 are judgment.**

---

## Case detail

**1 — S124 shipping report from memory.** Built the automated report as Jebrim-principal without loading the shipping-agent reference; *speculated whether the mart carries dimensions* when `tables.md` answers in one line. Knowledge home: `shipping-agent/reference/`. The guarantee was baked into the shipping-agent SUB-agent config; the principal path had no trigger. **Now caught** by the [[S144_9b67aceb_domain_grounding_cue_registry|S144]] domain-cue (`shipping`/`mart`/carrier cues). Live-fire from a real Jebrim mart prompt still pending.

**2 — [[S091_remove_leftover_windows_toast_notifier|S091]] cost-NULL misread.** Had the signal that a NULL cost = a shipment in flight (transient), but read it as a data-quality gap. **Now caught ([[S150_26ef6430_session-start-forced-read|S150]] `[FRD]`):** the jebrim `keepsake/current.md` pins the `cost_source` breakdown verbatim — `'invoice' 65%, 'expected' 24%, NULL/uncosted 8%, 'avg' 2%` — and `keepsake-forced-read.py` **hard-inlines that keepsake into context on the first prompt**, so "NULL is a normal 8% category (uncosted/in-flight)" is in view *before* the analysis, not a note the agent must decide to open. The live shipping `domain-cue` independently points at the reference. (Honest edge: the keepsake gives the *category*; the full "transient, don't treat as a gap" framing is in the bank note. Keepsake-fact + domain-cue together are enough to stop the misread — caught.)

**3 — [[S066_7f5db8c5_cockpit-sweep|S066]] property "new third".** Analyzed an apartment as a new, third property before recognizing it as [[S056_e433ac17_switchboard-osrs-chatbox|S056]] apartment #1. Prior analysis was in zezima `research/`+`bank`. Grounding-cue catches this *if* the prompt carried a continuation cue ("again", "back to"); a cold fresh framing slips. *Partial.*

**4 — [[S076_322cb5c3_cockpit-vscode-rename-and-focus|S076]] "the message I prepared".** Grepped the working repo for a prepared message; it was in jebrim's own `bank/notes`. Grounding-cue fires on "prepared earlier" → **caught.** (This is the case that motivated grounding-cue; it's the proof the pattern works.)

**5 — [[S095_f9310a45_cockpit-transcript-full-width-speech-bubble|S095]] folio cold.** Opened an uploaded folio and re-derived the whole analysis before recognizing it as Gate 2 of the [[S066_7f5db8c5_cockpit-sweep|S066]] deep-dive. **Now caught ([[S192_e6e7b78d_lorebook_cue_artifact_verify_obedience|S192]]).** The verify came first, and the original path FAILED it: the ARTIFACT_KEYS payload-field guess was dead code — the shipped binary builds the UserPromptSubmit input as `{...base, hook_event_name, prompt, session_title}`, no attachment field exists. What uploads DO leave is their placeholder in the prompt text — the binary's own grammar (`[Image #N]`, `[Pasted text #N +M lines]`, `[...Truncated text...]`), receipt: `[Image #1]` observed inline in a hook-written log (`chat.ndjson`, real Jebrim sid `36b49f0c`). The rebuilt detector matches those shapes (+ dragged absolute doc paths — plausible-but-unobserved, kept narrow), fires once per session for artifact-only prompts, and the [[S095_f9310a45_cockpit-transcript-full-width-speech-bubble|S095]] shape (a session opening on an upload) now triggers the "map the upload to prior work" nudge. `test_grounding_cue.py` 16/16.

**6 — [[S113_b072b8df_bankstander-question-and-godly-proposal-flow|S113]] fiction pipeline.** Asserted the system picks up Guthix's suggestions through a pipeline that didn't exist — a design story stated without checking the record. No topic cue; the failure is *asserting without verifying*. **Miss**, and not hookable — this is the `[CRIT]`/judgment residue (the brain's own "check the record before defending a design story" lesson).

**7 — [[S145_543c6caf_knowledge_loading_and_rule_adherence_audit|S145]] keepsake (this session).** A scout reported global+player keepsake "empty"; I repeated it as a cheap-win recommendation without opening the files (they were mostly full). Same class as #6: a claim asserted without verification. **Now partial ([[S150_26ef6430_session-start-forced-read|S150]] `[FRD]`):** `keepsake-forced-read.py` hard-inlines the keepsake into the *main* agent's context + the SessionStart directive says, in as many words, *treat any "layer empty" claim as a hypothesis to verify against the file* — aimed at this exact failure. So the main agent that *echoed* the claim now has the content in front of it. **Not caught**, because the false claim *originated* in a SCOUT sub-agent (skipped by the hook — `agent_type` set), and reconciling a sub-agent's report against context is judgment (`[CRIT]`). The hook closes the echo, not the origin. Still the strongest case that prose rules don't self-enforce — and the most direct argument for *forcing* over *naming*.

**8 — [[S098_b53fca39_obsidian_fit_and_dlink_migration|S098]] schema-add 500.** A serving change filtered on a new column before the data was regenerated; filtered views 500'd while "All" worked. The ordering rule was in jebrim `bank/notes` (`scm_alerts_entity_split.md` + the schema-add-ordering memory). **Still miss after [[S150_26ef6430_session-start-forced-read|S150]]** — and this is the honest boundary of `[FRD]`: the forced-read surfaces *keepsake + the active resume*, not an arbitrary bank note that is neither pinned nor in the current resume. The schema-add-ordering note is neither, so `[FRD]` does not reach it. Closing 8 needs the *topic* trigger — a deploy/schema `[DOM]` registry row (one dict in `cue_registry.py`), the cheap sibling build. `[FRD]` and `[DOM]` are complementary, not interchangeable: `[FRD]` forces *standing* context, `[DOM]` routes to *topical* knowledge homes.

**9 — S143 FIF gitignored config.** A deploy-critical lookup caught by a blanket `*.csv`/data ignore was nearly architected-around instead of fixed. The "gitignored config is a defect" lesson + the FIF note existed. **Miss**; an FIF/deploy domain row catches the topic.

**10 — lorebook decisions unread (general).** 18 live `lorebook/confirmed/D-NNN` decisions (the "~30" was stale) are not per-player-scoped and never load at respawn. **Now caught ([[S192_e6e7b78d_lorebook_cue_artifact_verify_obedience|S192]] `[LOR]`).** The honest finding while building it: most decisions were *already carried* by another tier — always-on CORE ([[D-025_offer-multiple-choice-with-recommendation|D-025]]/[[D-027_plain-text-deliverables-for-terminal-copy|D-027]] live in `communication-protocol.md`), ritual files read at their moment ([[D-026_graduate-complete-ready-quests-in-session|D-026]]/[[D-029_auto-graduate-unambiguous-complete-ready-quests|D-029]]/[[D-030_alching-sweeps-orphan-subagent-traces|D-030]]/[[D-031_partial-alch-by-draft-settledness|D-031]]), or the hooks themselves ([[D-028_grounding-precondition-needs-a-trigger|D-028]] IS grounding-cue; [[D-032_braindead_full_access|D-032]] is the floor bypass) — so the real gap was the residue with no carrier: [[D-023_powershell-utf8-readall-not-getcontent|D-023]] (PowerShell mojibake), [[D-024_scope-git-commits-with-pathspecs-parallel-sessions|D-024]] (pathspec commits), [[D-017_user-only-with-explicit-permission|D-017]] (user-only is default-no), [[D-034_guthix_executes_on_explicit_authorization|D-034]] (Guthix authorized execution). The mechanism: `lorebook/_index.md` maps every decision (distilled rule + cue patterns OR a `carried-by:` label), and the `[LOR]` arm in `domain-cue-reminder.py` force-inlines a matching decision's rule once per session, then goes silent (commit-shaped cues recur; a repeating 3-line reminder is wallpaper). Maintenance: promotion rituals add the row; `hygiene-check.py` Axis C flags unindexed/ghost entries. One honest residual: [[D-033_act-only-when-an-act-is-merited|D-033]] (act-only-when-merited) has no clean topic cue — its trigger is "the prompt is a question" — so it stays carried by memory/examine, named as such in the index.

**11 — [[S177_988f0608_brain_health_check_procedure_and_first_rating|S177]] SCM fires no retrieval.** The health-check dimension-5 finding made it concrete: *"Hey Jebrim, let's work on the SCM"* triggered **no** retrieval mechanism — `SCM` matched no `cue_registry.py` pattern, the keepsake force-read carries mart-not-SCM pins, and the ~6 real SCM bank notes loaded only on the model's discretion (the unmeasured layer). The knowledge existed; nothing forced it on-topic — the all-or-nothing-manual gap §Z was built to close (topic-routing had no ladder). **Now caught (`[DGST]`, §Z.B/Z.C):** alching synthesized the 6 notes into the `scm.md` digest (Z.A); `domain-cue-reminder.py` auto-discovers it from `players/jebrim/bank/domains/` and force-inlines its contents when `scm`/`shipping costs monitoring`/`alert engine`/`cost dashboard` fires; `keepsake-forced-read.py` always-inlines the `_index.md` domain map so the topic is on the player's radar even before the cue. Boundary-verified (`test_domain_digest_discovery.py` 23/23, `test_keepsake_forced_read.py` 25/25 incl. the §Z.B arm); live-fire from a real Jebrim "SCM" prompt is the hand-off (look for `domain-cue:domain-scm` + `forced-read:domain-index` rows in `switchboard/ritual-events.ndjson`).

---

## Notes & honesty

- Cases 6/7 are the **irreducible residue**: "verify before asserting" is judgment, not a bright line — a hook can't grep it. They belong to the critic-step / reasoning-prose bucket, and are the cases that prove *not everything can be hooked* (so the plan must not pretend otherwise).
- This set is **deliberately small and fixed.** It is an eval, not a survey — its job is to make each fix's payoff legible (move the scorecard), not to catalogue every miss.
- Re-score this table after each Phase 2/3 build. A fix that doesn't move it (or regresses a `caught`) hasn't earned its place.
- **Re-score log.** §X.5 @import-trim **Stage A** ([[S145_543c6caf_knowledge_loading_and_rule_adherence_audit|S145]]), **Stage B** ([[S147_79b03308_import_chain_trim_stage_b|S147]], `modes`+`write-rules` → JIT), and **Stage C** ([[S148_4d020f31_import_chain_trim_stage_c|S148]], the `communication-protocol` split — operational sidecars → new JIT `meta/intent-narration.md`) all left the scorecard **unchanged at 2/3/5** — expected, since they are load-order changes (what's *eagerly* expanded), not retrieval-mechanism changes (what *pushes* knowledge into view). No case here depends on `modes.md`/`write-rules.md`/intent-narration being eager. The trim's payoff is the cumulative weight cut (chain 23.3k→11.9k, ~49%) proven *not to regress* coverage — exactly the "smaller without getting dumber" bar.
- **Re-score log (§X.10, the `Reading:` preamble line, [[S148_4d020f31_import_chain_trim_stage_c|next session]]).** Scorecard **unchanged at 2/3/5** — and this is the *honest* result, not a disappointment. The `Reading:` line is **R3-tier (visible, not enforced)**: it surfaces the agent's grounding plan at the Plan line but does **not force** the read, so by this table's scoring rule (*would the right knowledge have been forced into view before the agent acted?*) it catches nothing new. Its value is **orthogonal to this scorecard**: it makes cue-obedience *observable* on the cases already marked `caught` (1, 4) — where a hook fires but obedience is unmeasured (#1 domain-cue "fired 14× in 30d — obedience unmeasured"). A future `ritual-stats` arm parsing the `Reading:` line against the hook's nudge is what would turn that observability into a number; the line itself is the precondition. CORE grew 3,722→4,036 tok (chain 11,899→12,213, +314) — the acceptable cost of a per-turn behavioral rule. The actual coverage move is **X.4** (the `SessionStart` forced-read), which this line *complements, not replaces*.
- **Re-score log (§X.4 — `keepsake-forced-read.py`, the SessionStart forced-read, [[S150_26ef6430_session-start-forced-read|S150]]).** **First scorecard move off 2/3/5: now 3 caught / 3 partial / 4 miss.** Unlike every prior §X build (all load-order, scorecard-neutral by design), this one is a *retrieval-mechanism* change — it adds a scorer. **Case 2 partial→caught** and **case 7 miss→partial**, both because the hook **hard-inlines the keepsake CONTENTS** (genuine forcing) rather than naming a read — the distinction that separates `[FRD]` from the `Reading:` line (§X.10) it pairs with. The honest non-moves: **case 8 held at miss** (`[FRD]` forces keepsake+resume, not an unpinned bank note — needs a deploy `[DOM]` row); **case 7 capped at partial** (the hook reaches the main agent's echo, not the scout sub-agent that originated the false "empty"). **Weight:** the `@import` chain is **unchanged at 11,886 tok** — X.4 adds *zero* to the eager load (it's a runtime hook injection, not an `@import`; the Stage-A–D trim is fully preserved). The runtime cost is the SessionStart directive (~130 tok) + the global keepsake (a placeholder today) every session, and the player keepsake re-injected once on the first prompt — but that player keepsake is *already* counted in `brain-weight`'s ritual-read subtotal (respawn nominally reads it), so X.4 doesn't add it as new weight; it **converts a counted-but-drifting ritual read into a guaranteed one**. That is the whole §X thesis in one mechanism: same content, moved from discipline to guarantee. **Live-fire pending** — the SessionStart arm fires only at a real session start and the player arm skips Braindead, so the boundary harness (19/19) is the proof today; first real Jebrim/Zezima session start is the hand-off (look for `forced-read:session-start` + `forced-read:player-inject` rows in `switchboard/ritual-events.ndjson`).
- **Re-score log (§X.5 @import-trim Stage D — `task-lists` split + R3 rule-index, same session).** Scorecard **unchanged at 2/3/5** — load-order again, not retrieval. `task-lists.md`'s *threshold* (the load-bearing per-turn decision + the durable-`inventory`-mirror trigger) stayed in the always-on CORE; only the *mechanics* (two-homes table, lifecycle, anti-patterns, scope → new JIT `meta/task-lists-mechanics.md`) left the eager chain. No regression case depends on task-list mechanics being eager. Net chain effect across X.10 + Stage D: `task-lists.md` 1,415→721 (−694) offset X.10's +314, so the chain landed **11,886 tok — fractionally below the 11,899 session-start** while *adding* the `Reading:` rule + the R3 rule-index. "Smaller without getting dumber" held a fourth time.
- **Re-score log (§X.3 — `draft-gate-rewrite.py` + the `deploy-schema` `[DOM]` row, this session).** Two builds, two axes. **(a) The draft-gate hook (X.3 proper)** is a *write-discipline* guarantee — it redirects a main-agent `bank/notes/`/`spellbook/skills/` write into the `drafts/` home so fresh knowledge goes through the alching gate by guarantee, not discipline. It moves **no** case on this scorecard *by design*: this table measures knowledge-*retrieval* misses (knowledge that existed and wasn't loaded), and X.3 governs *how knowledge is written*, not whether prior knowledge loads. It belongs to [[S145_543c6caf_knowledge_loading_and_rule_adherence_audit|S145]]'s other diagnostic half (decided-rules-drift → hook the bright line), whose eval is the adherence axis (ritual-stats / boundary harness 19/19), not this set. Logged here for the record; correctly scored nowhere. **(b) The `deploy-schema` `[DOM]` row** (the cheap sibling §X.4 left open, built in the same session since X.3 landed clean) IS a retrieval mechanism and **moved the scorecard: cases 8 + 9 miss→caught** (3/2/5 → **5/2/3**). One registry dict, patterns `deploy`/`schema`/`migration`/`DAG`/`gitignore`/`FIF`/column-add, pointing at jebrim's real deploy bank notes (`bi_analytics_deploy_topology.md`, `scm_alerts_entity_split.md`, `2026-05-28-ups-orwo-fif-data-quirks.md`); end-to-end verified (fires on case-8/9 prompts, silent on benign + shipping, shipping row unregressed). **Weight:** `@import` chain **unchanged at 11,886 tok** — both are runtime hooks, zero eager add (the draft-gate injects nothing into context unless a write triggers a redirect; the registry row only fires on a topic-cue match). Same §X thesis as X.4: convert discipline to guarantee at zero always-on cost. Also **corrected an off-by-one** in the [[S150_26ef6430_session-start-forced-read|S150]] summary (3/3/4 → the true 3/2/5 base) before applying the moves. **Live-fire pending** for the draft-gate (it skips Braindead, so a real player `bank/notes/` write is the hand-off — look for `draft-gate:redirect` rows in `switchboard/ritual-events.ndjson`) and the deploy-schema cue (a real Jebrim deploy/schema prompt → `domain-cue:deploy-schema`).
- **Re-score log (§Z.B + §Z.C — the per-player `bank/domains/` domain-knowledge layer, this session).** **First scorecard move since §X.3: now 6 caught / 2 partial / 3 miss (of 11)** — case **11 added and immediately caught**, the SCM no-retrieval miss the [[S177_988f0608_brain_health_check_procedure_and_first_rating|S177]] health check surfaced. The new scorer is `[DGST]`: a *per-player, auto-discovered* domain digest, distinct from the `[DOM]` registry rows (those are global + hand-listed, for external/specialist homes; `[DGST]` is the player's own topics, discovered from `bank/domains/*.md` frontmatter — adding a domain is an alching file-drop, not a hook edit). **Z.B** (always-inline the `_index.md` domain map, in `keepsake-forced-read.py`) gives the player the *map*; **Z.C** (auto-discover + cue-match the digest, in `domain-cue-reminder.py`) force-inlines the *digest* on-topic — the same forcing-over-naming move as `[FRD]`/§X.4, applied to topic-routing instead of standing context. **Why it's a genuine catch, not a renamed `[DOM]`:** the SCM notes were *un-routable* before §Z — `SCM` matched no registry pattern and no one was going to hand-add a row per topic (the named-but-unbuilt-for-weeks problem the diagnosis names). Auto-discovery removes the hand-add step, which is what makes the catch durable across *future* domains (EU-tender, carrier-contracts) without a build each. **Weight:** the `@import` chain is **unchanged at 12,232 tok** — both arms are runtime hook injections (zero eager add): the `_index.md` map (~1.8 KB) injects once on the first player prompt beside the keepsake (a counted ritual read converted to a guarantee, same as the keepsake itself), and a digest (~2 KB, byte-capped) injects only on a topic-cue match. Same §X/§Z thesis: convert a discretionary read into a guarantee at zero always-on cost. **Live-fire pending** (both arms skip Braindead): a real Jebrim "SCM" prompt → `domain-cue:domain-scm` (inline) + `forced-read:domain-index` rows in `switchboard/ritual-events.ndjson`. Boundary proof today: `test_domain_digest_discovery.py` 23/23 + `test_keepsake_forced_read.py` 25/25 (reads the live `scm.md`/`_index.md`, so the real digest/map are observed inlining).
- **Re-score log ([[S192_e6e7b78d_lorebook_cue_artifact_verify_obedience|S192]] — the `[LOR]` lorebook cue arm + the `[GND]` artifact verify-and-rebuild + the tier-3 obedience instrument; sid e6e7b78d, 2026-06-11).** **6/2/3 → 8 caught / 2 partial / 1 miss — the mechanical ceiling the set has named since it was built ("reachable ~8/10; 2/10 are judgment") is REALIZED.** Two genuine mechanism moves, one measurement: **(case 10 miss→caught, `[LOR]`)** the synthesized `lorebook/_index.md` (all 18 confirmed decisions — the "~30" was stale — distilled rule + literal cue patterns, or an honest `carried-by:` label for the 14 already carried by always-on CORE / ritual reads / hooks) + a new arm in `domain-cue-reminder.py` that force-inlines a matching decision's RULE once per session then goes silent (no name-nudge tail — "commit" recurs too often for one); 4 decisions cue-armed ([[D-017_user-only-with-explicit-permission|D-017]]/[[D-023_powershell-utf8-readall-not-getcontent|D-023]]/[[D-024_scope-git-commits-with-pathspecs-parallel-sessions|D-024]]/[[D-034_guthix_executes_on_explicit_authorization|D-034]]); maintenance = promotion rituals add the row + `hygiene-check.py` Axis C flags drift; `test_lorebook_cue.py` **22/22**, end-to-end live-fire from the real script entry point observed emitting the [[D-024_scope-git-commits-with-pathspecs-parallel-sessions|D-024]] rule. **(case 5 miss→caught, `[GND]`)** the verify came FIRST and the old path failed it — ARTIFACT_KEYS was dead code (the shipped binary constructs the UserPromptSubmit payload with NO attachment fields: `{...X9, hook_event_name, prompt, session_title}`); uploads instead leave PLACEHOLDERS in the prompt text per the binary's own grammar (`[Image #N]` / `[Pasted text #N +M lines]` / `[...Truncated...]`), receipt: `[Image #1]` inline in the hook-written `chat.ndjson` (real sid `36b49f0c`); detector rebuilt on those shapes (+ narrow dragged-doc-path arm, honestly unobserved), artifact-only fires once/session; `test_grounding_cue.py` **16/16** — the hook's first suite. **(the tier-3 number)** `adherence-rates.py --obedience` joins each real nudge to its transcript turn and scores `Reading:` lines + actual cued-home tool calls: **all-time either-signal obedience 50% (173/349 scored; shipping 47% n=268, grounding-cue 65% n=68; `Reading:`-line alone 15%)** — "fired 295×, obedience unmeasured" is now a number; caveat in-report: post-inline name-nudges (deploy-schema, domain-*) are not disobedience, the content was already injected. Snapshot appended (`kind:"obedience"`). Suites green: lorebook-cue 22/22, grounding-cue 16/16, domain-cue-inline 26/26, registry 39/39, digest-discovery 23/23, keepsake 25/25, adherence ALL PASS, hygiene 16/16 (incl. live-index sync). **Honest residuals:** (a) the remaining miss (6) + partial-half of 7 are the `[CRIT]` judgment residue — correctly un-hookable; (b) case 3 stays partial (a cold re-framing with no cue and no artifact still slips); (c) the dragged-file arm of `[GND]` is unobserved in 411 transcripts — placeholder arms are the verified core; (d) live-fire of the `[LOR]` arm from a real PLAYER session (vs the script-boundary live-fire) is the standing hand-off — look for `lorebook-cue:D-NNN inline` rows in `ritual-events.ndjson`; (e) still no real digest inline inside the Fable-5 window (checked this session — last real inline remains 2026-06-10 14:06 pre-switch).
- **Re-score log (Fable-5 re-baseline — model switch, not a mechanism build; [[S187_76f2e60f_fable5_rebaseline_and_hygiene_detectors|S187]]/76f2e60f, 2026-06-11).** **Tally holds at 6 caught / 2 partial / 3 miss (of 11)** — and unchanged is the *correct* result, not a null one: this table scores *mechanisms* (“would the right knowledge be forced into view”), and every scorer is a Python hook outside the model, so a model switch cannot move the table by construction. What the switch *could* have broken — payload contracts, hook wiring under the new harness — re-verified clean: **all seven boundary suites green** under the current environment (`test_keepsake_forced_read` 25/25, `test_domain_digest_discovery` 23/23, `test_domain_cue_inline` 26/26, `test_domain_cue_registry` 39/39, `test_actor_resolution` 5/5, `test_draft_gate_rewrite` 19/19, `test_adherence_rates` ALL PASS), and the scorers observed **firing on real Fable-5 sessions** in `ritual-events.ndjson` (post-switch window opens ~23:00 2026-06-10; [[S186_486d6682_fresh_audit_fixes_respawn_lock|S186]] = first Fable-5 session): `[FRD]` session-start + player-inject + the §Z.B domain-index on the parallel Jebrim alching session `c5915b03` and on `907d4e63`; `[GND]` grounding-cue nudge on [[S187_76f2e60f_fable5_rebaseline_and_hygiene_detectors|S187]]’s own opening prompt (**with observed obedience, n=1** — the referent was mapped to [[S186_486d6682_fresh_audit_fixes_respawn_lock|S186]]’s hand-off before work began); `[DOM]` shipping nudge (`907d4e63`); draft-gate `bypass-ritual` during the live alching (`c5915b03`); require-open allow + block. **Closed hand-offs from prior logs:** case 11’s “live-fire from a real Jebrim SCM prompt” landed 2026-06-09 — `domain-cue:domain-scm inline` on real session `85b0fcc3` (old-model but real; row updated to ✓✓); the §Z.C digest-inline arm has also fired live for `eu-tender`, `carrier-contracts`, `nfe-repo`, `production-times`, `shipping-mart`. **Honest residuals:** (a) no real *digest inline* observed inside the Fable-5 window yet (last real inline 2026-06-10 14:06, pre-switch) — mechanism-identical, watch for the first; (b) tier-3 obedience remains unmeasured beyond the n=1 above; (c) judgment cases 6/7 got one positive Fable-5 data point — [[S186_486d6682_fresh_audit_fixes_respawn_lock|S186]]’s scout-claim spot-verification disproved a false scout defect *before* echoing it, the exact case-7 failure shape, resisted. **Eval-hygiene defect found while re-scoring:** the boundary suites **append their synthetic events to the live `switchboard/ritual-events.ndjson`** (~173 events across 5 test days; sids `aaaa1111`/`scmone00`/`sess*`/`test1234`/`zv_*`/`zzzz9999`/`deadbe99` — several are hex-shaped, so no regex cleanly separates them) — polluting the adherence stream the sibling instrument reads. Handed to [[S187_76f2e60f_fable5_rebaseline_and_hygiene_detectors|S187]] step 3 (telemetry hygiene): tests should target a temp events file; until then, rate computations must filter the known synthetic sids.
