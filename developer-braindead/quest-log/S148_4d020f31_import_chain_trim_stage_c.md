# S148 — 2026-06-02 — @import-chain trim Phase 1 Stage C (communication-protocol split)

Dev-brain via "lets develop gielinor"; OPEN→CLOSING via `comms_append`; no live Braindead siblings ([[S147_79b03308_import_chain_trim_stage_b|S147]] 79b03308 closed 13:20). Continued the [[S145_543c6caf_knowledge_loading_and_rule_adherence_audit|S145]] anti-deterioration work: executed **Stage C** of the `@import` trim ([[plan|plan]] §X.5; blueprint `bank/research/2026-06-02-phase1-import-chain-trim-design.md`). The delicate, behavioral one — split `meta/communication-protocol.md` (5,590 tok, the **biggest single @import**). Edited `meta/` directly under [[D-032_braindead_full_access|D-032]] (no godly proposal).

- **The cut — behavioral CORE stays always-on, operational plumbing goes JIT.** The 4 contiguous visualizer/operational sections (Intent narration sidecar · `.mode` markers · Narration channel · Intent-vs-action incl. *render the cut*) were lines 159–213 — one clean block. Carved them verbatim into a **new** [[intent-narration|gielinor/meta/intent-narration.md]] (JIT, never @imported) and replaced them with a pointer. The behavioral sections (1–11: Understanding/Plan preamble · compression · five-lens · copyable-deliverables · dwarf-spawn annotation · task-list surfacing · applies-regardless · internal-rituals-silent · **wrong-instance** · **Guthix-routing** · **multiple-choice-w-recommendation**) are **byte-identical** — only the operational block left + a Related pointer added.
- **R1 re-trigger (design's coupling principle — only trim what a ritual reads).** Added a forced-read of `meta/intent-narration.md` to [[respawn|spellbook/rituals/respawn.md]]'s *Per-turn discipline* step — respawn runs every session and writes the first intent bubble, so the sidecar mechanics are reliably in context the moment narration starts (same pattern as Stage A's death-and-spawn read). [[CLAUDE|gielinor/CLAUDE.md]] JIT-list grew the 6th bullet + count A–B→A–C / five→six.
- **The behavioral/operational seam (judgment call, surfaced to Niklavs).** Kept **dwarf-spawn annotation** + **task-list surfacing** in CORE (they're about the visible *Plan line*); moved *render-the-cut* JIT (it's about the *bubble*). Niklavs did not redraw it.
- **Verified — smaller without dumber.** `brain-weight.py`: chain **13,639 → 11,899 tok** (5 files unchanged; `communication-protocol.md` 5,590→3,722, CLAUDE.md +128 for the bullet); **~49% off the pre-Stage-A 23,295**. The ~2.6k carved content lives in `intent-narration.md` *outside* the chain. Regression set re-scored **2 caught / 3 partial / 5 miss — unchanged** (load-order, not retrieval; no case depends on intent-narration being eager). Adherence unchanged (no hooks touched; Band A firing pattern identical).
- **Committed** by pathspec (the 4 gielinor files: `communication-protocol.md`, new `intent-narration.md`, `CLAUDE.md`, `respawn.md`) + this close.

**Open:** **Stage D** (the cleanup tail — `task-lists.md` split + the R3 rule-index + final CORE thinning toward the design's ~6.6k target); the *other* diagnosis half — the **X.2–X.4 hooks** that actually *move* the regression set (X.2 Stop-gate [PRECONDITION: fix `close_check` continuation false-FAIL first], X.3 draft-gate input-rewrite, X.4 SessionStart forced-read + fill keepsake); domain-cue live-fire still unmeasured; Khaan item 9; 2 [[S128_b64229ad_comms-append-lock|S128]] ritual one-liners; cockpit rebuild.

**Cascade.** `developer-braindead/`: this quest-log entry; `respawn.md` (prepended S148 block); `bank/plan.md` §X.5 (Stage C marked done); `verification/knowledge-miss-regression-set.md` (re-score log += Stage C); `comms/active.md` (OPEN + CLOSING).
**Main-brain changes.** `gielinor/meta/communication-protocol.md` (carved to behavioral CORE), **new** `gielinor/meta/intent-narration.md` (JIT operational half), `gielinor/CLAUDE.md` (JIT-list 6th bullet), `gielinor/spellbook/rituals/respawn.md` (R1 forced-read).
