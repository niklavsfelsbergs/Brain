# S074 — 2026-05-24 — Cockpit switchboard status pipeline review + fixes

- Reviewed the cockpit status path end-to-end ([[status-sidecar.py]] hook → manifest → [[backend.py]] ranking → [[board.js]] render). Surfaced two bugs + one drift; principal cued all three to fix.
- **#1 idle never derived.** The sidecar docstring promises a *reader*-derived idle (`waiting_for_user` + >5 min stale → idle); the cockpit rebuild never ported it (the old switchboard's `state.js`/`deriveSessionState` did). Consequence: `Stop` stamps `waiting_for_user` at every turn-end, so every finished-but-open session read "WAITING ON YOU", stayed `attention=true`, and counted in the "N need you" tally for up to the 1 h `LIVE_SESSION_SEC` gate. Fixed: [[backend.py]] `build_session_model` decays a stale `waiting_for_user` → `idle` (attention off, rank 7); `waiting_for_answers` (a live mid-turn question) stays hot. New `IDLE_AFTER_SEC = 300`.
- **#2 CLOSING was a dead chip.** Backend ranked it, [[board.js]] labelled it, `styles.css` coloured it — but nothing emitted it (the hook never stamps `closing`; the old client-side "Wrapping up SNNN" derivation wasn't ported, and `wrapped_up` covers close now). Removed from `STATE_RANK` + board `STATE_LABEL`. The `styles.css` `.state-closing` rules were ceded to live S073 sibling @d82c4fbc (owns that file this window) — pure dead-CSS, no breakage if it lingers.
- **#3 dead headless /chat driver — and it was the metered one.** The [[S066_7f5db8c5_cockpit-sweep]] B-pivot moved live sessions onto the PTY (on subscription); `fleet.js::place()` was never called and `openOwned()` was unreachable (its `owned` set never seeded), so backend `chat_handler` + the `/chat` WS route were dead headless `claude -p` (metered post-2026-06-15). Removed the route + handler + `_ws_send` + `NO_TTY_TOOLS`; slimmed [[fleet.js]] to read-only peek (`openPeek`/`loadHistory`); rewired [[main.js]] `selectRow` + import. `/pty` untouched.
- Verified: backend imports + `make_app` build (routes show no `/chat`, `/pty` intact), a synthetic-manifest test confirms the idle decay (stale `waiting_for_user` → idle/attention-off while a fresh park + open question stay hot), all client files `node --check`. Committed **802de1d** (4 files, −336/+46) solo with explicit pathspecs while two siblings were live on the cockpit (@322cb5c3 disk-backed `/rename`, @d82c4fbc terminal-scroll); D-024 composed cleanly — their S073 rename additions (`state-names.json`, `api_open_vscode`, board label) landed on top of my hunks without touching them.

**Cascade.** [[backend.py]], [[fleet.js]], [[main.js]], [[board.js]] (commit 802de1d); this quest-log entry; `respawn.md` S074 prepend; `comms/active.md` OPEN/UPDATE/CLOSING. `styles.css` `.state-closing` cleanup handed to @d82c4fbc.
**Main-brain changes.** none — cockpit + dev-brain only; no `gielinor/` writes.
