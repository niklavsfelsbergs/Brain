# S150 — 2026-06-02 — SessionStart forced-read injection (§X.4)

Dev-brain via "lets develop gielinor"; OPEN→CLOSING via `comms_append`; no live Braindead siblings ([[S149_c6985ff9_reading-preamble-line-and-import-trim-stage-d|S149]] CLOSED 14:05). Continued the [[S145_543c6caf_knowledge_loading_and_rule_adherence_audit|S145]] anti-deterioration work — the **hook half** that actually *moves* the regression set, the enforced pairing for [[S149_c6985ff9_reading-preamble-line-and-import-trim-stage-d|S149]]'s `Reading:` line. Built plan [[plan|§X.4]] under [[D-032_braindead_full_access|D-032]] (diff shown before commit, per the brief).

- **Built `gielinor/.claude/hooks/keepsake-forced-read.py`** — a new ADVISORY hook (additionalContext, exit 0 always; a bug can't brick a session, sibling of `grounding-cue`/`domain-cue`). **Two arms**, because SessionStart can't know the player (the address arrives *after* SessionStart): **SessionStart** inlines the GLOBAL `keepsake/current.md` + a forcing directive (read the active keepsake + the relevant `inventory/*-resume`; *treat any "layer empty" claim as a hypothesis to verify against the file* — aimed squarely at regression case 7). **First UserPromptSubmit** hard-inlines the **active player's `keepsake/current.md` contents** once (sentinel `~/.claude/status/<sid8>.fread`), actor via the hardened `_actor.resolve_actor` (status→intent anti-race). Scope: jebrim/zezima/guthix bear a keepsake; unscoped/wisp get only the global (no sentinel → a later player-address still injects); braindead + sub-agents skipped.
- **The load-bearing design call:** *inline the keepsake CONTENTS, don't inject a "go read it" directive.* A directive that says "read X" is barely stronger than the `Reading:` line we just shipped — it still only *names*. Inlining is what **forces the read the prose only names** (Niklavs' framing of why X.4 exists). The cost is the player keepsake re-entering context once per session — but `brain-weight` already counts that read in the ritual-read subtotal (respawn nominally reads it), so X.4 adds **zero new eager weight**; it converts a counted-but-drifting ritual read into a guarantee. The §X thesis in one mechanism: *same content, moved from discipline to guarantee.*
- **Registered** in brain-root `.claude/settings.json` on **SessionStart + UserPromptSubmit** (the default + cockpit launch dir) with a `_comment_forced_read`. The UserPromptSubmit arm hot-reloaded into this very session and correctly skipped (Braindead) — a partial live-fire; the real gielinor-player live-fire is the hand-off.
- **Verified:** `py_compile` + valid-JSON; **boundary harness `verification/test_keepsake_forced_read.py` 19/19 PASS** — asserts the *real* keepsake content is inlined (jebrim "Shipping Data Mart", zezima pins actually appear), sentinel once-only, braindead/unresolved/unscoped silent-no-sentinel, malformed→exit 0, unrelated event→silent.
- **Re-scored `verification/knowledge-miss-regression-set.md` — 2/3/5 → 3 caught / 3 partial / 4 miss, off 2/3/5 for the first time** (every prior §X build was load-order, scorecard-neutral; this one adds a *scorer*). **Case 2** (cost-NULL misread) partial→**caught** — jebrim keepsake pins `cost_source NULL/uncosted 8%` verbatim, now hard-inlined + live shipping `domain-cue`. **Case 7** (keepsake claimed empty) miss→**partial** — hook reaches the main agent's *echo* + the "verify empty" directive, but the scout sub-agent that *originated* the false claim is unreached (judgment residue). **Case 8** (schema-add 500) **held at miss** — honest: `[FRD]` forces *keepsake+resume*, not an arbitrary unpinned bank note; closing 8 needs a deploy/schema `[DOM]` row (the cheap sibling), so I did **not** fudge it. **Weight:** `@import` chain **unchanged at 11,886 tok** — X.4 adds zero to the eager load.
- **`keepsake/current.md` left empty** — the plan flags filling it as principal/alching work, not the hook's. No genuine cross-player global pin exists today (Jebrim shipping/data vs Zezima property — little overlap); manufacturing filler is anti-aligned. The hook surfaces "present, no pins yet" and reads live, so it self-heals the moment Niklavs pins something. Recommended leave-empty; flagged for his call.
- **Did NOT touch X.2 / `close_check`** (no continuation-false-FAIL risk taken on) and **did NOT build X.3** — Niklavs steered to wrap + hand off at the X.4-clean decision point.

**Cascade.** New `developer-braindead/verification/test_keepsake_forced_read.py`; updated `developer-braindead/verification/knowledge-miss-regression-set.md` (re-score), `developer-braindead/bank/plan.md` (§X.4 `[x]`), `developer-braindead/respawn.md`, this quest-log entry, `developer-braindead/bank/build-lessons.md`, `developer-braindead/comms/active.md` (OPEN→CLOSING).

**Main-brain changes.** New `gielinor/.claude/hooks/keepsake-forced-read.py`; `.claude/settings.json` registrations (SessionStart + UserPromptSubmit + `_comment_forced_read`). No `gielinor/` content/identity writes; `keepsake/current.md` deliberately untouched.
