# respawn.md — dev brain entry point

> Read this first when starting a fresh dev session. Where we are, what's open, the next concrete step.
>
> The ritual that reads this file is `spellbook/respawn-ritual.md`. The ritual that *updates* it is `spellbook/session-close.md`.
>
> **Discipline (enforced 2026-05-24 token audit).** Overwritten in place at session close — **NOT append-only**. Keep the current-state block + the most recent one or two session pointers; older per-session history lives in `quest-log/SNNN_*.md` (and in git). Cross-session build lessons live in `bank/build-lessons.md` — read on demand, never loaded at respawn. This file had grown to ~410 lines / ~49k tokens of stacked `Last updated` blocks read every session; that is the bloat the discipline exists to prevent. Don't let it regrow.
>
> **Parallel note ([[D-024_parallel_player_coordination]]).** Multiple dev sessions run at once. At close, **PREPEND** your single `Last updated` line to whatever this file currently says — do **not** overwrite it from a stale full copy you read at your own respawn, or you'll clobber a collapse like this one.

**Last updated (S099 [Obsidian §O.3 — dev-brain quest-link full-stem migration] — extended S098's Option-A rewrite from D-links to quest (`S-`) links; 357 rewrites across 87 tracked dev-brain .md, link-TEXT only, NO file renames; sid acf8fc80).** 2026-05-26 (end of [[S099_acf8fc80_obsidian_quest_link_migration]]. Dev-brain via "lets develop gielinor", mid-conversation; OPEN posted; NO live siblings at entry — two spawned mid-pass (braindead-3b333c4d agentic-OS reflection, braindead-be7867be transcript feasibility), both read-only on existing .md → no collision. **Done:** built a quest-aware, ID-prefix-generic migrator (`bank/research/obsidian-link-migrate.py`, saved for the §O.4 gielinor reuse — S098 ran it ephemeral); dry-run → principal approved. **327 clean** `[[SNNN]]`→main-entry rewrites (74 IDs / 87 files; D-links untouched, 367 already-full-stem left as-is) + **30 per-occurrence dupe disambiguations**: the 3 real dupes S038/S060/S086 were NOT auto-rewritten — resolved by the authors' own context tags (brain-underutil vs vscode-focus; self-audit vs embedded-chat; docs vs compose-bar), principal-signed-off occurrence-by-occurrence. S049 surfaced as a 4th ambiguous group but moot (0 inbound). Convention-doc illustrative examples (`spec.md:9/29`) left bare by design (the S098 self-clobber lesson). **Parallel:** be7867be prepended its S101 block onto this shared file + wrote untracked research; 3b333c4d wrote an untracked gap-survey + holds paste-ready plan.md §M text (Edit-race-deferred). Their new untracked files left UNMIGRATED (S098 precedent — sibling WIP stays theirs); carried be7867be's respawn prepend and migrated its 4 bare links per their OK. **Committed by pathspec** (my ~90 migration files + script + plan/spec status + S099 quest-log + shared respawn/comms) — siblings' untracked WIP NOT staged. **Open:** verify in Obsidian (open `developer-braindead/` as a vault — principal eyeball, still pending from §O.2); §O.4 gielinor pass next (reuse the script, `--vault gielinor/`); §O.5 cross-brain + 2 fuzzy D-001. Strategic next step UNCHANGED — §C shipping-mart pilot ([[D-027_inward_outward_build_imbalance]]). active-mode.txt LEFT `dev-brain` (siblings live). respawn now carries 9 Last-updated blocks — trim overdue, deferred to a quiet no-parallel-close session.)

**Last updated (S101 [transcript-as-sole-comms-surface feasibility — RESEARCH ONLY, nothing built] — can the cockpit transcript view become the only comms surface, read AND drive, never touch xterm? Verdict + hybrid design written to bank/research/; parked behind §C; sid be7867be).** 2026-05-26 (end of [[S101_transcript_as_sole_comms_surface_feasibility]]. Dev-brain via "lets develop gielinor", mid-conversation; OPEN posted; two live siblings — braindead-acf8fc80 (Obsidian §O.3 quest-link full-stem rewrite, link-TEXT across ~87 dev-brain .md incl. comms/respawn/plan, 327 applied + dupe-disambig pending) and braindead-3b333c4d (agentic-OS reflection, read-only). Both clear of cockpit/*. **Question:** sole-transcript comms = "stream the whole terminal content." **Grounded in code** (read-only): transcript.js is a **read-only render of the on-disk `.jsonl`** (`/history`, 2s poll); `/pty` (ptybridge) is the real interactive claude = the **subscription** path; [[S095_f9310a45_cockpit-transcript-full-width-speech-bubble]]'s compose-bar already proves a **DOM→PTY bracketed-paste input channel**. **Verdict:** prose 80% (read+type) is a clean build (stream the jsonl + lift a compose box); *fully* eliminating the terminal is NOT clean — the interactive 20% (permission/plan/`AskUserQuestion`/`@`-picker/autocomplete) is **never in the `.jsonl`** ([[S086_86020a52_cockpit-terminal-compose-bar]] limit), so a transcript-only view goes blind exactly when a decision is needed. "Stream the whole terminal" = rebuilding xterm in DOM, worse. **Real ceiling = billing:** structured/interactive-aware I/O lives on headless `stream-json` ([[S060_ac10ec71_switchboard-embedded-chat]]) which *meters*; subscription PTY is ANSI-only. **Recommended-when-built: hybrid auto-flip** — transcript default, auto-surface the terminal only on the sidecar's existing `waiting_for_user` signal (no new brittle ANSI parsing). Principal chose **research-only** → not built; load-bearing gap stays the outward §C pilot. Deliverable: `bank/research/2026-05-26-transcript-as-sole-comms-surface.md` + [[S101_transcript_as_sole_comms_surface_feasibility]] quest-log. My comms appends + this prepend ride in acf8fc80's shared tree (NOT staged — would sweep their link-rewrite WIP); commit scoped to my 2 NEW files only. NO cockpit/* writes, NO gielinor/ writes. active-mode.txt LEFT `dev-brain` (two siblings live). respawn now carries 8 Last-updated blocks — still overdue for a trim in a quiet, no-parallel-close session.)

**Last updated (S098 [Obsidian fit research + D-link full-stem migration pilot] — researched Obsidian, found the brain is already a `[[wikilink]]` vault but stock-resolver-phantom; amended founding decision [[D-004_stable_ids]]; ran the dev-brain `D-` link migration (341 rewrites / 97 files); sid b53fca39).** 2026-05-26 (end of [[S098_b53fca39_obsidian_fit_and_dlink_migration]]. Dev-brain via "lets develop gielinor", mid-conversation; OPEN posted; live siblings braindead-f9310a45 ([[S095_f9310a45_cockpit-transcript-full-width-speech-bubble]] cockpit) + braindead-b3c2b8dd ([[S097_b3c2b8dd_rag_research_and_obsidian_interlink]] RAG) — no overlap. **Research (WebSearch):** Obsidian = a vault of plain `.md` + `[[wikilinks]]` + graph/backlinks/Dataview; the brain IS one already, so adoption is a *lens*, not a format migration. **Crux:** stock Obsidian resolves `[[ ]]` by *exact filename*, so our `[[D-027]]`/`[[SNNN]]` ID-links render ~91% phantom. Modelled the resolver in a script (don't eyeball a GUI): single combined vault → 9% (alarming, WRONG topology); **per-brain vaults → 71%** — principal corrected me; the brains are per-vault scoped in practice and the "collisions" are mostly deliberate sub-agent `_dN/_pN` + quest+resume conventions, NOT debt. **Founding-decision tension:** full-stem links overturn [[D-004_stable_ids]]'s "short ID alone is canonical / suffix not load-bearing." Surfaced it (don't silently break a founding decision) → principal pushed "does an early rule still stand in the way?" → it didn't (guards a near-nonexistent, recoverable slug-rename) → **amended D-004** (stable-ID core kept, link-clause retired, original preserved). **Migration pilot (decisions only; link-TEXT rewrite, NO file renames → hook/cockpit filename parsing untouched):** 341 `[[D-NNN]]`→full-stem across 97 files; auto for 27 1:1 IDs + D-012; D-001/D-002 split by the "main brain" qualifier (the `bank/main-brain-construction/` mirror overloads those IDs); code-fenced + illustrative occurrences excluded/hand-edited. **Caught my own bug:** the script clobbered the spec doc's own illustrative `[[D-027]]` examples — fixed all 8. Spec + full rationale: `bank/research/obsidian-fit-and-migration-spec.md`; roadmap parked in `plan.md` §O; 6 lessons → `build-lessons.md` (1 → cross-conv memory). **Open:** verify in Obsidian (open `developer-braindead/` as a vault — principal eyeball); extend to dev-brain quests then gielinor; 2 fuzzy D-001 calls. NO `gielinor/` writes, NO cockpit/* writes. Strategic next step UNCHANGED — §C shipping-mart pilot ([[D-027_inward_outward_build_imbalance]]). `active-mode.txt` → unscoped. respawn now carries 7 Last-updated blocks after parallel S095/S097 closes — overdue for a trim in a quiet, no-parallel-close session.)

**Last updated (S095 [cockpit transcript redesign + compose-Enter fix + auto-name persistence + textbox-stuck #2 root-caused] — node-checked, RUNTIME-UNVERIFIED; two transient loggers still in; sid f9310a45).** 2026-05-26 (end of [[S095_f9310a45_cockpit-transcript-full-width-speech-bubble]]. Dev-brain via "lets develop gielinor", mid-conversation; OPEN posted; live siblings braindead-6acf797a (S096 docs), b3c2b8dd (S097 RAG), b53fca39 (Obsidian) — no cockpit overlap. Adopted braindead-c0143b9f's abandoned S093-follow transcript WIP as baseline (principal-approved synthesis). **Transcript redesign** (transcript.js + styles.css): agent text wrapped in a wide LEFT speech bubble (squared top-left tail), USER bubble mirrored right; killed the 66ch cap → 18px text at a ~78ch measure cap (fills the box via size, not line length — researched: wide-assistant/compact-user is the LLM-chat standard); de-speckled inline code (no chip bg); line-height 1.7→**1.38** + 1em paragraph gap; divider only BEFORE user turns so a Q&A reads as one block; both bubbles **near-black #0d0d0f + near-white #f4f4f4** for contrast. **Compose-Enter fix** (term.js): bracketed-paste then a SEPARATE delayed CR (`COMPOSE_SUBMIT_DELAY_MS=110`) — claude folded a text+CR burst into one paste so the CR landed as a literal newline (text sat unsent). **Auto-name persistence** (term.js): new `cockpit-term-labels` uuid-keyed store, saved on announce + restored on resume — a placed "Jebrim" keeps its name across restart instead of degrading to "chat" (manual rename still wins). Diagnosed via the re-enabled rename-diag trace: **`--resume` KEEPS the id (zero rotate)** — manual renames always survived; only the auto-label wasn't carried. **Issue #2 "textbox stuck" ROOT-CAUSED from `term-fit-diag.log`:** NOT over-fit (every sample had a row of *slack*) — it's the **open-while-hidden** path: selecting a row while the transcript view is up mounts the term display:none (frameH=0) → the open pin loop bails → the single RO re-show pin races the layout → prompt below the fold until a keystroke. Fix: new `term.js reshow()` multi-frame fit+pin, fired by a `main.js` effect on the transcript→terminal flip. board b89.4→**b91.0**; test_backend 9/9, node --check + py_compile green. **RUNTIME-UNVERIFIED — relaunch checklist:** black bubbles/white text + Q&A blocks + tight lines; compose Enter SENDS (+ multi-line); a player-placed session keeps its name across restart; open a session FROM transcript view then flip to terminal → prompt at bottom without typing. **Two transient loggers STILL IN (strip once #2 + names confirmed):** term.js `/api/termdiag`→`term-fit-diag.log` (open-frame only) + backend route; ptybridge rename-diag→`rename-diag.log`. Committed scoped to 7 cockpit files + S095 dev-brain artifacts. active-mode.txt → unscoped.)

**Last updated (S097 [RAG research + parked plan, interlinked with the live Obsidian session] — researched RAG, parked it as `plan.md` §N behind §C, and wired the exact dependency on the Obsidian link-migration; doc-only, sid b3c2b8dd).** 2026-05-26 (end of [[S097_b3c2b8dd_rag_research_and_obsidian_interlink]]. Dev-brain via "lets develop gielinor", mid-conversation; OPEN posted; live siblings braindead-f9310a45 ([[S095_f9310a45_cockpit-transcript-full-width-speech-bubble]] cockpit transcript) + braindead-b53fca39 (Obsidian link-migration spec) — no surface overlap. Researched RAG online (WebSearch): the brain is **already an agentic-search retrieval system** (grep + `@`-imports + `[[links]]`) — the architecture Claude Code/Cursor/Devin converged on *after* dropping RAG — so RAG enters **narrow + additive**, never as a grep replacement. Wrote `bank/research/2026-05-26-rag-for-the-brain.md` + new **`plan.md` §N** (Phase 0 gate → GraphRAG-over-`[[links]]` → semantic-recall index → MCP tool), parked **behind the §C outward pilot**. Principal then pushed to wire the *exact* interlink with the live Obsidian session: **b53fca39's Option-A full-stem `[[link]]` rewrite IS my Phase 0/1 input** — pre-migration ~91% of `[[D-027_inward_outward_build_imbalance]]`/`[[SNNN]]` links are phantom in Obsidian's resolver; Phase 1 reads the post-migration resolved-link graph, inherits their classification semantics + the per-brain-vault boundary, and depends on their forthcoming `D-NNN`. Posted a `→ @b53fca39` coupling flag. NO `gielinor/` writes, NO cockpit/* writes. Strategic next step UNCHANGED — §C shipping-mart pilot ([[D-027_inward_outward_build_imbalance]]). `active-mode.txt` left `dev-brain` (two siblings live).)

**Last updated (S096 [docs/ freshness audit + patch] — the repo-root `docs/` were frozen at S086 (May 24); patched the 4 post-S086 gaps + regenerated `brain-docs.html`; doc-only, sid 6acf797a).** 2026-05-26 (end of [[S096_docs_freshness_audit_and_patch]]. Dev-brain via "lets develop gielinor", mid-conversation from a brain-root session; OPEN posted; live sibling braindead-f9310a45 on `cockpit/web` transcript work ([[S095_f9310a45_cockpit-transcript-full-width-speech-bubble]]) — no overlap, steered fully clear of `cockpit/*`. Principal asked if `docs/` was current. Audit: a grep for `task-list|worktree|D-030|D-031|transcript|compose-bar` returned **ZERO matches** across all 12 pages — the source MD was frozen at [[S086_e668ec7e_brain-technical-docs]]. Patched (principal chose patch over full-sweep/report-only): **task-list discipline** ([[D-031_task_list_discipline]] / `gielinor/meta/task-lists.md`) + **worktree isolation** ([[D-030_worktree_isolation_for_parallel_sessions]]) into `docs/07`; cockpit **transcript-toggle + compose-bar** into `docs/09`; appendix **source-row + D-030/D-031/S090 decision-refs** into `docs/11`. Regenerated `docs/brain-docs.html` (107KB, 12 pages) — 4 fixes grep-verified in the build (8 matches). Adopted the orphaned May-25 `build-html.py` tweak (+11/-2, uncommitted from a prior docs session) into the commit since the HTML was built with it. NO `gielinor/` writes, NO `cockpit/*` writes. Strategic next step unchanged — §C shipping-mart pilot ([[D-027_inward_outward_build_imbalance]]).)

**Last updated (S094 [cockpit no-open — stale-process / profile-lock cleanup] — cockpit hung at `webview.start()` with no window; cleared stacked `cockpit/.venv` pythons + freed `:8770` → clean launch paints; principal-VERIFIED "works"; NO code change; sid 64acd968).** 2026-05-26 (end of [[S094_cockpit_noopen_stale_process_cleanup]]. Dev-brain via "lets develop gielinor", mid-conversation from a Jebrim cockpit-debug session; OPEN posted; no live siblings. Symptom: *"the terminal opens, the cockpit window itself does not."* Diagnosed: `webview.start()` hung — no native window, no traceback, **0 of 41 `msedgewebview2` hosts on the machine referenced `cockpit/.webview`** (the rest are Teams/Office/etc. — left untouched); `import webview` + `aiohttp` import fine; `cockpit/` code unchanged since [[S093_f3239bdc_transcript-readability-and-term-fit]] (principal-verified). Mechanism (**inferred, not stack-traced**): stacked morning launch attempts + my own leftover `backend.py` probes squatting `:8770` → the persistent [[S066_7f5db8c5_cockpit-sweep]] `.webview` `storage_path` lock-contended → every new `webview.start()` blocked. Fix (**verified works**): killed every `cockpit/.venv` python surgically by exe path, freed `:8770`; the next clean launch painted. NO cockpit code touched. Surfaced fragility: cockpit has **no single-instance guard + no stale-profile-lock recovery**, so one transient hang + a natural icon-retry self-DoSes into a lock pile-up (→ next-step #4 + [[build-lessons]]). Jebrim's [[S074_cockpit_switchboard_status_fixes]] cockpit-diagnostic hand-off stub resolved (in-progress→completed). NOTE: the uncommitted `cockpit/web/` WIP in-tree is **braindead-c0143b9f's abandoned S093-follow transcript work** — NOT mine, NOT touched, left for that thread to reclaim or a bankstanding to synthesize.)

**Last updated (S093 [cockpit transcript readability + terminal fit-while-hidden fix + rename-on-restart] — the S092 clean-text transcript made actually readable, a fit-collapse bug fixed, and S092's rename regression resolved; principal-VERIFIED "works"; only the `/clear`-then-restart rename path untested; sid f3239bdc).** 2026-05-25 (end of [[S093_f3239bdc_transcript-readability-and-term-fit]]. Dev-brain via "lets develop gielinor", mid-conversation; OPEN posted; no live Braindead siblings. Principal showed the live S092 transcript ("not very well designed") → three menu-driven rounds, all in `cockpit/web/`. **R1 structure** (transcript.js + styles.css): tool calls **COLLAPSE by default** (one-line head: chevron · name · arg · *N lines* · copy-output; errors auto-open; copy works collapsed — the prior render kept the result `<pre>` always-open, a regression vs the peek console's `<details>`); per-turn **speaker header + hairline divider**; prose width cap; redundant `tv-hint` dropped; **`strip line #s`** toggle (drops Read's `cat -n` prefix `^[ \t]*\d+\t` from view+copies, conservative so bash/grep pass through); **`A−/A+` font control** via `--tv-scale` (scoped sizes are `calc(base * var(--tv-scale))`), persisted. **R2 readability** ("hard on the eyes", styles.css + md.js): the surface wore the OSRS chrome — `.term-col` feTurbulence **paper-grain UNDER the body text**, warm monochrome, ~20 inline-code chips/turn, Trebuchet UI font, flat headings, and **`md.js` never parsed ordered lists** (`1.` fell to spaced `<p>`). Fixed (scoped to `.transcript-view` except md.js): grain off + calmer flat bg `#1c1813`, inline code → faint `rgba(...,.08)` wash, gold heading hierarchy, Segoe UI reading font, 66ch measure + em rhythm; **`md.js` now emits `<ol>`** (listBuf carries `{type,items}`; shared → peek benefits). **R3 terminal bug** (term.js): switching to transcript sets `<Term>` `display:none` → the container `ResizeObserver` fired `fitNow()` on a 0×0 box → FitAddon floored cols to its **2-col minimum** + resized the PTY → 3-char-wide TUI. Guarded **both** the RO (skip scheduling when no box) and `fitNow()` (bail when `clientWidth<2`); no-op while hidden, RO re-fires on re-show; also covers zoom-while-transcript (`applyTermZoom`). `node --check` green (transcript/main/term/md) + md.js OL parse tested by import. Committed S093 scoped to the 4 cockpit files + this session's dev-brain artifacts (NOT `_probe_ask.py`, sibling's throwaway). **EYES-ONLY (post-relaunch):** transcript reads easy (calm bg/no texture, code as wash, gold `##` headings, tight numbered lists, `A−/A+` scales+persists, `strip line #s` cleans a Read block + its copy, selection sticks while working); terminal stays full-width across repeated terminal⇄transcript toggles + a zoom-while-transcript. Open aesthetic calls if R2 isn't enough: serif instead of Segoe UI, tune `#1c1813`. **R4 rename-on-restart (principal: S092's rename fix STILL drops names — it shipped unverified, regressed):** refused patch #2 blind (2nd attempt); instrumented the announce/rotate/carry path to `switchboard/rename-diag.log` + shipped a resume-anchor carry (label from the resume-uuid's sid8 → the live id, both stores). One restart's trace was decisive — **`claude --resume` KEEPS the id (zero `rotate` events), correcting S092's "resume mints a new id" premise**; the name persists via the stable sid8 + durable localStorage, and the earlier breakage was the running window holding STALE code (+ `/clear`-rotation cases). Principal confirmed "works." Stripped the always-on disk logger (kept a zero-cost gated `__RENAMEDIAG` client trace); anchor carry stays as insurance for the `/clear`-then-restart path (unverified for that specific path). Lesson + S092 correction in `bank/build-lessons.md`. Committed `ptybridge.py` + `term.js`. **EYES-ONLY remaining:** the readability/terminal-fit checklist above is now principal-confirmed for the no-rotation restart; the `/clear`-then-restart rename path is the one untested branch. See [[S093_f3239bdc_transcript-readability-and-term-fit]].)

**Last updated (S092 [cockpit clean-text transcript + rename-persistence fix] — additive read/copy panel over the PTY; EYES-ONLY verify pending; sid 327b1216).** 2026-05-25 (end of [[S092_327b1216_cockpit_transcript_and_rename_persistence]]. Dev-brain via "lets develop gielinor", mid-conversation; OPEN posted; no live Braindead siblings. The S060 question inverted — chat-skin UI while the PTY stays the engine (subscription) — sharpened by the principal to the real goal: **CLEAN COPYABLE output** (xterm's fixed grid hard-wraps + bakes TUI chrome into copied text). Key find: the clean-content source is the transcript `.jsonl` (`/history`/`parse_transcript`), already rendering structured turns **for read-only peeks** — the *driven* PTY session just wasn't wired to it. Built a **terminal⇄transcript toggle** on driven rows: new `cockpit/web/transcript.js` renders the same session's `/history` as clean DOM (markdown + `<pre>` tool output) with copy buttons (per-turn / per-tool-output / copy-all) via the existing `POST /api/clipboard` bridge; `main.js` keeps `<Term>` mounted (hidden) so the PTY never drops; `backend.py` `&full=1` lifts the 4000-char result cap so copies aren't sheared. Then two fixes from principal feedback: **"can't select text"** — the 2s poll re-rendered the growing newest turn out from under a drag-select → now yields while a selection is live inside the panel (`_selectionInside`) + defensive `user-select:text` for chromeless WebView2; bigger transcript font. **"lost rename on reopen"** — labels are `sid8`-keyed but `claude --resume` (cockpit reopen) mints a NEW id (unlike `--session-id`, which pins) → orphaned; the old `_carry_name_across_clear` covered only in-process `/clear`, only disk, and assumed "resume reuses the UUID." Now carried across the rotation in BOTH stores: localStorage in `term.js` re-announce, disk in new `ptybridge._carry_disk_name`. `test_backend.py` **47/47** (+full-cap +name-carry), node --check + py_compile green. board b88.1→**b89.4**. 4 lessons → `bank/build-lessons.md` (one also to cross-conv memory). **EYES-ONLY (post-relaunch):** transcript renders + selection STICKS while the session works + copy buttons land; rename survives close/reopen (terminal `/rename` AND board double-click, incl. a `/clear`'d session); flip back to terminal stays live + prompt at bottom. active-mode.txt → unscoped. See [[S092_327b1216_cockpit_transcript_and_rename_persistence]].)

**Prior sessions (full detail in `quest-log/`):** [[S090_task_list_discipline]] task-list discipline — gielinor decomposes long tasks into tracked lists ([[D-031_task_list_discipline]] + new `gielinor/meta/task-lists.md`; threshold-gated, two-home). [[S089_35b35dc5_cockpit-duplicate-row-session-rotation]] cockpit duplicate-row fix — `ptybridge` re-announces the freshest live session bound to the PTY shell pid when claude rotates its id, so the board dedups the stale ghost (board b88.1). [[S088_f731b4e8_worktree-isolation-for-parallel-sessions]] worktree isolation decided ([[D-030_worktree_isolation_for_parallel_sessions]] — collision-avoidance is discipline not gates; the same-file Edit-race has no gate; principal chose git worktrees, build deferred; sub-agent `isolation:"worktree"` is free today). [[S087_23dcc534_cockpit-ctrl-c-copy]] cockpit terminal Ctrl+C copy (copy-on-selection, SIGINT fallback; new `POST /api/clipboard` write bridge; board b87.1; principal-VERIFIED "works") + diagnosed the "feed not showing my session" report as a view filter (the `actions`/`prose` toggles), not a bug. [[S086_e668ec7e_brain-technical-docs]] *(two parallel S086s, topic-disambiguated)* — cockpit compose-bar (fixed input below the PTY so you scroll history while typing; committed `dacb9bd`, UNVERIFIED, principal said "needs tweaks") **and** the brain's first consolidated technical docs at repo-root `docs/` (12 MD pages mapping-by-link to canonical files + a regenerable single-file `docs/brain-docs.html`; doc-only). [[S085_5f93bb32_cockpit-pty-auth-and-md-xss]] cockpit /pty RCE + md.js XSS fixed, and the brain-root hook-enforcement gap closed (the six guarantees were prompt-discipline, not gates, at brain root — proven by an `rm`; now fire repo-wide, gielinor wiring kept as redundant). [[S083_d71c4ab3_cockpit-full-audit]] cockpit full audit — terminal re-pin (prompt-below-fold), backend crash-guard, the regression gate (`test_backend.py`), Shift+Enter newlines, instant Esc busy-clear (a 90s backend timeout was reverted after it false-tripped a live Jebrim session). [[S084_454e276c_cockpit-switchbar-selection-and-nav]] cockpit switchbar — open-session gold-pop + keyboard nav (`Ctrl+↑/↓`, `Ctrl+1–9`), UNVERIFIED. [[S082_open_on_entry_discipline]] OPEN-on-entry discipline fix — made `OPEN` + sibling-detection non-skippable on *every* dev-brain entry, mid-conversation included (doc-only; the heavier hook-enforced OPEN was considered, not built). [[S079_e7008a60_cockpit-header-bar-overhaul]] cockpit header-bar overhaul — "N live" un-wrap + chat-header "grainy box" fix (styles.css, UNVERIFIED; peek `.console` same latent calc-width pattern FLAGGED). [[S077_e0f2af5d_cockpit-swarm-verification]] swarm verification — terminal scroll desync fix + board-side rename + `/rename` stranding fix (all principal-verified). [[S076_322cb5c3_cockpit-vscode-rename-and-focus]] cockpit VSCode parity — disk-backed `/rename` + click-row-opens-in-VSCode (VSCode jump still unverified end-to-end). [[S073_d82c4fbc_terminal-scroll-root-fix]] terminal scroll-lock fix. [[S074_cockpit_switchboard_status_fixes]] cockpit status fixes — idle decay + dead CLOSING/chat removal. [[S075_d27b2fe0_agent-prose-into-feed]] agent prose into the FEED via `kind:"say"`. [[S072_dbd41cc0_cockpit-fluid-ui]] cockpit fluid UI — scroll-zoom + resizable/collapsible pillars + Shift+Enter→\n. [[S071_e11aff01_neuron-overlay-design]] neuron-overlay v0 **design sketch** (spec at `bank/research/cockpit-neuron-overlay-design.md`, build still parked). [[S064_78824901_switchboard-cockpit-rebuild]]/[[D-028_switchboard_cockpit_rebuild]] the cockpit rebuild that retired the old switchboard map+client.

## Where we are

The **cockpit** (`cockpit/`) is the standalone fleet cockpit and the brain's real driver — interactive `claude` in a PTY (on-subscription past the 2026-06-15 billing change), three surfaces (board / console / feed) over one session model. It replaced the old `switchboard/` map+client ([[S064_78824901_switchboard-cockpit-rebuild]] / [[D-028_switchboard_cockpit_rebuild]]). The hooks (`status-sidecar.py`, `emit-event.py`) are its data source — preserved contracts; don't touch lightly.

S077 cleared the S073–S076 verification debt (scroll, rename); S078–S080 then reworked the status vocabulary (two-axis), header CSS, and the manifest liveness gate. S083 audited the lot: the status pipeline LOGIC is now provably correct (new `cockpit/test_backend.py`, 26/26 — stale-greying, attention tally, aliasing, heartbeat, crash-guards) and the server boots clean (aiohttp route smoke). The terminal prompt-below-fold fix is logically complete but **UNVERIFIED visually** (S083 eyeball checklist). S084 then made the switchbar usable: fixed a latent match-predicate bug that meant the open-session highlight NEVER applied to PTY-driven sessions (sid8 vs full-id), gave the open row a strong gold pop, and added keyboard nav (`Ctrl+↑/↓` move, `Ctrl+1–9` jump) + `Ctrl+Shift+\`` new-session — also **UNVERIFIED visually** (S084 checklist; board reads `b84.1`).

S085 broke the inward-polish streak with two security fixes (Codex review): the cockpit /pty RCE + md.js XSS, and — the consequential one — closing the brain-root hook-enforcement gap that meant the "six architectural guarantees" were prompt discipline, not gates, in the default mode (and for cockpit-launched sessions). That last one was integrity work, not polish. But the broader point below still stands: nearly everything since S073 has been inward. S086-cockpit (parallel to the docs S086) continued the inward run with a cockpit-terminal **compose-bar** (a fixed input under the PTY so you can scroll history while you write); committed (`dacb9bd`, board `b86.3`) but UNVERIFIED and flagged by the principal as "needs tweaks."

S086 produced the brain's first consolidated **technical documentation** — a navigable map at repo-root `docs/` (12 MD pages + a regenerable single-file `docs/brain-docs.html`) over the scattered `_about.md`/`meta/`/ritual files, plus the cockpit as one "how you drive it" section. Doc-only; it doesn't move the strategic gap below.

S087 (this session) added cockpit-terminal **Ctrl+C copy** (copy-on-selection, SIGINT fallback; new `POST /api/clipboard` write bridge) — **principal-verified "works"** (board `b87.1`, the first cockpit item confirmed live since the S083 batch). Also diagnosed (no code) that the feed "isn't showing my session" is a view filter, not a bug: a working session is almost all `say`+`action` events, both gated by the FEED `actions`/`prose` toggles. Still inward polish — doesn't move the §C gap.

**The load-bearing gap (per [[D-027_inward_outward_build_imbalance]], the S060 self-audit):** the brain optimized *inward* — observability, the cockpit — and never built outward *hands*. The operational half of the original plan (the §C pilot) is still unbuilt; the agent is still manual-invocation-only. That, not more cockpit polish, is the strategic next step.

## Next concrete step

1. **§C shipping-mart freshness pilot — the brain's first outward action ([[D-027_inward_outward_build_imbalance]] / `bank/plan.md` §C). The highest-value build; cockpit polish is now verified and stable, so there's no reason left to keep deferring it.** Two halves: **Braindead** wires a reusable scheduled trigger (harness `schedule`/cron — sidesteps §G) firing a fixed Jebrim brief ~08:30 daily, output to a file first (Slack draft only once trusted); **Jebrim** writes the read-only SQL checks against `shipping_mart` (recency / volume / cost-invariant `SUM(buckets)==total_eur==real_shipping_cost_eur` / `cost_source` coverage / order-item completeness / cross-fact agreement) + thresholds, in `shipping-agent/`.
2. **Worktree isolation for parallel sessions (decided [[D-030_worktree_isolation_for_parallel_sessions]], build deferred — principal will pick it up).** The fix for same-file Edit-retry-hell across parallel sessions. *Available today, zero build:* spawn fan-out sub-agents with `isolation:"worktree"` when they edit shared code. *The build:* the **hook-path audit** first (repo-relative writes — intent files, `switchboard/state-*` — must pin to one absolute shared location, else worktree sessions are invisible to the board; the [[S052_98d4ec5e_switchboard-rebuild]] `CLAUDE_PROJECT_DIR` lesson), then comms/intent sharing across worktrees (junction vs. hook-config) + cockpit worktree awareness + a merge convention. Rule: *isolate what you EDIT, share what you COORDINATE THROUGH.* Spec in [[D-030_worktree_isolation_for_parallel_sessions]].
3. **Neuron-overlay v0 (the "Sunday build", unblocked).** Build from the spec at `bank/research/cockpit-neuron-overlay-design.md` — new `cockpit/web/brain.js`, fixed overlay **outside** `.app-grid` (top-right, to dodge the zoom scaling landmine), polling `/api/feed`, OSRS palette. See [[S071_e11aff01_neuron-overlay-design]].
4. **Optional hardening (S077 finds #1/#2).** Manifest completeness when a session's in a long hook-silent turn; a staleness decay for a stuck `working`. Small, same status-pipeline area. Only if it recurs and annoys. **[S094] Cockpit single-instance guard + stale-profile-lock recovery — this one DID recur and annoy (higher priority than the rest of this item).** The persistent `.webview` profile lock-contends across stacked launches; one hang + a natural icon-retry self-DoSes (the principal hit exactly this). Add: (a) if `:8770` already serves a *cockpit*, focus the existing window and refuse a second instance; (b) on launch, detect + clear a stale WebView2 profile lock before `webview.start()`. See [[S094_cockpit_noopen_stale_process_cleanup]] + `bank/build-lessons.md`.
5. **Relaunch verification (do first — fast, clears the biggest unknowns). Board reads `SWITCHBOARD b89.4` when fresh code loads.** **S092 (NEWEST, eyes-only — clean-text transcript + rename persistence): on a driven session the header shows a `terminal | transcript` toggle; click `transcript` → the conversation renders as clean DOM, you can HIGHLIGHT text and the selection STAYS while the session keeps working (the 2s poll yields to a live selection), and the per-turn / per-tool-output / copy-all buttons land clean text on the clipboard; flip back to `terminal` → still live, prompt at bottom. Rename persistence: rename via terminal `/rename` AND via board double-click, then close+reopen the cockpit → BOTH names survive the `claude --resume` id rotation (carried in localStorage via term.js re-announce + on disk via `ptybridge._carry_disk_name`); try a session that's done a `/clear` too. If a name still drops, check whether the resumed session's new sid8 differs and whether the carry ran.** **S089 duplicate-row fix (eyes-only): drive a terminal, then `/clear` (or finish one task and start another in the same terminal) → within ~2s the board shows ONE row for it, and that row is DRIVABLE (click → terminal, not a read-only peek) — NOT a stale "chat" ghost beside a read-only manifest row. If it still splits, the re-announce isn't landing — check `proc.pid` actually appears in the new session's `claude_pid_chain` (the matching key in `ptybridge._session_for_shell_pid`).** (S087 Ctrl+C copy already verified "works"; if the feed looks empty for a live session, check the FEED `actions`/`prose` toggles are on — that's a filter, not a bug.) **S086-cockpit compose-bar (the new surface, "needs tweaks" per principal): the bar renders under the terminal + scales with Ctrl+wheel; wheel up to read history then type in the bar → terminal STAYS PUT; Enter sends + jumps to newest, Shift+Enter newlines; the regression that mattered — type `dssd` directly in the terminal, Esc, then send from the bar → arrives CLEAN (no `dssd` prefix); Esc in the bar clears the bar's own text; `/rename foo` via the bar relabels the row. Known limit by design: interactive TUI bits (slash autocomplete, @-picker, plan approval, AskUserQuestion menus) still need a click into the terminal.** **S085 (highest priority — a regression here means the token-gate broke the terminal): place a new session and confirm the terminal CONNECTS and claude comes up** (the /pty WS now requires `window.__CT`; if it shows `[session ended]`/403 immediately, the token plumbing is the suspect). Carried S084 checklist: selected row shows the gold ring/glow/name and tracks on click; `Ctrl+↑/↓` moves selection (wraps) + console follows; `Ctrl+1–9` jumps; `Ctrl+Shift+\`` opens the place modal (no stray backtick to the PTY). Carried S083: terminal prompt-at-bottom after a turn + after zoom/resize; Shift+Enter→\n; peek `.console` grainy-edge. If the prompt still clips, `window.__TERMDIAG=1` in DevTools gives the H1/H2 numbers.
6. **Leftover polish + documented-but-deferred finds.** S076 click-vscode-row→open-in-VSCode end-to-end; topbar density at 372px; the never-refreshed `claude_pid_chain` root fix (sidecar:1595); ptybridge UTF-8 boundary split; console.js dead drivable-composer branch.

## Files to read first

1. `respawn.md` (this file).
2. `quest-log/in-progress/S085_5f93bb32_cockpit-pty-auth-and-md-xss.md` — latest quest (cockpit security + the enforcement-gap fix; + S084/S083 for cockpit context).
3. `cockpit/_about.md` + `cockpit/backend.py` + `cockpit/web/` — the live surface.
4. `bank/decisions/D-028_*.md` — cockpit spec; `bank/decisions/D-027_*.md` — the inward/outward operability gap.
5. `bank/plan.md` §C — the parked pilot scope.
6. `bank/research/cockpit-neuron-overlay-design.md` — the S071 overlay spec.
7. `comms/active.md` (tail) — live siblings + hand-offs.
8. `bank/build-lessons.md` — cross-session build wisdom (read on demand).

## How to run the cockpit

Launch via the **cockpit / Switchboard** Desktop/Start-menu icon — it starts its own backend (`cockpit/Switchboard.vbs` → `app.py`; a pywebview window + aiohttp backend + PTY driver in one process). Hooks write state to `switchboard/state-*` files + `~/.claude/status/<sid8>.json`; the cockpit backend reads those. A relaunch is needed to pick up backend/JS changes (the running window holds stale code). `node --check` + `py_compile` are the cheap pre-commit gates for any web/hook edit.
