# S026 — 2026-05-22 — [[D-018_parallel_session_substrate_isolation|D-018]] shipped end-to-end + visualizer audit prep

- Shipped [[D-018_parallel_session_substrate_isolation]] chunks 1–3 in one session. Chunk 1 re-keys `state-actors.json` for player-class actors (`jebrim`/`zezima` get `{ byId: { sessionId: building } }` via new `get_actor_building`/`set_actor_building` helpers; `wisp`/`braindead`/`_mode*` keys stay flat). Chunk 2 re-keys `state-dwarves.json`/`state-gnomes.json` to `{ nextId, bySession }` so per-session `byToolUseId`/`pendingQueue`/`byAgentId`/`pendingAgentBind` partitions can't cross-pollute. Chunk 3 updates `gielinor/meta/communication-protocol.md` to mandate `<actor>-<sid8>.txt` intent filenames using `CLAUDE_CODE_SESSION_ID`. Smoke-tested helpers against legacy + new shapes; both pass.
- Live confirmation of the [[D-018_parallel_session_substrate_isolation|D-018]] motivating bug under observation: two parallel Jebrim sessions on screen both showed the same bubble text after a move, because `_reemit_intent_after_move` fell through to the bare `jebrim.txt` clobbered by whichever session wrote most recently. Chunk 3 fix lands but won't take effect for the still-running Jebrim sessions until their next turn re-reads `meta/`.
- Prepped the next session's visualizer audit. Three Explore dwarves walked `experiments/visualizer/index.html` (3249 lines) in parallel — D1 architecture, D2 event dispatch + state, D3 CSS/UX. Synthesized findings into [[visualizer-audit-S026-prep]]: how the renderer works (engine, 14-event dispatch table, in-memory state shapes, SVG layer model, mode boots), 9 bugs ranked by severity, 10 improvements ranked by payoff, 6 open questions for running-app verification. Recommended start order documented (B1 day/night-in-replay, B3 Braindead-COMMS, I1 color taxonomy, I2 scroll-lock first). Two long-running [[S021_visualizer_audit|S021]] audit items (C1 color taxonomy as I1, C2 Braindead COMMS as B3) surface again — still open.
- `respawn.md` reshuffled: Step 1 = visualizer audit, Step 2 = first live gnome spawn (still deferred, but [[D-018_parallel_session_substrate_isolation|D-018]]'s session-scoped substates remove a dependency), Step 3+ = drafts triage / browser verify / narration shakedown / [[Q-008_visualizer_aliveness|Q-008]] picks. Files-to-read leads with the prep doc.
- Per-session intent filename adoption began in this session — `braindead-cabedc6c.txt` was written alongside the bare file, exercising the new protocol immediately.

**Cascade.** [[D-018_parallel_session_substrate_isolation]] chunks 1–3 implementation (`developer-braindead/.claude/hooks/emit-event.py`), [[visualizer-audit-S026-prep]] (new in `bank/research/`), `respawn.md` rewrite, this quest entry.

**Main-brain changes.** `gielinor/meta/communication-protocol.md` updated to mandate per-session intent filenames; one principal-authorized direct edit per write-rules.md "explicit permission" clause.
