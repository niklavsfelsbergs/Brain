# S098 — 2026-05-26 — Obsidian fit research + D-link full-stem migration pilot

- **Researched Obsidian** (WebSearch) and assessed fit. Finding: the brain is *already* an Obsidian-shaped vault (plain `.md` + `[[wikilinks]]` + folders); Obsidian would be a read/navigate/tend **lens** (graph, backlinks, Dataview/Bases), not a format migration. Full write-up + migration spec in `bank/research/obsidian-fit-and-migration-spec.md` (Option A = full-stem rewrite, DECIDED).
- **The crux:** stock Obsidian resolves `[[ ]]` by *exact filename*, so our ID-prefix links (`[[D-027]]`, `[[SNNN]]`) render ~91% phantom out of the box. Modelled the resolver in a script rather than eyeballing the GUI. First model (single combined vault) → 9% resolve / "91% broken" — **wrong topology**; principal corrected to **per-brain vaults** → 71% clean. The remaining "collisions" are mostly deliberate conventions (sub-agent `_dN/_pN/_gN` run-logs share the session ID; quest+resume pairs), NOT debt.
- **Founding-decision tension + amendment.** Full-stem links overturn [[D-004_stable_ids]]'s "short ID alone is canonical; suffix not load-bearing." Surfaced it before acting; principal pushed back on treating an early rule as gospel; concluded the clause guards a near-nonexistent, recoverable slug-rename → **amended [[D-004_stable_ids]]** (stable-ID core kept, link-form clause retired, original preserved).
- **Migration pilot ran (dev-brain `D-` decisions only).** Link-TEXT rewrite, no file renames (hook/cockpit filename parsing untouched). **341 `[[D-NNN]]`→full-stem across 97 files**: 27 clean 1:1 IDs + [[D-012_close_session_harvest_pump|D-012]] auto; [[D-001_two_brain_split|D-001]]/[[D-002_folder_name|D-002]] split by the "main brain" qualifier (the `bank/main-brain-construction/` mirror overloads those IDs); code-fenced + convention-doc illustrative occurrences excluded/hand-edited (`bank/_about.md`, `spellbook/entry-formats.md`, [[D-006_dev_brain_restructure]] real-refs).
- **Caught a self-inflicted bug:** the rewrite script ran over the spec doc itself, clobbering its illustrative `[[D-027]]` examples (Option A/B both sides identical) — caught in diff review, fixed all 8 spots. Lesson: a mechanical convention-rewrite must exclude the docs that *describe* the convention.
- **Open / handoff (next session continues Obsidian integration):** (1) **verify in Obsidian** — open `developer-braindead/` as a vault, confirm the decision graph + backlinks light up (principal eyeball; can't be done headless). (2) extend the migration to **dev-brain quests** (`SNNN_dN/_pN` convention groups → main entry; the 3 real dupes [[S038_brain_underutilization_diagnosis|S038]]/[[S060_brain_self_audit_and_plan_reconciliation|S060]]/[[S086_e668ec7e_brain-technical-docs|S086]] need a human renumber-or-leave) then **gielinor/**. (3) two fuzzy [[D-001_two_brain_split|D-001]] judgment calls left as-set (`plan.md:29`, `S003:13`) — both resolve, flip if desired. (4) cross-brain refs (~4%, mostly root `docs/`) stay unresolved in per-vault Obsidian — decide later. Roadmap in [[plan]] §O.

**Cascade.** `bank/research/obsidian-fit-and-migration-spec.md` (new); [[D-004_stable_ids]] amended; 97 dev-brain files D-link-migrated; `bank/_about.md` + `spellbook/entry-formats.md` + [[D-006_dev_brain_restructure]] convention updates; [[plan]] §O added; `bank/build-lessons.md` (6 lessons); this entry; `comms/active.md` CLOSING; cross-conversation memory `evaluate-rules-pay-rent`.
**Main-brain changes.** none (NO `gielinor/` writes, NO `cockpit/*` writes).
