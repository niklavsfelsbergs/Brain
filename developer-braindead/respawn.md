# respawn.md — dev brain entry point

> Read this first when starting a fresh dev session. Where we are, what's open, the next concrete step.
>
> The ritual that reads this file is `spellbook/respawn-ritual.md`. The ritual that *updates* it is `spellbook/session-close.md`.
>
> **Discipline (enforced 2026-05-24 token audit).** Overwritten in place at session close — **NOT append-only**. Keep the current-state block + the most recent one or two session pointers; older per-session history lives in `quest-log/SNNN_*.md` (and in git). Cross-session build lessons live in `bank/build-lessons.md` — read on demand, never loaded at respawn. This file had grown to ~410 lines / ~49k tokens of stacked `Last updated` blocks read every session; that is the bloat the discipline exists to prevent. Don't let it regrow.
>
> **Parallel note ([[D-024_parallel_player_coordination]]).** Multiple dev sessions run at once. At close, **PREPEND** your single `Last updated` line to whatever this file currently says — do **not** overwrite it from a stale full copy you read at your own respawn, or you'll clobber a collapse like this one.

**Last updated ([[S118_e0a88f49_khaan-item-6-freshness-header|S118]] [Khaan item 6 — context-hash freshness, built as a hash-free resume header. sid e0a88f49].)** 2026-05-29 (end of [[S118_e0a88f49_khaan-item-6-freshness-header|S118]]. Dev-brain via "lets develop gielinor", mid-conversation; OPEN posted; no live Braindead siblings ([[S112_c7986694_cockpit-terminal-scroll-up-lock|S112]]–[[S117_fa446492_close-check-player-ritual|S117]] all CLOSED, only stale intent). Continued the Khaan thread — item 6, the agreed next cheap carry. **Decided WITH the principal (multiple-choice): build the CHEAP NON-BRITTLE version, NOT the literal `sha256(prompt)` port.** Khaan stamps every artifact `context_hash=sha256(prompt)[:16]` + rejects on mismatch; ported literally onto gielinor resume files it false-trips every turn (prompt churns each turn → always "stale" → alert fatigue), the exact brittleness that HELD item G ([[S116_caa72357_khaan-item-g-judgment-call|S116]]). **Gielinor adaptation = a hash-free freshness header** (`quest`/`sid8`/`ts`) on `inventory/<slug>-resume__<sid8>.md` — the explicit fields ARE the identity check (which task / which session / how old); same adaptation move as item 5. **Built 4 pieces:** (1) `## Freshness header` convention in BOTH players' `inventory/_about.md` (canonical spec); (2) `gielinor/spellbook/rituals/close-session.md` step 3 writes the header + migration note (existing headerless resumes get it on next close, mirrors [[D-024_scope-git-commits-with-pathspecs-parallel-sessions|D-024]]) + a 5th bullet in step 10's gate list — **user-only ritual, principal explicitly authorized**; (3) `gielinor/spellbook/rituals/respawn.md` reconciliation reads the header + SURFACES age/`quest`/`sid8` mismatch as a *note, never a hard block* — **user-only, authorized**; (4) a 5th `close_check.py --ritual player` arm `check_freshness_header_player` asserting the header on THIS session's own resumes — parse-lenient (substring scan top-15 lines, not strict YAML) + bounded to current sid8 so legacy resumes never false-trip. **Verified BOTH ways:** temp-tree STAMPED→PASS / UNSTAMPED→FAIL, vacuous (no in-progress quest)→PASS, integrated `--ritual player` wires the row in, dev path structurally unchanged, py_compile clean. Historical-sid FAIL on live `2ae1248b` is *correct* (predates the convention; read-only, left Jebrim's WIP alone). `bank/plan.md` §P.7 `[x]`; build-lesson logged (adapt-don't-port-literally, now 3-for-3 with items 5/G). **Open carries (down to one cheap):** `meta/write-rules.md` "enforced by hook" line via godly proposal at next bankstanding; item 4 (5-lens scaffold, optional); everything else later-phase. active-mode → unscoped. respawn NOT trimmed (A4 WATCH — small focused session, kept scope tight).)

**Prior ([[S117_fa446492_close-check-player-ritual|S117]] [close-completeness gate extended to the gielinor PLAYER close — [[D-034_close_ritual_enforcement|D-034]] follow-on, the [[S116_caa72357_khaan-item-g-judgment-call|S116]] best-ROI carry. sid fa446492].)** 2026-05-29 (end of [[S117_fa446492_close-check-player-ritual|S117]]. Dev-brain via "lets develop gielinor", mid-conversation; OPEN posted; no live Braindead siblings ([[S112_c7986694_cockpit-terminal-scroll-up-lock|S112]]–[[S116_caa72357_khaan-item-g-judgment-call|S116]] all CLOSED; only stale intent). **Decided WITH the principal (multiple-choice): GENERALIZE** `developer-braindead/verification/close_check.py` with `--ritual {dev,player}` (default `dev` → the wired dev `session-close.md` step-9 call is byte-unchanged) over a sibling file. Shared harness (`BANNER`/`_git_clean`/`_emit` print-exit loop); `run_dev` (5 checks, untouched) + new `run_player` (4 checks, keyed by **sid8 across `players/*/`** since a session can touch >1 player): CLOSING-in-gielinor-comms (skip if no OPEN), quest-log-present (or `players/inbox/` for unscoped), **inventory-resume-present** (every player with an in-progress quest for the sid8 has a `*-resume__<sid8>.md` = the step-9.1 enforcement clause mechanized), artifacts-committed (glob + `_git_clean` catches orphan-untracked too). **No `respawn.md` arm** (gielinor resume = per-player inventory) and **no `active-mode.txt` arm** (dev-brain-only marker); no brittle content-line asserts (G-style false-trip restraint). **Verified BOTH ways:** clean-pass on real sid8 `3760e65b` (Jebrim S120, 4/4 PASS exit 0); banner+exit-1 on `fa446492` (no gielinor trace) and `f41737e5` (Jebrim [[S118_e0a88f49_khaan-item-6-freshness-header|S118]] — caught a real missing-resume + an uncommitted quest file on a LIVE Jebrim quest, left for Jebrim). Dev path re-confirmed unchanged (FAILed only on this session's `active-mode=dev-brain`). **Wired as step 10** of `gielinor/spellbook/rituals/close-session.md` (close-completeness gate) + the forced 5 renumber cross-refs — `spellbook/rituals/` user-only, **principal explicitly authorized** the edit. `bank/plan.md` §P.6 `[x]`. **Open carries (unchanged):** `meta/write-rules.md` "enforced by hook" godly proposal at next bankstanding; item 6 context-hash freshness; item 4 5-lens scaffold (optional). active-mode → unscoped. respawn NOT trimmed (A4 WATCH — small session, kept scope tight). **Post-close consultation (same session, no brain writes):** walked the full Khaan 20-item ledger — close-enforcement thread + the HITL brakes-cluster (items 1/2/5/12) DONE, G HELD, H not-outstanding; **agreed next cheap carry = item 6 (context-hash freshness), to be picked up in a FRESH session** (not by reopening a closed one — scope it first against the item-6 detail in `bank/research/2026-05-28-khaan-learnings-implementation.html` + the [[D-024_parallel_player_coordination|D-024]] staleness model: where the hash stamps, what reads it). Item 4 (5-lens) + items 3/7/8 (recall/charges/digest, lean §N) behind it; autonomous half (13–18) = §C much-later.)

**Prior ([[S116_caa72357_khaan-item-g-judgment-call|S116]] [Khaan item G (phase-registry mutation guard) — judgment call resolved: HELD until a code pipeline earns it; no build. sid caa72357].)** 2026-05-28 (end of [[S116_caa72357_khaan-item-g-judgment-call|S116]]. Dev-brain via "lets develop gielinor", mid-conversation; OPEN posted; no live Braindead siblings (only stale intent braindead-2fbde8af ~40min, discussion-only, no overlap). **The [[S115_a9fe4749_khaan-failure-receipts-and-hygiene|S115]] hand-off** teed up item G as step 2's remaining half — a 4th check on `developer-braindead/verification/check.py` hashing an ordered pipeline's phase list against silent reorder — with a principal-flagged judgment call: G pays off on an ordered *code* pipeline (we have none; the §C chain is much-later), and applied to the only ordered surface now (the `respawn.md` markdown load-order) it's the brittle structure-check kind `check.py` already deferred ([[S115_a9fe4749_khaan-failure-receipts-and-hygiene|S115]] docstring lines 26–29), and `respawn.md` churns every close → a byte-hash would false-trip constantly. **Decided WITH the principal (multiple-choice): HOLD.** Recorded the deferral + revival trigger (first ordered *code* pipeline lands → hash *that*) in `bank/plan.md` §P.5 (`[-]`); `check.py` left unchanged at C1–C3; no build. **Sequence triage (principal asked before close):** step 1 done; step 2 now fully resolved (5 done + G held); step 3 effectively done — gate 1 ([[D-033_positive_enforcement_gate_open_on_entry|D-033]]) is self-escaping (satisfy = post OPEN, fail-open) so anti-lockout **H is NOT outstanding** (only mandatory with a future *trapping* gate, e.g. a grounding gate); steps 4–6 unbuilt; step 7 (autonomy) deferred per the much-later-phase steer. Best-ROI remaining: the two cheap carries (`meta/write-rules.md` "enforced by hook" godly proposal at next bankstanding; extend `close_check.py` to the gielinor player close-session — [[D-034_close_ritual_enforcement|D-034]] follow-on) + item 6 context-hash freshness. Doc-only session. active-mode → unscoped. respawn NOT trimmed (A4 WATCH — small session, kept scope tight).)

**Prior ([[S115_a9fe4749_khaan-failure-receipts-and-hygiene|S115]] [Khaan items 2+12 (locked failure receipts + engineering hygiene) AND item 5 (golden-file verification harness) — recommended-sequence steps 1+2. sid a9fe4749].)** 2026-05-28 (end of [[S115_a9fe4749_khaan-failure-receipts-and-hygiene|S115]]. Dev-brain via "lets develop gielinor", mid-conversation; OPEN posted; no live Braindead siblings all session ([[S112_c7986694_cockpit-terminal-scroll-up-lock|S112]]/[[S113_b072b8df_bankstander-question-and-godly-proposal-flow|S113]]/[[S114_277d9053_khaan-audit-and-open-gate|S114]] all CLOSED). Continued the [[S114_277d9053_khaan-audit-and-open-gate|S114]] hand-off. **Item 2 (locked failure receipts):** new `gielinor/spellbook/failure-banners.md` — one byte-locked banner per ritual (respawn/alching/drafts-triage/born-link lint) + the no-silent-fallback / emit-verbatim-and-halt doctrine; `born-link-lint.py` carries its banner as a `BANNER` constant and emits it on the block + migrate-fail paths (the one banner wired into real code); a uniform **Failure handling** pointer bound into all 3 user-only ritual files (principal OK'd the user-only edits via multiple-choice). **Item 12 (engineering hygiene):** 3 invariants in `build-lessons.md` (atomic-write all state JSON / never on a symlink; ship-dormant; minimal surface). **Item 5 (golden-file verification):** new `developer-braindead/verification/check.py` (home = dev-brain, principal-chosen) — one harness, 3 code-shaped checks: C1 banner-integrity (registry has 4 banners; born-link `.py`==registry byte-match), C2 born-link-golden (malformed→block exit1+banner, clean→exit0; inline fixtures since the commit hook lints all `.md`), C3 write-boundaries (the 4 sub-agent hooks block off-surface exit2 / allow on-surface exit0 / inert on untyped — the [[S110_144c0ca2_brain_full_audit|S110]] caveat). Dogfoods item 2 (own banner) + item 12 (ship-dormant — NOT auto-wired to pre-commit). **Verified BOTH ways:** 3/3 PASS, and fault-injected a banner drift → C1 FAIL + banner + exit1 → restored. **Substrate catch:** banners forced ASCII — the Windows console mangled the original `☠`/em-dash (booked to build-lessons, pairs with [[S037_terminal_switchboard_phase_3_click_to_focus|S037]]). 2+12 committed `8748c87`; item 5 + close in the close commit. **Then (same session, reopened): close-ritual enforcement [[D-034_close_ritual_enforcement|D-034]]** — after the principal caught a premature done-claim, built 3 layers making close hard to skip: **L1** `developer-braindead/verification/close_check.py` (ground-truth completeness gate, now **mandatory step 9** of `session-close.md`), **L2** `gielinor/.claude/hooks/close-cue-reminder.py` (UserPromptSubmit wind-down nudge, exit-symmetry of [[D-033_positive_enforcement_gate_open_on_entry|D-033]]; **verified firing LIVE** on this session's own wrap-up), **L3** `gielinor/.claude/hooks/session-end-safety-net.py` (best-effort auto-CLOSING stub on unmatched OPEN, **no auto-commit**). Both new hooks in root `settings.json`. Honest ceiling: L3 is async/unreliable on hard window-close; the cognitive half can't be hook-forced at exit → defers to next-respawn reconciliation. This very close ran through close_check (L1 dogfooded). **Pending (unchanged from [[S114_277d9053_khaan-audit-and-open-gate|S114]]):** `meta/write-rules.md` "enforced by hook" line via godly proposal at next bankstanding. plan §P.2/§P.3/§P.4 → done. active-mode → unscoped. respawn NOT trimmed (A4 WATCH — quiet window, kept scope tight).)

**Prior ([[S114_277d9053_khaan-audit-and-open-gate|S114]] [cloned + audited JustsCE/Khaan; built the brain's first POSITIVE enforcement gate (OPEN-on-entry), live-verified. sid 277d9053].)** 2026-05-28 (end of [[S114_277d9053_khaan-audit-and-open-gate|S114]]. Dev-brain via "lets develop gielinor", mid-conversation; OPEN posted; no live Braindead siblings all session. **Cloned JustsCE/Khaan** (another markdown-brain Claude-Code agent, ~1,900 files, cycle #116) → `Documents/Khaan`; produced two HTML deliverables in `bank/research/`: `2026-05-28-khaan-comparative-audit.html` + `2026-05-28-khaan-learnings-implementation.html` (20-item catalogue, 12 HITL + 8 autonomous, priority matrix + recommended sequence). Verdict: **complementary halves — borrow their engine (autonomous producer + scored retrieval, our §C/§N), keep our brakes (HITL gating, never-delete, rationale).** **Built HITL item 1 = the brain's first POSITIVE gate** ([[D-033_positive_enforcement_gate_open_on_entry]]): `gielinor/.claude/hooks/require-open-on-entry.py` blocks Edit/Write of brain content until the session posts its OPEN to comms (mechanizes the #1 historical leak). Hard-block (principal-chosen); escapes the OPEN/marker/intent/narration surface; skips sub-agents/guthix/wisp/unknown/non-brain; fails open. Registered in BOTH settings.json. **Boundary-tested 12/12 AND live-verified** — the "mid-session settings don't hot-reload" assumption was FALSE (they do); probe-instrumented run observed allow (braindead+OPEN→exit 0) + block (actor flipped to jebrim, no OPEN→exit-2 banner) through the real pipeline; probe stripped. Gate commit `9985837`; verification-doc updates + close artifacts in the close commit. **Pending:** `meta/write-rules.md` "enforced by hook" line via godly proposal at next bankstanding (meta user-only, [[D-032_godly_proposal_flow_and_code_bearing_seam]]). Added `bank/plan.md` §P (Khaan benchmark). active-mode → unscoped. respawn NOT trimmed (A4 WATCH stands — deferred again; quiet window but kept scope tight).)

**Prior ([[S112_c7986694_cockpit-terminal-scroll-up-lock|S112]] [cockpit terminal scroll-up LOCK root-caused + fixed; numbered-lists-all-"1." fixed. sid c7986694].)** 2026-05-28 (end of [[S112_c7986694_cockpit-terminal-scroll-up-lock|S112]]. Dev-brain via "lets develop gielinor"; OPEN posted; session spanned 05-27→05-28; no live Braindead siblings at close (572ea7f7 ~31min stale). **Scroll bug:** long-convo terminal, scroll up → jumps/locks, can't scroll down (up works), prompt + compose-bar unreachable until a keystroke; idle-only. Armed a WHEEL-path disk probe (no prior probe covered scroll) → `term-fit-diag.log`. **Root cause CONFIRMED — NOT the over-fit guard (exonerated a 3rd time, `overfit=-15px` slack):** the native `.xterm-viewport` `scrollHeight` LAGS xterm's buffer (`baseY`) by ~one viewport → wheel-down caps at the stale native floor (decisive: `wheelDn viewportY=120 baseY=191 follow=true`, 71 rows short of bottom) and `_isAtBottom`'s native-scrollbar branch false-positives → re-locks `_follow`. **Fix (`cockpit/web/term.js`):** `_isAtBottom` buffer-authoritative (`viewportY≥baseY`) + new `_syncViewport()`=`term.refresh(0,rows-1)` after every write + before each pin so `scrollHeight` tracks the buffer. **List bug (`md.js`):** a blank line flushed the `<ol>`, so a loose (blank-separated) list split into N single-item lists each restarting at 1 → deferred the flush across a blank (`pendingBlank`); 8/8 node unit tests; `styles.css` li margin 0.15→0.45em. **BOTH RUNTIME-UNVERIFIED** (need a cockpit relaunch) — probes kept ARMED for the verifying relaunch (strip after); verify checklist in the [[S112_c7986694_cockpit-terminal-scroll-up-lock|S112]] quest-log. Likely closes the recurring scroll-desync family ([[S083_d71c4ab3_cockpit-full-audit|S083]]/[[S093_f3239bdc_transcript-readability-and-term-fit|S093]]/[[S095_f9310a45_cockpit-transcript-full-width-speech-bubble|S095]]) at its ROOT. Committed scoped (cockpit/web/{term,md,styles} + [[S112_c7986694_cockpit-terminal-scroll-up-lock|S112]] + this respawn + build-lessons + comms); left all gielinor player WIP + diag logs + state.ndjson alone. active-mode → unscoped. Trim still deferred (A4 WATCH).)

**Prior ([[S113_b072b8df_bankstander-question-and-godly-proposal-flow|S113]] [design Q: should Braindead bankstand instead of Guthix? → corrected the godly-proposal flow + ratified the code-bearing seam. sid b072b8df].)** 2026-05-27 (end of [[S113_b072b8df_bankstander-question-and-godly-proposal-flow|S113]]. Dev-brain via "lets develop gielinor", mid-conversation; OPEN posted; live sibling braindead-c7986694 on the cockpit scroll-up bug — no overlap. **Principal asked** whether Braindead would be a better bankstander than Guthix, then *"do you actually pick up guthix's suggestions?"* I'd claimed a "Guthix proposes / Braindead builds" pipeline — **checked the record: it doesn't exist.** All 3 godly proposals self-landed by Guthix in bankstanding ([[B-006_2026-05-24_sixth-bankstanding|B-006]]×2 + [[B-009_2026-05-27_ninth-bankstanding|B-009]]'s grounding HOOK, commit `22a80d2` today); Braindead built 0. **Landed [[D-032_godly_proposal_flow_and_code_bearing_seam|D-032]]:** keep Guthix as bankstander (real reason = trigger+evidence, not propose-vs-build — he carries the lived operational misses an outside builder never sees); part 2 = **soft guideline** (principal's pick) — Guthix self-lands prose, *substantial* code-bearing proposals prefer a Braindead build, no hard gate yet (B-009's hook verified clean, N=1). **Open:** the gielinor-binding propagation (write-rules.md + proposals/_about.md + a lorebook D-NNN) is routed through a **godly proposal at the next bankstanding** — NOT a Braindead meta-edit (meta is user-only; dev `[[D-032]]` wouldn't resolve in the gielinor vault); flip `Hey Guthix, bankstand` to land it sooner. Doc-only. Committed scoped ([[D-032_godly_proposal_flow_and_code_bearing_seam|D-032]] + this respawn + [[S113_b072b8df_bankstander-question-and-godly-proposal-flow|S113]] quest-log); comms left UNSTAGED for the live sibling to carry. active-mode LEFT dev-brain (c7986694 still live). NOTE: respawn NOT trimmed — sibling live + mid-conversation; A4 WATCH stands.)

**Prior ([[S111_a268b008_shipping-agent-subagent-type|S111]] [shipping-agent promoted to a first-class sub-agent TYPE — agent def + cockpit crew chip + dedicated write-boundary hook; closes [[S101_612683db_shipping-agent-access-split|S101]] open item 6. sid a268b008].)** 2026-05-27 (end of [[S111_a268b008_shipping-agent-subagent-type|S111]]. Dev-brain via "lets develop gielinor", mid-conversation; OPEN posted; no live Braindead siblings. **Built 3 pieces:** (1) `.claude/agents/shipping-agent.md` — emulation brief: read `shipping-agent/how_to.md`, query gold `shipping_mart` via the Redshift MCP (read-only), gold-default + `CLAUDE.local.md`/upstream exception, deliverables land OUTSIDE the brain, quest-log trace inside; tools Read/Glob/Grep/Edit/Write/Bash + redshift MCP. (2) Cockpit: wired the new kind end-to-end — `emit-event.py` ROLE_CONFIG + `status-sidecar.py` SUBAGENT_STATE_PATHS + `backend.py` ROLE_FILES → `board.js` per-kind crew chip + `styles.css` `--ship` cyan **"S"** (distinct from dwarf/gnome/penguin); refreshed the stale "adding a new kind" comment (old switchboard sprite path is archived). (3) Write boundary (principal chose the hook over documented-globals): new `gielinor/.claude/hooks/shipping-agent-write-boundary.py` — brain-internal writes = quest-log + inventory only (bank/ blocked → alching picks); registered in BOTH `settings.json`; `block-sub-spawn` ROLE_PLURALS += shipping-agent. Doc: new *Shipping-agent role* in `meta/modes.md`. **Verified:** py_compile + node --check + JSON valid + test_backend 9/9; enforcement FIRED on synthetic payloads (blocks bank/examine, allows quest-log/inventory/NFE-outside-brain, no cross-gate interference); routing maps `subagent_type`→S. **RUNTIME-UNVERIFIED:** a LIVE `Agent(subagent_type:"shipping-agent")` spawn needs a FRESH session (config load — the [[S106_cf03bfe1_self-eating-audit-and-d8-enforcement-fix|S106]] fact) + cockpit relaunch to confirm `agent_type` populates + the cyan "S" chip renders (checklist in the quest-log). **Design beat:** principal floated dwarf→shipping-agent nesting; kept #6 as-is (reasoning on the [[S111_a268b008_shipping-agent-subagent-type|S111]] trace). **Flag:** updated Jebrim skill DRAFT `calling-the-shipping-agent.md` step 2 (stale "until built→dwarf") — for his alching review. Committed scoped (build files only; left live jebrim-55ea7bc0 WIP + transient diag logs + `gielinor/comms` alone). active-mode → unscoped. NOTE: respawn NOT trimmed this close — [[S111_a268b008_shipping-agent-subagent-type|S111]]/[[S110_144c0ca2_brain_full_audit|S110]]/[[S108_98592157_cockpit-place-session-name-and-prewrite|S108]]/[[S107_f4aadfed_comms-rotation-and-read-discipline|S107]]/[[S105_7c91117c_obsidian_o6_backlink_enrichment|S105]] + rollup stacked; trim still deferred (A4 WATCH).)

**Prior ([[S110_144c0ca2_brain_full_audit|S110]] [full brain-system audit — read-only 5-crew sweep, then principal-cleared follow-ups] — verdict: structurally healthy for a 7-day-old brain; two real findings: the self-perpetuating §C inward/outward gap + verification debt. sid 144c0ca2).** 2026-05-27 (end of [[S110_144c0ca2_brain_full_audit|S110]]. Dev-brain via "lets develop gielinor", mid-conversation; OPEN posted. Ran the audit read-only while sibling 3a599ead held the §O.9 doc surfaces; after it committed + ended, the principal cleared the follow-ups. **Audit = 5 parallel recon crews:** structure/routing, enforcement-reality, discipline, session-load bloat, plan-vs-reality. **Executed:** (1) **ENFORCEMENT LIVE-TEST — CLEAN PASS; closes the [[S106_cf03bfe1_self-eating-audit-and-d8-enforcement-fix|S106]] open item.** Spawned gnome/penguin/dwarf from this fresh brain-root session (the [[S106_cf03bfe1_self-eating-audit-and-d8-enforcement-fix|S106]] config-registration holds — no "agent type not found"); each agent_type-gated boundary hook FIRED with a verbatim block (gnome→`meta/`, penguin→`bank/`, dwarf→`examine/drafts/`), `confirmed/` blocked, positive controls succeeded → guarantees **#3/#4/#5 now PROVEN** (+ #1/#2 already were). #6 enforced harder: Task/Agent isn't in any typed agent's toolset (`block-sub-spawn.py` = unreachable backstop). **CAVEAT:** a plain `general-purpose` sub-agent is NOT boundary-gated. (2) **quest-log drained** — dev `in-progress/` 48→1 (47 graduated to flat root, the documented model; there is no `completed/` in the dev brain), jebrim 6→`completed/` (88→94). (3) plan.md §D.1/§D.3 → `[x]` (prose/checkbox contradiction). (4) [[D-012_close_session_harvest_pump|D-012]] applied-spec → `bank/archive/drafts/`. (5) re-grounded the stale "~70% OPEN-skip" claim across `CLAUDE.md`×2 + `respawn-ritual.md` + `AGENTS.md` (principal-approved; the leak is ~100%-closed now). **Open:** name the §C job (the HIGH unblock); a cockpit verification-only relaunch (7+ RUNTIME-UNVERIFIED); document the in-progress→root graduation in `session-close.md`/`quest-log/_about.md`. **Framing correction (principal steer, 2026-05-27):** §C / scheduled-autonomy is a *much-later* phase — the **current phase is hands-on collaboration**; retiring the reflexive "§C is THE load-bearing next step" refrain. The audit's inward/outward *observation* stands as history; the "name the §C job" prescription is withdrawn per the steer. active-mode → unscoped. NOTE: respawn not trimmed this close — [[S105_7c91117c_obsidian_o6_backlink_enrichment|S105]] block + the S089-block trim still deferred (A4 WATCH).)

**Prior ([[S108_98592157_cockpit-place-session-name-and-prewrite|S108]] [cockpit place-session: name field + click-to-open prewrite] — added a session-name slot to the place modal and tore out the message box: clicking an actor now opens the session with its address prewritten **unsubmitted** ("Hey Jebrim, "), user continues typing; node-checked + 47/47 backend, **RUNTIME-UNVERIFIED**; sid 98592157).** 2026-05-26 (end of [[S108_98592157_cockpit-place-session-name-and-prewrite|S108]]. Dev-brain via "lets develop gielinor", mid-conversation; OPEN posted (survived f4aadfed's mid-session comms rotation; SNNN bumped [[S107_f4aadfed_comms-rotation-and-read-discipline|S107]]→[[S108_98592157_cockpit-place-session-name-and-prewrite|S108]] after f4aadfed claimed [[S107_f4aadfed_comms-rotation-and-read-discipline|S107]] at close). Surface: `cockpit/web/{main.js,term.js,styles.css}` — uncontested (live siblings 3a599ead §O.8 born-linked conventions + f4aadfed comms-rotation are on gielinor/dev-brain `.md`, not cockpit). **Done:** (1) **NAME** — optional name input in `PlaceModal` → `c.label` (console title + `saveTermLabel` resume-persist) + `setName(sid8,name)` on ws-announce (board row + `/rename` store), guarded `!nameFor` so a real rename isn't clobbered; blank → old auto-label. (2) **PREWRITE** — dropped the textarea/preview/place button; actor cards place on click; `term.js` `openTerm(seed,{submit:false})` → `submitSeed=false` → `_seedSoon` types the seed with **no trailing `\r`**, so the address lands unsubmitted and the user continues (default `submit:true` preserves the old auto-send; `openTerm`'s only caller is `doPlace`). `styles.css`: name input + two-column clickable actor cards; retired `.place-prompt`/`.modal-preview`. **Verified:** `node --check` clean (main.js+term.js), `test_backend.py` 47/47 (no backend/hook change). **RUNTIME-UNVERIFIED — relaunch checklist:** modal has a name field + NO textarea/place button; name + click Jebrim → terminal opens with `Hey Jebrim, ` prewritten (cursor after, unsent) + the board row carries the name; actor w/o name → actor label; "plain chat" → empty prompt; edge — the seed types after the ~3s TUI-paint delay, so typing inside that window inserts the address at the cursor. **Commit:** by pathspec — `cockpit/web/{main.js,term.js,styles.css}` + the [[S108_98592157_cockpit-place-session-name-and-prewrite|S108]] quest-log ONLY; the comms `CLOSING` + this respawn prepend LEFT **UNSTAGED** for the comms holder to carry. Strategic next step UNCHANGED — §C shipping-mart pilot ([[D-027_inward_outward_build_imbalance]]).)

**Prior ([[S107_f4aadfed_comms-rotation-and-read-discipline|S107]] [comms/active.md rotation + read-discipline fix; sid f4aadfed]).** 2026-05-27 (Dev-brain via "lets develop gielinor", mid-conversation; OPEN posted. Principal asked whether sessions read the FULL append-only comms log every respawn. Root cause: both `comms/active.md` files were never pruned — the dev one had reached **976 lines / ~100k tokens read every respawn**, under the old "~500 entries" rotation trigger that never fires on token weight. **Done:** rotated both (sanctioned overwrite exception): dev 976→**300 lines** (127 entries → `comms/archive/active-2026-05-27.md`, kept 52); gielinor 402→**202** (61 → archive, kept 49); nothing deleted, git-reversible. Amended both `comms/_about.md`: rotation now triggers on **token/line weight (~300 lines / ~30k tokens)** not entry count, and Read cadence warns a bare `Read` returns the *oldest* entries first — seek to EOF for the live tail. Doc + file moves only. Strategic next step UNCHANGED — §C shipping-mart pilot ([[D-027_inward_outward_build_imbalance]]). active-mode → unscoped.)

**Prior ([[S105_7c91117c_obsidian_o6_backlink_enrichment|S105]] [Obsidian §O.6 backlink enrichment — FULL prose-ref wrap, both brains] — principal widened the narrow hand-off to MAXIMAL: wrapped every inline resolvable S/D-ref across both brains incl. quest-log prose, ~2,550 wraps; new `--mode prose` in `obsidian-link-migrate.py`; sid 7c91117c).** 2026-05-26 (end of [[S105_7c91117c_obsidian_o6_backlink_enrichment|S105]]. Dev-brain via "lets develop gielinor", mid-conversation; OPEN posted. **Done:** built a prose-wrap mode — UNbracketed resolvable ID → `[[stem|ID]]` alias (display preserved → prose reads identically; link-TEXT only, NO renames, idempotent, git-reversible). **~2,422 clean wraps** (~241 files: knowledge layers + quest-log) + **129 dupe wraps** (10 ambiguous IDs via a vault-local-guarded `PROSE_DUPE_RESOLUTION` table: same-session→canonical, `D-001/2` subtree rule for the `bank/decisions/` vs `main-brain-construction/` collision, topic rules for `S038`/`S062`/`S086` — all spot-checked correct). Safety: resolve-to-real-file guard, prefix whitelist (kills `L2448`/`M365`/`T11` noise), self-ref skip (469), per-vault resolution (cross-brain dangling per §O.5). **Excluded** as sources (kept as targets): `inventory`/`archive`/`rejected`, `comms`/`experiments`/`last-alched`, `CLAUDE.md`/`meta/`/`spellbook/rituals` (user-only rulebook — reverted the 4 it initially touched). Verified: idempotent re-run **0 wraps / 0 flagged** both vaults, **0 renames**, §O.4 gielinor links still clean. **Finding → §O.7** (`plan.md`): dev links-mode has ~103 bare `[[Q-/A-/I-/R-NNN]]` bracketed links unmigrated (§O.2/O.3 only did `D-`/`S-`); broken in Obsidian, pre-existing, NOT this pass — deferred. Sibling cf03bfe1 closed mid-session ([[S106_cf03bfe1_self-eating-audit-and-d8-enforcement-fix|S106]], self-eating audit + D8 enforcement fix, `5cd2f97`); left comms+respawn for me as holder, carried. **POST-CLOSE:** sublog→parent fix shipped (`143c3a0`) — self-skip now fires only for MAIN entries so sub-logs link their parent session (de-isolated ~36). **Graph verified by principal Obsidian reload: 350 nodes, 1207 edges, 74% connected / 26% isolated** (was 76% isolated pre-§O.6; the first "still sparse" screenshot was a stale cached graph). Residual ring = excluded dirs + research + structural = the expected floor. **§O.8 (semantic/topical cross-linking) CONSIDERED + DEFERRED** (plan.md §O.8) — recommendation NOT now: inward, navigational-only (retrieval needs no links), corpus too young; revisit with §N. Two commits: `5174201` (main pass) + `143c3a0` (sublog fix). Strategic next step UNCHANGED — §C shipping-mart pilot ([[D-027_inward_outward_build_imbalance]]). active-mode → unscoped.)

<details><summary>Prior: [[S104_78e596a8_obsidian_gielinor_link_migration|S104]] (§O.4 gielinor link migration APPLIED + respawn trim)</summary> 2026-05-26 (end of [[S104_78e596a8_obsidian_gielinor_link_migration]]. Dev-brain via "lets develop gielinor", mid-conversation from a brain-root session; OPEN posted; **deferred the apply until the live alching cleared**, then applied. **Done:** Guthix's B-008 bankstanding committed at `79359db` (alched both players, G2→global niksis8, grounding-trigger godly proposal) freed the gielinor write surface; re-ran the §O.4 dry-run **fresh** because B-008 had moved 2 targets (a godly proposal→`proposals/archive/`, a Jebrim draft→`bank/notes/`) and added 1 `[[S069]]` ref, so the stale 42/17 → **43/18**; convention-doc check **0 hits** (gielinor `meta/` ID-links are all real "See [[D-NNN]]" refs, not format illustrations → no `CONVENTION_DOCS` change needed, acf8fc80's worry (b) didn't bite); `--apply` → 43 rewrites/18 files; re-run dry-run → **0 remaining**, 129 full-stem links resolve. Diff is pure link-text, no renames → hooks/cockpit filename parsing untouched, fully git-reversible. Committed by pathspec (18 gielinor files + dev-brain artifacts); **EXCLUDED** the Zezima `S095_f60153e0` quest WIP + cockpit place-modal WIP (98592157) + runtime. Also **TRIMMED this respawn** (11 stacked Last-updated blocks → this one + the rollup below) — the long-overdue collapse, done in a quiet no-parallel window. **Eyeball done ([[S104_78e596a8_obsidian_gielinor_link_migration|S104]]):** opened `gielinor/` as an Obsidian vault — graph renders + links resolve, BUT it's sparse (350 files, **76% with no `[[link]]`**). Investigated (principal-directed, Jebrim layer): **NOT a migration miss** — gielinor notes carry their source-quest anchor as **plain text** (`**Observation (SNNN)**` / `## Anchor SNNN` / `Source: SNNN`), never wrapped as a link. → new **§O.6 backlink-enrichment** queued for a FRESH session (promote anchors → resolved `[[links]]` in `examine/confirmed` + `bank/notes` across players; SKIP `inventory`; DEFER `quest-log` outbound; per-player `SNNN` dupe disambiguation; needs a new anchor→link script mode). Inward — serves §N, does **not** displace §C. Strategic next step UNCHANGED — §C shipping-mart pilot ([[D-027_inward_outward_build_imbalance]]). active-mode → unscoped.)</details>

**Recent sessions (full detail in `quest-log/`):** [[S106_cf03bfe1_self-eating-audit-and-d8-enforcement-fix|S106]] (cf03bfe1) — "is the system eating itself?" critical audit (verdict: NOT — 6-day-old self-pruning system) + **D8 enforcement-gap fix**: registered dwarf/gnome/penguin configs at brain-root `.claude/agents/` so the 4 inert "architectural guarantees" fire (gate on `payload.agent_type`, only stamped for typed sub-agents — verify in a fresh cockpit session that a gnome's write to `confirmed/`/`meta/` is BLOCKED). Obsidian arc — [[S098_b53fca39_obsidian_fit_and_dlink_migration]] + [[S099_acf8fc80_obsidian_quest_link_migration]]: the dev-brain `D-`/`S-` link migrations (698 rewrites total), [[D-004_stable_ids]] amended, per-brain-vault topology, **verified in Obsidian**; [[S104_78e596a8_obsidian_gielinor_link_migration|S104]] (above) finishes the gielinor pass. Bankstanding **B-008** (commit `79359db`) — alched both players, G2→global niksis8, a grounding-trigger godly proposal. Research/parked — [[S103_5a457944_slack_mcp_check_and_niklavs_voice_profile]] Slack MCP confirmed live + Niklavs ~4-register voice profile parked (player-assignment open); [[S102_3b333c4d_agentic_os_gap_survey]] agentic-OS survey — brain has memory structure+gates but no DYNAMICS layer ("all inbox, no producer" → §C scheduled trigger is the missing half); [[S101_transcript_as_sole_comms_surface_feasibility]] transcript-as-sole-comms feasibility (read+type 80% clean, full elimination not — interactive 20% never in the jsonl); [[S097_b3c2b8dd_rag_research_and_obsidian_interlink]] RAG research (brain already agentic-search; RAG narrow+additive, parked as `plan.md` §N behind §C); [[S096_docs_freshness_audit_and_patch]] repo-root `docs/` freshness patch. Cockpit arc — [[S095_f9310a45_cockpit-transcript-full-width-speech-bubble]] transcript redesign + compose-Enter + auto-name (RUNTIME-UNVERIFIED, 2 transient loggers in), [[S093_f3239bdc_transcript-readability-and-term-fit]] / [[S092_327b1216_cockpit_transcript_and_rename_persistence]] transcript readability + rename persistence (verified), [[S094_cockpit_noopen_stale_process_cleanup]] stale-profile-lock cleanup.

**Prior sessions (full detail in `quest-log/`):** [[S090_task_list_discipline]] task-list discipline — gielinor decomposes long tasks into tracked lists ([[D-031_task_list_discipline]] + new `gielinor/meta/task-lists.md`; threshold-gated, two-home). [[S089_35b35dc5_cockpit-duplicate-row-session-rotation]] cockpit duplicate-row fix — `ptybridge` re-announces the freshest live session bound to the PTY shell pid when claude rotates its id, so the board dedups the stale ghost (board b88.1). [[S088_f731b4e8_worktree-isolation-for-parallel-sessions]] worktree isolation decided ([[D-030_worktree_isolation_for_parallel_sessions]] — collision-avoidance is discipline not gates; the same-file Edit-race has no gate; principal chose git worktrees, build deferred; sub-agent `isolation:"worktree"` is free today). [[S087_23dcc534_cockpit-ctrl-c-copy]] cockpit terminal Ctrl+C copy (copy-on-selection, SIGINT fallback; new `POST /api/clipboard` write bridge; board b87.1; principal-VERIFIED "works") + diagnosed the "feed not showing my session" report as a view filter (the `actions`/`prose` toggles), not a bug. [[S086_e668ec7e_brain-technical-docs]] *(two parallel S086s, topic-disambiguated)* — cockpit compose-bar (fixed input below the PTY so you scroll history while typing; committed `dacb9bd`, UNVERIFIED, principal said "needs tweaks") **and** the brain's first consolidated technical docs at repo-root `docs/` (12 MD pages mapping-by-link to canonical files + a regenerable single-file `docs/brain-docs.html`; doc-only). [[S085_5f93bb32_cockpit-pty-auth-and-md-xss]] cockpit /pty RCE + md.js XSS fixed, and the brain-root hook-enforcement gap closed (the six guarantees were prompt-discipline, not gates, at brain root — proven by an `rm`; now fire repo-wide, gielinor wiring kept as redundant). [[S083_d71c4ab3_cockpit-full-audit]] cockpit full audit — terminal re-pin (prompt-below-fold), backend crash-guard, the regression gate (`test_backend.py`), Shift+Enter newlines, instant Esc busy-clear (a 90s backend timeout was reverted after it false-tripped a live Jebrim session). [[S084_454e276c_cockpit-switchbar-selection-and-nav]] cockpit switchbar — open-session gold-pop + keyboard nav (`Ctrl+↑/↓`, `Ctrl+1–9`), UNVERIFIED. [[S082_open_on_entry_discipline]] OPEN-on-entry discipline fix — made `OPEN` + sibling-detection non-skippable on *every* dev-brain entry, mid-conversation included (doc-only). [[S071_e11aff01_neuron-overlay-design]] neuron-overlay v0 **design sketch** (spec at `bank/research/cockpit-neuron-overlay-design.md`, build still parked). [[S064_78824901_switchboard-cockpit-rebuild]]/[[D-028_switchboard_cockpit_rebuild]] the cockpit rebuild that retired the old switchboard map+client.

## Where we are

The **cockpit** (`cockpit/`) is the standalone fleet cockpit and the brain's real driver — interactive `claude` in a PTY (on-subscription past the 2026-06-15 billing change), three surfaces (board / console / feed) over one session model. It replaced the old `switchboard/` map+client ([[S064_78824901_switchboard-cockpit-rebuild]] / [[D-028_switchboard_cockpit_rebuild]]). The hooks (`status-sidecar.py`, `emit-event.py`) are its data source — preserved contracts; don't touch lightly.

The **Obsidian revamp (§O)** is essentially done: the brain is already a `[[wikilink]]` vault; the one-time full-stem link-TEXT migration (Option A, [[D-004_stable_ids]] amended) has now run across **both brains** (dev-brain `D-`/`S-` in [[S098_b53fca39_obsidian_fit_and_dlink_migration|S098]]/[[S099_acf8fc80_obsidian_quest_link_migration|S099]], gielinor in [[S104_78e596a8_obsidian_gielinor_link_migration|S104]]) — link-text only, no renames, so the hooks/cockpit that parse filenames are untouched. The only open item is the principal's Obsidian eyeball of the `gielinor/` vault; then §O closes.

**The inward/outward observation (per [[D-027_inward_outward_build_imbalance]], the [[S060_brain_self_audit_and_plan_reconciliation|S060]] self-audit):** the brain optimized *inward* — observability, the cockpit — more than outward *hands*. The §C pilot (a scheduled producer in front of the drafts→confirmed HITL inbox; [[S102_3b333c4d_agentic_os_gap_survey|S102]]'s "all inbox, no producer") remains unbuilt. **Recalibrated 2026-05-27 (principal):** this is a *known future phase, not the urgent next step* — the current phase is deliberate hands-on collaboration (operator-in-the-loop work is itself outward action), and scheduled autonomy comes much later. The observation stands; the urgency does not. See the steering note under *Next concrete step* and [[D-027_inward_outward_build_imbalance|D-027]]'s 2026-05-27 amendment.

## Next concrete step

> **Steering note (2026-05-27, [[S110_144c0ca2_brain_full_audit|S110]] close):** the principal reprioritized — §C / scheduled-autonomy is a *much-later* phase; the **current phase is hands-on collaboration**. Item 1 is kept for the record but is **no longer "the next step"** — don't reflexively surface it as such. The real next moves are the hands-on items the principal raises in-session.

> **★ NEXT — Khaan sequence largely resolved; remaining items are cheap carries + later-phase.** The [[S115_a9fe4749_khaan-failure-receipts-and-hygiene|S115]] hand-off's item G (phase-registry guard) was **decided HELD** ([[S116_caa72357_khaan-item-g-judgment-call|S116]], `bank/plan.md` §P.5) — no code pipeline to guard yet; revive when the first ordered *code* pipeline lands (§C chain / consolidation pass) and hash *that*. **Sequence status:** steps 1–2 DONE (items 2/12/5 + G held); step 3 effectively done — gate 1 ([[D-033_positive_enforcement_gate_open_on_entry|D-033]]) is self-escaping, so anti-lockout **H is NOT a standalone TODO** (build it *with* the next *trapping* gate, e.g. a grounding-before-advice gate); steps 4–6 unbuilt; step 7 (autonomy) deferred per the much-later-phase steer. Catalogue: `bank/research/2026-05-28-khaan-learnings-implementation.html`. **Best-ROI remaining, in order:** (1) the two cheap carries — the `meta/write-rules.md` "enforced by hook" line via a godly proposal at the next `Hey Guthix, bankstand` (meta user-only, [[D-032_godly_proposal_flow_and_code_bearing_seam|D-032]]); and the [[D-034_close_ritual_enforcement|D-034]] follow-on — extend `close_check.py` to the gielinor *player* close-session ritual ([[S115_a9fe4749_khaan-failure-receipts-and-hygiene|S115]] scoped it to the dev close). (2) item 6 — context-hash freshness (Low effort; pays down parallel-session staleness, [[D-024_parallel_player_coordination|D-024]]). (3) item 4 — 5-lens decision scaffold (Low effort, optional). Everything else is later-phase or polish. The real next moves are whatever hands-on items the principal raises in-session.

1. **§C shipping-mart freshness pilot — (DEFERRED, much-later phase per the steer above) the brain's first outward action ([[D-027_inward_outward_build_imbalance]] / `bank/plan.md` §C).** Two halves: **Braindead** wires a reusable scheduled trigger (harness `schedule`/cron — sidesteps §G) firing a fixed Jebrim brief ~08:30 daily, output to a file first (Slack draft only once trusted); **Jebrim** writes the read-only SQL checks against `shipping_mart` (recency / volume / cost-invariant `SUM(buckets)==total_eur==real_shipping_cost_eur` / `cost_source` coverage / order-item completeness / cross-fact agreement) + thresholds, in `shipping-agent/`.
2. **Obsidian §O.7 — links-mode top-up for `Q-`/`A-`/`I-`/`R-` prefixes (finding, [[S105_7c91117c_obsidian_o6_backlink_enrichment|S105]]).** §O.6 done (prose-ref wrap, both brains). Remaining: dev links-mode shows ~103 bare `[[ID]]` bracketed links unmigrated — §O.2/O.3 only covered `D-`/`S-`, so `[[Q-001]]`/`[[A-002]]`/`[[I-003]]`/`[[R-001]]` are broken in Obsidian. Fix = `obsidian-link-migrate.py --mode links --apply` on dev with the prefix filter widened + per-occurrence resolution for the 7 flagged dupes it re-surfaces. Quick + safe (0 dangling) but re-opens dupe disambiguation. Inward; does not displace §C. Scope in `bank/plan.md` §O.7.
3. **Worktree isolation for parallel sessions (decided [[D-030_worktree_isolation_for_parallel_sessions]], build deferred — principal will pick it up).** The fix for same-file Edit-retry-hell across parallel sessions. *Available today, zero build:* spawn fan-out sub-agents with `isolation:"worktree"` when they edit shared code. *The build:* the **hook-path audit** first (repo-relative writes — intent files, `switchboard/state-*` — must pin to one absolute shared location, else worktree sessions are invisible to the board), then comms/intent sharing across worktrees + cockpit worktree awareness + a merge convention. Rule: *isolate what you EDIT, share what you COORDINATE THROUGH.*
4. **Neuron-overlay v0 (the "Sunday build", unblocked).** Build from the spec at `bank/research/cockpit-neuron-overlay-design.md` — new `cockpit/web/brain.js`, fixed overlay **outside** `.app-grid`, polling `/api/feed`, OSRS palette. See [[S071_e11aff01_neuron-overlay-design]].
5. **Cockpit single-instance guard + stale-profile-lock recovery ([S094] — this one recurred and annoyed).** The persistent `.webview` profile lock-contends across stacked launches; one hang + a natural icon-retry self-DoSes. Add: (a) if `:8770` already serves a *cockpit*, focus the existing window and refuse a second instance; (b) on launch, detect + clear a stale WebView2 profile lock before `webview.start()`. See [[S094_cockpit_noopen_stale_process_cleanup]] + `bank/build-lessons.md`.
6. **Cockpit relaunch verification (fast, clears unknowns). Board reads `b91.0` when fresh code loads.** [[S095_f9310a45_cockpit-transcript-full-width-speech-bubble|S095]] (transcript redesign + compose-Enter + auto-name persistence) is node-checked but RUNTIME-UNVERIFIED — relaunch checklist in [[S095_f9310a45_cockpit-transcript-full-width-speech-bubble]]; two transient loggers still in (strip once verified): `term-fit-diag.log` + `rename-diag.log`.

## Files to read first

1. `respawn.md` (this file).
2. `quest-log/in-progress/S104_78e596a8_obsidian_gielinor_link_migration.md` — latest quest (Obsidian §O.4 gielinor migration); `bank/plan.md` §O for the full Obsidian roadmap.
3. `cockpit/_about.md` + `cockpit/backend.py` + `cockpit/web/` — the live driver surface.
4. `bank/decisions/D-027_*.md` — the inward/outward operability gap (the strategic gap); `bank/plan.md` §C — the parked outward pilot scope.
5. `bank/decisions/D-028_*.md` — cockpit spec; `bank/research/cockpit-neuron-overlay-design.md` — the [[S071_e11aff01_neuron-overlay-design|S071]] overlay spec.
6. `comms/active.md` (tail) — live siblings + hand-offs.
7. `bank/build-lessons.md` — cross-session build wisdom (read on demand).

## How to run the cockpit

Launch via the **cockpit / Switchboard** Desktop/Start-menu icon — it starts its own backend (`cockpit/Switchboard.vbs` → `app.py`; a pywebview window + aiohttp backend + PTY driver in one process). Hooks write state to `switchboard/state-*` files + `~/.claude/status/<sid8>.json`; the cockpit backend reads those. A relaunch is needed to pick up backend/JS changes (the running window holds stale code). `node --check` + `py_compile` are the cheap pre-commit gates for any web/hook edit.
