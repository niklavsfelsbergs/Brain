# S185 — 2026-06-09 — Brain-map top-down world visualizer (prototype, parked)

- Built a standalone **2D top-down (Stardew-style) visualizer** of the brain's structure at `experiments/brain-map/` — each player gets a hedged **cul-de-sac** (big **manor** = home + small **cottages** = their layers), a central **Commons**, **Braindead's Workshop**, roads = rituals, walking characters = sessions. HTML5 canvas renderer (`index.html`): drag-pan / wheel-zoom / F-fit camera, depth-sort by y, deterministic PRNG scatter, dusk grade + window/lantern glow, hover-name tooltips.
- Replaced the earlier CSS "notepad" map (`tools/brain_map.py`) and a first Sprout-Lands skin (twee/"girly-pink", non-commercial license) with the grittier **Ninja Adventure pack (CC0, pixel-boy)**. Slicer `prep_ninja.py` pins every sprite crop **deterministically** — numeric color/variance scan for terrain, connected-component bboxes for buildings/foliage — after auto-"greenest/brownest" scoring kept grabbing wrong tiles (flowered grass, autumn-orange bushes) from the multi-biome / autumn-palette source sheets.
- Iterated live on Niklavs' eyeball (image-view capped this session, so I verified crops by numbers instead): fixed the patchwork-quilt ground (mismatched-biome grass variants → one clean deep-green tile + sparse tufts), confined prop scatter to the **wilderness outside plots** (trees/bushes were piling on houses), swapped tan hay-bale bushes for green ones, and replaced the orange market-stall "hut" crop (the "wheelbarrow") with a scaled-down timber **cottage** off the verified manor art.
- **Parked.** Niklavs: *"i dont like it currently"* — may return. Plumbing is sound; the open question is art direction. Vendored sprite packs (14M) gitignored; `sprites_ready/*.png` (290K, CC0) + scripts + `README.md` committed so it runs after a re-fetch.
- Lesson harvested to [[build-lessons]]: auto-detecting sprites by color heuristic from a multi-palette sheet silently mis-picks; pin crops deterministically and, under the image-view cap, verify by mean-RGB / variance / connected-components rather than by eye.

**Cascade.** `experiments/brain-map/` (new: `index.html`, `prep_ninja.py`, `prep_assets.py`, `sprites_ready/*.png`, `.gitignore`, `README.md`); `bank/build-lessons.md` (+1 lesson); `quest-log/S185_*.md`; `respawn.md`; `comms/active.md` (CLOSING).
**Main-brain changes.** none.
