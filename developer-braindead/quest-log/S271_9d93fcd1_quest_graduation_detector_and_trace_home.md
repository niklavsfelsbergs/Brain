# S271 — 2026-06-18 — quest-graduation detector + sub-agent trace-home (B-020 Parts 1-3)

Mid-conversation pivot into dev-brain via "lets develop gielinor, pick this up" — continuing the Guthix bankstanding [[B-020_2026-06-18_memory-cap-trim-and-graduation-retire|B-020]] finding (quest promotion is discipline-gated and the ritual that does it gets skipped, so Jebrim's `in-progress/` had silently grown to 73). Built the principal-approved fix from the godly proposal, all three parts.

- **Part 1 (keystone)** — new read-only detector `developer-braindead/verification/quest-graduation-check.py` + `test_quest_graduation.py` (37/37). Per player it reads each quest's `inventory/*-resume__*.md` `open_dep` header and classifies GRADUATABLE (`open_dep: none` → should be in `completed/`) / HELD / NO-RESUME / TRACE (sub-agent run-logs by filename pattern). sid8+SNNN resume matching disambiguates parallel SNNN collisions ([[D-024_scope-git-commits-with-pathspecs-parallel-sessions|D-024]]). Mirror of `lesson-store-check.py` — "detectors hold, discipline drifts." **Wired into** the `respawn.md` reconciliation prompt (per-active-player) and `bankstanding.md` step 6 (all-players, flag-and-recommend) so the backlog self-surfaces every session.
- **Part 2 (trace-home)** — sub-agent run-logs now route to `quest-log/traces/`, not `in-progress/` (they were never quests and have no graduation path, so they accumulated). Widened the 4 `*-write-boundary.py` allow-lists (permissive, non-breaking; floor + bank/confirmed blocks regression-tested green); new *Sub-agent traces* section in `meta/modes.md`; redirected the run-log instruction in `spawning-{dwarves,gnomes,penguins}.md` + `.claude/agents/shipping-agent.md` + `respawn.md` step 7; `traces/` added to both players' `quest-log/_about.md`.
- **Part 3 (cleanup)** — drained Jebrim `in-progress/` **73 → 36**: graduated 9 stale-done quests (`open_dep: none`) to `completed/`, archived 28 sub-agent traces to `quest-log/archive/traces/` and 8 resumes to `inventory/archive/`. [[S245_3172630e_eu-tender-no-hermes-routing-ops-coherence|S245]] held (resume says none but the [[S254_shipagent_us-bi-carrier-invoice-export|S254]] comms keeps parked items — genuinely ambiguous). Committed `6993ee0` (65 files, pathspec-scoped via the staged set; a parallel ORWO session committed interleaved with no collision; 3 pre-existing foreign files left untouched per the [[S144_bd1a6513_picaapi-us-fathers-day-2025|S144]] guard) + a born-link follow-up.
- **Carried the Guthix B-020 footprint** into the same commit and **promoted [[D-035_retire-memory-lessons-on-graduation|D-035]]** (retire-a-warm-MEMORY-line-on-graduation) drafts → confirmed on principal cue, with its `lorebook/_index.md` entry. The MEMORY.md cap reconcile (26.3 → 23.3 KB) from the Guthix half is on disk (outside the git tree).

**Cascade.** New `developer-braindead/verification/quest-graduation-check.py` + `test_quest_graduation.py`; build-lessons appended; this quest-log entry; respawn updated; dev `comms/active.md` OPEN→UPDATE→CLOSING.

**Main-brain changes.** Extensive (Braindead full-reach, [[D-032_braindead_full_access|D-032]]): `gielinor/.claude/hooks/{dwarf,gnome,penguin,shipping-agent}-write-boundary.py`, `.claude/agents/shipping-agent.md`, `gielinor/meta/modes.md`, `gielinor/spellbook/rituals/{respawn,bankstanding}.md`, `gielinor/spellbook/skills/spawning-{dwarves,gnomes,penguins}.md`, `gielinor/players/{jebrim,zezima}/quest-log/_about.md`; promoted `gielinor/lorebook/confirmed/D-035_*.md` + `lorebook/_index.md`; the Guthix B-020 set (`deities/guthix/quest-log/completed/B-020_*`, `deities/guthix/bank/drafts/notes/2026-06-18-quest-graduation-*`, `deities/guthix/proposals/2026-06-18-quest-graduation-*`); Jebrim quest-log moves (9→completed, 28→archive/traces, 8 resumes→inventory/archive). Also outside-git: `MEMORY.md` trim + 5 `memory/archive/` moves.
