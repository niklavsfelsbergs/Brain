# S096 — 2026-05-26 — docs/ freshness audit + patch

- Principal asked, on dev-brain entry, whether the repo-root `docs/` (the [[S086]] consolidated technical docs — 12 MD pages + a regenerable `docs/brain-docs.html`) is up to date. Audited all 12 pages against current source-of-truth. Verdict: structurally sound and the "map, not copy" design held, but the source MD was **frozen at S086 (May 24)** and a grep for `task-list|worktree|D-030|D-031|transcript|compose-bar` returned **zero matches** — four real post-S086 gaps.
- Principal chose "patch the 4 gaps" (over a full sweep / report-only). Landed all four: (1) the biggest — **task-list discipline** ([[S090]] / [[D-031]] / `gielinor/meta/task-lists.md`) had no node anywhere; added a *Task-list discipline* section to `docs/07-communication-and-coordination.md` after the preamble. (2) **Worktree isolation** ([[D-030]]) — added an *Isolating parallel edits* note to 07's parallel-coordination section. (3) **Cockpit transcript toggle + compose-bar** ([[S087]]–[[S095]]) — extended the Session-console surface in `docs/09-cockpit.md` (added `web/transcript.js` to the refs). (4) **Appendix index drift** — added `task-lists.md` to the 07 source row and `D-030`/`D-031`/`S090` to the decision-reference list in `docs/11-appendix.md`.
- Regenerated `docs/brain-docs.html` via `docs/build-html.py` (12 pages, 107KB); grep-verified the 4 fixes landed in the built HTML (8 matches). Adopted the pre-existing orphaned `build-html.py` tweak (+11/−2, uncommitted from a May-25 docs session) into this commit since the regenerated HTML was built with it — committing the HTML without its build script would split a consistent pair.
- Sibling discipline held: detected live `braindead-f9310a45` (cockpit transcript full-width/speech-bubble, [[S095]]) at entry, posted an `OPEN` (read-only audit), steered fully clear of `cockpit/*`. No overlap.

**Cascade.** `developer-braindead/`: `comms/active.md` (OPEN + CLOSING), `respawn.md` (refresh), `quest-log/S096_*.md` (this).
**Main-brain changes.** `none` — `docs/` is a repo-root sibling of `gielinor/`, not inside it; no `gielinor/` writes this session. Edited `docs/07,09,11`, `docs/brain-docs.html`, `docs/build-html.py`.
