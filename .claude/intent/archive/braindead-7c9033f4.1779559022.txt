Shipped session takeover: click any board row (incl. VS Code sessions) → chatbox opens it, loads /history, resumes it. server.py /history now accepts an 8-char sid8 and resolves it to the full id from the .jsonl filename; terminal.js focusBySid8 creates+takes over board sessions. Restarted the live server with the new code (verified sid8 resolution). Awaiting principal hard-refresh + eyeball.