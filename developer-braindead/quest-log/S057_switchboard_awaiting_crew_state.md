# S057 — 2026-05-23 — Switchboard "awaiting crew" state (waiting_for_subagents)

- Principal opened a dev session asking how to show **a session that's blocked waiting for its sub-agents** in the switchbar — distinct from the existing `WAITING` (waiting-for-*you*) state. Designed and shipped a new `waiting_for_subagents` state rendered as the **`AWAITING CREW`** chip (label chosen by the principal via `AskUserQuestion` over DELEGATING / WAITING·CREW / SPAWNED).
- **Derived, not counted.** A counter in the status file loses increments under parallel spawns (concurrent hook processes race). Instead `status-sidecar.py` reads the spawn role-files emit-event.py already maintains (`state-dwarves/gnomes/penguins.json`, `bySession[sid].byToolUseId` + `pendingQueue`, background-excluded) and the `_write_manifest` per-row override owns the `working ↔ waiting_for_subagents` flip — so it refreshes on **any** live session's hook fire (the blocked parent emits nothing while waiting) and self-heals a missed PostToolUse. New helpers `_pending_subagents_by_session()` / `_count_pending_subagents()`; subtitle override reads `awaiting crew — D1 D2 P1`.
- **Pre/Post registration** (`.claude/settings.json`) extends the existing tight `AskUserQuestion|ExitPlanMode` matcher to `…|Task|Agent` (Pre sets the state, Post re-derives), covering the lone-blocked-session case where no other session is around to refresh the manifest. Background spawns (`run_in_background`) don't block the parent → their Pre is ignored. `WAIT_TOOLS`/`SUBAGENT_TOOLS` lifted to module constants; the Pre/Post block in `main()` restructured.
- **Client render:** [[switchboard.js]] `STATE_RANK` (rank 2, just under WORKING) + `STATE_LABEL: 'AWAITING CREW'`; [[styles.css]] steel-blue chip / row-tint / `sbCrewPulse` dot + sparkline bar (between working and closing). `state.js` needed no change — the new state passes through `deriveSessionState` and is correctly never downgraded to IDLE. Kept **out** of the yellow "needs you" / hero tally — awaiting-crew does not need the principal.
- **Tested:** synthetic role-file fixture confirms foreground spawns counted, background excluded, `pendingQueue` fallback works, unknown session → 0; `status-sidecar.py` syntax-clean, no duplicate constants; live read of the real role-files returned empty (no spawns in flight) without false positives.
- **D-024 lived in real time.** A parallel sibling **braindead-e433ac17** held `S056` (COMMS chatbox reskin) editing the *same* three client files. Verified my additive hunks survived their concurrent writes (they did — coexist cleanly), then committed only my **uncontested** files (hook + settings + these docs) and **handed off** the client-file hunks to e433ac17 via comms rather than fighting `git add -p` (non-interactive). Renumbered S054→S057 (S054 committed shipping-agent, S055 live gielinor, S056 the sibling).

**Cascade.** dev-brain: this quest-log entry, `respawn.md` (S057 line), `comms/active.md` (CLOSING + → @e433ac17 handoff). Brain-root: `developer-braindead/.claude/hooks/status-sidecar.py` (new state + helpers + manifest override), `.claude/settings.json` (Task|Agent matcher + comment). Client files `switchboard/{switchboard.js,styles.css,index.html}` carry my additive hunks but are **ceded to e433ac17's S056 commit** (concurrent edit).

**Main-brain changes.** none — no writes to `gielinor/`.
