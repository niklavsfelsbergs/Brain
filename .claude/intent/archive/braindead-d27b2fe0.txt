Wired agent prose into the FEED: status-sidecar.py tails each session's transcript .jsonl from a persisted byte offset, emits new assistant text blocks as chat.ndjson kind:"say" — skips thinking + sidechains, never replays backlog. feed.js renders them + a "prose" toggle. AST+node-check green, tail logic unit-tested. Backend untouched (sibling-clean). Uncommitted — awaiting principal eyeball after relaunch.
