# S275 — 2026-06-19 — confirmed-scan DANGLING calibration + bankstanding wire-in

Dev-brain via "lets develop gielinor, continue with this". Continued the `confirmed-scan.py` arc (the gated-layer contradiction/rot scan). The tool itself was built in a **prior conversation** and committed as `ae6ca4c` (tool + test + 2 DPD notes) but **never given a dev close** — no quest-log, respawn, or comms entry; an orphan in the dev record. This session is its reconciliation + the point-3 hand-off (run it in real use, decide bankstanding-wiring, resolve the title-token RELATED limitation).

- **Ran the scan live — and it reframed the plan.** Over 263 gated notes the **HARD/DANGLING tier was ~80% false-positive** (28 flags, ~22 were concept-slug links like `[[distinguish-fixture-from-live]]` resolving to real files under a `feedback_`-prefixed or date-suffixed stem) — directly contradicting the tier's own "near-zero false positives" claim. So "wire once it's earned trust" became **fix the resolver first, then wire**. Quantified the FP rate before deciding (verify-the-thing).
- **Calibrated the resolver** in `confirmed-scan.py`: prefix-stripping is now symmetric on link-target AND corpus stems and covers the `feedback_`/`reference_` memory type-prefixes + the `D-NNN_`/`S018` lorebook/quest IDs (so a concept-slug resolves to its prefixed file); unresolved **multi-word concept-slugs demote to soft LOOSE**, leaving only bare-IDs + single-word placeholders as HARD DANGLING. Live result: **28 → 8 genuine** (bare-ID links `[[D-012]]`/`[[D-015]]`/`[[S018]]`/`[[I-003]]` + `[[keepsake]]`/`[[wikilink]]`). Cleaner title tokens also promoted the [[D-021_lorebook-folder-naming-correction|D-021]]↔[[D-022_lorebook-folder-naming-correction-expanded-scope|D-022]] near-duplicate to COLLISION.
- **Wired into bankstanding** as **step 9** ([[bankstanding|gielinor/spellbook/rituals/bankstanding.md]]): HARD flags = worklist (propose for globals, flag-and-recommend for per-player); RELATED/COLLISION = a **review queue the principal approves via the in-session semantic pass, never an auto-fix** — carrying the DPD-PL false-positive as the cautionary case in the ritual text. **Body-similarity widening deferred + documented** in the tool (the demonstrated need was DANGLING precision, not RELATED recall; widening adds noise to the one clean tier).
- **Extended the test** `test_confirmed_scan.py` for the new policy (prefix-resolution, concept-slug→LOOSE, bare-ID/placeholder→DANGLING) — **16/16**; full batch **20/20** via `run_tests.py`.
- **Ran the live semantic pass** over the 4 candidate pairs end-to-end (the gielinor lorebook folder-naming-correction pair, the graduate-complete-ready-quests pair, the DPD base/no-Hermes pair, the rerating bank-note/examine pair) → **0 real contradictions** — all supersession/additive/complementary and already cross-linked. (Bare `D-NNN` IDs avoided in prose here: those are *gielinor* lorebook decisions, and a bare ID auto-wraps to the colliding *dev* D-NNN.) Validated the review-queue flow: nominate → human read → judge → propose nothing. No cry-wolf, no false fixes.
- **Committed `6757a54`** by explicit pathspec (4 files) — a **live Jebrim sibling (S274 a1f05d25)** had staged `2026-06-19` drafts + a promotion-pass in the shared index; pathspec kept its work untouched ([[D-024_parallel_player_coordination|D-024]] hazard, handled). Not pushed.

**Standing input for the next real bankstand:** the 8-DANGLING worklist (bare-ID links needing full-stem rewrites, most in `lorebook/confirmed/`) + 7 UNDATED bank notes.

Cascade. `developer-braindead/verification/confirmed-scan.py` (resolver calibration + docstring), `developer-braindead/verification/test_confirmed_scan.py` (new-policy cases), `developer-braindead/comms/active.md` (OPEN + CLOSING), `respawn.md`, this quest-log. Prior orphan `ae6ca4c` now reconciled into the record.

Main-brain changes. `gielinor/spellbook/rituals/bankstanding.md` — new step 9 (confirmed-scan as a review queue) + a Related cross-ref. No identity/`confirmed/`/`meta/` writes.
