# S147 — 2026-06-02 — @import-chain trim Phase 1 Stage B (modes + write-rules → JIT)

- Continued the [[S145_543c6caf_knowledge_loading_and_rule_adherence_audit|S145]] anti-deterioration work: executed **Phase 1 Stage B** of the `@import`-chain trim ([[plan|plan]] §X.5, blueprint `bank/research/2026-06-02-phase1-import-chain-trim-design.md`). Moved `gielinor/meta/modes.md` + `gielinor/meta/write-rules.md` out of the eager `@import` chain to **just-in-time**, edited `meta/` directly under [[D-032_braindead_full_access|D-032]] (no godly proposal).
- **The cut** ([[CLAUDE|gielinor/CLAUDE.md]]): dropped the two `@import` lines; added JIT bullets for both + a **draft→approve CORE one-liner** so the everyday write discipline survives without the full table. modes' boundary summary rides the existing *six architectural guarantees*; `layer-routing.md` (still always-on) carries the everyday `drafts/` routing.
- **The re-triggers** (design's coupling principle — only trim what a ritual reads or a hook enforces): forced-reads of `modes.md` added to all three spawn skills ([[spawning-dwarves]], [[spawning-gnomes]], [[spawning-penguins]]) at spawn time (R1); forced-reads of `write-rules.md` added to [[alching]] / [[drafts-triage]] / [[bankstanding]] / [[close-session]] at ritual entry (R1). Wired **all four** rituals (not just the two the design named) so the CLAUDE.md "read at …" claim is true, not fiction — the brain's own *check-the-record-before-asserting-a-design-story* lesson applied proactively. R2 backstop: the four `*-write-boundary.py` hooks (confirmed firing 3× each in 30d `ritual-stats`).
- **Verified the bar (smaller without dumber):** `brain-weight.py` chain **21,304 → 13,639 tok** (7→5 files; net of +422 CORE summary), **41% off the pre-Stage-A 23,295**. Regression set re-scored **2 caught / 3 partial / 5 miss — unchanged** (load-order change, not retrieval; no case depends on eager modes/write-rules). Adherence unchanged (no hooks touched). Re-score log added to [[knowledge-miss-regression-set]].
- Committed mechanism + docs at **95fd387** by pathspec (11 files incl. born-link auto-wrap of the regression-set re-score). Build-lesson harvested (PowerShell here-string in the Bash tool broke the first commit; proactive-fiction-guard worth carrying). **Open:** Stage C (the delicate `communication-protocol.md` split, ~3.8k) + Stage D (task-lists split + rule-index + final thinning); the other diagnosis half — X.2–X.4 hooks that *move* the regression set.

**Cascade.** [[S147_import_chain_trim_stage_b]] (this quest); [[respawn]] updated; [[plan]] §X.5 Stage B → DONE; [[knowledge-miss-regression-set]] re-score log; [[build-lessons]] += S147; `comms/active.md` OPEN+CLOSING.
**Main-brain changes.** `gielinor/CLAUDE.md` (import block + JIT list + draft→approve core line); `gielinor/spellbook/skills/spawning-{dwarves,gnomes,penguins}.md` (modes forced-read); `gielinor/spellbook/rituals/{alching,drafts-triage,bankstanding,close-session}.md` (write-rules forced-read). All committed 95fd387.
