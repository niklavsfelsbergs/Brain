# respawn.md — dev brain entry point

> Read this first when starting a fresh dev session. Where we are, what's open, the next concrete step.
>
> The ritual that reads this file is `spellbook/respawn-ritual.md`. The ritual that *updates* it is `spellbook/session-close.md`.
>
> **Discipline (enforced 2026-05-24 token audit).** Overwritten in place at session close — **NOT append-only**. Keep the current-state block + the most recent one or two session pointers; older per-session history lives in `quest-log/SNNN_*.md` (and in git). Cross-session build lessons live in `bank/build-lessons.md` — read on demand, never loaded at respawn. This file had grown to ~410 lines / ~49k tokens of stacked `Last updated` blocks read every session; that is the bloat the discipline exists to prevent. Don't let it regrow.
>
> **Parallel note ([[D-024]]).** Multiple dev sessions run at once. At close, **PREPEND** your single `Last updated` line to whatever this file currently says — do **not** overwrite it from a stale full copy you read at your own respawn, or you'll clobber a collapse like this one.

**Last updated (S087 [cockpit Ctrl+C copy] — terminal copy-on-selection; committed, principal-VERIFIED "works"; sid 23dcc534).** 2026-05-25 (end of [[S087]]. Dev-brain via "lets develop gielinor", mid-conversation; OPEN posted. Two cockpit reports while a live Jebrim PTY session ran beside the feed. **(1) "messages not popping up in my feed" was a filter, not a bug** — curled the live `/api/feed` (8770) and found the session's events all present (11 items, all `say`+`action`); a *working* session is ~213/300 items in those two kinds and both are gated by the FEED `actions`/`prose` toggles (default on, but localStorage-persisted off) → looks dead between PICKED UP and DONE. No code; re-tick the boxes. **(2) Ctrl+C couldn't copy** — `term.js` never handled it, so xterm fired raw `\x03` (SIGINT). Fixed (principal chose copy-on-selection, SIGINT fallback): Ctrl+C copies when `term.hasSelection()` + swallows the key, else falls through to interrupt (Esc already interrupts here); new `_copyToClipboard` → new backend `POST /api/clipboard` write bridge (`_write_clipboard_text`, ctypes mirror of the reader — WebView2 gates native writes like reads, why paste already needed the bridge), navigator fallback. board b86.3→**b87.1**. node --check + py_compile clean; `test_backend.py` **39/39** (+1 no-clobber route assertion). Principal relaunched: **"works."** Lesson → [[build-lessons]] (bug-vs-filter: read the live payload first). active-mode.txt → unscoped. See [[S087]].)

**Last updated (S086 [cockpit compose-bar] — fixed input below the PTY; committed, UNVERIFIED, "needs tweaks"; sid 86020a52).** 2026-05-25 (end of [[S086]]-cockpit, the *parallel* S086 alongside e668ec7e's S086-docs above — both kept S086, topic-disambiguated, per the S038 precedent + e668ec7e's CLOSING-CORRECTION. Dev-brain via "lets develop gielinor". Principal: in the PTY terminal keep the input box fixed at the bottom while the chat scrolls — *"i cant scroll and write at the same time."* Root: a PTY couples input to the scroll grid; the fixed-composer UX only existed on the headless `Console` (metered path), so the subscription terminal lacked it. Chose **compose-bar** over live-forwarding / scroll-only (recommended 3-choice). Built: a fixed `<textarea>` under the xterm piping text+Enter to the PTY — `TermConn.submitComposed` (/rename parity, single-line direct / multi-line bracketed-paste, re-pin to bottom) + `sendEsc` + new `TermComposer` component; `main.js` renders it under `<Term/>`; `styles.css .term-composer` zoomed like the header (kept off xterm's unscaled subtree). board b85.1→**b86.3**. **Principal caught a merge bug on first live use:** text half-typed into claude's in-terminal box survives Esc and prepends to the next composed send — fixed by clearing the `_linebuf` mirror (the /rename backspace trick) on Esc + in `sendEsc` + atop `submitComposed`; then removed the placeholder + made the bar 3× taller (min-height 116px); a 3rd round cleared the bar's own text on Esc. Commit dacb9bd (subject "S086: cockpit terminal compose-bar"). **UNVERIFIED (eyes-only): relaunch + the whole bar; principal said it "will need some tweaks."** Lesson → [[build-lessons]] (the shared-surface state lesson). active-mode.txt → unscoped. See the S086-cockpit quest.)

**Last updated (S086 — full technical docs of the brain at repo-root `docs/`; doc-only, sid e668ec7e).** 2026-05-25 (end of [[S086]]. Dev-brain via "lets develop gielinor". Principal: build full technical documentation, cockpit just one "how you drive it" section, "let me know what you'd include." Scoped via a 3-choice (all recommended): repo-root `docs/` tree, multi-file, **map that LINKS to canonical files** (meta/, _about.md, rituals) not a copy → won't drift. Built 12 MD pages (README + 01-orientation/02-glossary/03-layers-and-memory/04-write-discipline/05-actors-and-modes/06-rituals/07-communication/08-enforcement-and-hooks/09-cockpit/10-dev-brain/11-appendix), sourced by 3 read-only recon dwarves (rituals / hooks+machinery / cockpit+actors), authored solo for coherence. Then principal: "where is the HTML version?" → added regenerable `docs/build-html.py` (python-markdown) rendering one portable self-contained `docs/brain-docs.html` (~104 KB, sidebar + scroll-spy); 81 in-page anchors all resolve, 0 broken, 22 tables. **Recorded honestly in the docs: the S085 brain-root enforcement model + role-hooks-inert-for-principal, AND a live bug — `emit-commit-event.py` still writes the legacy `experiments/visualizer/state.ndjson` while every other hook moved to `switchboard/` in S052, so commit banners land in a dead stream (future fix).** NO gielinor/ or cockpit/ writes — docs only. Commits eb03491 (MD tree) / a85cab2 (HTML + generator) / + this close. Lesson ([[build-lessons]]): clarify the *form* of a "documentation" deliverable up front (the markdown-then-"where's the HTML" beat). active-mode.txt → unscoped. See [[S086]].)

**Prior sessions (full detail in `quest-log/`):** [[S085]] cockpit /pty RCE + md.js XSS fixed, and the brain-root hook-enforcement gap closed (the six guarantees were prompt-discipline, not gates, at brain root — proven by an `rm`; now fire repo-wide, gielinor wiring kept as redundant). [[S083]] cockpit full audit — terminal re-pin (prompt-below-fold), backend crash-guard, the regression gate (`test_backend.py`), Shift+Enter newlines, instant Esc busy-clear (a 90s backend timeout was reverted after it false-tripped a live Jebrim session). [[S084]] cockpit switchbar — open-session gold-pop + keyboard nav (`Ctrl+↑/↓`, `Ctrl+1–9`), UNVERIFIED. [[S082]] OPEN-on-entry discipline fix — made `OPEN` + sibling-detection non-skippable on *every* dev-brain entry, mid-conversation included (doc-only; the heavier hook-enforced OPEN was considered, not built). [[S079]] cockpit header-bar overhaul — "N live" un-wrap + chat-header "grainy box" fix (styles.css, UNVERIFIED; peek `.console` same latent calc-width pattern FLAGGED). [[S077]] swarm verification — terminal scroll desync fix + board-side rename + `/rename` stranding fix (all principal-verified). [[S076]] cockpit VSCode parity — disk-backed `/rename` + click-row-opens-in-VSCode (VSCode jump still unverified end-to-end). [[S073]] terminal scroll-lock fix. [[S074]] cockpit status fixes — idle decay + dead CLOSING/chat removal. [[S075]] agent prose into the FEED via `kind:"say"`. [[S072]] cockpit fluid UI — scroll-zoom + resizable/collapsible pillars + Shift+Enter→\n. [[S071]] neuron-overlay v0 **design sketch** (spec at `bank/research/cockpit-neuron-overlay-design.md`, build still parked). [[S064]]/[[D-028]] the cockpit rebuild that retired the old switchboard map+client.

## Where we are

The **cockpit** (`cockpit/`) is the standalone fleet cockpit and the brain's real driver — interactive `claude` in a PTY (on-subscription past the 2026-06-15 billing change), three surfaces (board / console / feed) over one session model. It replaced the old `switchboard/` map+client ([[S064]] / [[D-028]]). The hooks (`status-sidecar.py`, `emit-event.py`) are its data source — preserved contracts; don't touch lightly.

S077 cleared the S073–S076 verification debt (scroll, rename); S078–S080 then reworked the status vocabulary (two-axis), header CSS, and the manifest liveness gate. S083 audited the lot: the status pipeline LOGIC is now provably correct (new `cockpit/test_backend.py`, 26/26 — stale-greying, attention tally, aliasing, heartbeat, crash-guards) and the server boots clean (aiohttp route smoke). The terminal prompt-below-fold fix is logically complete but **UNVERIFIED visually** (S083 eyeball checklist). S084 then made the switchbar usable: fixed a latent match-predicate bug that meant the open-session highlight NEVER applied to PTY-driven sessions (sid8 vs full-id), gave the open row a strong gold pop, and added keyboard nav (`Ctrl+↑/↓` move, `Ctrl+1–9` jump) + `Ctrl+Shift+\`` new-session — also **UNVERIFIED visually** (S084 checklist; board reads `b84.1`).

S085 broke the inward-polish streak with two security fixes (Codex review): the cockpit /pty RCE + md.js XSS, and — the consequential one — closing the brain-root hook-enforcement gap that meant the "six architectural guarantees" were prompt discipline, not gates, in the default mode (and for cockpit-launched sessions). That last one was integrity work, not polish. But the broader point below still stands: nearly everything since S073 has been inward. S086-cockpit (parallel to the docs S086) continued the inward run with a cockpit-terminal **compose-bar** (a fixed input under the PTY so you can scroll history while you write); committed (`dacb9bd`, board `b86.3`) but UNVERIFIED and flagged by the principal as "needs tweaks."

S086 produced the brain's first consolidated **technical documentation** — a navigable map at repo-root `docs/` (12 MD pages + a regenerable single-file `docs/brain-docs.html`) over the scattered `_about.md`/`meta/`/ritual files, plus the cockpit as one "how you drive it" section. Doc-only; it doesn't move the strategic gap below.

S087 (this session) added cockpit-terminal **Ctrl+C copy** (copy-on-selection, SIGINT fallback; new `POST /api/clipboard` write bridge) — **principal-verified "works"** (board `b87.1`, the first cockpit item confirmed live since the S083 batch). Also diagnosed (no code) that the feed "isn't showing my session" is a view filter, not a bug: a working session is almost all `say`+`action` events, both gated by the FEED `actions`/`prose` toggles. Still inward polish — doesn't move the §C gap.

**The load-bearing gap (per [[D-027]], the S060 self-audit):** the brain optimized *inward* — observability, the cockpit — and never built outward *hands*. The operational half of the original plan (the §C pilot) is still unbuilt; the agent is still manual-invocation-only. That, not more cockpit polish, is the strategic next step.

## Next concrete step

1. **§C shipping-mart freshness pilot — the brain's first outward action ([[D-027]] / `bank/plan.md` §C). The highest-value build; cockpit polish is now verified and stable, so there's no reason left to keep deferring it.** Two halves: **Braindead** wires a reusable scheduled trigger (harness `schedule`/cron — sidesteps §G) firing a fixed Jebrim brief ~08:30 daily, output to a file first (Slack draft only once trusted); **Jebrim** writes the read-only SQL checks against `shipping_mart` (recency / volume / cost-invariant `SUM(buckets)==total_eur==real_shipping_cost_eur` / `cost_source` coverage / order-item completeness / cross-fact agreement) + thresholds, in `shipping-agent/`.
2. **Neuron-overlay v0 (the "Sunday build", unblocked).** Build from the spec at `bank/research/cockpit-neuron-overlay-design.md` — new `cockpit/web/brain.js`, fixed overlay **outside** `.app-grid` (top-right, to dodge the zoom scaling landmine), polling `/api/feed`, OSRS palette. See [[S071]].
3. **Optional hardening (S077 finds #1/#2).** Manifest completeness when a session's in a long hook-silent turn; a staleness decay for a stuck `working`. Small, same status-pipeline area. Only if it recurs and annoys.
4. **Relaunch verification (do first — fast, clears the biggest unknowns). Board reads `SWITCHBOARD b87.1` when fresh code loads.** (S087 Ctrl+C copy already verified "works"; if the feed looks empty for a live session, check the FEED `actions`/`prose` toggles are on — that's a filter, not a bug.) **S086-cockpit compose-bar (the new surface, "needs tweaks" per principal): the bar renders under the terminal + scales with Ctrl+wheel; wheel up to read history then type in the bar → terminal STAYS PUT; Enter sends + jumps to newest, Shift+Enter newlines; the regression that mattered — type `dssd` directly in the terminal, Esc, then send from the bar → arrives CLEAN (no `dssd` prefix); Esc in the bar clears the bar's own text; `/rename foo` via the bar relabels the row. Known limit by design: interactive TUI bits (slash autocomplete, @-picker, plan approval, AskUserQuestion menus) still need a click into the terminal.** **S085 (highest priority — a regression here means the token-gate broke the terminal): place a new session and confirm the terminal CONNECTS and claude comes up** (the /pty WS now requires `window.__CT`; if it shows `[session ended]`/403 immediately, the token plumbing is the suspect). Carried S084 checklist: selected row shows the gold ring/glow/name and tracks on click; `Ctrl+↑/↓` moves selection (wraps) + console follows; `Ctrl+1–9` jumps; `Ctrl+Shift+\`` opens the place modal (no stray backtick to the PTY). Carried S083: terminal prompt-at-bottom after a turn + after zoom/resize; Shift+Enter→\n; peek `.console` grainy-edge. If the prompt still clips, `window.__TERMDIAG=1` in DevTools gives the H1/H2 numbers.
5. **Leftover polish + documented-but-deferred finds.** S076 click-vscode-row→open-in-VSCode end-to-end; topbar density at 372px; the never-refreshed `claude_pid_chain` root fix (sidecar:1595); ptybridge UTF-8 boundary split; console.js dead drivable-composer branch.

## Files to read first

1. `respawn.md` (this file).
2. `quest-log/in-progress/S085_5f93bb32_cockpit-pty-auth-and-md-xss.md` — latest quest (cockpit security + the enforcement-gap fix; + S084/S083 for cockpit context).
3. `cockpit/_about.md` + `cockpit/backend.py` + `cockpit/web/` — the live surface.
4. `bank/decisions/D-028_*.md` — cockpit spec; `bank/decisions/D-027_*.md` — the inward/outward operability gap.
5. `bank/plan.md` §C — the parked pilot scope.
6. `bank/research/cockpit-neuron-overlay-design.md` — the S071 overlay spec.
7. `comms/active.md` (tail) — live siblings + hand-offs.
8. `bank/build-lessons.md` — cross-session build wisdom (read on demand).

## How to run the cockpit

Launch via the **cockpit / Switchboard** Desktop/Start-menu icon — it starts its own backend (`cockpit/Switchboard.vbs` → `app.py`; a pywebview window + aiohttp backend + PTY driver in one process). Hooks write state to `switchboard/state-*` files + `~/.claude/status/<sid8>.json`; the cockpit backend reads those. A relaunch is needed to pick up backend/JS changes (the running window holds stale code). `node --check` + `py_compile` are the cheap pre-commit gates for any web/hook edit.
