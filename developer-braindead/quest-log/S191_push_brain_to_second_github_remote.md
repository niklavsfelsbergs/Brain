# S191 — 2026-06-11 — Push the brain to a second GitHub remote (nfelsbergs/Brain)

- Niklavs asked to push the brain to a second repo, `https://github.com/nfelsbergs/Brain.git` (distinct from `origin` = `niklavsfelsbergs/Brain.git`). Ops/advice session — no brain-content edits.
- **Pre-flight safety:** confirmed no secret leak in tracked files — `gielinor/.mcp.json` is empty `{}`, the credential-bearing root `.mcp.json` + all live telemetry/state are gitignored. Flagged that the repo carries real TCG data → must be **Private**. Confirmed the brain was already on `origin` (local `main` was 194 commits ahead of stale `origin/main`).
- **The `404`-not-`401` diagnosis:** first push failed instantly with no login popup. Root cause: GitHub returns `404 "Repository not found"` (not `401`) for a private repo the caller can't see, to avoid leaking existence — and git only prompts for credentials on a `401`, so it silently gave up using the cached `niklavsfelsbergs` identity. Niklavs confirmed the repo exists in his browser → it was a wrong-account auth problem, not a missing repo. Fix: put the username in the URL (`https://nfelsbergs@github.com/...`) so GCM treated it as an uncached credential key and triggered a fresh browser login as `nfelsbergs`. Push succeeded (new `main` branch, all 194 commits).
- **Fan-out push wired (Niklavs's follow-up):** configured `origin` with two push URLs (`set-url --add --push` ×2) so a single `git push origin` updates both `niklavsfelsbergs` and `nfelsbergs`; restored `main`'s upstream to `origin/main` so a bare `git push` fans out. Verified: `git push origin main` pushed `75a1e59..5ba1a65` to niklavsfelsbergs and reported `Everything up-to-date` for nfelsbergs — both remotes now at `5ba1a65`.
- No brain-content writes; the pre-existing dirty working tree (sibling `e6e7b78d` retrieval residue + a dead Jebrim alching pass) was left untouched — not this session's to commit.

**Cascade.** `developer-braindead/comms/active.md` (OPEN + CLOSING), this quest-log entry, [[build-lessons]] (the 404-vs-401 + dual-push lesson), `respawn.md`. Git remote/push config changes live in `.git/config` (untracked).
**Main-brain changes.** none.
