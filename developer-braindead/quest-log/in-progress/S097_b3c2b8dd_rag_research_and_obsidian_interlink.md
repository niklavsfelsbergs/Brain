# S097 — 2026-05-26 — RAG research + parked plan, interlinked with the live Obsidian session

Dev-brain via "lets develop gielinor", mid-conversation from a brain-root session. OPEN posted (steps 6–8 of the respawn ritual run on every entry). Research + planning only — no code, no `gielinor/` writes, no hooks.

- **Researched RAG online** (WebSearch ×4) and wrote `bank/research/2026-05-26-rag-for-the-brain.md`: what RAG is (embed→retrieve→generate), the variants (naive / agentic / GraphRAG / agent-memory) and how each maps onto structures the brain already has, and the load-bearing finding — **the brain is already an agentic-search retrieval system** (grep + `@`-imports + `[[links]]`), the architecture Claude Code/Cursor/Devin converged on *after* dropping RAG. So RAG enters narrow + additive, never as a grep replacement.
- **Principal asked to plan it and park it** (he's mid-revamp to enable Obsidian). Added a phased "Plan (parked)" section to the note and a new **[[plan.md]] §N** pillar (forward-parked, behind the §C outward pilot): Phase 0 gate → Phase 1 GraphRAG-over-`[[links]]` (cheapest) → Phase 2 semantic-recall index (only if Phase 1 proves need) → Phase 3 MCP tool.
- **Principal then pushed: check the live Obsidian session and wire the *exact* interlink.** Found **braindead-b53fca39** speccing the Obsidian link migration (`bank/research/obsidian-fit-and-migration-spec.md`) — DECIDED **Option A full-stem `[[link]]` rewrite**, per-brain vaults, because stock Obsidian resolves links by exact filename so ~91% of `[[D-027_inward_outward_build_imbalance]]`/`[[SNNN]]` links are phantom today. Rewrote my handover: their migration **is** my Phase 0/1 input (GraphRAG reads the post-migration resolved-link/backlink index), Phase 1 **inherits their classification semantics** instead of rebuilding a parser, per-brain vaults bound traversal to one brain, and both arms trace to [[A-002]]/[[Q-001]]. Added a 7-point *Interlink* subsection + tightened §N.0/§N.1 to depend on b53fca39's forthcoming `D-NNN`.
- **Posted a `→ @braindead-b53fca39` comms flag** so the Obsidian session knows a downstream consumer depends on its migration, with one ask: name a readable resolved-link artifact and keep cross-brain links unresolved.
- **Learning harvested** (`bank/build-lessons.md` + cross-conv memory): when parked work depends on something the principal is *actively doing*, inspect the live session/artifacts and wire the concrete interlink — don't abstract a live, inspectable dependency into a vague gate. (The principal's "check what the obsidian session is doing" was the correction.)

**Cascade.** New `developer-braindead/bank/research/2026-05-26-rag-for-the-brain.md`; `bank/plan.md` (+§N); `comms/active.md` (OPEN + @-flag + CLOSING); `bank/build-lessons.md` (+1 lesson); `respawn.md` (prepended Last-updated); this quest entry. `active-mode.txt` left `dev-brain` (two live siblings — f9310a45/[[S095_f9310a45_cockpit-transcript-full-width-speech-bubble|S095]], b53fca39/Obsidian — so not despawning the workshop).

**Main-brain changes.** none.
