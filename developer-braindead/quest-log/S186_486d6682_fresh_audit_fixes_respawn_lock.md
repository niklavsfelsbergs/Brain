# S186 — 2026-06-10 — Fable-5 fresh audit + rulebook-rot fixes + respawn.md locked write path

First session on **Fable 5** (Niklavs switched the model, then: *"take a fresh look, dont rely on previous audits"*). Ran a fresh whole-system audit, then executed two of its fixes on his go, then (his *"lets do it"*) closed the file's last unlocked write path. A Jebrim alching pass ran live in parallel the whole session — player namespaces untouched, committed by pathspec.

- **Fresh audit (4 parallel Explore scouts + own spot-verification).** Verdict delivered in-chat: **structure A− / upkeep C+ / net B**. Strong: all six guarantees real + live-firing, the §Z read-path ([[S182_c206d2ba_domain_knowledge_layer_zB_zC|S182]]) the standout, @import flat ~12k, parallel-session coordination holding. Weak: *hand-enforced caps drift while hooks hold* (respawn.md regrew 49k→62k tok in 17 days; Jebrim in-progress regrew to 27 after [[S158_dda4c966_jebrim-active-state-alching-cleanup|S158]]'s cleanup; 16 unpicked research files), rule burden near saturation, 2 unaudited 2k-line observability hooks, telemetry unrotated, all adherence numbers old-model. **Scout-claim verification paid:** 1 of the rulebook scout's defects was false (intent-narration trigger EXISTS — `respawn.md:132`), 1 overstated (the require-open "divergent resolver" was an import-failure fallback only). Build-lesson logged.
- **Rulebook rot fixed (audit item 3, Niklavs go).** `lorebook/decisions/` → `lorebook/confirmed/` — **16 replacements across 14 live files** incl. [[gnome-write-boundary|gielinor/.claude/hooks/gnome-write-boundary.py]]'s phantom blocklist path, both gnome agent defs, `gielinor/CLAUDE.md` guarantee #4, 5 `_about.md`s, `ticks.md`; done **byte-level via Python** (the [[D-023_powershell-utf8-readall-not-getcontent|D-023]] mojibake lesson — that decision records a PowerShell pass at this exact replacement destroying every em-dash); historical records (gielinor lorebook `D-021_lorebook-folder-naming-correction` / `D-022_…-expanded-scope` / `D-023_powershell-utf8-readall-not-getcontent`, B-001/B-002) deliberately untouched. The **~100% OPEN-posting fiction** → measured truth ([[S177_988f0608_brain_health_check_procedure_and_first_rating|S177]]: ~72% first-try, `require-open` gate backstops to ~0 never-posted) in root `CLAUDE.md`, dev `CLAUDE.md`, [[respawn-ritual|developer-braindead/spellbook/respawn-ritual.md]]. **AGENTS.md ×3 resynced** via `tools/sync_agents_md.py`.
- **require-open-on-entry.py de-duplicated.** The stale local resolver (single-intent-dir read, silently diverged from the S184-hardened `_actor.py`) REMOVED; `from _actor import resolve_actor` now unconditional — an import failure fails LOUD (non-2 exit = visible, still fail-open, can't brick). Verified: `py_compile`, `test_actor_resolution` 5/5, real-payload live-fire through the hook binary (own sid, gated write, exit 0 via the full resolver→comms chain).
- **respawn.md size cap mechanized, then the write path locked (audit item 1 + the parallel-sessions question).** First built a rotation tool and ran it live (**140KB → 28KB**, 42 `<details>` blocks evicted verbatim to NEW `quest-log/archive/respawn-history.md`); then, on Niklavs's *"how does respawn even work when multiple sessions are running?"* → *"lets do it"*, superseded it with NEW [[respawn_update|tools/respawn_update.py]]: **one locked atomic write** ([[S128_b64229ad_comms-append-lock|S128]] lock primitive) = stdin prepend + `Last updated`→`Prior` relabel + older-Prior→`<details>` wrap + rotation (keep 2). Parser line-anchored (prose `<details>` mentions can't fool it; single-line blocks handled; unterminated → refuse, file untouched). NEW [[test_respawn_update|developer-braindead/verification/test_respawn_update.py]] **16/16** incl. a real two-process concurrent-prepend race — no lost update, loser demoted to Prior. [[session-close|developer-braindead/spellbook/session-close.md]] step 3 + the [[D-024_parallel_player_coordination|D-024]] parallel note rewired; the raw hand-prepend convention retired. **This session's own close is the tool's first real firing.**
- **First-firing catch — Windows stdin cp1252 mojibake (fixed in BOTH stdin tools + on-disk repair).** The tool's first real prepend landed em-dashes as `â€"`: Windows stdin defaults to the console codepage, piped UTF-8 mojibakes silently — the 16-case suite was green because every case piped ASCII. Sibling sweep found `comms_append.py` carried the identical bug since [[S127_b2e3bea8_jebrim-conversation-and-read-comms-tts|S127]] (two weeks of mojibake'd comms entries, misread as a display quirk). Fixed both (`sys.stdin.reconfigure(encoding="utf-8")`), repaired respawn.md + dev `comms/active.md` on disk (reversible cp1252→utf-8 roundtrip under the lock; 86 lines healed), added a UTF-8 stdin regression case → suite **17/17**. Two build-lessons logged. Residual for handoff: comms archives + gielinor comms still carry historical mojibake.
- **Consultation beats (in-chat deliverables):** top-10 features actually enabling Niklavs's Claude Code utilization; content-quality verdict on the layers (recent strata genuinely good — correction-recording notes, dense digests, real resume state; slop concentrated in never-re-audited week-1 files + unpicked research); honest retrieval odds (digested domains ~85-90%, undigested bank 30-50%, unpicked research ~0 — and all measured rates are old-model). → the ★ NEXT handoff: re-baseline on Fable 5.

**Cascade.** NEW `tools/respawn_update.py` (supersedes same-session `respawn_rotate.py`, removed before ever tracked), NEW `verification/test_respawn_update.py`, NEW `quest-log/archive/respawn-history.md` (42 rotated blocks); `respawn.md` (rotated + header/parallel-note rewired + stale-tail trim at close), `spellbook/session-close.md` step 3, `spellbook/respawn-ritual.md` OPEN-claim true-up, `CLAUDE.md` OPEN-claim true-up, `AGENTS.md` regenerated, `bank/build-lessons.md` +2 (scout-verification; Windows-stdin-cp1252), `tools/comms_append.py` stdin-UTF8 fix + dev `comms/active.md` mojibake repair (86 lines), this quest entry, comms OPEN→2×UPDATE→CLOSING.

**Main-brain changes.** `gielinor/CLAUDE.md` (guarantee #4 lorebook path), `gielinor/AGENTS.md` (regenerated), `gielinor/.claude/hooks/require-open-on-entry.py` (resolver dedup), `gielinor/.claude/hooks/gnome-write-boundary.py` (phantom path), `gielinor/.claude/agents/gnome.md` + brain-root `.claude/agents/gnome.md`, `gielinor/spellbook/skills/spawning-gnomes.md`, `gielinor/spellbook/rituals/drafts-triage.md`, `gielinor/spellbook/_about.md`, `gielinor/keepsake/_about.md`, `gielinor/players/_about.md`, `gielinor/ticks.md`, `gielinor/deities/guthix/{_about,proposals/_about,keepsake/_about,bank/_about}.md`, root `CLAUDE.md` + `AGENTS.md`. No `confirmed/` writes, no deletes of tracked files, no player-namespace writes.
