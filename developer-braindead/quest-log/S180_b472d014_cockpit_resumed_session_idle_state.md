# [[S180_b472d014_cockpit_resumed_session_idle_state|S180]] — 2026-06-09 — Cockpit: a resumed session comes back idle, not active

Reopened post-[[S178_b472d014_cockpit_wrappedup_interrupt_transcript_pilljump|S178]]-close (same sid b472d014); fresh OPEN→UPDATE→CLOSING via `comms_append`; no live Braindead siblings (heavy parallel Jebrim WIP + the `d7285bce` §Z.A session ran alongside — committed by pathspec, nothing swept). **Numbered [[S180_b472d014_cockpit_resumed_session_idle_state|S180]]** (asked to close as [[S179_d7285bce_domain_knowledge_layer_zA_scm_pilot|S179]], but `d7285bce` had already claimed [[S179_d7285bce_domain_knowledge_layer_zA_scm_pilot|S179]] — parallel SNNN race, renumbered to next free per convention).

- **The report (Niklavs, screenshot):** after a cockpit restart, sessions that were **idle** at close popped up showing **YOUR MOVE / active** with fresh ages. First framed as "why did ended sessions resurrect" — but the clarification *"it was idle when I closed"* redirected it: the bug is the resumed **state**, not resurrection.
- **Diagnosis pivot.** My first proposal (prune the owned-set the instant a session ends, via a `ptybridge` EOF signal) solved the *wrong* problem — these were idle, legitimately resumed. **Did NOT touch `ptybridge.py`.** Real cause: [[S164_b472d014_cockpit_chat_numbering_history_restore_and_ux_fixes|S164]]'s resumed-idle gate was `!fs` (no feed event yet), but `claude --resume` fires SessionStart-era startup events → the feed looks active → the gate opened → a parked session showed YOUR MOVE.
- **Fix** ([[term|cockpit/web/term.js]] + [[main|cockpit/web/main.js]]): a `userActive` flag on `TermConn`, set only on a **real** terminal keystroke (`_handleData`) or composer send (`submitComposed`), exposed via `liveTerms`. The merge's resumed-idle gate is now `isResume && !userActive` and **forces `state="idle"`**, so resume-startup feed noise can't un-idle a parked row — it returns idle (dimmed, sunk to rank 7, `idle` sub) until the user actually drives it, then normal state logic resumes.
- **Verified sufficient, not partial.** `SessionStart` writes **no manifest record** (only the first user prompt does — confirmed in `status-sidecar.py`), so a resumed-untouched session stays a cockpit-own row this fix fully governs — no manifest-row path bypasses it. `node --check` clean on both. Live render = Niklavs' relaunch eyeball. Left as an offered follow-up: idle rows still show the "YOUR MOVE" chip (dimmed) per the [[S139_7311cd20_board-status-taxonomy-and-crew-state-fix|S139]] convention — a board-wide "IDLE" chip is a separate taxonomy tweak.

**Cascade.** Edits to [[term|cockpit/web/term.js]], [[main|cockpit/web/main.js]]; dev-brain [[S180_b472d014_cockpit_resumed_session_idle_state|this quest-log]], `respawn.md`, `bank/build-lessons.md` (+1), `comms/active.md`.

**Main-brain changes.** none — `cockpit/` is brain-root tooling, not `gielinor/`; no `gielinor/` content/identity/rulebook touched; `@import` chain untouched.
