# [[S160_fb951652_cockpit-clickable-paths-and-sleep-wake-resume|S160]] — 2026-06-04 — Cockpit clickable paths + sleep/wake terminal resume

Dev-brain via "lets develop gielinor"; OPEN→UPDATE→CLOSING via `comms_append`; no live Braindead siblings ([[S159_5010cd4b_commit-policy-and-cue-registry-enrichment|S159]] `5010cd4b` closed 13:25). Wrote the `braindead-fb951652.txt` intent bubble as the first action. Two principal-raised cockpit UX fixes, both **confirmed working by Niklavs** before close ("works"). Both are stopgaps on the *current* cockpit, which is slated for a from-scratch rebuild ([[S142_a31a2bc0_brain_health_audit_and_gitignore_hygiene|S142]]) — flagged that tension to Niklavs; the backend endpoint carries into the rebuild, the front-end linkify may not.

- **Click-to-open file paths.** A path token in the cockpit transcript or terminal now opens its file (default app — HTML→browser) on click, or reveals it in Explorer (folder open, file selected) on Alt/Ctrl-click — "so I don't navigate to the folder manually." Built as a near-clone of two things already present: [[backend|cockpit/backend.py]]'s `api_open_vscode` (the server-side `os.startfile` click→host-action bridge) + `api_file` (the `resolve()`+`relative_to(BRAIN_ROOT)` repo-scoped path-safety). NEW `/api/open-path?path=…[&reveal=1]` endpoint reuses both patterns verbatim (strips a trailing `:line[:col]`; file→`os.startfile`, reveal→`explorer /select,`, dir→open). Because it's server-side it works from the pywebview window *and* a remote phone alike (opens on the host, where the files live).
- **The linkify front-end.** NEW `cockpit/web/links.js` — one shared module: a conservative path matcher (requires a folder segment **and** a dotted extension, which kills `2/3/5`/`06/04` false-positives), `linkifyPaths()` (run on the assembled HTML by [[md|cockpit/web/md.js]], skipping text inside existing tags/anchors so it never double-links a markdown link), a built-in xterm `registerLinkProvider` for the terminal grid ([[term|cockpit/web/term.js]], no new vendor file), and a once-installed document-level click delegate. `.pathlink` style added to `cockpit/web/styles.css` (gold dotted underline).
- **Sleep/wake terminal resume.** On laptop sleep the PTY WebSockets drop and [[ptybridge|cockpit/ptybridge.py]]'s WS-close `finally` runs `proc.terminate(force=True)` — killing each claude — so terminals come back dead ("can't even type in it", Niklavs confirmed the *window* survives), and the resume-on-open effect only runs at page LOAD, never on wake. Fix (Niklavs picked **reload-on-wake** over in-place reconnect): a `useEffect` wall-clock-gap detector in [[main|cockpit/web/main.js]] — JS timers freeze while suspended, so a >60s gap between 10s ticks ≈ a wake; on a detected wake with owned terminals it `location.reload()`s, re-running the proven resume-on-open path (`claude --resume` from disk). Guaranteed-correct (same code a manual restart uses), lossless for sessions; trade-offs accepted: unsent compose-bar text dropped, reloads even if a connection happened to survive. Because the trigger is the wake gap (not socket-deadness detection), it's robust to the half-open "can't type" case. Single self-contained effect — `ownedTermIds` was already imported.
- **Verification (testable layers — the physical sleep/wake can't be reproduced here).** `api_open_path` 9/9 (empty→400, traversal/abs-outside→403, missing→404, file→`startfile`, `:line` stripped, `reveal`→`explorer /select`, dir→open, abs-inside-repo resolves) with `os.startfile`/`subprocess` mocked. Real `linkifyPaths` 7/7 (catches `.js`/`.html`/`foo.md:12`, skips ratios/dates/`and/or`, won't double-link inside an existing `<a>`). Wake predicate 7/7 (normal/jittery no-fire, sleep fires, no-owned no-fire, strict `>60s` boundary, no post-reload loop). `py_compile` + `node --check` clean on all touched files. **RUNTIME-UNVERIFIED till relaunch then since CONFIRMED by Niklavs live: click-to-open navigation "works".** The xterm terminal-grid provider (link-provider column indexing) was the flagged higher-risk half.

**Cascade.** [[S160_fb951652_cockpit-clickable-paths-and-sleep-wake-resume|this quest]], [[respawn]] (prepended), [[plan|bank/plan.md]] (cockpit stopgap note), `comms/active.md` (OPEN/UPDATE/CLOSING). No dev-brain build-lessons harvested — the session ran clean, no correction/revert/misjudgment (Niklavs confirmed working).

**Main-brain changes.** None to `gielinor/` rulebook or identity. The changes are all under `cockpit/` (a tooling surface, not the brain proper): NEW `cockpit/web/links.js`; `cockpit/backend.py` (+`api_open_path` + route); `cockpit/web/md.js`, `cockpit/web/term.js`, `cockpit/web/main.js`, `cockpit/web/styles.css`. No `meta/`, no `confirmed/`, no hooks.
