# S006 — 2026-05-21 — Hand-off note precondition fix + spawning-dwarves skill

Session opened at brain root with no player address, user cued dev-brain (near-miss phrasing — `lets develop` not `Lets develop gielinor` — confirmed before switching). Two rule-text fixes landed plus a new spellbook skill.

- **Trigger / failure mode.** On the dev-brain switch I attempted to bulk-read Jebrim's in-progress `S002_2026-05-20_shipping-data-mart-v1-gap-analysis.md` to "write a hand-off note." Niklavs flagged twice: (a) the hand-off note doesn't need a full quest read, it's a one-line marker; (b) no player was active *this session* at all, so there was nothing to hand off. Root cause was wording — `brain/CLAUDE.md`'s "(if any)" and `gielinor/CLAUDE.md`'s "previous player" both read as "any prior in-progress file on disk" rather than "outgoing player in this session."

- **Change 1 — hand-off note precondition.** Three files tightened: `gielinor/spellbook/rituals/respawn.md` (mini-respawn step 1 now leads with an explicit **Precondition** — skipped when no outgoing player exists this session — and the step itself defines the note's shape: one-line marker with date + trigger + incoming mode, explicit "do not re-read or summarize the quest's existing content"); `gielinor/CLAUDE.md` (clarified "outgoing player" semantics inline); `brain/CLAUDE.md` (no-op clause for dev-brain entry when no player was active). The wording fix kills the conceptual error; the shape spec kills the bulk-read reflex.

- **Change 2 — spawning-dwarves skill (new).** New `gielinor/spellbook/skills/spawning-dwarves.md` codifies the dwarf workflow end-to-end: trigger heuristic (≥2 independent paths + at least one amplifier — scope size, wall-time pressure, context-bloat), pre-flight check (visible in the Plan line, principal can veto), briefing template, **background-by-default** channel (principal returns control to user immediately after spawn — does not block), status-on-ping behavior (tail since last check, not full sibling read), completion-weave (notification surfaces in next response, synthesis gated on all returns), dwarf quest-log streaming discipline (turn-by-turn lines so status checks have something real to surface), anti-patterns. Heuristic seeded from Jebrim's own [[S002_dev_brain_runescape_restructure|S002]] observation: *scope size × independent paths × wall-time pressure is dwarf-shaped, every time*.

- **Change 3 — dwarf-spawn annotation in communication protocol.** Added a subsection to `gielinor/meta/communication-protocol.md`: when the dwarf-spawn heuristic fires, the Plan line names the dwarves inline (manifest format) instead of describing the work serially. Piggybacks on the already-in-force Understanding/Plan preamble, no separate channel needed.

- **Deferred.** Hook-enforcement of dwarf quest-log streaming — same posture as [[S005_alching_preamble_protocol_brain_routers|S005]]'s per-turn quest-log discipline (start with discipline, harden only if drift shows up in real use). Two follow-ups also drafted-but-not-written: a `D-NNN` decision capturing the hand-off wording fix rationale, and an `I-NNN` examine draft on conflating disk state with session state. User cued close before these landed.

**Cascade.** Dev-brain files: this quest-log entry; `respawn.md` overwritten with new "where we are" + "next concrete step."

**Main-brain changes.** New: `gielinor/spellbook/skills/spawning-dwarves.md`. Edited in place: `brain/CLAUDE.md`, `gielinor/CLAUDE.md`, `gielinor/meta/communication-protocol.md`, `gielinor/spellbook/rituals/respawn.md`. Net main-brain delta: +1 file, 4 files edited.

**Discipline observation.** The session itself surfaced the failure mode it then fixed — and the user pushed back twice before I converged on the right framing. The first push ("why read the whole file for a marker?") got me partway; the second ("no player was active this session") got the rest. Pattern worth flagging at next bankstanding alongside [[S004_main_brain_corrections_post_s003]]'s "build the verification surface alongside the artifact" and [[S005_alching_preamble_protocol_brain_routers]]'s "structure-first, content earns its way in": **principal pushback is the primary signal that a wording-level rule is ambiguous**. Worth a candidate `examine/` identity entry on the main-brain side after one more occurrence.
