# S190 — 2026-06-11 — Lesson-store two-funnel bridge (cap + reconcile)

Dev-brain via "lets develop gielinor", pivoted from a plain-Claude outside review of the brain (same session, pre-pivot). `OPEN`→`CLOSING` via `comms_append`. No live Braindead siblings (`braindead-a0b39f49` intent was 06-09, CLOSED ×4). The `e6e7b78d` retrieval-part-2 residue + a dead Jebrim alching pass sat staged/modified in the shared tree — **left untouched, committed by pathspec only.**

- **Reframed the review's headline finding before building.** The review said "three overlapping lesson stores, collapse to one." Grounding (`[[2026-06-11-lesson-store-grounding|bank/research/2026-06-11-lesson-store-grounding.md]]`) corrected it: it's **two funnels** (the brain's gated/portable `examine/` + the harness always-loaded auto-memory `MEMORY.md`/`memory/*.md`) plus a designed `keepsake` pin tier — and **"collapse to one" was already rejected 2026-06-01 (B-015 / [[G-001_2026-06-01_examine-emptiness-and-store-drift|G-001]])**. Standing steer = keep both; the never-built piece is the *bridge*. Honored it.
- **Acute cap fix.** Compressed the harness `MEMORY.md` index to one-rule-per-line, 28.3 KB → 22.3 KB (under the ~24.4 KB load-truncation cap), all 104 entries preserved (index↔topic bijection verified). MEMORY.md is outside the repo — not in git.
- **Built the detector + test.** `[[lesson-store-check|developer-braindead/verification/lesson-store-check.py]]` + `test_lesson_store_check.py` (14/14): cap / long-line / integrity / examine↔MEMORY **drift** checks. Live: **28 dupes (7 exact, 21 likely), 23 unlinked** — produces the reconcile worklist automatically.
- **Codified the bridge in the rulebook.** `bankstanding.md` **step 8** (reconcile the two funnels — keep one canonical examine anchor + cross-link the warm MEMORY copy, never merge; retire on graduation/supersession only) + an `alching.md` non-scope pointer + a `layer-routing.md` row (auto-mirrored to `AGENTS.md` by `sync_agents_md.py`). Decision recorded: `[[D-035_lesson_store_two_funnel_bridge|D-035]]`.
- **Verified the loop end-to-end.** Reconciled one exact-unlinked pair (`reconcile_definition_before_numbers`): trimmed the warm copy to rule + cross-link, re-ran the detector → flipped **UNLINKED → linked**, unlinked 23 → 22. The remaining ~27 backfill incrementally at future bankstandings (B-015: backfill is optional; step 8 caps "a few per pass").

**Cascade.** `developer-braindead/`: `bank/decisions/D-035_lesson_store_two_funnel_bridge.md`, `verification/lesson-store-check.py` + `verification/test_lesson_store_check.py`, `bank/research/2026-06-11-lesson-store-grounding.md`, this quest-log, `respawn.md`, `comms/active.md`. Harness auto-memory (outside the repo, not committed): `MEMORY.md` recompressed + `memory/feedback_reconcile_definition_before_numbers.md` reconciled + new `memory/feedback_remind_what_we_build_next.md`.

**Main-brain changes.** `gielinor/spellbook/rituals/bankstanding.md` (new step 8), `gielinor/spellbook/rituals/alching.md` (pointer), `gielinor/meta/layer-routing.md` (row), `gielinor/AGENTS.md` + root `AGENTS.md` (hook-synced row). All ritual/routing additions — no `confirmed/` identity, no hooks, no deletes.
