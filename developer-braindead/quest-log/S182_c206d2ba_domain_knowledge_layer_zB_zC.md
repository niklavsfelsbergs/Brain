# S182 — 2026-06-09 — Domain-knowledge layer §Z.B + §Z.C (always-read index arm + per-player digest auto-discovery)

Dev-brain via "lets develop gielinor" (fresh sid c206d2ba), continuing §Z after [[S179_d7285bce_domain_knowledge_layer_zA_scm_pilot|S179]] stood up the layer + the SCM pilot digest. OPEN→CLOSING via `comms_append`; no live Braindead siblings ([[S179_d7285bce_domain_knowledge_layer_zA_scm_pilot|S179]] `d7285bce` CLOSED; the parallel [[S180_b472d014_cockpit_resumed_session_idle_state|S180]]/[[S181_b472d014_cockpit_transcript_full_width_response|S181]] are `b472d014` cockpit work, separate tree). Niklavs said "continue as you think is best" → built both Z.B and Z.C in one pass; the minimum-viable §Z build (Z.A+Z.B+Z.C) is now complete.

- **Z.B — the always-read map.** Extended [[keepsake-forced-read|gielinor/.claude/hooks/keepsake-forced-read.py]]: the active player's `bank/domains/_index.md` force-inlines on the first prompt **beside** the keepsake — same `UserPromptSubmit` emission, same once-per-session sentinel, budget-capped (`INDEX_BYTE_CAP=4000` → over-cap names instead of inlining). The index path is **derived** (`<player_root>/bank/domains/_index.md`, beside the keepsake) not a second map, so a new player's domains layer needs no hook edit; an actor with no index (zezima/guthix) reads `""` and skips cleanly. The "just knows the map" guarantee.
- **Z.C — the auto-discovered digest.** Extended [[domain-cue-reminder|gielinor/.claude/hooks/domain-cue-reminder.py]]: `_discover_digests(actor)` globs the active player's `bank/domains/*.md` (skips `_`-prefixed infra), parses frontmatter `patterns`/`domain`/`title` via a minimal stdlib `_parse_frontmatter`, and builds domain entries whose `inline_homes` is the digest file itself — reusing the §X.3 `_maybe_inline` machinery (once/session sentinel, `INLINE_BYTE_CAP`, sub-agent exclusion) to force-inline the matching *digest* on a topic cue. Discovered patterns are `re.escape`d (literal substrings, not regex like the registry). Global [[cue_registry|gielinor/.claude/hooks/cue_registry.py]] retained for external/specialist (shipping). **Durable win:** adding a domain is now an alching file-drop, not a hook build — EU-tender/carrier-contracts become catchable without code.
- **Verification.** [[test_keepsake_forced_read|developer-braindead/verification/test_keepsake_forced_read.py]] 14→**25/25** (real jebrim `_index.md` observed inlining in the same emission as the keepsake; zezima no-block; over-budget names-not-inlines); NEW [[test_domain_digest_discovery|developer-braindead/verification/test_domain_digest_discovery.py]] **23/23** (reads the live `scm.md` — real digest inlines on an "SCM" prompt; once/session; fresh-session re-inline; zezima/braindead skip; sub-agent name-only; coexists with static shipping). Existing `test_domain_cue_inline.py` 26/26 + `test_domain_cue_registry.py` 39/39 **unregressed**. 113 assertions green total.
- **Eval + weight.** [[knowledge-miss-regression-set|developer-braindead/verification/knowledge-miss-regression-set.md]]: NEW `[DGST]` mechanism + **case 11** (the [[S177_988f0608_brain_health_check_procedure_and_first_rating|S177]] "Hey Jebrim, let's work on the SCM" no-retrieval miss) added → **caught**; scorecard 5/2/3 → **6 caught / 2 partial / 3 miss (of 11)** — first move since §X.3. `brain-weight.py`: `@import` chain **flat at 12232 tok** — both arms are runtime hook injects (zero eager add); a counted-but-drifting ritual read converted to a guarantee, same §X/§Z thesis.
- **Process note (minor).** Implemented before posting the OPEN (the task was pre-scoped via the [[S179_d7285bce_domain_knowledge_layer_zA_scm_pilot|S179]] handover prompt + no live siblings, so no collision risk materialized) — but the OPEN-before-work discipline is the rule; posted it as soon as the build was underway. Recorded here so the discipline-leak ledger stays honest. No correction/reversal from Niklavs this session → no `build-lessons.md` harvest (routine build; the two test-bug FAILs in Z.C were my own harness bugs — list-membership-vs-substring + colliding sid8 prefixes — fixed in place, not lessons).

**Cascade.** Dev-brain: [[knowledge-miss-regression-set]] (case 11 + `[DGST]` mechanism + §Z re-score log), NEW [[test_domain_digest_discovery]], extended [[test_keepsake_forced_read]], [[plan|bank/plan.md]] (§Z status + Z.B/Z.C marked `[x]`), `respawn.md`, this quest-log entry, `comms/active.md` (OPEN+CLOSING).

**Main-brain changes.** Two gielinor hook edits (runtime mechanism, **not** `@import`/identity/`confirmed/`): [[keepsake-forced-read|gielinor/.claude/hooks/keepsake-forced-read.py]] (Z.B index arm) + [[domain-cue-reminder|gielinor/.claude/hooks/domain-cue-reminder.py]] (Z.C digest auto-discovery). The `@import` chain is untouched (flat 12232 tok); no `gielinor/` identity or rulebook *source* changed.
