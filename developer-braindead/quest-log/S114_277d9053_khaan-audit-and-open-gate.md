# S114 — 2026-05-28 — Khaan comparative audit + the first positive enforcement gate

Dev-brain via "lets develop gielinor" (mid-conversation); OPEN posted; no live Braindead siblings the whole session (572ea7f7 ~78min stale at entry, c7986694 CLOSED). sid 277d9053.

- **Cloned + audited `JustsCE/Khaan`** (another markdown-brain Claude-Code agent, ~1,900 files, brain-cycle #116) into `Documents/Khaan` (outside both brains). Read its doctrine, neuroscience memory model (cortex regions × L1–L4), the hook-enforced per-turn cognitive loop (recall→identity→decision), recall/inverted-index retrieval, the 10-phase learning-consolidation cycle, autonomy-engine, golden-file verification, and the dashboard digest. Produced two standalone HTML deliverables in `bank/research/`: `2026-05-28-khaan-comparative-audit.html` (convergence + what-each-learns-from-the-other) and `2026-05-28-khaan-learnings-implementation.html` (the full 20-item catalogue — 12 HITL + 8 autonomous — each with reason/benefit/how-in-gielinor/caveat, a priority matrix, and a recommended sequence). Verdict in one line: *borrow their engine, keep our brakes.*
- **Built HITL item 1 — the brain's first POSITIVE enforcement gate** ([[D-033_positive_enforcement_gate_open_on_entry]]). `gielinor/.claude/hooks/require-open-on-entry.py` blocks `Edit/Write/MultiEdit/NotebookEdit` of brain content in player + Braindead sessions until this sid8 has posted an OPEN to its `comms/active.md` — mechanizing the #1 historical discipline leak (OPEN-on-entry, ~70% skip pre-S110). Principal chose hard-block over warn-only. Escapes the OPEN/marker/intent/narration surface; skips sub-agents, guthix/wisp/unknown, non-brain paths; fails open on every error. Registered in both `settings.json` (S085 redundancy pattern).
- **Verified at the boundary (12/12 synthetic payloads) AND live.** The pre-build "mid-session settings don't hot-reload" assumption was **wrong** — they do. Probe-instrumented the hook, then through the real pipeline observed: allow path (braindead+OPEN → exit 0, write succeeded) and block path (status-actor flipped to jebrim to reproduce a no-OPEN player session → exit-2 banner, file never created). Probe stripped; hook byte-identical to committed.
- **Recorded + deferred.** The `meta/write-rules.md` "enforced by hook" line + a gielinor `lorebook/` entry are NOT landed — `meta/` is user-only and a dev `[[D-033]]` won't resolve in the gielinor vault ([[D-032_godly_proposal_flow_and_code_bearing_seam]] precedent); routed to a godly proposal at next bankstanding. Added `bank/plan.md` §P (Khaan learnings benchmark).
- **Next session hand-off:** implement catalogue items **2 (locked failure receipts) + 12 (engineering hygiene)** — hours of work, pure safety, zero behavioural risk; establishes "loud, consistent failure" as house style before anything else leans on it. Then item 5 (golden-file verification) so every later item ships with a check.

**Cascade.** New: [[D-033_positive_enforcement_gate_open_on_entry]], `quest-log/S114_277d9053_khaan-audit-and-open-gate.md`, `bank/research/2026-05-28-khaan-comparative-audit.html`, `bank/research/2026-05-28-khaan-learnings-implementation.html`. Updated: `respawn.md`, `bank/plan.md` (§P), `bank/build-lessons.md`, `comms/active.md`.

**Main-brain changes.** Yes (Braindead build into gielinor): new `gielinor/.claude/hooks/require-open-on-entry.py`; `gielinor/.claude/settings.json` + brain-root `.claude/settings.json` register it. No writes to any gielinor knowledge layer, `meta/`, or `confirmed/`.
