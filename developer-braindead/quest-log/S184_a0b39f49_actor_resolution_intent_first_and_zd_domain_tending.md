# S184 — 2026-06-09 — Actor-resolution intent-first fix + §Z.D domain-tending loop

Dev-brain via "lets develop gielinor", pivoted mid-conversation from a just-closed Jebrim **[[S181_b472d014_cockpit_transcript_full_width_response|S181]]** (same sid `a0b39f49`) that ran the player-side §Z chain. Niklavs greenlit the §Z follow-ups I flagged from that session; "take over here" → Braindead. No live Braindead siblings ([[S183_6c5170d1_session_id_sid8_self_reference|S183]] `6c5170d1` CLOSED; the parallel `fb97e6a4` brain-map session has an OPEN/no-CLOSING + stale intent → parked/abandoned, fully non-overlapping targets, not synthesizing its CLOSING).

- **Actor-resolution bug — fixed (the load-bearing one).** During the Jebrim phase the `require-open` gate blocked `bank/domains/` writes claiming `Actor: braindead` for a "Hey Jebrim" session. Instrumented (not guessed): `switchboard/state-instances.json` had `a0b39f49` registered under BOTH `jebrim`(3) and `braindead`(4), and `status-sidecar._actor_from_instances` breaks ties by **highest instance number** → braindead(4) won; with no intent bubble written yet, that mis-stamp reached the status file, and the shared resolver trusted status over the intent anchor. Fix ([[_actor|gielinor/.claude/hooks/_actor.py]]): `resolve_actor` is **intent-first**; `actor_from_intent` reads **both** intent dirs (brain-root + gielinor — the same CWD-scatter `status-sidecar.MODE_INTENT_DIRS` already handles for `.mode`) and **newest-by-mtime wins** (a mid-session pivot leaves two bubbles for one sid8 → freshest = current actor). Aligned the [[require-open-on-entry|gielinor/.claude/hooks/require-open-on-entry.py]] inline fallback. NEW [[test_actor_resolution|developer-braindead/verification/test_actor_resolution.py]] 5/5; binary-fired require-open (allow); all actor-keyed hook suites green. The one stale case in [[test_domain_cue_inline|developer-braindead/verification/test_domain_cue_inline.py]] true-upped (a "shipping mart" prompt now correctly force-inlines Jebrim's new `shipping-mart` digest via §Z.C discovery; ran case-4 as zezima to isolate the cue_registry path) → 26/26. + a [[respawn|gielinor/spellbook/rituals/respawn.md]] discipline note: **write the intent bubble before the first brain write** (the anchor that makes the heuristic a non-issue; the dev respawn already does this).
- **§Z dev-state truth-up.** [[plan|bank/plan.md]] §Z still listed the player chain as pending — recorded Z.clean/Z.bootstrap/Z.E/Z.deepen as LANDED (Jebrim [[S181_b472d014_cockpit_transcript_full_width_response|S181]]: 44 quests graduated, 4 digests, keepsake slimmed, eu_tender note de-staled).
- **Z.verify (focused).** The 4 new Jebrim digests (eu-tender / carrier-contracts / nfe-repo / shipping-mart) force-inline on their cues with correct content; `@import` chain flat at **12,232 tok** (§Z is zero eager add); in-progress 46→2; keepsake 685w→180w. Full Health-Index re-score deferred to its own clean pass (not faked).
- **§Z.D-mechanism BUILT** (the "defer until domains multiply" trigger met at 5 digests). NEW [[domain-coverage|developer-braindead/verification/domain-coverage.py]] — read-only detector: STALE digests (a `corpus[]` note's mtime > the digest's `synthesized:`), UNCOVERED note-clusters (candidate domains), NO-LAYER players (e.g. Zezima — flagged), MISSING corpus. [[test_domain_coverage|developer-braindead/verification/test_domain_coverage.py]] 8/8. + alching **step 2b** ([[alching|gielinor/spellbook/rituals/alching.md]]) — digest re-synthesis/tending (light gate, gnome-draftable, ~2 KB budget, cap 0–2/pass). **The domains layer now self-maintains** — the lifecycle (born/tended/surfaced) is closed.

**The §Z domains implementation is complete** (machinery + index + discovery + tending loop + Jebrim populated). Remainder is *content, not implementation*: Zezima's bootstrap (optional scaffold or her first-digest pass) + a full re-score. Niklavs confirmed others don't matter now — Jebrim deepens his.

Build-lesson harvested ([[build-lessons]]): when a gate misfires on identity, instrument the resolution source and fix it (intent-first) — don't satisfy the gate by acting as the wrong actor.

## Cascade.
Dev-brain: this quest-log entry; `bank/plan.md` §Z truth-up + Z.D `[x]`; `bank/build-lessons.md` +1; NEW `verification/domain-coverage.py` + `verification/test_domain_coverage.py` + `verification/test_actor_resolution.py`; `verification/test_domain_cue_inline.py` (case-4 true-up); `respawn.md` (this update); `comms/active.md` (OPEN/UPDATE/CLOSING).

## Main-brain changes.
Four `gielinor/` edits, none in the `@import` chain (hooks + JIT rituals — eager weight unchanged): `gielinor/.claude/hooks/_actor.py` (intent-first resolution + both-dir/mtime intent read), `gielinor/.claude/hooks/require-open-on-entry.py` (inline fallback aligned), `gielinor/spellbook/rituals/alching.md` (new step 2b), `gielinor/spellbook/rituals/respawn.md` (intent-bubble-first discipline note). No `confirmed/`/identity/`meta/` writes.
