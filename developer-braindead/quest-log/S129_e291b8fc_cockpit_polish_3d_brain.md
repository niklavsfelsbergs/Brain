# S129 — 2026-05-29 — Cockpit polish: transcript width, gold scrollbars, header family, 3D brain map

Dev-brain via "lets develop gielinor", mid-conversation; OPEN posted (`braindead-e291b8fc`). Live sibling `braindead-03861733` (Khaan item 11 ritual analytics) on hooks/switchboard the whole session — **zero overlap** with `cockpit/web`. A run of principal-driven, eyeball-iterated cockpit polish — all in `cockpit/web/{styles.css,brain.js}`, all RUNTIME-UNVERIFIED (a relaunch is needed; the running window holds stale code).

- **Transcript bubble width.** Principal drew a target wider than the old `78ch` reading-measure cap on `.asst-bubble` + `.t-user .bubble`. First widened to `max-width:100%` → principal corrected ("not the whole window, like 3/4ths") → settled on **`max-width:75%`** for both bubbles. The first-pass over-shoot is the captured lesson (see `bank/build-lessons.md`).
- **Gold scrollbars.** No scrollbar styling existed (default gray). Added a global themed block to `styles.css` — `--gold-dk` thumb → full `--gold` on hover, **transparent track + transparent inset thumb border** (`background-clip:padding-box`) so the bar adapts to every surface (board / panels / the transcript's own `#1c1813`) without a per-surface track colour. `::-webkit-scrollbar` (Chromium/WebView2) + `scrollbar-color`/`-width` fallback.
- **Header family + BRAIN titlebar.** The BRAIN panel had only a JS floating label, outside the shared header treatment. Folded a new **`.brain-head`** into the `topbar`/`console-head`/`feed-head` family (grain+gradient bg, gold-dk underline, corner rivets, gold title, 50px) and lifted the whole family's bevel (brighter top sheen + a dark line seating the gold underline + a soft drop-shadow). Restyled `brain.js`'s `re-settle` button as an OSRS bevelled stud to match `release`/`new`/the bell. The old leading status dot was dropped (rivets carry the gold accent now).
- **3D brain map.** Principal: make the graph 3D + rotatable "again" with a very slow auto-spin, else unchanged. **Grounding:** `brain.js` has only ever been the 2D canvas force-graph (single commit [[S126_c0dd1afb_cockpit-brain-graph-bubble-map]]) — no prior 3D version in git, so converted **in-place**, not a revive. Added a `z` axis (3D repulsion/spring/gravity sim + Fibonacci-ball seed), a **yaw/pitch orbit camera** (drag = rotate, wheel = zoom, pan removed since the cloud stays centred), **weak-perspective projection** with depth-based size/alpha + back-to-front draw order, and a constant **`AUTO_SPIN ≈ 0.001 rad/frame`** (~105s/revolution). Kept regional colours, hover-focus, live pulses, labels, the new header. `node --check` clean; no brain test file references the changed internals.

**Verification debt (relaunch checklist).** Board bumps to a fresh `b91.x` on load. Confirm: (1) transcript bubbles ~3/4 panel width, still opposite-aligned; (2) gold scrollbars on all panels; (3) BRAIN bar matches feed/cockpit headers (rivets, gold underline, stud button); all four headers read more bevelled; (4) brain graph is a slowly self-rotating 3D sphere, drag orbits, wheel zooms, hover still focuses. Tunables if off: `AUTO_SPIN` (spin speed), `FOCAL = R*2.6` (perspective strength), the `75%` bubble cap.

Cascade. `developer-braindead/`: this quest-log entry, `respawn.md` (prepend), `comms/active.md` (OPEN + 3 UPDATEs + CLOSING), `bank/build-lessons.md` (the over-shoot lesson). `cockpit/web/styles.css` + `cockpit/web/brain.js` (the polish itself — cockpit infra, neither brain's knowledge layer).

Main-brain changes. none — no `gielinor/` files touched.
