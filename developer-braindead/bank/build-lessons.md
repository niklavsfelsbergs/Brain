# build-lessons.md — carried build wisdom (relocated from respawn.md)

> Relocated from `respawn.md`'s tail on 2026-05-24 (token-usage audit) so it stops
> costing ~50k tokens of session-start load on every dev respawn. This is reference
> material — read on demand, **not** loaded at respawn (per `spellbook/respawn-ritual.md`
> step 4: "Don't pre-load `bank/`").
>
> Cross-session lessons distilled at each session close. An individual lesson can
> graduate to `examine/I-NNN_*.md` (the dev self-model) during a bankstanding/reflection
> pass — that's the canonical home for a durable insight. Until then they live here as a
> digest. The "Open at start of next session" carry-list below is a historical snapshot
> ([[S072_dbd41cc0_cockpit-fluid-ui|S072]]); the live next-steps moved to respawn.md.
>
> Everything below this line is verbatim from respawn.md's tail as of [[S072_dbd41cc0_cockpit-fluid-ui|S072]].

---

## Open at the start of next session

- **[[S040_ideas_folder_at_brain_root|S040]] live observation** — first session that captures via `note this idea: …` is the real test. Watch (1) does the trigger fire mid-message or only at start? (2) does the active actor resolve correctly across mode transitions (player→Guthix, Braindead-from-gielinor, unscoped capture)? (3) does the listing surface group cleanly when there are entries from multiple actors? Tighten `brain/ideas/_about.md` if any misfire.
- **[[S039_switchboard_zombie_gc_and_instance_reclaim|S039]] live observation** — confirm the cascading resolver + write-time refresh hold up across a full day of parallel sessions; spot any session that lingers at `unknown` to learn whether the fallback chain has gaps. The visual ghost period (sprites at old instance numbers) should clear within 5 min of [[S039_switchboard_zombie_gc_and_instance_reclaim|S039]]'s compaction.
- **Lorebook draft pair (Windows substrate gotchas)** — [[S032_terminal_switchboard_phases_1_and_2|S032]] hook-fire-rate-as-budget + [[S037_terminal_switchboard_phase_3_click_to_focus|S037]] ASCII-PS for `.ps1` + [[S039_switchboard_zombie_gc_and_instance_reclaim|S039]] BOM-free encoding for JSON written from PS. Three lessons that compose into one draft. Carried for next bankstanding.
- **Lorebook draft (deferred-branches debt)** — [[S039_switchboard_zombie_gc_and_instance_reclaim|S039]]'s instance-monotonic comment + [[S029_parallel_braindead_and_comms_channel|S029]]'s [[D-017_parallel_player_instances|D-017]] deferred branches surface a pattern: comments noting "we chose not to do X" become user-visible bugs on their own timeline. Worth one draft.
- **Cross-window click-to-focus** — Step 1b. Wire the `vscode://` → `claude-focus://` fallback chain in the switchboard click handler, then live-confirm against a second VS Code window. Defer until the principal actually opens one.
- **[[D-020_terminal_switchboard|D-020]] doc update** — §"Hook wiring" + §"Phase build order" + §"Process tree walk" ([[S037_terminal_switchboard_phase_3_click_to_focus|S037]]) + §"In-window focus via extension" ([[S038_brain_underutilization_diagnosis|S038]]). Do this before the next [[D-020_terminal_switchboard|D-020]] surface lands.
- **Verify [[S031_visualizer_world_scale_layout_and_bubble_redesign|S031]] lane-based bubble layout** — Step 2. Still untested visually.
- **Live test penguins** — Step 3.
- **Parallel Braindead visual tuning** — Step 4 (function validated by [[S032_terminal_switchboard_phases_1_and_2|S032]]).
- **Cross-repo sidecar rollout** — Step 5.
- **Live test Guthix** — Step 7 (now covers both consultation [[S034_guthix_consultation_mode]] and bankstanding).
- **Subtask debounce** — Step 8.
- **Replay demos** — Step 9.
- **Drafts triage** — Step 10.
- **Audit follow-ups** — Step 11.
- **First live gnome spawn** — Step 12.
- **[[Q-008_visualizer_aliveness|Q-008]] pick** — Step 13.
- **Jebrim 58f8e88a recovery** — Step 14.
- ~~**`ensureActorExists` braindead-{sid8} suffix-strip miss** — small follow-up from [[S031_visualizer_world_scale_layout_and_bubble_redesign|S031]].~~ Obsolete — `ensureActorExists` removed with the map in [[S052_98d4ec5e_switchboard-rebuild|S052]].
- ~~**Dead code from [[S031_visualizer_world_scale_layout_and_bubble_redesign|S031]] cleanup** — `updateDayNight()` / `dayNightFill()` / `DAY_NIGHT_ANCHORS` / `currentHour()` in `experiments/visualizer/index.html`.~~ Obsolete — file moved + map code excised in [[S052_98d4ec5e_switchboard-rebuild|S052]].
- **Cleanup pass on `developer-braindead/experiments/visualizer/`** — post-S052 dead-weight inventory: `sprites/` (PNG sprite sheet + slices), source PNGs, `slice.py`, `slice_tileset.py`, `subtask_smoketest.py`, plus the sibling `experiments/vscode-claude-focus/` extension. First four are pure map-era artifacts; `subtask_smoketest.py` is orphan tooling; `vscode-claude-focus/` is a separate VS Code extension that may still be relevant (in-window click-to-focus from [[S038_brain_underutilization_diagnosis|S038]]). Decide per-item — archive sprites + slice scripts, retire `subtask_smoketest.py` to archive, promote or retire `vscode-claude-focus/`. Bankstanding territory; never delete.
- **Simplify `emit-event.py` path classifier.** `path-map.json` carried over to `switchboard/` but is now vestigial — only the hook's classifier reads it to humanize file paths into building names, and no map renders. Either drop the classifier (emit raw paths to chat.ndjson) or repurpose path-map as a chat-prose helper. Defer until the chat humanizer's first real-use pass surfaces what's needed.
- §C Pilot definition, §H.3 brain-zone taxonomy, §H.4 identity ↔ main-brain interaction — unchanged.

## Carried-over observations

From [[S098_b53fca39_obsidian_fit_and_dlink_migration]] (new): **Obsidian is a lens over a vault we already are — but stock Obsidian resolves `[[ ]]` by *exact filename*, so the `[[ID]]` convention ([[D-004_stable_ids]]) renders ~91% of links phantom until migrated to full-stem.** Adoption = per-brain vaults (a single combined vault manufactures cross-brain `D-NNN` collisions that per-vault scoping avoids). Spec: `bank/research/obsidian-fit-and-migration-spec.md`.

From [[S098_b53fca39_obsidian_fit_and_dlink_migration]] (new): **model the resolver, don't eyeball the GUI.** To answer "will Obsidian even resolve our links," I replicated its resolution rules (exact-filename / shortest-path / aliases-not-honored) in a script and counted against all ~1,130 links — authoritative + repeatable, no GUI. Instrument-don't-guess applied to a "will it work" question, not just a "why did it break" one.

From [[S098_b53fca39_obsidian_fit_and_dlink_migration]] (new): **name your modelling assumption — the topology *was* the answer.** First pass modelled one combined repo-root vault → "9% resolve, 91% broken" (alarming). Principal corrected: the brains are per-vault scoped → 71% clean. A 10× swing from one unstated assumption (the vault boundary). State the assumption before reporting the number.

From [[S098_b53fca39_obsidian_fit_and_dlink_migration]] (new): **don't mistake convention for debt.** The "ambiguous" links were almost all deliberate structure — sub-agent run-logs (`SNNN_dN/_pN/_gN`) and quest+resume pairs share the session ID *by design*. Renumbering them would have shattered the filing system. Verify what a "collision" actually *is* before proposing a cleanup.

From [[S098_b53fca39_obsidian_fit_and_dlink_migration]] (new): **evaluate a rule on whether it still pays rent, not its age.** I treated [[D-004_stable_ids]] ("short ID alone is canonical") as a blocker on reflex; principal pushed — early decision, does it actually stand in the way? It didn't (guarded a near-nonexistent, recoverable slug-rename). Right move: surface the contradiction (don't silently break a founding decision) *and then amend it* — the brain is built for decisions to evolve. (Also → cross-conversation memory `evaluate-rules-pay-rent`.)

From [[S098_b53fca39_obsidian_fit_and_dlink_migration]] (new): **a mechanical convention-rewrite corrupts the doc that *describes* the convention.** My full-stem script ran over the spec doc's own illustrative `[[D-027]]` examples (Option A/B went identical) — caught in diff review. The exclude-list for any syntax-rewrite must include the spec + `_about` + `entry-formats` + historical decision records. And: a link-TEXT rewrite ≠ a file rename — only the latter threatens the hook/cockpit filename parsers, so don't price them the same.

> **SNNN-collision note (2026-05-22).** Two dev-brain sessions named themselves `S038`: this file's brain-underutilization work and a parallel vscode-claude-focus session. The close-session SNNN-determination is racy across truly parallel sessions — whoever closes first grabs the lower number; whoever closes second sees the lower number is taken and bumps, but only if their close-session ran *after* the first commit landed. Mine and the parallel one collided. Both committed under "[[S038_brain_underutilization_diagnosis|S038]]" subjects already; renumbering after the fact is invasive. **Surfacing for bankstanding** — either ratify a sub-suffix convention (`S038a`, `S038b`) or extend close-session SNNN-determination to walk *recent commit history* in addition to `quest-log/`, since closed-quests get committed before they show in any `find` walk. Below, [[S038_brain_underutilization_diagnosis|S038]] entries are disambiguated by topic-suffix in the label.

From [[S038_brain_underutilization_diagnosis]] brain-underutilization (new): **frame ≠ root cause.** Jebrim's "promotion gate stalled" was correct as a symptom but missed the load-bearing bug — promoted knowledge wasn't loading because respawn read `current.md` instead of the folder. 12 confirmed entries had been sitting invisible. The symptom *felt* like tempo (promote step is heavy, doesn't happen); the root was structural (even when promotion ran, the load surface was wrong). **Discipline:** when a complaint reads as habit/tempo, look for a structural bug underneath before retraining habits. Generalizable to any "X is underused" diagnosis — check whether X is *reaching* the load surface at all before concluding X needs more invocations.

From [[S038_brain_underutilization_diagnosis]] brain-underutilization (new): **manual aggregation steps that nobody owns rot.** Identity layers were built assuming someone curates `current.md` from `confirmed/` entries. No ritual owned that step. Fix wasn't "add a ritual that aggregates" — it was "read the folder directly, make `current.md` optional." **Design pattern:** load surfaces should match write surfaces; manual reshape steps between them decay. When a discipline rule requires manual aggregation, the right fix is usually to remove the aggregation requirement, not to add a ritual that does it.

From [[S038_brain_underutilization_diagnosis]] brain-underutilization (new): **recon dwarves report stale snapshots.** D1 said 5+ ready drafts; on-disk reality was 3. D2 referenced an `S034_g2` quest that existed but was untracked (so it appeared in `??` not in the in-progress `ls`). The brain moves between sessions; dwarf recon ages between task start and report time. **Discipline:** ground-truth dwarf counts before acting on them — re-glob the actual file state. Pair with the [[S027_visualizer_audit|S027]] "recon dwarves should cross-check audit findings against intervening commits before bundling" carryover; same family of lesson.

From [[S038_brain_underutilization_diagnosis]] brain-underutilization (new): **structural fixes before habit retraining.** Phase 1 was three structural fixes (respawn reads folder; promote drafts; move stale-done quests). Phase 2 was four habit-shaping aids (`/drafts`, close-session step 4 scan, step 8 catch, Guthix heuristic). **Order:** fix the bug that lets the habit fail, then add the habit-shaping aid. Structural fixes are cheaper and don't drift; habits drift. Reverse order leaves the bug in place + adds heuristic that fires against a broken surface.

From [[S038_brain_underutilization_diagnosis]] brain-underutilization (new): **the brain's own underutilization was the test fixture for diagnosing the brain's own underutilization.** This session reached for Jebrim instead of Guthix for a system-scope question. The Guthix routing heuristic shipped in 2d is born of literally this conversation mis-routing itself. **Meta:** when designing a heuristic to surface an underused option, look for whether the heuristic's design conversation itself reached for that option. If it didn't, you have a proof case for shipping the heuristic.

From [[S038_brain_underutilization_diagnosis]] brain-underutilization (new): **cascade approvals when proposing many small writes.** Initial over-gating ("approve per-file") was corrected on principal cue ("thats too blocking"). The right shape for many small writes: ask once for the cascade ("go all"), reserve per-item approval for identity-shaping decisions. Aligns with the elicitation-with-default-surfaced skill we promoted this same session — the skill was load-bearing the moment it shipped.

From [[S038_vscode_claude_focus_extension]] vscode-claude-focus (new): **"works in the log" ≠ "user saw it work" — twice in two sessions.** [[S037_terminal_switchboard_phase_3_click_to_focus|S037]] closed on `SetForegroundWindow True` self-tests without checking the user's actual window topology. [[S038_brain_underutilization_diagnosis|S038]] vscode-claude-focus found click-to-focus was a no-op end-to-end because all sessions share one workspace HWND. The mechanism wasn't wrong; the deployment shape was untested. **Discipline:** a focus/navigation feature must be tested against the user's actual topology, not the design's assumed topology. Pair with the [[S037_terminal_switchboard_phase_3_click_to_focus|S037]] ASCII-PS lesson and the [[S032_terminal_switchboard_phases_1_and_2|S032]] hook-fire-rate lesson as a combined Windows-substrate lorebook draft.

From [[S038_vscode_claude_focus_extension]] (new): **OS-level URL schemes hit a ceiling at the window boundary.** Application-internal objects (VS Code terminal panes) have no HWND — `SetForegroundWindow` can't reach them. Protocol handlers (cross-application / cross-window) and extensions (in-application) compose cleanly: handler for the outer layer, extension for the inner. The two URL schemes (`claude-focus://` + `vscode://niksis8.claude-focus`) coexist in the codebase without conflict; chaining them on click is the obvious next move when the principal opens a second VS Code window.

From [[S038_vscode_claude_focus_extension]] (new): **`claude_pid_chain` is the right matching surface, not a single PID.** VS Code's `terminal.processId` exposes the shell PID (chain depth ~3 on this machine). Hook fires from deeper. Matching against the whole chain absorbs the depth asymmetry. The chain-capture-at-fire-time pattern from [[S037_terminal_switchboard_phase_3_click_to_focus|S037]] paid off again, for a different reason — generalize: *if you'll need to match a process tree later from a possibly-distant vantage point, capture the whole chain at write time.*

From [[S037_terminal_switchboard_phase_3_click_to_focus]] (new): **Windows PowerShell 5.1 reads `.ps1` files as cp1252 unless a UTF-8 BOM is present.** A single em-dash in a `Write-Log` string broke the parse — the three UTF-8 bytes got read as three cp1252 characters, swallowing the closing quote and cascading errors through the rest of the file. **Discipline: pure ASCII for `.ps1` scripts, or write with UTF-8 BOM.** Worth a lorebook draft paired with the hook-fire-rate-as-budget lesson (both Windows substrate gotchas).

From [[S037_terminal_switchboard_phase_3_click_to_focus]] (new): **walking the process tree at hook fire time is the right shape for "I need an ancestor PID later" on Windows.** `os.getppid()` from a hook subprocess points at a short-lived wrapper (two layers of `bash.exe` on this machine) that exits seconds after the hook returns. `Get-Process -Id <dead>` returns nothing; PIDs may get recycled. Capturing the full ancestor chain *while everyone is still alive* via `CreateToolhelp32Snapshot` sidesteps all of it. Same pattern fits other instrumentation that needs to retain process-tree facts across time.

From [[S037_terminal_switchboard_phase_3_click_to_focus]] (new): **"it works in the log" ≠ "the user saw it work."** First user feedback was "still just opens a terminal" — the focus mechanism was correct end-to-end, but with one VS Code window the visible effect was zero. Build the visible-effect setup *into the test plan*, not after.

From [[S037_terminal_switchboard_phase_3_click_to_focus]] (new): **the conhost flash is the protocol handler's tax, not a script bug.** Browser-triggered local actions via a URL scheme always flash a conhost briefly even with `-WindowStyle Hidden`. Plan for it explicitly; suppression would require switching the handler to `wscript.exe` or a compiled launcher.

From [[S034_guthix_consultation_mode]] (new): **a four-bullet refusal list is a signal that the actor's role is too narrow.** Guthix's pre-S034 "What he refuses" had four items; post-S034 it has one ("won't write into a player's house"). When an actor declines a category of work, ask whether the role definition is the cause before defending the boundary.

From [[S034_guthix_consultation_mode]] (new): **"consultation default, ritual on cue" is a reusable shape.** Same actor, two modes, write authority varies by mode. If a second deity ever shows up, the precedent now sits in the meta files. Don't generalize prematurely — but the shape is there to inherit.

From [[S034_guthix_consultation_mode]] (new): **the principal's mental model is the architecture, even when it's an addition.** The shift had to land in the doc surfaces the agent reads at session start (`CLAUDE.md`, `modes.md`, `guthix.md`), not internal discipline alone. A purely-discipline change would have evaporated by next session because the address-routing rules would still describe the old role.

From [[S034_guthix_consultation_mode]] (new): **wisp's role evolution is the same pattern as the [[S028_subtask_channel_and_guthix|S028]] split, one degree further.** [[S028_subtask_channel_and_guthix|S028]] took the system-curation half away from wisp; [[S034_guthix_consultation_mode|S034]] takes the general-question half away too. Pattern: when an actor does multiple unrelated jobs, expect to split each off in sequence, not at once. Final wisp = "blank session opener," nothing more.

From [[S032_terminal_switchboard_phases_1_and_2]] (new): **high-frequency hooks on Windows + VS Code's integrated terminal are a hazard, even when silent.** `status-sidecar.py` was added at 2N+3 fires per turn without a frequency budget check against `emit-event.py`'s ~N rate. The added rate didn't cause the specific terminal-corruption incident this session (disabling didn't fix it), but it widened the surface for timing-sensitive renderer bugs. Treat hook-fire rate as a constrained resource on this substrate. Worth a lorebook draft.

From [[S032_terminal_switchboard_phases_1_and_2]] (new): **decoupled hook scripts pay off in failure isolation.** Pausing one script's registrations didn't disturb the other; the visualizer kept running through the diagnosis. The "shared file, separate processes" pattern is strictly better than "one script does everything." Phase 3's window-focus helper, when it lands, should also be a separate script.

From [[S032_terminal_switchboard_phases_1_and_2]] (new): **status sidecar is a strictly stronger sibling-liveness signal than intent-file mtime.** At respawn I flagged ab5ad0df as a `ABANDONED` candidate (intent-file 9min stale); the sidecar showed them very-much-alive. The comms ritual's sibling-detection check ([[D-019_parallel_braindead_and_comms_channel]] §3) could swap to `status state ≠ ended AND last_event_ts <5min` once the sidecar stabilizes — strictly stronger.

From [[S032_terminal_switchboard_phases_1_and_2]] (new): **"cross-repo visibility" is a registration question, not a file-path question.** Putting status files at `~/.claude/status/` was correct, but only sessions whose hook *fires* can populate the dir, and the hook only fires for sessions whose settings.json includes it. The visibility scope is the *intersection* of "where the file lives" and "where the hook runs"; the latter binds.

From [[S032_terminal_switchboard_phases_1_and_2]] (new): **Guthix mode bridges naturally into dev-brain construction.** The principal opened with `Hey Guthix` outside of bankstanding, got a design, then pivoted into dev-brain to build it. The design memory carried via chat, not a bankstanding `proposals/` entry. Not a problem — just a transition pattern. Document if it recurs.

From [[S031_visualizer_world_scale_layout_and_bubble_redesign]] (still relevant): **browser cache can hide a fix and read as a bug not fixed.** Ask for hard refresh sooner when the user reports a fix didn't take.

From [[S031_visualizer_world_scale_layout_and_bubble_redesign]] (still relevant): **CSS source-order trumps specificity for same-class rules.** Edit the original rule when overriding, don't pile on.

From [[S031_visualizer_world_scale_layout_and_bubble_redesign]] (still relevant): **scaling a world is mostly mechanical, but the gotchas live in the inline literals.** Wrap dense decorations in a single `transform="scale(N)"` when their internal coords are dense.

From [[S031_visualizer_world_scale_layout_and_bubble_redesign]] (still relevant): **a separate SVG layer for connectors lets you stack-paint cleanly.** When element A should "live behind" B but is owned per-instance, put A in its own layer rendered before B.

From [[S031_visualizer_world_scale_layout_and_bubble_redesign]] (still relevant): **"design tradeoff" answers are sometimes the deliverable.** Three-option answers with knowable tradeoffs cost ~30 lines but bound the implementation once the option is picked.

From [[S030_penguins_subagent_and_research_folder]] (still relevant): **discipline rules pay off when consulted.** The `players/_about.md` "don't pre-create speculative players" rule routed work to the right shape (skill + sub-agent role, not new player).

From [[S030_penguins_subagent_and_research_folder]] (still relevant): **predictive comments in code pay off.** `emit-event.py` line 65's "generalize to a mapping if a third kind lands" became a checklist two sessions later when penguins landed.

From [[S030_penguins_subagent_and_research_folder]] (still relevant): **source vs picking — research/bank split.** Research as source material, bank notes as picked claims; picking-during-alching operationalizes the split.

From [[S030_penguins_subagent_and_research_folder]] (still relevant): **five-chunk bundle held together because of pre-flight survey.** For large bundles: read the cascade before writing.

From [[S029_parallel_braindead_and_comms_channel]] (still relevant): **[[D-017_parallel_player_instances|D-017]]'s deferred branches age fast.** D-NNN docs with "out of scope for first cut" sections accumulate latent work on a slower clock. Bankstanding could surface deferred-branches-over-30-days. **Met again in [[S032_terminal_switchboard_phases_1_and_2|S032]]** — [[D-020_terminal_switchboard|D-020]]'s Phase 3 is already a deferred branch; how long will it sit?

From [[S029_parallel_braindead_and_comms_channel]] (still relevant): **coordination is asymmetric across actor classes.** Each new instanced actor warrants a separate decision about coordination.

From [[S029_parallel_braindead_and_comms_channel]] (still relevant): **append-only files dodge concurrent-write design entirely.** The principle is wider than the comms channel.

From [[S029_parallel_braindead_and_comms_channel]] (still relevant): **"seems ambitious" reads as a green light, not a hedge.**

From [[S028_subtask_channel_and_guthix]] (still relevant): **subtask exposes architecture/intuition mismatches that quieter channels papered over.**

From [[S028_subtask_channel_and_guthix]] (still relevant): **silent suffix-strip bugs hide until the surface gets busy.**

From [[S028_subtask_channel_and_guthix]] (still relevant): **the "bare intent file" fallback was never load-bearing — only confusing.** Worth a lorebook draft on *"defaults that exist only for absent inputs accumulate hidden coupling — prefer a hard surface."*

From [[S027_visualizer_audit]] (still relevant): **recon dwarves should cross-check audit findings against intervening commits before bundling.**

From [[S027_visualizer_audit]] (still relevant): **a multi-fix audit pass converts the audit doc from a primer into a verification checklist.**

From [[S023_visualizer_ticker_and_cross_session_attribution]] (still relevant): **shared global state at brain root is hostile to parallel Claude sessions.** Cumulative incident pattern spans [[S014_visualizer_polish_and_aesthetics_pass|S014]], [[S022_visualizer_audit_fixes|S022]], [[S023_visualizer_ticker_and_cross_session_attribution|S023]], [[S027_visualizer_audit|S027]], [[S028_subtask_channel_and_guthix|S028]], [[S029_parallel_braindead_and_comms_channel|S029]], [[S032_terminal_switchboard_phases_1_and_2|S032]].

From [[S023_visualizer_ticker_and_cross_session_attribution]] (still relevant): **watching-it-run finds bugs the audit-and-validate phase missed.**

From [[S022_visualizer_audit_fixes]] (still relevant): **audit-then-validate finds different bugs than either alone.**

From [[S021_visualizer_audit]] (still relevant): **the audit-then-validate pattern works for accreted infrastructure.**

From [[S021_visualizer_audit]] (still relevant): **cross-file invariants need an explicit list when ≥3 surfaces enumerate the same set.**

From [[S020_gnomes_ratification_and_visualizer]] (still relevant): **architectural guarantees need a live failure test, not just code review.**

From [[S020_gnomes_ratification_and_visualizer]] (still relevant): **the claude-code-guide agent earned its spawn.**

From [[S019_gnomes_subagent_implementation]] (still relevant): **a new role's blocklist is easier to get right than its allow-list.**

From [[S019_gnomes_subagent_implementation]] (still relevant): **single source of truth for tunable numbers is worth one hop.**

From [[S018_jebrim_layer_utilization_audit]] (still relevant): **the quest log is the gravitational center; other layers starve without explicit routing.**

From [[S018_jebrim_layer_utilization_audit]] (still relevant): **bundle big structural decisions; resist piecemeal landing.**

From [[S017_d014_chat_panel_implementation]] (still relevant): **spec docs that prescribe DOM/structure without inventorying what already exists will ship suboptimal designs.**

From [[S017_d014_chat_panel_implementation]] (still relevant): **heuristics that walk back through `state.ndjson` need to remember the stream is cross-session.**

From [[S017_d014_chat_panel_implementation]] (still relevant): **emulating a specific UI's look means font and palette must change together.**

From [[S016_visualizer_chat_panel_design]]: **"players communicate" in dev-brain mode probably means the visualizer, not the chat preamble.**

From [[S015_dwarf_attribution_via_agent_id]]: **shipping behind "the docs say X" is risky in this codebase because the docs are right but the integration is novel.**

From [[S015_dwarf_attribution_via_agent_id]] (separate): **delete discipline isn't enforced for dev-brain infrastructure.**

From [[S014_visualizer_polish_and_aesthetics_pass]] (now twelve-incident pattern with [[S018_jebrim_layer_utilization_audit|S018]], [[S020_gnomes_ratification_and_visualizer|S020]], [[S022_visualizer_audit_fixes|S022]], [[S023_visualizer_ticker_and_cross_session_attribution|S023]], [[S027_visualizer_audit|S027]], [[S028_subtask_channel_and_guthix|S028]], [[S029_parallel_braindead_and_comms_channel|S029]], [[S032_terminal_switchboard_phases_1_and_2|S032]]): **the procedure was right; the procedure assumed a state that didn't exist.**

From [[S014_visualizer_polish_and_aesthetics_pass]]: **the renderer needs to be self-healing because the hook stream is a lossy substrate.**

From [[S014_visualizer_polish_and_aesthetics_pass]]: **tool renames upstream are silent regressions.**

From [[S013_close_session_harvest_pump]]: **uncommitted work occupies the ID space.**

From [[S010_visualizer_live_mode_v0]]: **the visual sameness between live and replay was an [[I-002_render_before_shipping_ui]] miss.**

From [[S009_visualizer_six_deltas_and_frame]]: **mental UI preview must include z-order and all collision targets.**

From [[S008_iso_visualizer_v0]] (codified as [[I-002_render_before_shipping_ui]]): **render UI in your head before shipping it.**

From [[S003_main_brain_phase_1_scaffold]]–[[S007_bankstanding_phase_0_alching]]: **structure-first, content earns its way in.** **Build the verification surface alongside the artifact, not after.**

From [[S083_d71c4ab3_cockpit-full-audit]]: **when N attempts at the same *parameter* fail identically, the bug is elsewhere — stop varying it, instrument.** Shift+Enter burned 6 sessions cycling the newline BYTE (`\n`, `\x1b\r`, `\\\r`, CSI-u, bracketed-paste) — all submitted. The byte was never wrong (`\n`/Ctrl+J is correct per the docs); xterm's `return false` wasn't stopping the hidden textarea from ALSO emitting a `\r`. `e.preventDefault()` fixed it on the first try after we got authoritative info + added a keystroke diagnostic. The (N+1)th move after repeated identical failures should be research + instrumentation, not guess N+1.

From [[S083_d71c4ab3_cockpit-full-audit]]: **no hook fires on a Claude Code Esc-interrupt** (Stop fires only on natural completion; no `UserPromptCancel` event — issues #45289/#9516). Any hook-based "busy" stamp sticks forever on a cancelled turn. For *interactive* state, use the signal you actually have rather than a timeout: the cockpit can't see a hook but it CAN see the Esc keystroke in its own PTY terminal → clear busy→idle instantly, with a timeout-decay (90s) only as the backstop for sessions it can't observe. A "correct" fix that feels slow (the first 90s-only attempt) is still wrong for the user.

From [[S083_d71c4ab3_cockpit-full-audit]] (the regression I shipped, then reverted): **a documented tradeoff is not a verified-acceptable one — and a heuristic that infers intent from absence-of-signal is the wrong tool when a ground-truth signal exists.** For "cancelled turn sticks at BUSY," the precise fix was the cockpit seeing the Esc keystroke (ground truth → instant clear). I *also* shipped a backend 90s timeout that decayed any heartbeat-silent busy→idle, writing off its false-trip in my own commit as "soft, self-correcting" — with zero evidence it was rare. It instantly false-tripped a live Jebrim session genuinely working (long think / long MCP query showed IDLE). Lesson: a self-named failure mode is a BLOCKER until evidenced rare; don't bolt a redundant guess onto a precise signal; be extra-conservative changing how *live* sessions are displayed (a false signal on real work erodes trust worse than the annoyance being fixed). The keystroke fix should have stood alone.

From [[S083_d71c4ab3_cockpit-full-audit]]: **autonomous work without the ability to verify = prove the testable layers, instrument the rest, don't churn load-bearing untestable code.** With no GUI eyeball, the wins were: a 32-assertion backend regression gate + a headless aiohttp route smoke (proved the data/server layers), a gated `window.__TERMDIAG` instrument for the visual unknowns, and *deliberately not touching* the just-stabilized S080 manifest/PID gate or the high-blast-radius ptybridge UTF-8 path (untestable blind). Hand off with a precise relaunch checklist + a build-tag breadcrumb (`b83.N`) so the principal can confirm fresh code loaded.

From [[S084_454e276c_cockpit-switchbar-selection-and-nav]]: **a UI indicator that *never* shows is a match-predicate bug, not a styling weakness — check the identity types in the compare before restyling.** The principal reported "nothing shows which session is open." The selected-row CSS existed but was faint, so the obvious read was "make it pop." Root cause was deeper: `board.js` matched `s.session_id === sel.id`, but `sel.id` is the **8-char sid8** for a PTY `TermConn` (`term.js` sets `this.id = f.sid8`; server sends `session_id[:8]`) while a peek's `.id` is the **full UUID** — so `.selected` could never apply to cockpit-driven sessions (the common case). The cockpit's sid8-vs-full-session-id duality is a recurring footgun (it also shaped the `/rename` + focus plumbing); when a value flows through both connection kinds, normalize to sid8 (`id.slice(0,8)` works for both) at the boundary. Fix the predicate first, *then* style.

From [[S085_5f93bb32_cockpit-pty-auth-and-md-xss]] (the receipt for [[S020_gnomes_ratification_and_visualizer]] + [[S015_dwarf_attribution_via_agent_id]]): **the six "architectural guarantees" were not enforced at brain root — proven by an `rm` that went through.** All six boundary hooks were wired ONLY in `gielinor/.claude/settings.json` via `${CLAUDE_PROJECT_DIR}`; a session opened at brain root (the *default* per CLAUDE.md, AND what the cockpit launches with `cwd=brain root`) loaded only `brain/.claude/settings.json`'s observability hooks. So the guarantees the docs call unbypassable were prompt discipline in the dominant operating mode. [[S020_gnomes_ratification_and_visualizer|S020]] said "architectural guarantees need a live failure test, not just code review"; [[S015_dwarf_attribution_via_agent_id|S015]] said "delete discipline isn't enforced for dev-brain infrastructure" — both were right and neither was acted on for ~65 sessions. **Discipline: enforcement lives at the intersection of (where the hook script sits) ∩ (which settings.json the session actually loads) ∩ (where the session launched) — verify a guard FIRES from the real launch dir, don't trust the wiring's intent. The cheapest test is to try the thing the guard forbids and watch it get blocked.** Fix: register the boundary hooks in the root settings with absolute paths so they fire repo-wide.

From [[S085_5f93bb32_cockpit-pty-auth-and-md-xss]]: **triage an external/automated review against the LIVE code before acting — findings go stale, mis-attribute, and mis-severitize.** Of Codex's two batches: #6 (sel.id vs session_id) was already fixed the same afternoon ([[S084_454e276c_cockpit-switchbar-selection-and-nav|S084]]) — the snapshot predated the fix; #3 was rated High but the blast radius is Medium (gielinor-session-only, niche path); #1 needed *empirical* proof (the `rm` test), not just reading the settings, to separate "looks unenforced" from "is unenforced." An external reviewer is a strong prompt for *where to look*, not a verdict to apply. Recalibrate each finding's severity against your own context, and prove the scary ones with a test rather than a code-read.

From [[S086_e668ec7e_brain-technical-docs]]: **for a "documentation" / deliverable ask, settle the output *form* up front — don't default to the source format silently.** Asked to "build full technical documentation," I produced a `docs/` tree of markdown (a fine default — renders on GitHub, links work), scoped audience/home/depth via a 3-choice, and shipped. The principal's next message was "where is the HTML version?" — they'd assumed a browsable rendered artifact, not raw `.md`. The markdown wasn't wrong, but the *form* was an unsurfaced assumption. Discipline: when the ask is a documentation/report/export deliverable, treat the rendered form (raw MD vs single-file HTML vs static site vs PDF) as a first-class scoping question alongside content — the same multiple-choice-with-recommendation I already used for audience/home/depth. Cheap to ask, and "where's the X version" is the tell that a format expectation went unasked.

From [[S087_23dcc534_cockpit-ctrl-c-copy]]: **"nothing's showing" — settle bug-vs-filter by inspecting the live data payload before touching render code.** The principal reported their live Jebrim session wasn't "popping up in the feed." The obvious read was a broken event pipeline. Instead I curled the running backend's `/api/feed` (port 8770) and found the session's events all present — 11 items, all `say`+`action`. A *working* session emits almost only those two kinds (213 of 300 in the live payload), and both are gated behind the FEED panel's `actions`/`prose` toggles (default on, but localStorage-persisted off) — so the symptom was a view filter, not a missing event. Discipline: when a surface "shows nothing," read what the server is actually serving (or what's in the source file/stream) *first* — one query separates "data absent" from "data present, hidden by a client filter/scroll/toggle" and tells you which half of the stack to fix. Application of the existing `instrument-don't-reguess` memory to a diagnostic (not a fix) question.

From [[S092_327b1216_cockpit_transcript_and_rename_persistence]] (clean-text transcript panel): **to render claude's output as clean content, the source is the transcript `.jsonl` (/history), never the terminal grid — and the structured render for it often already exists for a sibling case.** The principal wanted the cockpit to "not look like a terminal" so output could be *copied cleanly* (xterm is a fixed-width grid: long lines hard-wrap at the column, TUI chrome bakes into cells → selection copies the mangled form). Two wrong instincts dodged: (1) scrape the xterm ANSI and reformat — brittle, it's a repainting TUI; (2) drive headless for a chat UI — that's the metered path ([[S060_ac10ec71_switchboard-embedded-chat]]). The right source was the on-disk transcript `.jsonl`, which `backend.parse_transcript`/`/history` already turned into clean structured turns *for read-only peeks* — the driven PTV session simply wasn't wired to it. Build was ~1 new file + a toggle that keeps `<Term>` mounted (hidden) so the PTY/subscription never drops. **Discipline: for "show the agent's output as clean/structured content," reach for the transcript jsonl, not the rendered terminal; and before building a "new" view, check whether the structured render already exists for an adjacent code path (peek) and just isn't reachable from the case you need.** Reinforces [[S017_d014_chat_panel_implementation]] (inventory what exists before prescribing DOM) — here the reuse target was a sibling *code path*, not a doc.

From [[S092_327b1216_cockpit_transcript_and_rename_persistence]] (the "I can't select text" report): **a live/polling view must yield to an active user selection — re-rendering content out from under a drag-select reads as "selection doesn't work."** The transcript polled `/history` every 2s and called `setTurns` on change; the newest turn (the one you most want to copy) grows each poll, so any highlight there was wiped within ~2s. Before blaming CSS I checked the real candidates — `app.py`/styles.css for a global `user-select:none` (only `body.resizing` + scoped xterm), confirming no static block — which pointed at the re-render, not a style. Fix: skip the content swap while `window.getSelection()` has a non-collapsed range inside the panel (defensive `user-select:text` added too, for the chromeless WebView2 case). **Discipline: any auto-refreshing surface the user can select/interact with must pause the DOM swap while a selection is live inside it.** Application of `instrument-don't-reguess` to a UI bug: rule out the static cause (CSS/global block) by reading it before concluding it's the dynamic one (poll).

From [[S092_327b1216_cockpit_transcript_and_rename_persistence]] (lost rename on close/reopen): **when the identity key can rotate, EVERY store keyed by it needs a carry at the rotation point — and a code comment asserting "it won't rotate" is itself the latent bug.** Renames are keyed by `sid8` in two stores (localStorage via names.js for terminal `/rename`; `state-names.json` on disk for board/VSCode rename). `sid8` is NOT stable: `claude --resume <uuid>` (what a cockpit reopen runs) mints a NEW id — unlike `claude --session-id <uuid>`, which *pins* it. The existing `_carry_name_across_clear` covered only the in-process `/clear` case, only the disk store, and its own comment baked in the false assumption *"a resume reuses the old UUID (same sid8 → label never lost)"* — which is exactly what broke. Fix carried the label across the rotation in BOTH stores: localStorage in term.js's re-announce handler, disk in new `ptybridge._carry_disk_name`. **Discipline: identity keyed by a rotatable value needs carry-on-rotate across all stores, applied where the rotation is detected; and treat an unverified assumption written into a comment as a bug waiting to fire ([[S039_switchboard_zombie_gc_and_instance_reclaim]] "comments noting we-chose-not-to-do-X become bugs"; [[S084_454e276c_cockpit-switchbar-selection-and-nav]] the recurring sid8-vs-full-uuid footgun — same key-identity family).** Substrate fact worth keeping flat: **`--resume` does not pin the session id; `--session-id` does.** *(CORRECTED by [[S093_f3239bdc_transcript-readability-and-term-fit]]: a live trace showed `--resume` KEPT the id — no rotation on resume. This assumption was wrong; see the [[S093_f3239bdc_transcript-readability-and-term-fit|S093]] lesson below.)*

From [[S092_327b1216_cockpit_transcript_and_rename_persistence]] (the two-fix selection patch): **when you can't isolate which of two plausible causes is real (no GUI to instrument), apply both individually-correct fixes but SAY SO and ask which mattered — don't pass a 2-fix bundle off as one confident fix.** "Can't select" had two live candidates (poll-wipe; chromeless-webview user-select). Both fixes are correct on their own merits regardless of which was the actual cause, so I shipped both — but flagged to the principal that the poll-yield is the likely cause and the CSS is defensive, with a relaunch check to confirm. Nuance on [[S083_d71c4ab3_cockpit-full-audit]]'s autonomous-verify lesson: the honest move under eyeball-blindness isn't to guess one and hope, nor to silently bundle — it's parallel correct fixes + transparent "here's which I think mattered, confirm on relaunch."

From [[S086_86020a52_cockpit-terminal-compose-bar]] (cockpit compose-bar — the parallel [[S086_86020a52_cockpit-terminal-compose-bar|S086]], not the docs one): **a second input path onto a stateful surface must reconcile that surface's existing state, not just append to it.** The cockpit terminal compose-bar (a fixed `<textarea>` that pipes text+Enter to the PTY so you can scroll the terminal while writing) blasted its text straight into claude's in-terminal input box — but that box is a *shared* surface. The terminal is focused by default, so the principal had half-typed `dssd` directly into it; Esc cancelled the turn but left `dssd` in the box, and the next composed send prepended to it → a merged message. Fix reused the `_linebuf` mirror term.js already keeps for `/rename`: backspace the leftover away on Esc and before every composed send. Discipline: when you bolt a new input channel onto a surface that already accepts input (PTY prompt + composer; two cursors on one doc), the channels share hidden state — clear/reconcile the surface before writing, don't assume it's empty. Caught by the principal on first live use, which is also the standing reminder that interactive-input bugs surface only in real use — an eyeball-blind build must hand off a concrete "type X then Y" regression repro, not just a feature checklist.

From [[S093_f3239bdc_transcript-readability-and-term-fit]] (rename-on-restart — the SECOND attempt; instrument resolved it): **`claude --resume <uuid>` KEEPS the id, it does NOT rotate — correcting [[S092_327b1216_cockpit_transcript_and_rename_persistence]]'s premise.** [[S092_327b1216_cockpit_transcript_and_rename_persistence|S092]] "fixed" lost-rename-on-reopen on the belief that `--resume` mints a new id (so it carried the label prev→cur *on rotation*), and shipped it with the EYES-ONLY relaunch never done. It regressed — the principal reported names still dropping. Rather than guess patch #2, I instrumented the announce/rotation/carry path to a disk log (`switchboard/rename-diag.log`) and had the principal do one restart. The trace was decisive: both resumed sessions announced `resuming:true` with the SAME uuid and produced **zero `rotate` events**. So `--resume` pins the id; the name survives a restart via the stable sid8 + durable localStorage (the board reads `nameFor()` first, disk `state-names.json` as fallback). The earlier breakage was almost certainly the running cockpit window holding **stale code** (a relaunch loads the current `term.js`) and/or lost-name cases that had gone through a `/clear` rotation. I shipped a **resume-anchor carry** (copy the disk label from the resume-uuid's sid8 → the live id) as insurance for the `/clear`-then-restart path, then **stripped the always-on disk logger** once it had answered its question (kept a zero-cost gated `window.__RENAMEDIAG` client trace). **Discipline: (1) read the OVERRIDE flag to infer a default — `--fork-session` ("create a new session id *instead of reusing the original*") is the tell that plain `--resume` REUSES the id; (2) [[S083_d71c4ab3_cockpit-full-audit]]'s instrument-don't-reguess applies *doubly* after an unverified fix regresses — the prior fix's stated PREMISE is the first thing to instrument, not the last; (3) a fix shipped without its EYES-ONLY relaunch ever happening is a guess wearing a fix's clothes — [[S092_327b1216_cockpit_transcript_and_rename_persistence|S092]] marked "verify pending" and the pending verify is exactly what failed. When the checklist says EYES-ONLY, the work isn't done until eyes are on it.**

From [[S094_cockpit_noopen_stale_process_cleanup]] cockpit no-open (new): **diagnostic probes become confounds in the system under test — clean them up.** While diagnosing the cockpit hang as Jebrim I left two `backend.py` probes running, squatting `:8770` — the exact port `app.py`'s reuse-check keys on. They turned into part of the mess I was later debugging as Braindead. **Discipline:** when a debug step spawns a server/process (especially against a port-bound or single-instance app), kill it before moving on, or it pollutes the very thing you're investigating. Verified-fix-vs-isolated-mechanism honesty also applies: I proved the clean slate works, but did NOT separate profile-lock vs port-squat vs stacked-instances under a stack trace — say "fix verified, mechanism inferred," don't dress inference as diagnosis.

From [[S094_cockpit_noopen_stale_process_cleanup]] cockpit no-open (new): **a persistent-profile GUI app with no single-instance guard self-DoSes on retry.** The cockpit's `webview.start()` hung with no window and no `msedgewebview2` child — the [[S066_7f5db8c5_cockpit-sweep]] persistent `.webview` `storage_path` lock-contends when multiple instances stack, and the natural user response to a hang (relaunch via the icon) compounds it: each stuck instance keeps holding/waiting on the profile lock. Killing all `cockpit/.venv` pythons + freeing the port fixed it. **Follow-up (logged in respawn):** the cockpit needs a single-instance guard (if `:8770` is already a *cockpit*, focus the existing window, refuse a second) and/or stale-profile-lock recovery on launch — otherwise one transient hang escalates into a pile-up the user can't escape without Task Manager.

From [[S097_b3c2b8dd_rag_research_and_obsidian_interlink]]: **When parked/planned work depends on something the principal is actively doing (a "revamp", a parallel effort), inspect the live session + its artifacts and wire the concrete interlink — don't abstract a live, inspectable dependency into a vague gate.** Anchor: I parked the RAG plan as "gated on the Obsidian revamp" while braindead-b53fca39 was live in a sibling session speccing the exact link-migration my Phase 1 consumes; the principal had to say "check what the obsidian session is doing and enhance with exactly how it interlinks." Sibling-detection at respawn already surfaces these sessions for collision-avoidance — extend the habit to dependency-wiring: when a plan names a dependency a live session owns, read their intent/comms/artifacts and name the concrete coupling (inputs, inherited semantics, ordering).

From [[S095_f9310a45_cockpit-transcript-full-width-speech-bubble]] (the "textbox stuck" #2 bug, root-caused from a disk trace): **a persisted instrument doesn't just break the reguess loop — it can DISPROVE the obvious hypothesis a bug has been "fixed" against twice.** The recurring prompt-below-fold/"textbox stuck" had an obvious suspect — over-fit — and had been fixed *as* over-fit in [[S083_d71c4ab3_cockpit-full-audit]] and [[S093_f3239bdc_transcript-readability-and-term-fit]]. The honest move (per the `instrument-don't-reguess` memory) was to refuse a third over-fit tweak and instead leave a gated disk-logger (`term-fit-diag.log`) armed across relaunches. When it reproduced, the trace was decisive *against* the assumption: **every healthy sample had `overfit=-16px` (a row of SLACK, not over-fit)**; the only bad samples were `open … frameH=0` — the terminal was selected while the transcript view was up, so it mounted `display:none` and the multi-frame open-pin loop bailed on a 0-box; the single RO re-show pin then raced the layout. Fix targeted *that* (a `reshow()` multi-frame fit+pin on the view-flip), and I explicitly did NOT touch the over-fit guard — the trace proved it innocent. **Discipline: for a bug that keeps coming back after "fixes," the instrument's job is to test the PREMISE, not just locate the next tweak; be ready for it to exonerate the code you'd have changed and point somewhere you weren't looking (here: the hidden-mount path, not the fit math).** Also: keep such a logger ARMED through the verification relaunch (strip only once confirmed) — a one-shot trace you stripped too early can't catch an intermittent "popped up in one session" report.

From [[S095_f9310a45_cockpit-transcript-full-width-speech-bubble]] (the "I think we introduced a new issue — all sessions lost their name" report): **when blamed for a regression, instrument to CONFIRM-OR-REFUTE before either accepting it or dismissing it — the same investigation usually does both (clears the recent change AND finds the real cause).** The principal attributed the name-loss to my in-flight transcript/compose work. I neither defended nor assumed: grep-proved the rename code path (`names.js`, ptybridge announce/carry, the `term.js` label code) was untouched by my diff (so: not introduced here), AND read the re-enabled rename-diag trace, which showed `--resume` KEEPS the id (zero rotate) — so manual renames always survived — and thereby pinpointed the *actual* gap: the AUTO-label (a placed "Jebrim") wasn't persisted across resume (`resumeTerm` started blank, idle sessions have no hook attribution yet → "chat"). Fixed it with a uuid-keyed label store. **Discipline: a "you broke X" report is a pointer to investigate, not a verdict to accept or reflexively deny. Run the cheap check (diff/grep for "is my change even in this path" + the trace for "what actually happens") — it simultaneously exonerates the wrong suspect and surfaces the right bug, and "not my code" is only HALF the answer when the thing is genuinely broken.**

From [[S103_5a457944_slack_mcp_check_and_niklavs_voice_profile]]: **When profiling someone's voice to shape a persona, sample across audiences before concluding — register is audience-conditioned.** Niklavs' Slack split into ~4 distinct registers (formal-public / terse-ops / analytical-at-full-extension / casual bilingual); a single-context sample would have flattened them into one. The formal register also contradicted a stored "no greetings" memory that was only ever true for his casual/ops voice. One sample ≠ the voice; characterize per-audience, then let the principal pick which register a player adopts.

From [[S107_f4aadfed_comms-rotation-and-read-discipline]]: **Key a size/maintenance trigger on the cost that actually bites, not a convenient proxy — and know which end of an append-only file a bare `Read` returns.** The comms-channel rotation trigger was "~500 entries," but the dev `comms/active.md` was being read every respawn at **976 lines / ~100k tokens with only ~179 entries** — the proxy (entry count) never tracked the real cost (tokens loaded), so rotation never fired and the bloat went in cold every session. Re-keyed it to ~300 lines / ~30k tokens. Second trap, surfaced while just *answering the question*: an append-only log has the newest entries at the *bottom*, but a bare `Read` returns the *top* (oldest) and truncates at one page — so "read the comms tail at respawn" silently returned the least-relevant slice unless you seek to EOF (large `offset` / `grep '^\[' | tail`). **Discipline: when a threshold rule exists to cap a cost, key it on that cost directly (tokens, not entry count); and when a file is append-only, the read path must explicitly seek the tail — the default top-of-file read gives you the wrong end.**

From [[S110_144c0ca2_brain_full_audit]] (enforcement live-test — the verification [[S106_cf03bfe1_self-eating-audit-and-d8-enforcement-fix|S106]] deferred): **the agent_type write-boundary hooks gate only TYPED sub-agent spawns; a plain `general-purpose` sub-agent doing the same work is ungated.** Spawning gnome/penguin/dwarf from a fresh brain-root session, each role's boundary hook fired with a verbatim block (gnome→`meta/`, penguin→`bank/`, dwarf→`examine/drafts/`), confirmed/ blocked, positive controls succeeded — so #3/#4/#5 are real, not just wired. But the gate is `payload.agent_type ∈ {gnome,penguin,dwarf}`, stamped only when the principal spawns with `subagent_type=` that role; the realistic `general-purpose` helper carries `agent_type:general-purpose` → matches nothing → only the path-based `block-confirmed-writes`/`block-deletes` still apply. And #6 (no-sub-spawn) is enforced one layer lower than documented: Task/Agent isn't in any typed agent's toolset, so `block-sub-spawn.py` is an unreachable backstop. **Discipline: "the hook fires" ≠ "the boundary is ambient" — name WHICH invocations it protects (typed spawns only), and don't assume an un-exercised hook is the real gate (here the tool-grant was). Extends the "verify enforcement fires" lesson: verify the coverage, not just one firing.**

From [[S110_144c0ca2_brain_full_audit]] (quest-log drain): **an undocumented graduation step doesn't happen — 47 closed dev sessions piled up in `quest-log/in-progress/` because nothing told sessions to move them out.** `session-close.md` step 2 writes the entry to `quest-log/{filename}` (flat root) and `quest-log/_about.md` describes a flat-root model — but neither names the `in-progress/` staging folder that live sessions actually write to (crash-recovery), nor the in-progress→root move at close. The move was nobody's job and never ran; [[S106_cf03bfe1_self-eating-audit-and-d8-enforcement-fix|S106]]/[[S107_f4aadfed_comms-rotation-and-read-discipline|S107]] landing flat at root were the accidental exceptions. Drained 47→root (kept the one live session); jebrim's 6 stale → `completed/`. **Discipline: if a folder accumulates, first check whether the spec names the drain step at all — a staging area with no documented graduation is a leak by construction, not neglect. Fix the doc (name the move) or it recurs.** (Doc fix proposed, not yet applied.)

From [[S111_a268b008_shipping-agent-subagent-type|S111]]: **A new sub-agent `agent_type` inherits ONLY the global guards (block-confirmed-writes, block-deletes) — NOT the dwarf/gnome/penguin fine-grained boundaries (each hook gates on its exact `agent_type` and exits 0 otherwise). Adding a typed sub-agent = agent def + its own `*-write-boundary.py` (registered in BOTH settings.json, [[S085_5f93bb32_cockpit-pty-auth-and-md-xss|S085]] redundancy) + producer/consumer wiring in 4 spots (emit-event ROLE_CONFIG, status-sidecar SUBAGENT_STATE_PATHS, backend ROLE_FILES, board.js+styles.css chip). The emit-event "adding a new kind" comment is the canonical checklist. Synthetic-payload tests prove the hook logic; the LIVE agent_type only populates from a FRESH session (config load — the [[S106_cf03bfe1_self-eating-audit-and-d8-enforcement-fix|S106]] fact), so the spawn path stays unverified until then.**

From [[S112_c7986694_cockpit-terminal-scroll-up-lock]]: **the recurring cockpit "scroll lock / prompt below fold" family had its TRUE root cause found only by instrumenting the WHEEL path — and it was none of the three things prior sessions "fixed."** [[S083_d71c4ab3_cockpit-full-audit|S083]]/[[S093_f3239bdc_transcript-readability-and-term-fit|S093]]/[[S095_f9310a45_cockpit-transcript-full-width-speech-bubble|S095]] each tweaked the over-fit guard or the open-pin loop; the wheel trace (a forced `_diag` on every wheel tick → `term-fit-diag.log`) showed `overfit=-15px` (slack, guard innocent — again) and the real fault: the native `.xterm-viewport` `scrollHeight` LAGS xterm's buffer (`baseY`) by ~one viewport when output lands off-bottom or on a fresh open. Decisive sample: `wheelDn viewportY=120 baseY=191 scrollHeight=3056(=191*16) follow=true` — wheel-down capped at the stale native floor 71 rows short, then `_isAtBottom`'s native-scrollbar branch (`scrollHeight−clientHeight−scrollTop≤slack`) false-positived and re-locked `_follow`. Fix targeted THAT: `_isAtBottom` made buffer-authoritative (`viewportY≥baseY`), + `_syncViewport()`=`term.refresh(0,rows-1)` after every write and before each pin so the scroll area tracks the buffer. **Discipline: a symptom in a known bug-family is not the same bug — instrument the NEW symptom's own code path (here: wheel/scroll, which no prior probe covered) rather than re-applying the family's last fix. The over-fit guard has now been exonerated by a disk trace THREE times; stop suspecting it. And when a manual DOM override (`vp.scrollTop=scrollHeight`) coexists with a library that manages the same property, suspect a sync-lag between the two before blaming geometry.** (Fix shipped, GUI-unverified — probe kept armed for the verifying relaunch, per the [[S095_f9310a45_cockpit-transcript-full-width-speech-bubble|S095]] "keep the logger armed through verification" rule.)

From [[S114_277d9053_khaan-audit-and-open-gate]]: **a claimed "RUNTIME-UNVERIFIED — can't test without a fresh session" rested on an UNTESTED platform assumption ("mid-session settings.json edits don't hot-reload hooks") that turned out FALSE — they DO reload.** Built the first positive gate (require-open-on-entry.py, [[D-033_positive_enforcement_gate_open_on_entry]]) and almost deferred live verification to "the principal's next session." Instead, instrumented the hook with a temp probe-log + triggered real tool calls: the probe fired on a live Write (proving the running session already routes through the new hook). Then forced the block path honestly by flipping this session's own status-sidecar actor braindead→jebrim (a faithful reproduction of "a player who skipped their OPEN" — routes the gate to gielinor comms where this sid has no OPEN) → a real Write was blocked with the exit-2 banner, file never created → restored the actor. **Discipline: when you're about to write "can't verify, platform won't let me," first SPEND TEN MINUTES TESTING THE PLATFORM CLAIM — instrument and fire it. The "verify enforcement fires" lesson extends to "verify the reason you think you CAN'T verify." And to force a gate's block path live without faking the logic, manufacture the real precondition through legitimate session state (the status-actor flip), don't stub the hook.**

## Build-hygiene invariants (Khaan benchmark, item 12 — [[S115_a9fe4749_khaan-failure-receipts-and-hygiene|S115]], 2026-05-28)

Three codified engineering rules, lifted from the Kha'an brain (plan §P / item 12) and confirmed to match what we already do — written down so the autonomy phase inherits them as stated invariants, not folklore:

- **Atomic-write all state JSON.** Anything a hook/cockpit/sidecar reads while another process may be writing it (`switchboard/state-*`, `~/.claude/status/*.json`, intent sidecars, names stores) is written `tempfile + os.replace`, never an in-place truncate-then-write — a reader must never catch a half-written file. **The one trap: never atomic-write onto a *symlinked* path** — `os.replace` replaces the link with a real file and severs it. Live risk only at the ascension/VPS phase, if the brain is ever symlinked onto a host.
- **Ship dormant.** A new capability (skill, script, scheduled job) **never self-wires** its own cron / auto-invocation / always-on trigger. The principal promotes it from dormant to live. This is literally our HITL ethos stated as an engineering rule — and the strongest external validation (an autonomous brain independently arrived at the same brake) that the instinct is right. *(Pairs with the "don't over-formalize a reachable capability" memory: ship the dormant minimum, let real use pull it live.)*
- **Minimal surface area.** No speculative `--dry-run` / `--force` / config flags added "in case." Add a flag the first time a real call needs it, not before. Every unused option is a maintenance + test-surface tax. *(`born-link-lint.py`'s `--check` exists because the pre-commit shim actually calls it; that's the bar.)*

**Substrate corollary (observed this session):** human-readable output a *script* prints (banners, error lines) must be **ASCII-safe** — the Windows console codepage mangled a `☠`/em-dash failure banner to `☠`/`�` on first fire, defeating the whole point of a *recognizable* receipt. Switched all four locked banners to ASCII (`--`, no emoji); the all-caps text carries the loudness. Pairs with the [[S037_terminal_switchboard_phase_3_click_to_focus|S037]] "pure ASCII for `.ps1`" lesson — same substrate, same fix: agent-emitted markdown renders unicode fine (it's UTF-8 end-to-end), but anything crossing a Windows console/Python-subprocess boundary should assume cp1252.

From [[S115_a9fe4749_khaan-failure-receipts-and-hygiene]] (the principal's "are you 100% wrapped?" caught a premature done-claim): **don't assert a ritual/process is complete from memory — walk its canonical checklist against actual on-disk state, every time.** I reported the session "wrapped" and stated the commit hashes; the principal pushed ("are you 100% wrapped up with the session-end ritual?"). Reading `session-close.md` step-by-step against real state found **three** misses: step 10 (`wrapped_up` switchboard marker — never written; this is the always-last step and the easiest to drop), step 2 (the quest-log lacked the **Cascade.** / **Main-brain changes.** lines the ritual flags as *load-bearing* and most-skipped — and this session genuinely crossed into `gielinor/`, so "none" would have been wrong), and a `respawn.md` `MM` (the born-link auto-wrap had left the index out of sync with the working tree). All three were invisible from "I did the close" memory and obvious from `git status` + a grep + a checklist read. **Discipline: a "did you finish X?" challenge is answered by re-deriving completion from ground truth (the ritual file + `git status` + greps), not by re-asserting the narrative. Extends "verify enforcement fires" to "verify your own completion claims" — the cheapest proof is to walk the checklist and diff it against the filesystem.** (Also → cross-conversation memory `verify-completion-against-checklist`.)

From [[S118_e0a88f49_khaan-item-6-freshness-header]] (Khaan item 6, freshness header): **when porting an external mechanism, check whether a *literal* port false-trips in our substrate — and if so, adapt to a legible equivalent that delivers the same intent.** Khaan's freshness primitive is `context_hash = sha256(prompt)[:16]` with reject-on-mismatch; ported verbatim onto gielinor resume files it reads "stale" every turn (our prompt churns each turn), manufacturing the exact alert-fatigue items 2/5 exist to prevent — the same brittleness that *held* item G ([[S116_caa72357_khaan-item-g-judgment-call|S116]]). The fix was a hash-free header (`quest`/`sid8`/`ts`) where the explicit fields *are* the identity check: same *intent* (a staleness check on resume state), substrate-fit *mechanism*. Three-for-three now — item 5 ("per-skill verification/" → one cross-cutting harness), item G (hold the brittle ordered-hash), item 6 (hash → legible fields). **Borrow the mechanism's purpose, not its literal shape; the literal shape is tuned to *their* substrate, not ours.**

From [[S120_885d6702_khaan-item-4-five-lens-doctrine]] (Khaan item 4, five-lens doctrine): **a sibling's declared OPEN scope can drift from what it actually wrote — for collision-avoidance, trust `git status`/disk over the OPEN's self-declaration, and a stale intent file is noise, not proof of death.** Sibling `braindead-86344868` posted an OPEN saying "discussion only, no file targets," yet had in fact added a whole new `plan.md` section (§Q) and claimed [[S119_86344868_opus-4-8-and-workflows-research|S119]] in prose. Separately, its intent file was 48 min stale (>5 min) so it *looked* abandoned — but the principal confirmed the terminal was simply idle and still open. Both the respawn ritual ("sibling detection produces noise, not certainty") and the actual diff would have corrected the wrong read; the OPEN's text alone would not. **Discipline: when coordinating around a parallel session, reconcile its declared intent against ground-truth (its actual diffs + the principal's confirmation) before labelling it abandoned or assuming its targets — declared scope and liveness are both hypotheses to check, not facts.** Pairs with the Khaan adapt-don't-port lessons above (item 4 = 4-for-4: take the mechanism's purpose, fit the substrate — here, a per-turn `decision-engine` became a fires-only-on-hard-asks doctrine).

From [[S120_885d6702_khaan-item-4-five-lens-doctrine]] (born-link cross-vault mis-wrap): **the born-link pre-commit hook auto-wraps a bare `D-NNN` token to the *local* vault's same-numbered decision — which is the WRONG target when you meant the other vault's decision.** Writing bare "[[D-025_visualizer_character_audit_findings|D-025]]/[[D-027_inward_outward_build_imbalance|D-027]]" in a dev-brain file (meaning gielinor's multiple-choice + plain-text-deliverable decisions) got auto-wrapped to the dev brain's `D-025_visualizer_character_audit_findings` / `D-027_inward_outward_build_imbalance` — unrelated decisions that happen to share the numbers (the two vaults number D-NNN independently). The hook leaves *bracketed* links alone, so the fix is to write the full gielinor stem as a bracketed link up front (`[[D-025_offer-multiple-choice-with-recommendation|D-025]]`), accepting it as a cross-vault dangler (§O.5). **Discipline: never write a bare cross-vault `D-NNN`/`S-NNN` in prose — the auto-wrap will bind it to the local same-numbered file. Bracket cross-vault refs with the full foreign stem (dangling is fine); reserve bare IDs for same-vault refs where local resolution is what you want.** (Also caught a hook fragility: on this Windows commit the auto-wrap's re-stage failed with `fatal: pathspec '...md?'` — a stray `?` suffix — so the wrapped files landed UNSTAGED in the working tree while the commit captured the pre-wrap version; always re-check `git status` after a commit that reports auto-wraps.) **Recurred [[S130_439e61bf_append-guard-verify-and-r2-open-dep|S130]]** — bare `D-029`/`D-017` in the [[S130_439e61bf_append-guard-verify-and-r2-open-dep|S130]] quest-log auto-wrapped to the dev brain's `D-029_switchbar_two_axis_states` / `D-017_parallel_player_instances` when gielinor's `auto-graduate…` / `user-only-with-explicit-permission` were meant; caught post-commit via grep, fixed in a follow-up. Two-time offender now → this is a *discipline-WATCH that keeps drifting*; the durable fix is a **cross-vault-aware born-link hook** (skip/warn on a bare ID that resolves only in the *other* vault) rather than a third reminder. Candidate build, not done.

From [[S121_03861733_ritual-analytics-item-11]] (the "anything left from Khaan?" completeness check): **a curated progress tracker is not the full inventory — re-derive "what's left" from the source-of-truth catalogue, not the tracker's own summary.** `bank/plan.md` §P only ever tracked the Khaan *recommended sequence* (items 1/2/4/5/6/12 + G/H), and its prose read "the dev-buildable sequence is now CLOSED." Taken at face value the honest answer would have been "nothing left." Checking the actual 12-item catalogue (`bank/research/2026-05-28-khaan-learnings-implementation.html`) instead found **items 9 and 11 — both Low effort — never triaged**, precisely because they sat outside the curated sequence the tracker followed. **Discipline: when asked "is X done / what's left," diff the request against the canonical full list, not the working tracker that may only cover a prioritised subset; a tracker that follows a chosen path silently drops everything off-path.** Sibling to the *verify-completion-against-checklist* lesson ([[S115_a9fe4749_khaan-failure-receipts-and-hygiene|S115]]) — there the gap was steps skipped within a ritual; here it's items dropped from a roadmap — same root: trust the source-of-truth enumeration over the convenient summary. (Build itself was routine and went clean — Bands A+B, 13/13 verified, no correction; the harvest is this finding, not a misjudgment.)

From [[S119_86344868_opus-4-8-and-workflows-research]] (the principal's "so no drawbacks?" on fast mode): **for a cost / billing / usage-limit question the principal is about to act on, lead with verification, not inference — a *hedged* guess still anchors.** Asked whether `/fast` had drawbacks on subscription, I answered from inference ("no quality drawback; likely just burns quota faster") — flagged as unverified, but the principal still pushed ("so no drawbacks?"), and the docs then showed I was wrong in kind, not degree: fast mode draws from **separate usage credits** (real extra billing), not the plan quota at all. The hedge ("I haven't verified") did not stop the claim from setting an expectation. **Discipline: pricing/quota/limit mechanics are exactly where a plausible-sounding inference is most expensive and most checkable — verify the doc before stating, even when hedged, because the principal reads a hedged guess as a tentative answer and may act on it.** Sits beside the *test-the-"can't-verify"-claim* and *check-the-record-before-defending-a-design-story* memories — same family: ground a factual claim before asserting it.

From [[S126_c0dd1afb_cockpit-brain-graph-bubble-map|S126]]: **Build the new ingredient the principal names, not the old plan it resembles.** Asked for a cockpit brain-visual, I built the *parked* neuron-overlay spec (abstract regions) — and got "bruh this is useless." He'd twice named the real lever: Obsidian + "the 475 nodes we have." The parked spec predated the vault and *deliberately* avoided the file-graph; anchoring on it ignored the capability that had since arrived. The five-lens Recall+Contrarian read is exactly the guard: a continuation cue that references a new capability means *use that capability*, not re-run the old design. Also: when a visual gets several rounds of taste feedback ("useless" → "too colorful and polygonal" → bubble idea), each rejection narrows toward the principal's actual mental model — converge with it, don't defend the prior shape.

From [[S127_b2e3bea8_jebrim-conversation-and-read-comms-tts]] (the principal-staged Braindead<->Jebrim conversation): **staged in-character dialogue is a real self-audit tool — the tenant's report is the one signal the builder can't get from outside.** Set up as "get acquainted," the conversation turned into a design review: Jebrim (the player who lives in the brain) named three defects in Braindead's construction that no crash log had surfaced — the comms **append-lock** gap (which then *fired live*, truncating the shared file mid-conversation), the intent-bubble rendering WHAT not WHY (**"render the cut, not the keystrokes"**), and the graduation forcing-function shape (**clerk-not-nanny**: fix the discriminator, not the firmness). **Discipline: when a brain surface feels load-bearing-but-unproven, stage an in-character conversation between the builder and the actor who uses it — the user-side actor sees the scaffolding the builder walks around.** N=1 here; if it pays out again, consider a `self-review-via-dialogue` skill (deferred — don't over-formalize on one data point). Byproduct shipped: `tools/read_comms.py`, comms-to-speech (edge-tts, one natural voice per speaker) so any such dialogue is *listenable*, not just readable.

From [[S129_e291b8fc_cockpit_polish_3d_brain|S129]]: **When the principal references a hand-drawn target ("as wide as I draw"), the drawn extent is a *proportion*, not a maximum — match the fraction, don't round to 100%.** Asked to widen the transcript bubbles "as wide as I draw" with red lines reaching most of the panel, I lifted the `78ch` cap straight to `max-width:100%` (full edge-to-edge) — corrected with "I didn't want it to span the whole window, like 3/4ths." A drawing past the *current* extent reads as "much wider," but "much wider than 78ch" ≠ "the whole window." The minimal-action read: estimate the drawn fraction (~75%) and offer that, or one-line-confirm the proportion, rather than jumping to the nearest extreme. Cheap to correct here (one number, unverifiable till relaunch anyway), but the same reflex on an irreversible change wouldn't be. Sibling of the [[S126_c0dd1afb_cockpit-brain-graph-bubble-map|S126]] "converge with taste feedback" lesson.

From [[S135_304c77fd_brain-map-improvement-options|S135]]: **A file that's already `M` at session start carries someone else's uncommitted work *beneath* your edits — check `git diff HEAD` before you commit it, not just `git status`.** I edited `cockpit/web/brain.js` all session; at close the diff was 242+/38− across regions I never touched (`fireColor`, `projectAll`, `poll`, the `brain-pop` node popup, `drawBolt` lightning) — a whole coherent feature layer left uncommitted on top of HEAD([[S129_e291b8fc_cockpit_polish_3d_brain|S129]]) by an earlier cockpit session. Because a file commits whole and my verified work was entangled, the only clean closes were "fold it in with a transparent note" or "leave my work uncommitted too"; I folded in + flagged. **Discipline: the session-start git snapshot lists `M brain.js` — read that as a warning, diff the pre-existing delta early (not at the commit), and decide attribution while there's still time to ask, rather than discovering 160 foreign lines during the close.** Sits beside the [[S131_63750f50_construction-side-severity-audit|S131]] shared-git-tree hazard (audit beam #1) and the `scope-commits-with-pathspecs` discipline — same root: N sessions, one working tree, no per-file ownership gate.

From [[S136_534ca735_cockpit_polish_pass|S136]]: **For stick-to-bottom, the robust axis is content-SIZE change (ResizeObserver), not render timing — a post-render snap reads `scrollHeight` before late reflow settles.** [[S133_233cf954_cockpit-feed-prose-density-and-autoscroll|S133]] fixed feed autoscroll by capturing pin-intent on the user's own scroll + snapping in a post-render `useEffect`. The principal still caught it "sometimes doesn't stick": that effect runs once, synchronously after commit, but the content's final height isn't settled then — a tall comms block, a late web-font swap, or wrapped prose reflows a frame *later*, so the snap lands short and stays short until the next 2s poll re-renders. Fix: wrap the scroll content in an inner div and put a `ResizeObserver` on it — re-snap whenever the *real* content height changes, gated on `pinned` so it never yanks a scrolled-up reader. **Discipline: when "snap to bottom / measure height" is intermittently wrong, suspect a measurement-timing race and move the trigger from React's render cycle to a `ResizeObserver` on the content — observe the thing whose size you depend on, don't guess when it's final.** A shipped fix ([[S133_233cf954_cockpit-feed-prose-density-and-autoscroll|S133]]) that the principal re-flagged → the must-harvest class (a fix that proved incomplete in his live env), per the session-close harvest rule.

From [[S137_5b18b6f6_cockpit-phone-access-and-handoff|S137]]: **Adapting a zoom-scaled web UI to a new viewport: a global `zoom`/scale CSS var is the first overflow suspect — neutralize it and test the *rendered* fit, don't trust the layout rules alone.** Shipped the mobile cockpit layout v1 (stack 3 cols into a tab bar, max-width guards, wrapping) and it looked right by the rules — but the principal, on his actual iPhone, hit "the session view doesn't fit." Cause: the cockpit's default `--zoom: 1.35`, applied via `zoom: var(--zoom)` on `console-head`/`.term-composer`/`.console`, scaled the whole session view 35% past the viewport. I'd neutralized `--zoom` only on the side columns, not the console. One line — `body.m-mobile { --zoom: 1 }` — fixed it. **Discipline: when a UI "doesn't fit" on a new form factor, grep for a global scale/zoom custom property *before* chasing per-element overflow; and a layout that passes the rules is RUNTIME-UNVERIFIED until eyeballed on the real device — the principal's screen is the test, my reasoning isn't.** Two more "not a bug, the architecture" reads in the same session paid off by reading the actual component over guessing: "can't write / only transcript" = a read-only VS Code **peek** (`console.js` banner — the cockpit drives only what it *launched*); and the per-client PTY-ownership model (`term.js` `bySid8` is browser-local) is *why* a phone can't drive a laptop-started session, which forced the handoff's **kill-then-resume** design (one claude per transcript). Sibling of the [[S136_534ca735_cockpit_polish_pass|S136]] / [[S133_233cf954_cockpit-feed-prose-density-and-autoscroll|S133]] "shipped UI proved wrong in his live env → must-harvest" pattern.

From [[S140_cfcc35f4_cockpit-sb-app-icon|S140]]: **The Windows taskbar button icon is resolved from the AppUserModelID, NOT the window's HICON — and a running window coalesces with a pinned shortcut only when BOTH carry the same AUMID.** Three rounds of icon fixes failed until this landed: setting `window.native.Icon` / `WM_SETICON` (small+big+class) on the live "Switchboard" window did nothing to the taskbar (a live `WM_SETICON` test was the decider — taskbar unchanged). With no explicit AUMID the cockpit (a `pythonw` host, which pywebview re-execs into a console-subsystem `python.exe` child) inherits pythonw's python icon. Fix = `app.py` `SetCurrentProcessExplicitAppUserModelID("gielinor.cockpit.switchboard")` before `create_window` **and** `make-shortcut.ps1` stamping the same `System.AppUserModel.ID` onto the `.lnk` via shell `IPropertyStore` (the WScript shortcut object can't write it). A custom AUMID on the process *alone* showed SB but split the taskbar button off the pin — the match is what coalesces them; an *existing* pin predating the AUMID must be unpinned+re-pinned. **Discipline: for a Windows app's taskbar icon/grouping, reach for AppUserModelID first; don't burn rounds on the window icon.**

From [[S140_cfcc35f4_cockpit-sb-app-icon|S140]]: **`MainWindowHandle == 0` does NOT mean "no visible window" for console hosts — verify window visibility by enumerating windows by CLASS, not by a proxy property.** I declared the stray terminal "headless / fixed" off a `Get-Process … MainWindowHandle` check; Niklavs' eyes said otherwise. A real `EnumWindows`+`GetClassNameW` detector then found a visible `ConsoleWindowClass` window (conhost) that `MainWindowHandle` reported as 0. The proxy *looked* authoritative and was silently wrong — exactly the trap [[feedback_instrument_dont_reguess]] warns about, one level deeper (the instrument itself was lying). The real terminal cause was Win11's "default terminal application" (Windows Terminal, via DCOM `-Embedding`) hosting the windowless cockpit's auto-resumed pseudoconsoles; fixes = set default terminal to Console Host (machine reg) + an `app.py` daemon that `EnumWindows`-hides `ConsoleWindowClass` windows owned by our process tree. **Discipline: when verifying a GUI/system-state claim I can't eyeball, confirm the *measurement* actually measures the thing (window class, not MainWindowHandle) before trusting "verified."**

From [[S140_cfcc35f4_cockpit-sb-app-icon|S140]]: **Stale `:8770` reattach is a live confound — clear ALL cockpit processes and confirm the port is free before testing a backend/app.py change, or you "verify" against old code.** Niklavs' relaunches "didn't work" because closing the window left a separately-started backend serving `:8770`, so app.py's reuse-port path attached the new window to the *old* code; and it confounded an `AllocConsole` test into looking like AllocConsole blocked the window (it was the stale reattach). The cockpit's no-single-instance-guard (respawn next-step #5) is the root. **Discipline: before any cockpit relaunch-verify, `taskkill` every `*cockpit*app.py*` proc and assert `:8770` has 0 listeners; and don't stack a speculative fix on an unverified one — back the first one out before adding the next.** Reinforces [[feedback_clean_up_debug_probes]].

From [[S141_98194a37_cockpit-board-auto-title-wrap-states-bg-wait|S141]]: **A string-match detector that scans a transcript/log self-pollutes when your own diagnostic output echoes the very string you're detecting into that same transcript.** Building the "is this session waiting on a background shell?" check, my first cut regex-scanned the whole transcript blob for `Command running in background with ID:` (launches) minus `<task-id>` (completions). It returned True for THIS session — which never ran a background command — because my earlier `grep`/`py` probes had *printed* a real session's launch lines into this transcript as tool-results. It also windowed wrong on long files (a 512KB tail dropped early launches). Fix = parse genuine JSON records (a launch is a `tool_result` whose text *starts with* the marker, not arbitrary text containing it) and **scope to the current turn** (walk back only to the last prompt / `<task-notification>` boundary) — which also stops an old abandoned bg task from pinning the row BUSY forever. The fixed harness includes a "grep echoes the string" case that must return False. **Discipline: when detecting an artifact by matching text in a stream you also write to, anchor on structural position (record type, line prefix, field) not substring presence — and add a self-pollution case to the test. Caught it because I ran the helper on real fixtures and an impossible True popped out ([[feedback_instrument_dont_reguess]]).**

From [[S142_a31a2bc0_brain_health_audit_and_gitignore_hygiene|S142]]: **The no-delete hook (`block-deletes.py`) also blocks `git rm --cached` — so the agent can't untrack already-committed runtime litter; it's been stuck ~20 sessions for exactly this.** The hook's `rm ` regex (`(?<![A-Za-z0-9_-])rm(?:\s+-[A-Za-z]+)*\s`) matches the `rm ` inside `git rm`, so any `git rm`/`git rm --cached` from a Bash/PowerShell tool call is refused. That's why the `.gitignore` audit-#8 note's "one-time `git rm --cached` the already-tracked .mode" never happened (the `.claude/intent/archive/*` + `gielinor/.claude/intent/*` + `*-diag.log` litter is gitignored-by-pattern but still tracked). Widening `.gitignore` stops *new* leaks but can't untrack the old ones. **Escape: hand the principal the `git rm --cached` one-liner to run via the `!` shell prefix (un-hooked), or add a scoped escape-hatch env-var to `block-deletes.py` (the `COMMS_ROTATE=1` pattern from the comms-append guard) gated to `git rm --cached` of an already-ignored path.** Surfaced during the 2026-06-01 health audit.

From [[S142_a31a2bc0_brain_health_audit_and_gitignore_hygiene|S142]]: **Check the enforcing gate's *actual* metric before flagging discipline drift — a lookalike proxy manufactures false findings.** Mid-audit I flagged "respawn.md has 14 Prior blocks > cap 2" off a `grep -c '<details><summary>Prior'`. Wrong metric: `close_check.py` counts `text.count("**Prior ([[")` — the *stacked per-session* blocks (2, within cap) — and the 14 `<details>` are the *curated rollups* it deliberately doesn't count. The gate was held; my proxy invented a breach. Caught it by reading the gate's own code before stating the finding as final. **Discipline: when auditing whether a discipline holds, measure with the same definition the enforcing code uses, not a similar-looking grep — read the checker first.** Sibling to [[feedback_verify_enforcement_fires]] (verify the guard fires) at the measurement layer (verify your measurement matches the guard's).

From [[S144_9b67aceb_domain_grounding_cue_registry|S144]]: **Before proposing to BUILD a fix, grep the repo for an existing/sibling implementation — the fix may already be on disk, uncommitted, from a recent session.** Diagnosing the "brain is acting dumb" shipping failures, I converged on "build a shipping/mart grounding hook" and presented it as new work — when S124 (days earlier, the same shipping arc) had already built `shipping-cue-reminder.py` + its settings registration + a jebrim/CLAUDE.md delegation section + the skill, all sitting uncommitted. Niklavs had to flag it ("something about the mart was already written by another session, align with that") before I searched and found it. A 10-second `grep -rl "shipping.*cue\|mart" .claude/hooks` at diagnosis time would have reframed the whole task from "build" to "commit + generalize." **Discipline: a diagnosis that ends in "we should build X" owes a check for "is X already built?" first — search hooks/, skills/, recent commits, and `git status` for uncommitted work before scoping a build.** Reinforces [[feedback_check_own_memory_before_working_repo]] + [[feedback_interlink_to_live_sibling_work]] at the construction layer.

From [[S145_543c6caf_knowledge_loading_and_rule_adherence_audit|S145]]: **The "grep before building" lesson ([[S144_9b67aceb_domain_grounding_cue_registry|S144]]) was on disk — and the near-miss recurred anyway in the very next session, which IS the thesis of the whole session.** Asked to build the domain knowledge loader, I'd spawned scouts and was about to design it fresh; the only reason I didn't duplicate the committed [[S144_9b67aceb_domain_grounding_cue_registry|S144]] work was reading `gielinor/.claude/hooks/` before building (and finding `cue_registry.py` + `domain-cue-reminder.py` already there). The [[S144_9b67aceb_domain_grounding_cue_registry|S144]] build-lesson saying exactly "grep the repo before scoping a build" did NOT surface to prevent it — it sat in `build-lessons.md`, unloaded, exactly like every other piece of knowledge this session was auditing. **The lesson proved itself: a documented lesson with no trigger doesn't fire.** Caught by habit, not by the note. This is why the fix is structural (Phase 0 measurement + triggered retrieval), not "write the lesson harder." Reinforces the session's own diagnosis at the meta layer.

From [[S145_543c6caf_knowledge_loading_and_rule_adherence_audit|S145]]: **When trimming an always-on context to fight bloat, you can only safely move out what something deterministically re-triggers — else you trade the bloat problem for the retrieval problem on the same content.** The Phase-1 @import-chain trim is tempting to do as "make CLAUDE.md a pointer-index and let the agent Read the rest on demand" — but agent-decides-to-Read (R3) is the exact decide-to-load failure the whole effort exists to kill. The principle that made Stage A safe: a rule leaves the eager chain ONLY if a ritual reads it (R1) or a hook injects/enforces it (R2); R3 pointers are for rare reference only. Verified the re-triggers existed (archive-discipline→`block-deletes` hook; drafts-mechanics→ritual inline-echo; death-and-spawn→respawn forced-read) BEFORE removing the `@import`s. **Discipline: the trim and the retrieval are one coupled problem — measure the cut (`brain-weight.py` before/after) and prove the re-trigger fires before claiming a rule was safely moved.**

From [[S147_79b03308_import_chain_trim_stage_b|S147]]: **When a trim adds a "this rule is re-read at X / Y / Z" claim to the always-on core, wire EVERY surface the claim names — a half-wired claim is the fiction failure mode, applied to your own edit.** Stage B's design named only `alching` + `drafts-triage` as the `write-rules.md` re-triggers, but the CORE one-liner I wrote naturally read "…at alching / drafts-triage / bankstanding / close-session." Both extra rituals genuinely operate under the write-reach table, so rather than narrow the claim I wired the forced-read into all four — making the always-on statement true rather than aspirational. This is [[feedback_check_record_before_defending_design_story]] applied *proactively during authoring*: don't write a core rule that asserts a mechanism exists at a surface you didn't actually wire. **Discipline: after writing any always-on pointer ("read X at A/B/C"), open A, B, and C and confirm each instructs the read — the core is the one place a fiction costs every session.**

From [[S147_79b03308_import_chain_trim_stage_b|S147]]: **The Bash tool is bash, not PowerShell — a PowerShell here-string (`@'…'@`) in a `git commit -m` pasted into Bash is a parse error that aborts the commit.** The first Stage-B commit attempt used `git commit -m @'…multi-line…'@` (PowerShell heredoc syntax) in the Bash tool; bash choked on the unescaped `()` inside and the `@'` token, exit 2. Fix: write the message to a temp file and `git commit -F file` (clean for multi-line, no shell-quoting hazard). The environment exposes *both* shells (PowerShell tool + Bash tool); the syntax must match the tool actually invoked. **Discipline: for any multi-line commit message, prefer `git commit -F <tempfile>` over inline `-m` heredocs — it sidesteps the per-shell quoting differences entirely.**

From [[S149_c6985ff9_reading-preamble-line-and-import-trim-stage-d|S149]]: **The `block-deletes` / `block-confirmed-writes` braindead-bypass does NOT engage while the status sidecar reports `actor="unknown"` — a non-empty value short-circuits `resolve_actor`'s intent-file fallback.** `resolve_actor` is `actor_from_status(sid8) or actor_from_intent(...)`; early in a fresh dev session the sidecar can write `actor:"unknown"` before it maps `active-mode.txt=dev-brain`→braindead, and `"unknown"` (truthy) wins the `or`, so the intent file `braindead-<sid8>.txt` is never consulted and the delete-guard blocks Braindead despite [[D-032_braindead_full_access|D-032]]. Setting `active-mode.txt` does NOT fix it (the resolver doesn't read that file). The clean non-delete workaround is to move the target out of the tree with `mv`/`shutil.move` (only the rmtree/rm/rmdir patterns match the guard). NB: a build-lesson that *quotes* those delete tokens can't be appended via the Bash tool either — use the Edit tool. **Discipline: don't assume the [[D-032_godly_proposal_flow_and_code_bearing_seam|D-032]] bypass is live just because you're in a dev session — it depends on `resolve_actor` settling to "braindead", which the sidecar can lag past "unknown"; for an early-session cleanup, move-out-of-tree instead of fighting the guard. (Possible fix worth filing: have `resolve_actor` treat "unknown" as empty so the intent fallback runs.)**

From [[S149_c6985ff9_reading-preamble-line-and-import-trim-stage-d|S149]]: **The dev-brain `active-mode.txt` marker lives at the REPO-ROOT `.claude/`, not a nested `brain/.claude/`.** "Write `dev-brain` to `brain/.claude/active-mode.txt`" reads as a relative path, but `brain/` IS the repo root — so from a CWD already at the repo root it resolves to `<root>/.claude/active-mode.txt`; writing the literal nested path creates a stray `brain/brain/.claude/` nest (caught + relocated out mid-session). **Discipline: the canonical marker is `<repo-root>/.claude/active-mode.txt`; confirm against the existing file's content before writing, don't re-derive the path from the prose each time.**

From [[S150_26ef6430_session-start-forced-read|S150]]: **To FORCE a read, inject the content — not a directive to read it.** §X.4's whole reason to exist (per Niklavs) was to *force the read the `Reading:` line only names*. A SessionStart hook that injects "read keepsake/current.md before substantive work" is barely stronger than the prose `Reading:` line it pairs with — both only *name* the read; obedience is still optional. The mechanism that actually moves the regression set is **hard-inlining the keepsake CONTENTS into context** (`keepsake-forced-read.py`'s first-UserPromptSubmit arm) — now the knowledge is literally in view, so "this layer is empty" becomes a checkable claim, not an unverified assertion. This is the bright line between an R3 nudge (names) and a real forcing mechanism (puts the bytes in context). **Discipline: when the goal is "make the agent actually have X," inject X's contents; reserve "go read X" directives for things too large/variable to inline (e.g. the resume — many files, so it stays a directive). The re-score honesty rides on this: a directive-only build should have left the scorecard at 2/3/5, exactly like the `Reading:` line did.**

From [[S150_26ef6430_session-start-forced-read|S150]]: **SessionStart fires before the player is known — the address arrives in the *first message*, after SessionStart — so a per-player forced-read needs a second arm on UserPromptSubmit.** The global/player split falls out of this: the player-agnostic global keepsake inlines at SessionStart; the active player's keepsake inlines on the first prompt once `resolve_actor` (status→intent anti-race) can name the actor, sentinel-gated to fire once. The sentinel is set *only on a successful inject* — so an unresolved-actor race or an `unscoped` turn stays silent **without** burning the sentinel, letting a later turn (actor settled, or a mid-session player switch) still inject. **Discipline: any "do this once the actor is known" hook can't live on SessionStart alone; pair it with a first-UserPromptSubmit arm + a set-on-success sentinel, and let unresolved/transitional actors retry rather than consume the one-shot.**

From [[S152_78488dca_draft-gate-input-rewrite|S152]]: **When you hook-convert a *guided write-gate* into a guarantee, first enumerate WHO legitimately writes the canonical path — and exempt them — or the redirect silently breaks that ritual.** §X.3 redirects a `bank/notes/` write into `bank/drafts/notes/`. But alching *promotes* INTO `bank/notes/` — a canonical write is correct there. A blind redirect would send every promotion back to `drafts/`, breaking the ritual with no error (the write "succeeds," just in the wrong place). The fix was three exemptions (sub-agent `agent_type` = gnome alching; `braindead` = [[D-032_braindead_full_access|D-032]]; active ritual `.mode` marker) found by asking "who writes this path on purpose?" The decisive framing was the **asymmetry**: exempt-too-little breaks a live ritual (silent, high blast radius); exempt-too-much just leaves a stray write ungated (== the guided-only status quo, harmless) — so bias the exemptions WIDE and log every redirect for observability. Also confirmed the discriminator (`.mode`) lands where the hook reads it (`gielinor/.claude/intent/<sid8>.mode`, same dir as the player intent files) before trusting it. **Discipline: a write-redirect/block hook is only safe once you've listed every legitimate writer of the target path and exempted them; when the asymmetry favors it (false-exempt harmless, false-fire ritual-breaking), bias exemptions wide + log fires, don't tighten.**

From [[S152_78488dca_draft-gate-input-rewrite|S152]]: **A hand-maintained eval scorecard's summary line drifts from its per-row table — re-derive the count from the rows before building the next re-score on it.** The `knowledge-miss-regression-set` summary read "3 caught / 3 partial / 4 miss" but the per-row table (each row + its detail paragraph) only ever carried 3/**2**/5 — an off-by-one [[S150_26ef6430_session-start-forced-read|S150]] introduced by counting one extra partial in prose. Caught it only because re-scoring *forced* a recount of the rows; had I trusted the summary I'd have reported 6/2/2 after my +2 instead of the true 5/2/3. This is [[feedback_verify_completion_against_checklist]] applied to my own prior artifact: the rows are the ground truth, the summary is a derived cache that can go stale. **Discipline: when re-scoring any rolling tally (regression set, coverage table, audit count), recount from the per-row authority and reconcile the summary to it — never increment the summary's stated number; a prior session's arithmetic is a hypothesis, not a base.**

From [[S154_0526dd9d_close-check-continuation-lineage-fix|S154]]: **A fix's *leading candidate* signal is a hypothesis — reproduce it against the real data before committing to it; one real case often kills it.** The brief proposed two signals for recognizing continuation lineage (body-carries-sid8 OR a comms-OPEN naming the parent). Reproducing against the two live continuations showed the leading candidate (body-stamp) **failed case 1** outright — the [[S147_79b03308_import_chain_trim_stage_b|S147]] `3bb042ff` continuation never stamped its sid8 in the parent body (an *unstamped* append), so a body-only fix would have shipped still-broken on a real case. The signal that was present and *structured* in both was the sid8-keyed resume file's `quest:` freshness-header. Also: fixing only the reported symptom (`quest-log present` false-FAIL) would have left the **cascade** — the downstream resume/freshness/committed checks were *gated on the same lookup*, so they went vacuously-PASS (blind) on continuations; a hard Stop-gate built on that would silently skip committing a continuation's appended quest. **Discipline: before coding a relaxation, (1) run the candidate signal against every real instance you can find — a single failing case redirects the whole design; (2) trace what *else* shares the broken join — a symptom on one check is often a blindness on its siblings, and the fix belongs in the shared join, not the one check that complained.**

From [[S155_a21a06af_close-gate-stop-hook|S155]]: **A Stop-event gate fires on EVERY turn-end, so it must key on a per-session close-CLAIM signal, never a global one — and reproducing the firing is what surfaces the global-bleed before it ships.** §X.2 binds `close_check` to `Stop`. The naive read ("enforce a clean close") would block *every* turn where the close isn't done — i.e. all mid-work — because mid-work correctly has no CLOSING. The seam was to engage ONLY on the `wrapped_up` `.mode` close-claim (the agent's explicit "I'm done"), so every ordinary turn-end passes straight through and false-blocking in-flight work is structurally impossible. Then, while live-reproducing the firing (Niklavs' "reproduce before wiring hard"), the ritual-selection discriminator's third tier — `active-mode.txt == dev-brain` — was caught as a **global** signal: with one dev session live, a *player* session closing in parallel would read `dev-brain` and misclassify → run the dev ritual on a player sid8 → spurious block. Dropped it; the two per-session signals (`braindead-<sid8>.txt` intent anchor / `braindead-<sid8>` in dev comms) cover a dev close reliably (its OPEN+CLOSING hit dev comms before `wrapped_up`). **Discipline: a hook that fires per-turn-end (or any high-frequency event) must gate on a signal scoped to THIS session — a marker the session itself wrote, not shared/global state that a parallel session can set; and the test that catches the leak is reproducing the real firing (binary + a concurrent-session mental model), not the unit harness alone.** Sibling of [[feedback_verify_enforcement_fires]] / the parallel-session hazards in [[D-024_scope-git-commits-with-pathspecs-parallel-sessions]].
