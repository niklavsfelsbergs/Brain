# S027 — 2026-05-22 — Visualizer audit (S026 prep executed)

- Worked the S026 audit primer end-to-end in `developer-braindead/experiments/visualizer/index.html`. **Shipped**: B1 day/night follows replay timeline (`dayNightFill()` reads new `currentHour()` that branches on `LIVE`; `tick()`/`seekTo()` drive `updateDayNight()` so the slider walks midnight→noon→dusk across the 87s TOTAL_MS); B3 `deriveSpeaker` reordered to prefer `braindeadActive`/`wispActive` over a stale `activePlayer` — the actual coherence gap, since the HTML/CSS/dot chrome the audit primer thought was missing had already been shipped by S022; B4 `setIntent('')` for a sub-agent now no-ops so dwarf/gnome task labels survive empty-text noise; B5 `pollLive()` detects `state.ndjson` truncation/rotation, calls `resetWorld()` and bootstraps from byte 0; B6 fetch/parse errors surface as `console.warn` (first 5) and a `LIVE · poll error (N)` brand-subtitle badge that self-clears on next ok poll; B7 `instanceKey()` warns once per actor on explicit `instance:0` and coerces to 1; B8 bubble width clamped at 300px in `renderIntent()`; I1 last hardcoded per-actor hex on `.log-entry.commit` swapped to `var(--commits-text)` + `color-mix(...)` (rest of C1 was already done by S022); I2 COMMS scroll-lock with jump-to-latest pip, distance-from-bottom threshold of 12px, OSRS-styled button, unread counter, resets on `resetWorld()`; I3 `.speech-bubble` got a `drop-shadow` filter; I4 per-tab unread badges (chip with count, gated on `data-filter !== speaker`, cleared on tab click and `clearTabUnread('all')`).
- **Verified-not-a-bug**: B2 actorMoveTimers — already keyed by `actorKey` (the instanceKey) at the only call site (`moveActor(actorKey, ev.to, dur)` line 2994). D-017's instance-routing work fixed this incidentally; the audit primer was based on the static line ref without tracing the call site. Pattern worth noting: audit primers written from cold reads of one snapshot can carry forward closed items if dwarves don't cross-check git history. Worth a `bank/decisions/` note at next bankstanding — *recon dwarves should cross-check audit findings against intervening commits before bundling.*
- **Skipped per audit "low payoff" framing**: I5 (idle-despawn configurable), I6 (color cycle wrap), I7 (label offsets dynamic), I8 (vignette follow), I9 (live freeze toggle), I10 (wisp glow precomputed). All iteration-menu nice-to-haves, not bugs. Carry into [[Q-008]] / iteration menu.
- **Out of scope**: B9 hook-side D-018 read race (flagged in primer, not visualizer-bound).
- **Live observation** — screenshot mid-session showed Jebrim·1 stuck on stale bubble "CSV rework home — synthesis, awaiting smoke" from 12:52 because both running Jebrim sessions are pre-D-018-chunk-3 and still write only to bare `jebrim.txt`. `_reemit_intent_after_move` falls through to bare and emits the clobber. Expected in-flight residual per [[S026_d018_shipped_visualizer_audit_prep]]; resolves on those sessions' next turn. Did not remediate from this side — bare-file move would have been speculative without knowing which session it belongs to.
- **Open follow-ups** for next session: running-app verification of the 11 fixes (especially I2 scroll-lock UX, I4 badge counter accuracy, B1 day/night cycling cadence — 87s/day may be too fast and want tuning), the six S026-prep open questions (cross-browser jitter, parallel-instance tint visibility, live bootstrap perf, despawn-instance:1 behavior, idle-sweep granularity, gnome render path under sub-agent activity), and `state-instances.json` added to gitignore so D-017's runtime instance map doesn't get accidentally committed.

**Cascade.** `developer-braindead/experiments/visualizer/index.html` (12 audit changes), `developer-braindead/quest-log/S027_visualizer_audit.md` (this entry), `developer-braindead/respawn.md` (rewritten — S027 outcomes + next-step shuffle), `.gitignore` (state-instances.json added).

**Main-brain changes.** none.
