# S138 — 2026-05-31 — Hermes AI recon + parking the lessons

- Entered dev-brain mid-conversation via "lets develop gielinor"; ran the entry sequence (read [[respawn]] + comms tail, sibling-detection, set `active-mode` → dev-brain, posted `OPEN` via `tools/comms_append.py`). Three live cockpit-adjacent siblings (304c77fd brain map, 534ca735 polish, 5b18b6f6 mobile) — zero file overlap with a research writeup.
- Principal asked **what "Hermes AI" is.** Disambiguated the overloaded name (Hermes *Agent* vs the Hermes *LLM* series vs noise) and pulled **primary sources** (GitHub + official site) rather than trust the SEO summaries, which contradicted each other on numbers (46k/140k/174k stars). Answer: **Hermes Agent** by **Nous Research** — open-source (MIT, Python-first), autonomous, **persistent memory + self-authored self-improving skills + subagents + MCP + 40 tools**, local-first ($5 VPS → serverless), Feb 2026, weekly releases (v0.13 "Tenacity" = durable multi-agent Kanban w/ zombie-detection).
- Key finding: it's the **closest sibling to gielinor in the wild** — same architectural family, forked at *automate-vs-gate*. They ship autonomous-capability-for-everyone; we built trustworthy-capability-for-one (the draft→approve identity gate). Wrote the recon + the full gielinor-vs-Hermes parallel table to [[2026-05-31-hermes-ai]].
- On principal's "tell me what we could learn" → synthesized 5 ranked lessons (lifecycle hardening from their Kanban; heartbeat nudge for the harvest pump; `agentskills.io` interop; chat-platform front doors for mobile; VPS substrate de-risked) + 2 guardrails (don't copy free self-modification; don't copy generality). On "park the ideas" → recorded them as **plan [[plan|§V]]** (parked, none building now), cross-linked to §C / §S / §U / §G / [[S137_5b18b6f6_cockpit-phone-access-and-handoff|S137]].
- Anomaly (known): `active-mode.txt` reset `unscoped` by something external mid-turn (same as [[S121_03861733_ritual-analytics-item-11|S121]]) — re-set at close per ritual. No corrections from the principal this session; routine recon, nothing reverted.

**Cascade.** [[2026-05-31-hermes-ai]] (new research), [[plan|§V]] added (Hermes lessons parked), this quest-log [[S138_81f1509c_hermes-ai-research|S138]], [[respawn]] prepended, `comms/active.md` OPEN+CLOSING, `.claude/active-mode.txt` → unscoped.

**Main-brain changes.** none.
