# S079 — 2026-05-24 — Cockpit header-bar overhaul

- Principal sent a screenshot of the cockpit's top header strip with two circled defects and asked for an overhaul. Diagnosed against the live markup ([[board.js]] `.topbar`, [[main.js]] `.console-head`, [[feed.js]] `.feed-head`) + [[styles.css]]. `cockpit/web/styles.css` ONLY (+21/−5).
- **Fix #1 — "N live" wrapping to two lines.** `.topbar` was overcrowded at the 372px board width (`SWITCHBOARD` 0.22em letter-spacing + count + four right-side buttons) and `.count` had no `white-space:nowrap`, so the count broke "N" onto its own line above "live". De-crowded the bar (gap 14→9, h-padding 20→14, h1 letter-spacing 0.22→0.13em ≈ +29px slack) and added `white-space:nowrap` to `.count` and the h1.
- **Fix #2 — the dark "grainy box" in the chat header.** It was an uncovered gap, not an element: [[S073_d82c4fbc_terminal-scroll-root-fix|S073]]'s `.term-col > .console-head { zoom; width: calc(100%/--zoom) }` UNDER-fills in this WebView2 build (zoom scales the header's content + height but not its explicit calc-width), rendering it ~74% wide → the bare right quarter exposed `.term-col`'s grain-over-transparent-dark backing. Replaced with `width:100%` and gave `.term-col` a solid `--panel` `background-color` + `overflow:hidden` — robust to both under-fill (fills) and over-fill (clipped), no transparent seam possible.
- Left the peek `.console` (styles.css:280) same-pattern `calc(100%/--zoom)` latent bug **FLAGGED, untouched** — a separate read-only view I couldn't eyeball.
- **UNVERIFIED** — CSS only, diagnosed from the screenshot not the live window. Pending principal cockpit relaunch to confirm: count on one line, chat header filling full column width with no dark patch, all three bars at one height. Committed at close for durability per dev convention; follow-up commit (never amend) if the eyeball finds issues.
- Also this session, committed **separately** (not part of S079): relocated a misplaced `_comment_session_start` key out of `.claude/settings.json` `hooks{}` to a top-level `_comment_*` key, clearing a `/doctor` "unknown hook event" warning. Documentation preserved; no hook behavior changed; `SessionStart` registration intact.

**Cascade.** [[styles.css]] header overhaul; this quest-log entry; [[respawn.md]] S079 prepend; [[comms/active.md]] OPEN + CLOSING. Separate commit: `.claude/settings.json`.
**Main-brain changes.** none.
