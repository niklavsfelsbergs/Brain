# S060 — 2026-05-23 — Chat persistence, /rename, and VS Code takeover

Continuation of the S060 embedded-chat quest ([[S060_ac10ec71_switchboard-embedded-chat]] ac10ec71 built the engine; 3b367751 added thinking/cancel/render polish). This session, principal-driven, added the persistence + naming + cross-surface layer. Took over `terminal.js` after 3b367751 ENDED without the persistence client half (the principal hit "refresh loses my sessions" live).

- **Session persistence (option B — disk is source of truth).** Principal picked: a refresh must not lose open chats. Design locked over discussion: server reads claude's on-disk transcript; the browser persists *only* the list of open session ids, never transcripts. New `GET /history?session=<id>` in [[server.py]] parses `~/.claude/projects/<slug>/<id>.jsonl` into visual turns (consecutive assistant records merged; `tool_use`↔`tool_result` paired; `isSidechain` sub-agent turns filtered; empty thinking dropped; result text capped 4000c). `?resume=<id>` (added earlier this session) reconnects via `claude --resume`. [[terminal.js]]: `localStorage` id-list (`sb-chat-sessions`), dormant-pill restore on load, **lazy** resume — a restored pill fetches `/history` + reconnects only when clicked. Verified the full cycle through the live server (seed→close→/history→resume recalled the seeded word).
- **Mid-turn cancel — server half.** 3b367751 built the Stop button (emits `{type:"interrupt"}`); I added the [[server.py]] handler that turns it into a stream-json `control_request{subtype:interrupt}` to claude's stdin. Verified end-to-end: turn ends, process survives for the next message.
- **`/rename` unified with the sidebar.** A client-side `/rename <name>` command in [[terminal.js]] writes the *same* `localStorage` key the switchboard already reads (`sb-session-names`, keyed by sid8) — one command relabels both the pill and the board row, no `switchboard.js` edit. Works mid-turn (it's a local command, not sent to claude). Priority: rename > ai-title > sid8.
- **VS Code → app takeover.** Clicking a switchboard row for a session not already a pill (e.g. one started in VS Code) now opens it in the chatbox and resumes it. `/history` accepts an 8-char sid8 (board rows only carry that) and resolves it to the full uuid from the `.jsonl` filename; `focusBySid8` creates+takes over. Principal chose full takeover over view-only ("fine if it can't be taken back in vscode"). One-way; clicking a row now opens-in-app rather than focusing the IDE window.
- **Ops fix:** the board had reverted to plain `python -m http.server` (no `/chat` → pill stuck "starting…"); swapped to `server.py` (background) and restarted it after the sid8 change so the new code is live.

**Still open (quest stays in-progress):** principal eyeball of the VS Code takeover render (persistence + /rename confirmed working); the `index.html`/`styles.css` chat-skin client hunks still uncommitted in the shared tree (ceded to the reskin owners); a dedicated dormant-pill dot style (cosmetic, `styles.css` — contested); thinking-block live-display already shipped by 3b367751; the observer→interactor `D-NNN` decision still unwritten; the "How to run" docs still say plain http.server (should say `server.py`).

**Cascade.** `developer-braindead/`: this quest-log entry, `respawn.md`, `comms/active.md` (OPEN + split contract + takeover claim + CLOSING). `switchboard/`: `server.py` (+`/history`, `?resume=`, interrupt handler, sid8 resolution), `terminal.js` (persistence, lazy resume, `/rename`, board takeover).
**Main-brain changes.** none.
