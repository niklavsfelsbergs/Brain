# respawn.md — dev brain entry point

> Read this first when starting a fresh dev session. Where we are, what's open, the next concrete step.
>
> The ritual that reads this file is `spellbook/respawn-ritual.md`. The ritual that *updates* it is `spellbook/session-close.md`.
>
> **Discipline (enforced 2026-05-24 token audit).** Overwritten in place at session close — **NOT append-only**. Keep the current-state block + the most recent one or two session pointers; older per-session history lives in `quest-log/SNNN_*.md` (and in git). Cross-session build lessons live in `bank/build-lessons.md` — read on demand, never loaded at respawn. This file had grown to ~410 lines / ~49k tokens of stacked `Last updated` blocks read every session; that is the bloat the discipline exists to prevent. Don't let it regrow.
>
> **Parallel note ([[D-024]]).** Multiple dev sessions run at once. At close, **PREPEND** your single `Last updated` line to whatever this file currently says — do **not** overwrite it from a stale full copy you read at your own respawn, or you'll clobber a collapse like this one.

**Last updated (S077 — cockpit swarm verification (S073–S076) + scroll desync fix + 2 rename fixes; all verified by principal, 3 commits, sid e0f2af5d).** 2026-05-24 (end of [[S077]]. Dev-brain via "lets develop gielinor". Principal cleared the S073–S076 verification debt on a fresh relaunch; we fixed what the eyeball turned up. **(1) Terminal scroll desync — completes S073, commit `62d5128`.** S073 fixed the idle scroll-*lock*, but a desync remained: content rendered at the bottom (prompt visible) while the native `.xterm-viewport` scrollbar sat at 0, so a wheel-down jumped to the top. Root cause: `scrollToBottom()` moves xterm's displayed line (`ydisp` → screen render) but NOT the native scrollbar `scrollTop`. Fix in [[term.js]] `<Term>` open effect: a bounded pin-every-frame loop (~0.8s/48f, re-fits the early reflow frames, bails on wheel-up) driving BOTH `scrollToBottom()` AND `vp.scrollTop = vp.scrollHeight`. Same pin-every-frame philosophy as console.js. **Confirmed live.** **(2) Board-side rename — commit `5738e8c`.** `/rename` rides UserPromptSubmit, so it's gated to turn boundaries (can't rename a busy VSCode session). Made renaming a board op, decoupled from the prompt pipeline: NEW `POST /api/rename {sid8,name}` in [[backend.py]] writes the SAME `state-names.json` the hook uses (identical sanitize; empty clears); [[board.js]] double-click a row name → inline input (Enter/blur commits, Esc cancels, fires once via blur); [[main.js]] `renameSession` POSTs + optimistically patches the local model; `.rename-input` in styles.css. **(3) `/rename` stranding fix — commit `6cde459`.** `/rename` fires UserPromptSubmit → status-sidecar stamps `working`, but rename-intercept blocks (exit 2) so no turn runs and no Stop ever clears it → the session showed WORKING with nothing happening. [[status-sidecar.py]] now no-ops a `/rename` prompt (matches rename-intercept's two shapes — `<cockpit-rename>` sentinel + raw `/rename …`), leaving session state untouched; also kills the S076 rename-as-`first_prompt` edge. Detector unit-tested 9/9. **All three verified by principal ("its all working").** A parallel Guthix B-006 bankstanding committed `65e2b83` between my commits — no collision (different files). active-mode.txt cleared to `unscoped` at close (no live dev sibling). **New finds parked, NOT blocking:** (#1) the manifest sometimes lists only the firing session — a long working turn fires no hooks, so its status goes stale and may drop from the board; same family as S039/S042 manifest stability. (#2) No staleness decay for a stuck `working` (S074 decays `waiting_for_user`→`idle` only); the `/rename` strand was the main cause and is now fixed at source, and a naive decay risks false-decaying a genuinely long turn (no per-tool heartbeat — sidecar doesn't fire on ordinary tool calls). See the [[S077]] quest-log.)

**Last updated (S076 — cockpit VSCode parity: disk-backed /rename + click-row-opens-in-VSCode; write-half confirmed live, sid 322cb5c3).** 2026-05-24 (end of [[S076]]. Two features so the cockpit fleet drives from VSCode too. **(1) `/rename` for any host:** NEW `brain/.claude/commands/rename.md` (expands to a `<cockpit-rename>$ARGUMENTS</cockpit-rename>` sentinel) + NEW [[rename-intercept.py]] (`UserPromptSubmit`, registered before status-sidecar; writes `switchboard/state-names.json[sid8]`, exits 2 to block, 0 model turns); [[backend.py]] `build_session_model` loads it → `name` field; [[board.js]] `nameFor||name||actor`; gitignored. **(2) click vscode-host row → open in VSCode:** `GET /api/open-vscode` → `os.startfile(vscode://niksis8.claude-focus/...)`; [[main.js]] selectRow branch. Write-half + display-half + the strand all since resolved in S077; the click-to-open-VSCode jump is the one piece still unverified end-to-end.)

**Prior sessions (full detail in `quest-log/`):** [[S073]] terminal scroll-lock fix — un-scaled the terminal's ancestry (styles.css). [[S074]] cockpit status fixes — idle decay + dead CLOSING/chat removal. [[S075]] agent prose into the FEED via `kind:"say"`. [[S072]] cockpit fluid UI — scroll-zoom + resizable/collapsible pillars + Shift+Enter→\n. [[S071]] neuron-overlay v0 **design sketch** (spec at `bank/research/cockpit-neuron-overlay-design.md`, build still parked). [[S064]]/[[D-028]] the cockpit rebuild that retired the old switchboard map+client.

## Where we are

The **cockpit** (`cockpit/`) is the standalone fleet cockpit and the brain's real driver — interactive `claude` in a PTY (on-subscription past the 2026-06-15 billing change), three surfaces (board / console / feed) over one session model. It replaced the old `switchboard/` map+client ([[S064]] / [[D-028]]). The hooks (`status-sidecar.py`, `emit-event.py`) are its data source — preserved contracts; don't touch lightly.

S077 cleared the S073–S076 verification debt: the terminal scroll is solid, board-side rename works mid-turn, and `/rename` no longer strands a session at WORKING — all principal-verified. The cockpit is in a stable, eyeballed state for the first time in several sessions. Two non-blocking status-pipeline finds are parked (manifest completeness; stuck-`working` decay — see the S077 block).

**The load-bearing gap (per [[D-027]], the S060 self-audit):** the brain optimized *inward* — observability, the cockpit — and never built outward *hands*. The operational half of the original plan (the §C pilot) is still unbuilt; the agent is still manual-invocation-only. That, not more cockpit polish, is the strategic next step.

## Next concrete step

1. **§C shipping-mart freshness pilot — the brain's first outward action ([[D-027]] / `bank/plan.md` §C). The highest-value build; cockpit polish is now verified and stable, so there's no reason left to keep deferring it.** Two halves: **Braindead** wires a reusable scheduled trigger (harness `schedule`/cron — sidesteps §G) firing a fixed Jebrim brief ~08:30 daily, output to a file first (Slack draft only once trusted); **Jebrim** writes the read-only SQL checks against `shipping_mart` (recency / volume / cost-invariant `SUM(buckets)==total_eur==real_shipping_cost_eur` / `cost_source` coverage / order-item completeness / cross-fact agreement) + thresholds, in `shipping-agent/`.
2. **Neuron-overlay v0 (the "Sunday build", unblocked).** Build from the spec at `bank/research/cockpit-neuron-overlay-design.md` — new `cockpit/web/brain.js`, fixed overlay **outside** `.app-grid` (top-right, to dodge the zoom scaling landmine), polling `/api/feed`, OSRS palette. See [[S071]].
3. **Optional hardening (S077 finds #1/#2).** Manifest completeness when a session's in a long hook-silent turn; a staleness decay for a stuck `working`. Small, same status-pipeline area. Only if it recurs and annoys.
4. **Leftover polish.** S076 click-vscode-row→open-in-VSCode end-to-end; S072 Shift+Enter→\n live-confirm; topbar density at 372px.

## Files to read first

1. `respawn.md` (this file).
2. `quest-log/in-progress/S077_e0f2af5d_cockpit-swarm-verification.md` — latest quest (+ S076 for cockpit VSCode-parity context).
3. `cockpit/_about.md` + `cockpit/backend.py` + `cockpit/web/` — the live surface.
4. `bank/decisions/D-028_*.md` — cockpit spec; `bank/decisions/D-027_*.md` — the inward/outward operability gap.
5. `bank/plan.md` §C — the parked pilot scope.
6. `bank/research/cockpit-neuron-overlay-design.md` — the S071 overlay spec.
7. `comms/active.md` (tail) — live siblings + hand-offs.
8. `bank/build-lessons.md` — cross-session build wisdom (read on demand).

## How to run the cockpit

Launch via the **cockpit / Switchboard** Desktop/Start-menu icon — it starts its own backend (`cockpit/Switchboard.vbs` → `app.py`; a pywebview window + aiohttp backend + PTY driver in one process). Hooks write state to `switchboard/state-*` files + `~/.claude/status/<sid8>.json`; the cockpit backend reads those. A relaunch is needed to pick up backend/JS changes (the running window holds stale code). `node --check` + `py_compile` are the cheap pre-commit gates for any web/hook edit.
