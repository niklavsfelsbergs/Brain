# S156 — 2026-06-02 — AGENTS.md Codex entrypoint sync (§Y.1)

- **Built plan [[plan|§Y.1]]** — the first item of the [[S155_a21a06af_close-gate-stop-hook|S155]] Codex external-audit triage. **Verified problem:** root `AGENTS.md` did `@gielinor/AGENTS.md` + referenced `developer-braindead/AGENTS.md`, but **neither existed** — so a non-Claude agent (Codex) at brain root got a 44-line router stub with two dead imports while Claude got the full `CLAUDE.md` @import chain. Divergent behavior by agent; the reason Codex itself ran degraded here.
- **The load-bearing seam — verified, not assumed (the brief's explicit caution).** Confirmed via the OpenAI Codex docs + the open feature-request [codex#17401](https://github.com/openai/codex/issues/17401) that **Codex does NOT resolve `@import`/`@include`** — it composes instructions purely by **directory-walk layering** (one `AGENTS.md` per directory level, root→cwd, nearest-overrides). So a thin `@import` pointer-shim is **dead** for Codex; the rulebook content must be **physically inlined** into each `AGENTS.md` on Codex's walk path. This decisively ruled out the thin-shim option before any wiring.
- **Design choice surfaced as multiple-choice (Niklavs picked "Generated + auto-sync hook").** Built [[sync_agents_md|tools/sync_agents_md.py]] — recursively flattens each `CLAUDE.md`'s `@import` chain into its sibling `AGENTS.md` with a `GENERATED — DO NOT EDIT` banner (`CLAUDE.md` = source of truth, `AGENTS.md` = build artifact; stdlib-only, deterministic, idempotent, `--check` mode). Generated all **three** mirrors: root `AGENTS.md` (44→502 lines, full gielinor chain inlined), new `gielinor/AGENTS.md` + new `developer-braindead/AGENTS.md` (the subfolder-session entrypoints — both needed, since Codex's directory walk reads one `AGENTS.md` per level).
- **Wired auto-sync into [[born-link-pre-commit|born-link-pre-commit.sh]]** (+15 lines, reinstalled to `.git/hooks/pre-commit`): when a commit stages any `CLAUDE.md`/`meta/*.md`, it regenerates the mirror and re-stages the `AGENTS.md` so it travels in the same commit — the "mechanically-synced shim," the §X discipline→guarantee move applied to the entrypoint surface.
- **Sub-decision flagged to Niklavs:** the regen reverts the old root `AGENTS.md`'s hand-applied `CLAUDE.md`→`AGENTS.md` / `Claude Code`→`Codex` text swaps in favor of a faithful flatten (banner names `CLAUDE.md` as canonical source). Chosen because a blind `s/CLAUDE.md/AGENTS.md/` is corruption-prone (dev `CLAUDE.md` legitimately discusses "CLAUDE.md" as a layer) and the functional rules are agent-agnostic + fully present. A Codex-native phrasing pass remains an optional targeted follow-up.
- **Verified:** `py_compile` + `sh -n` clean; `--check` idempotent; zero surviving `@import` lines; zero missing-import markers; full rulebook physically present on Codex's walk path. **Live-fired the auto-sync end-to-end** (controlled, fully reverted): staged a `CLAUDE.md` edit + corrupted the mirror → the hook regenerated it from live `CLAUDE.md` content and re-staged all three, exit 0, no false-block; the trigger fires on `CLAUDE.md`/`meta` and skips everything else.
- **Build-lesson:** the brief's "don't assume @import works in AGENTS.md" caution was correct and load-bearing — the verification (Codex has no @import) is what killed the cheapest-looking option (thin shim) and forced the generated-mirror. Verifying the resolution mechanic *before* wiring saved building a shim that would have left Codex degraded while looking fixed.

**Cascade.** New `tools/sync_agents_md.py`; new `gielinor/AGENTS.md` + `developer-braindead/AGENTS.md`; regenerated root `AGENTS.md`; `developer-braindead/bank/research/born-link-pre-commit.sh` (+auto-sync, reinstalled to `.git/hooks/pre-commit`); `developer-braindead/bank/plan.md` §Y.1 `[x]`; this quest-log + `respawn.md` + `comms/active.md` CLOSING; `bank/build-lessons.md`.

**Main-brain changes.** `gielinor/AGENTS.md` created (new generated mirror of `gielinor/CLAUDE.md`) and root `AGENTS.md` regenerated — both are generated build artifacts, no change to any `gielinor/` rulebook *source* (`CLAUDE.md`/`meta/`/rituals/hooks all untouched). No identity/`confirmed/` writes. `@import` chain unchanged (the mirror is read by Codex, not Claude — Claude still reads `CLAUDE.md`, so brain-weight is unchanged).
