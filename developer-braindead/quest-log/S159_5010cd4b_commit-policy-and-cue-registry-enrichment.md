# S159 — 2026-06-03 — Commit-policy consolidation + cue-registry enrichment (§Y.4 + §Y.5)

Dev-brain via "lets develop gielinor"; OPEN→UPDATE→CLOSING via `comms_append`; no live Braindead siblings ([[S158_dda4c966_jebrim-active-state-alching-cleanup|S158]] `dda4c966` closed 12:21). Wrote the `braindead-5010cd4b.txt` intent bubble as the **first action**. Cleared the last two open items of the [[S155_a21a06af_close-gate-stop-hook|§Y]] Codex external-audit triage — **§Y is now fully closed.**

- **§Y.4 — one consistent commit policy.** Verified gap: both close rituals restated "principal pre-authorized the close commit; always-ask outside" in *slightly different words*, the global pref (`~/.claude/CLAUDE.md` "always ask before committing") is **Claude-only**, and root [[CLAUDE|CLAUDE.md]]/[[AGENTS|AGENTS.md]] stated **no** commit policy — so **Codex** (reads `AGENTS.md`, never the Claude global) got *zero* commit policy. Fix (prose-only, no hooks): added a canonical `## Commit policy` section to root **`CLAUDE.md`** (the source) — commits require explicit authorization (always ask); the single standing exception is the session-close ritual (it *is* the authorization for its own end commit); never push. Repointed **both** close rituals ([[close-session|gielinor/spellbook/rituals/close-session.md]] step 9 + dev [[session-close|developer-braindead/spellbook/session-close.md]] step 8) to *reference* the canonical statement instead of each restating it. Regenerated root **`AGENTS.md`** via [[sync_agents_md|tools/sync_agents_md.py]] (the generated mirror — **not** hand-edited; `--check` idempotent), so Codex now carries the policy. Aligned dev session-close too (brief named only gielinor's; leaving dev's divergent would re-create the inconsistency Y.4 exists to kill — Niklavs-flagged, vetoable). Checked the rest of the brain for divergent restatements: `docs/06-rituals.md` (describes the override) + dev `player/preferences.md` ("always ask") are both *consistent* (override-half + default-half), left as-is.
- **§Y.5 — enrich `cue_registry.py` DOMAINS (extend, NOT a parallel manifest).** Codex wanted a cue→{canonical files, source repos, agents, freshness, read-before} manifest; built it as **structured fields on the existing table** (a new parallel structure would trip the brain's most-repeated failure mode — over-formalizing a reachable capability). Niklavs picked **normalize** (single source of truth) over additive-with-duplication. Each entry gained `canonical_files` / `specialist` / `freshness` / `read_before`; `message` slimmed to a one-line lead (so paths no longer live in both prose and fields → no drift). [[domain-cue-reminder|gielinor/.claude/hooks/domain-cue-reminder.py]] gained `_render(d, matched)` that composes the nudge **from** the fields (backward-compatible: a message-only entry still renders). The fields surface real signal: **shipping**'s home is the *external* `picanova/shipping-agent` repo → `specialist` is the reliable loader, `freshness` flags "stale-by-default, spawn it"; **deploy-schema**'s home is *in-brain* notes (no specialist). **Verified canonical paths before writing them** — caught that the FIF note lives under `bank/notes/projects/`, not `notes/` as the old prose implied (corrected). No `settings.json` change (the registry's whole point: the Nth domain is one row).
- **Verification.** New boundary harness [[test_domain_cue_registry|developer-braindead/verification/test_domain_cue_registry.py]] — **39/39** (structured fields present; in-brain `canonical_files` resolve on disk; `_render` composes the tail; backward-compat fallback; e2e shipping nudge; combine path; benign pass-through; braindead skip; malformed→exit 0). `py_compile` clean on both hooks. **Honest residual:** the domain-cue hook skips braindead, so the enriched nudge **could not be live-fired** this dev session — standing live-fire debt (fires on a real Jebrim shipping/deploy prompt; watch `domain-cue:*` in `switchboard/ritual-events.ndjson`), same shape as [[S144_9b67aceb_domain_grounding_cue_registry|S144]].
- **Commit discipline.** A live parallel Jebrim session ([[S124]] report + [[S148_4d020f31_import_chain_trim_stage_c|S148]] fedex) had uncommitted worktree mods; committed by **explicit pathspec** listing only my own files ([[D-024_parallel_player_coordination|D-024]] / [[feedback_pathspec_commit_from_first_commit|pathspec-from-first-commit]]). The [[S158_dda4c966_jebrim-active-state-alching-cleanup|S158]] resweep trap does not bite here — it only fires when a sibling file is *under* the committed pathspec, and none of mine are.

**Cascade.** [[S159_5010cd4b_commit-policy-and-cue-registry-enrichment|this quest]], [[respawn]] (prepended), [[plan|bank/plan.md]] (Y.4 + Y.5 → `[x]`; §Y closed), [[build-lessons|bank/build-lessons.md]] (+1), `comms/active.md` (OPEN/UPDATE/CLOSING), new [[test_domain_cue_registry|verification/test_domain_cue_registry.py]], `developer-braindead/spellbook/session-close.md` (Y.4 repoint).

**Main-brain changes.** Yes. `CLAUDE.md` (root, +`## Commit policy`) + `AGENTS.md` (root, regenerated mirror); `gielinor/spellbook/rituals/close-session.md` (Y.4 repoint); `gielinor/.claude/hooks/cue_registry.py` + `gielinor/.claude/hooks/domain-cue-reminder.py` (Y.5 enrichment). No identity/`confirmed/` writes; no `meta/` rulebook source touched.
