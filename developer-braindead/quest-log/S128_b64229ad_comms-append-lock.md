# S128 — 2026-05-30 — comms append-lock (§R.1)

Dev-brain via "lets develop gielinor", mid-conversation; OPEN posted (steps 6–8 fired). No live Braindead siblings. Picked up the [[S127_b2e3bea8_jebrim-conversation-and-read-comms-tts|S127]] §R.1 hand-off: the comms append truncation bug.

- **Root cause sharper than the hand-off framed.** The hand-off said "append-only blocks the overwrite but nothing forces an atomic write." True, but the deeper fault: both `comms/_about.md` files *claimed* appends were safe because they used `open(path,'a')` — a **fiction**. Nothing appended that way. Every comms post was a read-modify-rewrite (Edit rewriting around the last entry, or Write rewriting the whole file). That clobbers concurrent writers (lost update) AND truncates on an interrupted/raced rewrite (the zero-out Jebrim saw). The doc described a safe mechanism the code never used. The dev log's own tail carried the debris live (duplicated CLOSINGs, an `Actesdf` garble, a `... wait` line with no terminating newline).
- **Proposed before building** (principal directive). Surfaced the diagnosis + a multiple-choice on enforcement strength. Principal chose **tool + guard hook** over discipline-only — the born-link reason: §O.9 discipline-only didn't hold, §O.10's commit hook did; *gate the bad write, never force the safe one* (the [[S125]] audit's "hooks hold, discipline-WATCH drifts").
- **Built:**
  - `tools/comms_append.py` — exclusive cross-process lock (`msvcrt` on Windows / `fcntl` on POSIX) on a sidecar lockfile → true `open(a)`+fsync append, with leading-newline normalization so entries stay one-blank-line apart (and it self-heals an unterminated tail). Stdlib-only; `--vault dev|gielinor` (CWD auto-detect) / `--file` / `--entry` / stdin.
  - `gielinor/.claude/hooks/comms-append-guard.py` — PreToolUse; blocks raw Edit/Write/MultiEdit/NotebookEdit of either vault's `comms/active.md`, points at the tool. `COMMS_ROTATE=1` escape for the one legitimate whole-file rewrite (rotation). Fail-open on malformed payload. Logs `block` via `ritual_log` ([[S121_03861733_ritual-analytics-item-11|S121]]). Registered in **both** settings.json (brain-root absolute + gielinor `$CLAUDE_PROJECT_DIR`).
  - Docs: both `comms/_about.md` Concurrent-write-safety sections rewritten (fiction corrected, tool + hook + escape documented); `tools/README.md` section; dev `respawn-ritual.md` step 8 + `session-close.md` step 4 tool pointers.
- **Verified both ways.** Tool: py_compile clean; both settings.json valid; **concurrent-stress** 12 thread-workers each spawning subprocess appends, 96/96 survived, seed intact, **0 concatenation-garble, file never zero**; plus a live dogfood (this session's entries posted via the tool against the real dev log, exit 0). Hook: **9/9 synthetic payloads** (clean-JSON harness) — blocks both vaults' `active.md` (forward-slash + Windows-backslash paths), allows `_about`/archive/other-files/Bash/malformed, rotation-escape allows.
- **Honest ceiling — guard hook live-UNVERIFIED.** A same-session real-Edit probe (non-existent `old_string`, no mutation) returned string-not-found, **not** a block → the newly-added hook does **not** load mid-session. Live firing needs a fresh session — the exact trajectory require-open ([[D-033_positive_enforcement_gate_open_on_entry|D-033]]/§P.1) took: unit-verified at build, live-verified next session. The *tool* is fully verified and in use.
- **User-only carry (sign-off, not applied):** two gielinor ritual one-liners pointing OPEN/CLOSING at the tool — `respawn.md` step 6.g + `close-session.md` CLOSING step. Surfaced to the principal; the hook's own block message already redirects, so not load-bearing for correctness.
- **Process churn (honest).** A context-window replay mid-session showed phantom "successes" for edits that had not actually persisted; re-verified from disk + git (`show --stat`, greps) before trusting anything. Harness false-trips: `block-deletes` caught `rm`/`shutil.rmtree` in stress-test cleanup (rewritten to leave temp files), an early settings.json edit missed on a wrong anchor (brain-root file uses forward-slash, not escaped-backslash, paths), and a Bash error inside a parallel batch repeatedly cancelled queued Edits (lesson: never batch Bash with the edits). The "instrument, don't re-guess" line held — git was the authority, not the replayed transcript.
- **Rotation:** dev `comms/active.md` crossed the 300-line cap (my entries tipped it over) → rotated bulk to `comms/archive/active-2026-05-30.md`, kept the recent tail, per `comms/_about.md`.
- **Consolidation (principal multiple-choice):** wrap at §R.1; §R.2 (graduation = clerk-not-nanny) + §R.3 (render-the-cut godly proposal) stay teed up as the next steps.

**Cascade.** NEW `tools/comms_append.py` + `gielinor/.claude/hooks/comms-append-guard.py`; `.claude/settings.json` + `gielinor/.claude/settings.json` (register the hook); both `comms/_about.md`; `tools/README.md`; dev `spellbook/respawn-ritual.md` + `session-close.md`; `bank/plan.md` §R + §R.1; this quest-log; `respawn.md` prepend; dev `comms/active.md` OPEN/UPDATE/CLOSING (via the tool) + rotation to `comms/archive/`.
**Main-brain changes.** `gielinor/.claude/hooks/comms-append-guard.py` (new) + `gielinor/.claude/settings.json` (register) + `gielinor/comms/_about.md` (safety section). The two gielinor *ritual* edits are DEFERRED to principal sign-off (user-only).
