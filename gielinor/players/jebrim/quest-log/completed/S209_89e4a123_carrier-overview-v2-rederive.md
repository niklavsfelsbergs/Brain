# S209 — carrier_overview_v2 field-wide re-derive (resumes [[S208_9399f067_ups-cascade|S208]]/9399f067 handoff)

- sid8: 89e4a123 · 2026-06-11 · parent quest: S208_9399f067_ups-cascade
- Mission: the one open cascade step — bring the UPS tender engine into `2_analysis/carrier_overview_v2/` as the 10th new-offer competitor. UPS-with-engine is a NEW entrant → [[S185_e9821cdf_eu-tender-report-signoff|S185]] re-derive of the whole competitive grid + every carrier's relative prose (win-counts, superlatives, within-10% sets), reconciled to parquet ([[S180_4766eb11_dpd-current-report-refresh|S180]]), not to the old cards.

## Turn log

- T1: Respawn + ground (keepsake, ups-cascade resume, [[S208_9399f067_ups-cascade|S208]] quest-log, PLAN + ledger + all lib/build_*.py). Mapped the pipeline. **Key finding:** carrier_overview_v2 reads the *2025 full-year* matrix `data/cost_matrix/cost_matrix_2025-*.parquet`, which still had **10 carriers, no ups** (written Jun 10, before the cascade's `_ENGINES += ups` edit). The cascade's "11 engines" rebuild was the *separate* `cost_matrix_2026q1` decision matrix. `competitive_map.py` auto-includes any carrier in the matrix (no ALL_CARRIERS filter there — that constant only drives `build_hand_cards`). So step 0 = regen the full-year matrix with ups. UPS confidence tag: **firm** (gated ups-2.0.1 port, invoiced incumbent → engine-vs-invoice comparable; not new/held/current). Posted OPEN (flagged the shared-matrix regen). Kicked `cost_matrix.py` in background.
- T1 (cont): Matrix regen DONE — 11 engines, 31.6M rows, ups 99% eligible €6.14 Q1. **Caught + verified** dhl_paket DE Std ≤1 €2.92→€3.11: not corruption — engine drift (dhl_paket 2.0→2.2.0 Warenpost+fuel) since the v2 build; population unchanged (2,875,235); traced to a clean current-engine decomposition. Ran `competitive_map` → `build_summary` → `build_hand_cards`. **New winner counts:** maersk 15 (#1, was 14), dpd_pl_current 13 (was 16), guell 8 (was 10), dhl_paket 7, **ups 7 (NEW)**, gls 2, hermes 0 (was 3), dhl_express 0 (was 1), fedex 0-mean/cheapest-6, austrian_post 0, dpd_pl 0. Pulled the full per-segment winner table as the gating ground truth.
- T1 (cont): Principal chose **dwarf fan-out** (multiple-choice) for the prose re-derive. Added ups to ALL_CARRIERS + CARRIER_LABELS. Spawned **10 dwarves** (d1–d10, one per carded carrier, narrate-off-card, background). Wrote `dpd_pl_current.md` myself (cardless — narrated off the summary parquet: 13 wins not 16, UPS took FR Bulky 1–2 + Std 2–5, routing numbers attributed/caveated).
- T1 (cont): All 10 dwarves returned clean (summaries cross-checked vs my parquet table — gls €4.50/€5.29 ✓, guell CH-loss-to-ups ✓, hermes 0/79% ✓, ups 5-of-7-hollow ✓). **Gate:** read ups/maersk/dhl_paket/hermes in full vs the cards (PASS); fixed 3 stale engine-version stamps (dhl_paket→2.2.0, hermes→2.2.0, maersk→3.2.0); accepted the lighter-change sections on their card-matched summaries. Wrote the cross-carrier `EXEC{}` one-liners + re-ranked EXEC_ORDER (decision-relevant) myself.
- T1 (cont): Rendered — `carrier_overview.html` 473 KB + `exec_brief.html` 23 KB. **Verify PASS:** 11 carrier pages incl. page-ups, 0 template-brace/None leak, key numbers match parquet (€3.11/€17.23/€4.50/€5.84/79%/85%), exec brief 11 rows. Updated `verification/ledger.md` (S209 re-derive section + source-of-truth re-attest) + `phase3_reconciliation.md` (auto). Resume: `inventory/carrier-overview-v2-rederive-resume__89e4a123.md`.
- T2: **Principal challenged "keep today's UPS"** — if the current UPS contract is being replaced, "keep today" is fiction; signing is the baseline. Correct. Then asked **what drives the FR/Nordics/CH cost jumps** + **is "new ≈ Q1" without the GRI?** Decomposed both sides from cost_matrix (engine components) + actuals (real_*_eur): **CH +65-78% = the operative-tier BASE** (engine €12.01 vs today €7.49 — the parked [[S205_f08474c9_ups-round1-reply-review|S205]]/[[S206_01871b26_ups-2.0.0-engine-build|S206]] CH/GB finding, revisit trigger = before signature); **Nordics +27% = higher base + oversize/LPS layer**; **FR +4-6% = basically flat (base ±€0.15, fuel-timing)**. Line-haul is a wash (today's "truck €0.72" = engine €0.75). **GRI:** carrier_overview "today" = GRI-free raw Q1; routing/decision applies UPS's +5% GRI keep-side-only (do-nothing). Against today ×1.05 the **FR slices flip to break-even+**; CH/Nordics stay (tier+oversize). Matches findings.md (offer ~6-14% cheaper than GRI'd billing on EU core).
- T2 (cont): **Re-cut the UPS framing (principal-authorized): assume today going away.** ups.md — one-line, where-it-wins intro, 5 per-segment verdicts (drop "keep the incumbent" → negotiate CH tier / route to Güll / dispute oversize / sign), "practical surface", IT Std 2-5, analyst-take + net all reframed; added the GRI lens throughout. EXEC ups one-liner reframed. Methodology note: GRI-free caveat + "where today=UPS and the contract's being replaced, keep isn't an option; where today=kept contract, keep still holds." Re-rendered (476 KB), verify clean (0 leak, GRI in report+exec). **COMMITTED bi-analytics `0611bed`** (27 files, pathspec carrier_overview_v2/; data/cost_matrix gitignored; never pushed). Residual flagged: other hands (maersk/fedex/dpd_pl/dhl_paket) still carry "keep the incumbent" on UPS-today tail slices — governed by the global methodology note, not surgically swept (out of authorized scope; offered).
- T2 (cont): Principal: **sweep those for consistency.** Done — reframed every UPS-today "keep the incumbent" across maersk (IT Std 2-5, ROW Bulky ×2, where-it-wins intro), fedex (FR bulky + analyst-take), dpd_pl (Nordics ×2 + header), dhl_paket (ROW header + tail + analyst-take), and the generic segment-map renderer (→ "GRI-free, see Methodology"). Audit: only 4 legit keeps remain (Maersk-FR ×2 + DPD kept contract + the methodology rule). Re-rendered, verify clean. **COMMITTED `a5e3de5`** (7 files, pathspec carrier_overview_v2/).
- T3: Principal asked "did we regenerate all reports?" — **verified (file mtimes + 2026q1 matrix engine versions):** carrier_overview (this session) + routing/annual/final ([[S208_9399f067_ups-cascade|S208]] cascade, Jun 11 19:3x off cost_matrix_2026q1) are all current AND coherent (the 2026q1 matrix carries the same refreshed engines incl ups-2.0.1). **Lone straggler: `decision_report.html` (Jun 10 17:10)** — predates the cascade, carries no current canon numbers, ups engine only 3× → stale (the cascade re-ran the scorer but didn't re-render this HTML). Handed off to next session (regen decision_report).

## Close (S209)

- **No pending external actions.**
- **Quest COMPLETE** — carrier_overview_v2 re-derive + UPS GRI re-cut + consistency sweep, shipped and committed (`0611bed` + `a5e3de5`). Graduating → `completed/` (unambiguous: shipped+committed, open_dep none). The decision_report regen is a *new* quest the next session seeds.
- **Harvest:** 1 examine draft (`vs-today-baseline-assumes-incumbent-persists` — question-5, the principal's sign-decision pushback) + 1 bank draft (`ups-new-offer-vs-today-cost-drivers`) + 1 cross-conv memory (same lesson). The "all reports current?" catch is a recurrence of [[chain-regen-must-enumerate-all-derived-renderers]] ([[S208_9399f067_ups-cascade|S208]]) — noted, not duplicated.

### Next-session seed — regenerate decision_report

> Hey Jebrim, regenerate the EU tender **decision_report** — the lone straggler from the UPS cascade. `2_analysis/decision_report/decision_report.html` is Jun 10 (pre-cascade), carries no current canon (no €1,908,707 / €395,197 / €1,442,782), ups engine only 3× → stale. Every other report is current+coherent (routing/annual/final from the cascade off cost_matrix_2026q1; carrier_overview from S209; all on dhl_paket-2.2.0 / maersk-3.2.0 / hermes-2.2.0 / ups-2.0.1). Task: read `decision_report/report.py` + `report_2026q1.py` (confirm inputs), re-run its chain to pick up ups-as-renewable + the cascade canon (paid 2,955,020 → do-nothing 3,055,317 → plan 2,660,120 = €395,197 Q1 / €1,908,707 ann; firm 990,225 / DBS-contingent 918,482; rate moves 483,133), confirm the six-to-sign portfolio is unchanged + UPS = sign (contract being replaced, "keep today" off the table — per the S209 carrier_overview re-cut), verify no leak + canon present + engine versions match, commit pathspec decision_report/ only. Read first: ups-cascade-resume__9399f067 ([[S208_9399f067_ups-cascade|S208]] canon) + this file. Also: eu-tender digest headline two canons stale → re-stamp candidate.
