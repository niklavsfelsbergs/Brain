# S225 — guell-2.0.0 build + EU-tender portfolio-fit read

**Session:** sid8 `9f716f1f`, 2026-06-12. **Parent:** continues the build phase handed off by [[S223_6ae2971e_guell-reply-review-doc-sync|S223]] (`6ae2971e`, guell-reply-review-doc-sync) — that session reviewed Güll's Round-1 reply + synced the docs; this one built the engine and answered the payoff question. Survived two laptop crashes mid-build; all artifacts verified intact on disk after each.

**Player:** Jebrim. **Repo:** `bi-analytics-main/NFE/projects/2_EU_tender_2026/2_analysis/` (work edits UNCOMMITTED, separate repo).

## What was asked

Build `guell-2.0.0` per PLAN §B.21 (all carrier inputs confirmed in the reviewed reply), then read off the regenerated scoring whether cheaper Güll (a) strengthens `renew_maersk_plus_guell`, (b) unlocks retiring an AT/CH carrier, or (c) just holds.

## What shipped

- **Engine `carriers/guell/` → guell-2.0.0, 19/19 fixtures.** Fuel 0.2727→0.15 on base+B2C+bulky (not Maut); Maut 0.30→0.32; CH energy flat 0.04→per-month `CH_ENERGY_CHF_BY_YM` (Jan-25→Jun-26 from the reply PDF, extracted via pdftotext); CHF→EUR single-point→per-month `CHF_TO_EUR_BY_YM` **mechanism** (flagged 1.08 proxy — the reply gave the mechanism, not the rate values); PEAK=0; 4 new line-haul surcharges (inbound sprinter 955÷1200≈0.80, line_haul_at 24.50÷150, line_haul_ch 40÷150, ch_declaration 40÷800); new 4th service `at_oversize` (45 EUR 2nd-carrier recovery, ~4k/yr, was a hard reject). Engine now requires `shop_order_created_date`.
- **Chain regenerated:** cost_matrix (31.6M rows, 2025 full-year, all guell-2.0.0) → decision_scorer → scenarios.parquet → decision_report.html → bias_table.md.
- **report.py edits:** removed `guell` from `HELD_ENGINES` (rebuilt → trustworthy); rewrote the Güll per-carrier verdict + the Q11/Q7 open-items row + dropped Güll from the held-engine prose lists.
- **`_refresh_bias_table.py`:** repointed to `load_cost_matrix()` (legacy single file was stale) + column-pruned the collect (the un-pruned 31M-row collect OOM'd — that was the bias-refresh failure).
- **Docs:** PLAN §B.21 `[x]` closed; `carriers/guell/CLAUDE.md` version-history 2.0.0 + surcharges table; `ASSUMPTIONS.md` verdict line.

## Decisions made in-flight

- **Sprinter density (load-bearing, 8× swing):** the brief said "parcels-per-sprinter 150" but DECISIONS.md 2026-05-19 fixed it as 8 pallets × 150/pallet = 1,200 → €0.80/parcel. Anchored to that, not the literal reading (which would give €6.37 and flip the answer). Anchor-to-existing-state paid off.
- **FX values gap:** the reply confirmed the *mechanism* (strongest-CHF-of-month, Commerzbank) but no rate values exist in the corpus. Wired the per-month lookup + populated a flagged 1.08 period proxy (above v1's 1.05 — the documented adverse direction); did NOT fabricate monthly precision. Round-2 data pull.
- **CH 140 overflow:** documented but NOT wired (~100 items/yr, immaterial, not in the build-scope sentence) — minimal-action.

## THE ANSWER — (c) HOLD; "cheaper Güll" is FALSE

Clean v1→v2 isolation (restored guell-1.0.0 from git HEAD, ran both on the same full-year pop): engine +30% (+€330.8k), **like-for-like +14.5%**; CH PostPac €7.08→€8.37 (+18%), AT €4.33→€4.97 (+15%). The confirmed line-haul outweighs the favourable fuel/Maut/energy cuts — exactly as DECISIONS.md 2026-05-19 predicted. Within-matrix portfolio marginal: Güll adds **+€163,897** to `all_renewals` (was ~€255k apparent on v1 + a stale multi-engine matrix). (a) claws +€186k back on `renew_maersk` but the set stays −€629k (Maersk-alone is the value-destroyer). (b) **displaces Austrian Post** — Güll > AP €105k; bias winning-slice AP wins collapse 8,803→486; does NOT unlock CH-leg retirements (Güll's CH got dearer). Güll in every top-8 portfolio; +€61k at full portfolio; minimum-disruption leader stays `renew_maersk_plus_hermes`. **Caveat front-and-centre:** brand-new carrier, no parity validation, fixtures only; FX/density/Q11 flagged.

**Confound flagged:** raw scenario *absolutes* also moved because the whole 12-engine matrix refreshed to current code (Maersk 3.2.0, Hermes 2.2.0, UPS cascade hadn't been baked into the prior matrix) — so the clean Güll signal is the within-matrix marginal + the per-parcel isolation, not the raw scenario delta.

## Mishap + recovery

A careless `git archive HEAD <path> | tar -x --strip-components=5 -C .` extracted guell-1.0.0 INTO the live `carriers/guell/` (tar can't delete → overwrote my 2.0.0 base files, the 4 new-only modules survived), then `mv guell guell_v1` renamed the hybrid away. Restored `carriers/guell` to clean 2.0.0 by Read-then-Write of the 6 clobbered files (content all in-conversation), re-verified 19/19. Lesson saved to memory + examine draft: extract old git versions to /tmp, never into a live package dir.

## Pending external actions

- **bi-analytics-main edits UNCOMMITTED** (engine + report.py + bias + PLAN + ASSUMPTIONS + CLAUDE.md) — await Niklavs' commit go (separate work repo, pathspec-scoped, never push).
- **Next session:** does adding Güll to the 5-carrier NO-HERMES portfolio give meaningful savings? (Niklavs deferred it. Full prompt in `inventory/guell-2.0.0-build-resume__9f716f1f.md`.)
- Round-2 Q11 outbound per-pallet (drafted `questions_round2.md`) + Commerzbank strongest-CHF FX-value pull. Both non-blocking.

Cascade. bi-analytics-main only (engine + 4 regenerated artifacts + report.py/_refresh_bias_table.py logic + 3 docs); no cascade into other carriers (only Güll changed; the matrix re-ran all engines at current code as a side effect of the canonical rebuild).
Main-brain changes. This S225 quest-log entry, the `guell-2.0.0-build-resume__9f716f1f.md` inventory resume, one examine draft (tar-extract lesson), one memory file, the comms OPEN+CLOSING, and the [[S223_6ae2971e_guell-reply-review-doc-sync|S223]] graduation move.
