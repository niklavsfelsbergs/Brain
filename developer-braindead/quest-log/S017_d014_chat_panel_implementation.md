# S017 — 2026-05-21 — D-014 chat panel implementation + OSRS restyle

- Implemented [[D-014_visualizer_chat_panel]] end-to-end across hook, renderer, protocol, and gitignore. Hook (`developer-braindead/.claude/hooks/emit-event.py`) bumps `INTENT_MAX_LEN` 60 → 100, adds `handle_narration_write` for `.claude/narration.txt` → `narrate` event, and replaces tool-call `log` emissions with typed `action` events for Edit/Write/MultiEdit/NotebookEdit/Bash/Glob/Grep (Read intentionally emits move only — no chat noise). Bash gets its own branch (no path → no move, just an action). `spawn-dwarf` / `despawn-dwarf` log msgs reworded to `* D1 spawned by parent — desc` / `* D1 returns to parent`. `Bash` added to brain-root `.claude/settings.json` PostToolUse matcher so it actually fires.
- Renderer (`experiments/visualizer/index.html`) gained `applyEvent` cases for `intent` (chat line + bubble), `action`, `narrate`; muted `* X walks to Y` line on every move event; `wrapBubbleText` + rewritten `renderIntent` for two-line centered bubble at cap-100; new `logChatLine` helper wraps actor name in a `.user` span so per-speaker color cascades from CSS. History cap 200 → 400 lines.
- Main-brain `gielinor/meta/communication-protocol.md` updated: cap 60 → 100 (two-line bubble), new "Narration channel" subsection (system voice, examples), new "Intent vs action" discipline rule (intent = why/scope, action = which file/command), `wisp.txt` → `braindead.txt` drift fix for dev-brain mode. `.gitignore` adds `.claude/narration.txt` alongside existing intent sidecars.
- **OSRS restyle iteration.** Original styling shipped dark/parchment-tinted; user wanted classic RuneScape chatbox. Iterated through VT323-bold-on-dark (too pixelly at scaled sizes — VT323 is a bitmap font and interpolation degrades it), then Trebuchet MS bold (smoother, closer to OSRS chat font on Windows), then user-requested light-tan background with black body text. Final palette: `#c8b48a` panel with `#d0bc94→#beaa82` body, `#1a0f06` text, darker per-actor username tints (Jebrim deep blue `#1f4b8a`, Zezima rust `#a44a14`, Dwarves brown-amber `#8a5a10`, Braindead teal `#1a6660`, Wisp olive-gold). Header stays dark wood for contrast. Box height 170px → 340px. Lesson: emulating a specific UI's look (OSRS) means font *and* palette must change together — half-measures (dark bg + smooth font) read as uncanny.
- **Spec deviations worth flagging.** [[D-014_visualizer_chat_panel]] called for a "new `#chat-panel` div"; the existing COMMS logbox already provides per-actor filtering, scrolling, color coding, so we *evolved* COMMS instead of duplicating. D-014 also specified `PreToolUse` for action events; we kept `PostToolUse` (symmetric with all other tool telemetry) so failed/rejected tool calls don't chatter the chat. Both deviations are improvements rather than concessions, but they make the design doc lightly stale.
- **Smoke-test catch.** First Bash test attributed to stale `jebrim` intent in dev-brain mode (the recency walk in `infer_dwarf_parent` locked onto a 600s-window intent from a prior gielinor session). Tightened `current_main_actor()` to hard-prefer `braindead` when `active-mode.txt == dev-brain`, recency walk only applies outside dev-brain. Worth holding the bias: any heuristic that reads back through `state.ndjson` needs to consider that the stream is cross-session and stale entries inside the recency window can fool it.

**Carried observation.** Spec docs that prescribe DOM/structure (`new #chat-panel div`) without inventorying what already exists in the renderer will ship suboptimal designs. [[D-014_visualizer_chat_panel]] would have been tighter if it had read `index.html` first and named the existing COMMS panel as the surface to evolve. Pattern: when designing a renderer-side change, the design phase should include a "what's already there" scan. Cheap to add; would have saved a re-read this session.

**Browser-side verify still pending.** Smoke tests pass; live mode not yet eyeballed. The chatbox restyle was iterated against screenshots, so it's been seen, but the dwarf-attribution + new action/narrate streams haven't been exercised against a real Jebrim Task spawn yet — that was Step 1 from S016's close (`agent_id` attribution in the wild) and remains the first thing the next session should verify before declaring D-014 fully landed.

**Cascade.** [[S017]] (this entry), `respawn.md` updated, `developer-braindead/.claude/hooks/emit-event.py`, `developer-braindead/experiments/visualizer/index.html`, brain-root `.claude/settings.json`, brain-root `.gitignore`.
**Main-brain changes.** `gielinor/meta/communication-protocol.md` (cap, narration channel, intent-vs-action discipline, wisp→braindead drift fix).
