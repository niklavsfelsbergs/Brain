# S116 — 2026-05-28 — Khaan item G judgment call (HELD)

Dev-brain via "lets develop gielinor", mid-conversation; OPEN posted; no live Braindead siblings (S112/S113/S114/S115 all CLOSED; only stale intent `braindead-2fbde8af` ~40min, discussion-only cockpit token-usage — no overlap). sid `caa72357`.

- **The ask (S115 hand-off):** implement Khaan recommended-sequence **step 2's remaining half — item G (phase-registry mutation guard)** — a 4th check on the existing [[S115_a9fe4749_khaan-failure-receipts-and-hygiene|S115]] `developer-braindead/verification/check.py`. The principal flagged a judgment call up front: *G pays off on an ordered code pipeline (which we don't have), and applied to the respawn markdown load-order it's the brittle structure-check kind* — so **decide with him** whether to build G-on-respawn now or hold it.
- **Grounded the call** against G's spec (catalogue HTML `2026-05-28-khaan-learnings-implementation.html`, lines ~498–509 + matrix), `check.py` (its docstring already deferred markdown-structure checks as brittle — lines 26–29), and `plan.md §P`. Read: G guards an *executable* sequence against silent refactor reorder; we have no code pipeline (§C chain is the much-later phase), and the only ordered surface today is `respawn.md`/`respawn-ritual.md` *markdown* — which churns every close, so a byte-hash would false-trip constantly and re-introduce exactly the brittleness [[S115_a9fe4749_khaan-failure-receipts-and-hygiene|S115]] deliberately avoided.
- **Decision (principal-confirmed via multiple-choice): HOLD G until a code pipeline earns it.** Recorded the deferral + **revival trigger** (first ordered code pipeline lands → hash *that*) in `bank/plan.md` §P.5 (`[-]`). `check.py` left unchanged at C1–C3. No build.
- **Stale-framing catch on the rest of the sequence:** step 3's "never ship gate 1 without anti-lockout H" is no longer outstanding — gate 1 (OPEN-on-entry, [[D-033_positive_enforcement_gate_open_on_entry|D-033]]) already shipped and is *self-escaping* (satisfy = post OPEN, always allowed; fail-open), so H is only mandatory when a *trapping* gate (e.g. a grounding gate) is added. Triaged the full 7-step sequence at the principal's request before close: steps 1–2 done (G now resolved), step 3 effectively done, steps 4–6 unbuilt, step 7 (autonomy) deferred per the much-later-phase steer. Best-ROI unbuilt items: the two cheap carries (write-rules godly proposal; extend `close_check.py` to the gielinor player close) + item 6 context-hash freshness.

**Cascade.** `bank/plan.md` (§P.5 — item G held, `[-]`, with revival trigger); this quest-log `S116`; `respawn.md` (Last-updated prepend + ★ NEXT refresh: G resolved → point to remaining carries/items); `comms/active.md` (OPEN + CLOSING).

**Main-brain changes.** none (no `gielinor/` writes this session — decision + dev-brain doc only).
