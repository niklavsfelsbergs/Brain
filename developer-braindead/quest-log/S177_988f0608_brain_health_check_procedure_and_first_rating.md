# S177 — 2026-06-09 — Brain health-check procedure + first scored rating

- Niklavs asked for a prompt/plan to do an in-depth health check of how the brain operates + a rating, then "do it" → "perform the health check." Built the reusable procedure first, then ran it end-to-end.
- **Built [[brain-health-check]]** (`developer-braindead/spellbook/brain-health-check.md`) — a scored audit: 7 weighted dimensions (structural integrity, enforcement & six guarantees, rule adherence, knowledge hygiene, knowledge retrieval, context economy, active-state & drift), 0–5 rubric → weighted Health Index /100 + grade, a required judgment overlay (don't mechanize the verdict), an instrument map, a snapshot-for-trend step, and a copy-ready runnable prompt. Indexed it in [[_about]] (spellbook). Successor to the ad-hoc [[S060_brain_self_audit_and_plan_reconciliation|S060]]/[[S110_144c0ca2_brain_full_audit|S110]]/[[S142_a31a2bc0_brain_health_audit_and_gitignore_hygiene|S142]]/[[S145_543c6caf_knowledge_loading_and_rule_adherence_audit|S145]] audits.
- **Ran the health check.** Instruments: `brain-weight.py` (@import chain ~12.2k tok, flat since 06-03, ~49% off the 23.3k peak), `check.py` 3/3, `hook-manifest-check.py` 38/38 OK, `adherence-rates.py` (OPEN first-try **72%**, promote rate 91%), `ritual-stats.py` (2265 events). **Live-fired all six guarantees this run** — block-confirmed (exit 2), block-deletes (exit 2), block-sub-spawn (exit 2), draft-gate (redirect), plus close-gate (12 real blocks) + forced-read (342 session-start / 31 player-inject) from the event log; check.py C3 fired the 4 boundary hooks live. Two Explore corpus passes (structural+drift; hygiene+active-state). Regression set re-scored **5/2/3** (best yet).
- **Rating: Health Index 83/100, Grade B** — snapshot written to [[brain-health-snapshots|switchboard/brain-health-snapshots.ndjson]] (first datapoint). Strong: structure (5), enforcement (5), context economy (5). Top issues by blast radius: (1) **Jebrim active-state regrowth — 44 in-progress / 23 inventory** (from ~3/4 at [[S158_dda4c966_jebrim-active-state-alching-cleanup|S158]], 6 days ago — poisons respawn + X.4 resume-pickup); (2) OPEN first-try 72% vs the ~100% `CLAUDE.md`/`respawn.md` *claim* (gate backstops → 0 never-posted, so non-harmful but the doc claim is now fiction); (3) ~10 unpicked Jebrim research files + Zezima's 278B examine stub (alching picking overdue).
- **Stale-backlog finding:** the dev backlog's "live-fire debt X.2/X.3/X.4/domain-cue — need a real player session" is substantially **retired in practice** — the event log shows `forced-read player-inject=31`, `draft-gate redirect=8`, `domain-cue:deploy-schema=14` all fired in real sessions. That open item can be closed/downgraded.
- **Answered the alching-vs-bankstanding routing question** (consultation, read `alching.md` + `bankstanding.md`, not from memory): Jebrim's tending is **alching**, always run in his own namespace (self or gnome); Guthix's bankstanding is one trigger (Phase 0) but **defaults to skipping a player with in-progress quests** — so it would skip Jebrim's 44-quest state. Refined the highest-leverage fix: the trigger isn't missing, it's blocked by the in-progress bloat → graduate done EU-tender quests to `completed/` first, then explicit `Hey Jebrim, /alch`.

**Cascade.** NEW `developer-braindead/spellbook/brain-health-check.md`; edited `developer-braindead/spellbook/_about.md` (index line); NEW `switchboard/brain-health-snapshots.ndjson`; `bank/build-lessons.md` (+1); `respawn.md`, this quest-log, `comms/active.md` CLOSING.
**Main-brain changes.** none — dev-namespace authoring + read-only audit of `gielinor/`; no `gielinor/` writes (live sibling braindead-e0f6de1a held the Guthix/NFE rulebook work; zero overlap).
