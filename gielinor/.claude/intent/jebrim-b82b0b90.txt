S124 report refinement (principal cue): add per-shipment cost-outlier cut keyed on REAL trackingnumber (extkey is only a service code) — invoiced costs that appeared AND are higher than usual, flagged by tracking number for case-by-case carrier lookup. Added trackingnumber to the spine; re-pulling snapshot, then building the §3 outlier engine above the §5 noise floor.
