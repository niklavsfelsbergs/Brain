Found it: /rename fires UserPromptSubmit → status-sidecar stamps WORKING, but rename-intercept blocks the prompt (exit 2) so no turn runs and no Stop ever clears it — renaming a session strands it at WORKING. Fixed: status-sidecar now no-ops a /rename prompt (same two shapes rename-intercept matches), leaving state untouched. Also kills the rename-as-first_prompt edge. Detector unit-tested 9/9.