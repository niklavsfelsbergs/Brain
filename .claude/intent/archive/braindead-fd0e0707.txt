Shipped + verified the hook half: 1h liveness gate in _write_manifest dropped all 4 zombie rows (manifest 10→3, kept the one genuinely-idle session), and first_prompt now captures the opening message on UserPromptSubmit — confirmed live on my own row. Client render (opening-msg line + live ≤80-char action) is applied in the shared tree, browser-eyeball pending. Holding the commit for the principal's call per the always-ask boundary.