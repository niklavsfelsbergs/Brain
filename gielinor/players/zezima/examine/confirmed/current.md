# Zezima — examine/confirmed/current.md

> Read at respawn (when Zezima is active). The rolling, in-force self-model for Zezima as a character. Under size budget (~3k tokens). User-only; the agent proposes via `drafts/`.

*(empty — grows through observation-backed drafts)*
