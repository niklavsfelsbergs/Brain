# S021 — 2026-05-21 — Visualizer audit (Step 1)

- **Deliberate sweep over the visualizer's accreted state** across [[S008_iso_visualizer_v0|S008]]–[[S020_gnomes_ratification_and_visualizer|S020]]. Targets: `developer-braindead/experiments/visualizer/index.html` (~2933 lines), `developer-braindead/.claude/hooks/emit-event.py` (630 lines), and the state files (`state.ndjson`, `state-actors.json`, `state-dwarves.json`, `path-map.json`). Read both target files end-to-end rather than spot-checking — needed the whole picture to surface cross-file invariants.
- **Output landed at [[visualizer-audit-S021]]** (`developer-braindead/bank/research/visualizer-audit-S021.md`). 15 bugs, 7 consistency issues, 18 improvements, 4 doc gaps. Each finding cites file:line. No fixes in this pass — per respawn's discipline, fixes are next session.
- **Highest-priority finds.** (a) **B1** — FIFO `pendingAgentBind` in `emit-event.py:548-577` misattributes sub-calls when two spawns fire close together and the first returns instantly. Latent today; first parallel-dwarf-spawn pattern surfaces it. (b) **B3** — `infer_dwarf_parent` (`emit-event.py:146-198`) lacks the dev-brain override that `current_main_actor` has; early-session dev-brain spawns can attribute to a phantom player from the prior gielinor session within the 10-min recency window. (c) **B7/B8** — despawn-on-crash leak + non-atomic state writes; architectural debt that compounds as sub-agent usage scales.
- **Most-easily-fixed finds.** B2 (parse-failure vs window-past sharing the same `break` — two-line change), B13 (`deriveSpeaker` regex capped at `D[1-3]`/`G[1-3]` while `speakerFor` is unbounded — already-relevant: `state-dwarves.json` at `nextId: 8`), C2 (Braindead has `data-speaker` styling but no COMMS tab/filter/dot/legend row).
- **Documentation debt.** D1 — `_README.md:29-34` still lists "Steps still pending (per [[D-009_visualizer_live_mode_v0|D-009]])" for work that shipped [[S010_visualizer_live_mode_v0|S010]]–[[S020_gnomes_ratification_and_visualizer|S020]]. Pre-dates [[D-014_visualizer_chat_panel|D-014]], [[S012_braindead_character_and_workshop|S012]], [[D-016_gnomes_subagent|D-016]], narration channel, active-mode marker, and the whole sub-agent attribution model.
- **Pairing recommendation in the note.** The audit + the deferred first-live-gnome spawn pair naturally. Step 2 exposes preconditions B1/B3/B6 that the static read can't see, and a Jebrim alching gnome exercises the boundary hook end-to-end. Next session can do either order — fixes-then-spawn (fix the leaks, then validate live) or spawn-then-fixes (let live behavior shape which fixes matter most).

**Cascade.** New: `developer-braindead/bank/research/visualizer-audit-S021.md`, `developer-braindead/quest-log/S021_visualizer_audit.md`. Modified: `developer-braindead/respawn.md` (by close-session).
**Main-brain changes.** none.

## Carried-over observations

- **The audit-then-validate pattern worked.** Reading both files end-to-end before running anything live surfaced things a spawn-and-watch wouldn't have caught (the FIFO race in B1 was a static read; no live test would have hit it yet). The respawn note had it right: do the deliberate sweep first, then the validation pass. Worth canonicalizing as a pattern for any future "infrastructure has accreted, audit it" moment.
- **Cross-file invariants need an explicit list.** The audit found ~6 surfaces enumerating the same 10 buildings (path-map.json, BUILDINGS, STAND, BUILDING_DESC, LABEL_Y_OFFSET, +1) — all in sync today, but the cost of adding an 11th building is touching 6 places. Worth considering a single config-shape that radiates outward.
- **The audit is uncommitted with a fresh principal-cued direction.** Closing handed off cleanly to "next session does the fixes" — no half-built work to reconcile. Cleanest dev-brain close since [[S013_close_session_harvest_pump|S013]].
