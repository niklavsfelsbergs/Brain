# OPEN — brain underutilization frame

**Opened.** 2026-05-22 (chat session, no specific quest).
**Status.** Open. Frame captured; "improve" work flipped to dev-brain.

## Why this exists

Principal opened a chat with Jebrim ("just want to chat"), pivoted to "we are underutilizing a lot of things in the brain — discuss then improve." Frame surfaced from Jebrim's desk before the dev-brain flip. Captured here so it doesn't evaporate.

## The frame — what's underutilized

1. **Drafts → confirmed promotion gate is stalled.** Jebrim's `examine/confirmed/current.md` empty. Jebrim's `niksis8_character/confirmed/current.md` empty. Global `examine/confirmed/current.md` empty. Global `keepsake/current.md` empty. S034 alching parked all 8 drafts — zero promotions, zero rejections. Drafts is functioning as a dead-letter queue, not a holding pen for review. The capture half works; the promote half doesn't run.

2. **Bank capture is post-hoc, not in-flight.** Premise is "bank grows through use." Practice: quest-log captures the narrative, bank notes don't get written alongside. Capture should be a turn-level reflex, not a session-close harvest. `bank/notes/` is thinner than the work justifies.

3. **Close-session discipline has slipped.** 11 in-flight inventories for Jebrim (S001, S002, S015, S023, S024, S026, S031, S032, S034, plus dashboard-agent and main-merge-AWS OPENs). Some are likely actually done — never walked to `completed/`. Resume foreground is fragmented; respawn is noisier than it should be.

4. **Consultation / Guthix is barely visited.** Designed precisely so cross-cutting questions land in the right voice with full read across players. Practice: cross-cutting questions get asked to whichever player is active (this conversation was an example — system-level brain question asked to Jebrim). Guthix's `bank/notes/` empty.

5. **Skills graduation stalled too.** Same shape as drafts — capture works, promotion doesn't.

## The structural read

Architecture is precise and write paths are exercised. *Promote* and *consult* paths aren't. Drafts/proposals are write-only in practice. That's the underutilization in one line.

## Routing flag (raised before flip)

Fixes to `meta/`, `spellbook/rituals/`, hooks, or architecture itself are godly proposals — Guthix's territory in bankstanding mode. Alternatively, dev-brain mode (Braindead) for the construction work. Principal chose dev-brain. This entry preserves the frame so Braindead can read it on entry without re-deriving.

## Hand-off

- 2026-05-22 — chat with principal; produced underutilization frame above. Trigger `lets develop gielinor. This is more the devs rea[lm]` → flip to dev-brain (Braindead). No pending action in flight on Jebrim's side. Frame above is the load for the dev-brain session.
