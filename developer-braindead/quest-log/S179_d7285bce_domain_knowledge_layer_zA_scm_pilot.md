# S179 — 2026-06-09 — §Z.A: per-player domain-knowledge layer stood up + SCM pilot digest

Dev-brain via "lets develop gielinor" (fresh sid `d7285bce`, reopened after the [[S177_988f0608_brain_health_check_procedure_and_first_rating|S177]]/[[S178_b472d014_cockpit_wrappedup_interrupt_transcript_pilljump|S178]] closes). OPEN→CLOSING via `comms_append`; no live Braindead siblings. Heavy parallel Jebrim WIP on disk (shipping / EU-tender quests + drafts) — only **read** his 6 SCM bank notes as synthesis source, committed by pathspec, nothing of his swept.

- **Built §Z.A** — the first build phase of the per-player domain-knowledge layer (`developer-braindead/bank/plan.md` §Z, designed [[S177_988f0608_brain_health_check_procedure_and_first_rating|S177]]). NEW `gielinor/players/jebrim/bank/domains/` with three files:
  - **`_about.md`** — the layer spec: two-tier model (always-on `_index.md` map + cue-loaded per-domain digests), the digest **frontmatter schema** (`domain / title / patterns / corpus / specialist / freshness / synthesized`), the `_`-prefix = infra-not-digest **loader convention**, the **~2 KB budget** (a domain that won't fit is the signal to *split*, not grow), and the alching-owned **born / tended / surfaced** lifecycle. Retains the global `cue_registry.py` for external/specialist domains; `bank/domains/` is primary for a player's *own* topics, auto-discovered from frontmatter.
  - **`_index.md`** — the always-read map: SCM as the one digested domain + EU-tender / shipping-mart / carrier-contracts on the "not yet digested" Z.bootstrap worklist.
  - **`scm.md`** — the pilot digest (~2.4 KB), **content signed off by Niklavs**, synthesized from 6 SCM bank notes ([[shipping_costs_monitoring_nextjs_vocab]] the anchor, [[scm_alerts_entity_split]], [[scm_nextjs_duckdb_oom_modes]], [[2026-06-02-scm-serving-node-oom-mode]], [[shipping_costs_dashboard_csv_export_architecture]], [[dashboard_and_shipping_agent_convergence]]). Covers: two-runtime architecture, the `cost_for_routing` basis + 11-bucket invariant, the alert engine + ORWO/TCG entity split, the three-OOM-mode family, CSV export + deploy ordering. It is the Z.B/Z.C test fixture **and** the template every later digest copies.
- **Updated `bank/plan.md` §Z** — status → in-progress; **Z.A marked `[x]` done** (corrected the note count 8→6 — the plan over-counted; the vocab note carries the "frontend/pipeline" surface itself); recorded the landed frontmatter schema + budget.
- **Added Z.deepen as the last §Z step** (Niklavs-flagged, *"remember this"*): a digest is only as good as its corpus — the bootstrap digests are synthesized from *existing* notes, but some domains' clusters are thin/stale, so the digest stays shallow. Z.deepen is the deliberate deepening pass: per domain, judge corpus sufficiency, re-investigate live sources (repos / penguin / mart / unpicked `research/`), fill gaps as new bank notes, then re-synthesize. Flagged **needed, not optional polish** for any domain whose digest reads thinner than the work warrants (EU-tender, carrier-contracts likely first); SCM's corpus is comparatively rich.
- **No learning harvested** — routine build, no principal correction or reverted change. The 8→6 note-count was a plan over-count I corrected in place, not a misjudgment worth a build-lesson. The reusable design (frontmatter schema + loader convention) is captured in `_about.md` itself.

**Cascade.** `developer-braindead/`: NEW `quest-log/S179_d7285bce_domain_knowledge_layer_zA_scm_pilot.md`, `bank/plan.md` (§Z status + Z.A done + new Z.deepen step), `respawn.md` (new Last-updated), `comms/active.md` (OPEN + CLOSING via `comms_append`).

**Main-brain changes.** NEW `gielinor/players/jebrim/bank/domains/{_about.md, _index.md, scm.md}` — a new layer in Jebrim's bank (semantic-memory content, not identity/`confirmed/`; `scm.md` principal-signed-off). No `meta/` / ritual / hook / `confirmed/` writes; the `@import` chain is **untouched** (digests are cue-loaded, the always-on index arm is Z.B, not yet built — zero eager weight added this session).
