# keepsake/current.md — global always-surface pins

> Read at respawn, before identity layers. The pinned items that matter every session regardless of active player. Under size budget (~2k tokens). User-only; the agent proposes via `proposals/`.

*(empty — pin the items that must surface every session, regardless of player)*
