# S022 — 2026-05-21 — Visualizer audit fixes

- Landed audit-fix bundles 1–6 from [[visualizer-audit-S021]]: hook bugfixes (B2 parse-failure continue, B3 dev-brain override in `infer_dwarf_parent`, B6 stderr trail on silent attribution fallthrough, B14 append-before-save on mode transition), renderer fixes (B10 `ensureActorExists` warns on unknown actors, B11 `resetWorld` clears `currentBuilding` fully, B12 clears `currentEventWallTime`, B13 `deriveSpeaker` regex unbounded `D\d+`/`G\d+`), Braindead COMMS first-class (C2 tab/filter/dot/legend, C4 dead `.log-entry.write` pruned), state-file robustness (B8 atomic temp+rename in `save_json`, B7 `gc_stale_subagents` with 1h threshold + `spawnedAt` field, B4 stale-marker detection swapping the chat line for a narrate, B1 LIFO+warning replacing FIFO `pendingAgentBind`), color taxonomy consolidation (C1 `--<actor>-text` / `-dot` / `-swatch` CSS vars across chat user prefix, tab dots, speaker bullets, legend), docs (D1 `_README.md` rewrite covering [[D-014]] / [[D-016]] / narration / active-mode / sub-agent attribution; D2 event vocabulary table; D3 path-map precedence notes; D4 `ROLE_CONFIG` docstring). Deferred B5/B9/B15 with documented reasons; confirmed C5/C6/C7 as non-findings.
- Caught two real bugs mid-audit-validation that the static read missed: **Bash writes to sidecar files bypass `handle_active_mode_write` / `handle_intent_write` / `handle_narration_write`** because Bash isn't a `WRITE_TOOL`. The respawn ritual's `echo > .claude/active-mode.txt` left `_mode` stale, no `spawn-braindead` emitted, narration silent, intent bubbles never appeared. Fixed by adding regex-driven sidecar dispatch inside `handle_bash` (with dedupe per-path) — validated live by repairing the missing `spawn-braindead` event for this session.
- Caught a second-order bug: **`active-mode.txt` is a single file at the brain root, shared across all parallel Claude sessions.** When this dev-brain session set the marker, a parallel Jebrim session inherited it and its bi-analytics Bash commands attributed to Braindead via the dev-brain override. Narrowed `current_main_actor()` to prefer the recency walk over the dev-brain marker (the marker is now a fallback, not a hard override); extracted `_walk_recent_actor()` so `infer_dwarf_parent` can keep its dev-brain short-circuit for spawn-parent inference. New load-bearing convention: intent narration must be written regularly per turn (already required by `meta/communication-protocol.md`), since the most-recent intent now wins attribution races between parallel sessions.
- Parked future visualizer aliveness work as [[Q-008]] (idle sprite breath, per-building ambient particles, day/night hue, NPC wanderers, trail echoes). Trigger condition: ship audit fixes + first live gnome spawn first, so the attribution layer is solid before adding more motion. Recommendation captured: start with idle breath + ambient particles, all gated behind `prefers-reduced-motion`.
- Pattern surfaced: **audit-then-validate finds different bugs than either alone.** The static read produced 15 bugs + 7 consistency findings + 18 improvements. Two of the most disruptive bugs (Bash sidecar bypass, cross-session active-mode bleed) only surfaced when *running the fixes* exposed weird stream behavior. Candidate for [[Q-007]]-adjacent or a new `bank/decisions/` note at next bankstanding; reinforces the [[S020]] observation that architectural guarantees need a live failure test.

**Cascade.** `developer-braindead/.claude/hooks/emit-event.py` (B2/B3/B6/B14, B8/B7/B4/B1, D4, Bash sidecar dispatch, cross-session attribution narrowing), `developer-braindead/experiments/visualizer/index.html` (B10/B11/B12/B13, C2/C4, C1), `developer-braindead/experiments/visualizer/path-map.json` (D3), `developer-braindead/experiments/visualizer/_README.md` (D1+D2 rewrite), new `developer-braindead/bank/open-questions/Q-008_visualizer_aliveness.md`, this quest-log entry, `respawn.md` refreshed.

**Main-brain changes.** None.
