# B-011 — scoped bankstanding: land the "render the cut" proposal

**Date.** 2026-05-30. **Shape: scoped bankstand**, flipped from consultation on the principal's explicit `bankstand` cue. Not a full pass — Phase 0 (per-player alching) and the 7-step cross-cutting sweep were **deliberately skipped**; the flip's sole purpose was to gain the write-reach to draft and land one godly proposal. Skip flagged to the principal at the flip, not silent.

## What this pass did

Drafted and landed the **§R.3 godly proposal** — *render the cut, not the keystrokes* — the third and final finding from the principal-staged Braindead↔Jebrim self-review conversation (dev-brain plan §R; the other two findings, comms append-lock and graduation = clerk-not-nanny, were already built in the dev brain).

- **Observation.** The Intent bubble drifts to rendering *what's happening* (a keystroke echo) when the load-bearing signal in a multi-step turn is *the cut* — the decomposition reasoning. The existing *Intent vs action* rule already forbade echoing actions but framed "why" as one undifferentiated thing, blurring scope-why (which the bubble can hold) from the cut (which it can't, in ≤280 chars).
- **Change landed.** One refinement to `meta/communication-protocol.md` → *Intent vs action — discipline rule*: the Intent bullet now names **scope-why** as a cockpit status line, and a new paragraph (**"render the cut, not the keystrokes"**) routes the decomposition reasoning to the Understanding/Plan preamble + task-list, where the principal reads it. Backward-compatible; preserves the in-voice scope-status the voice cards require.
- **Authority.** `meta/` is user-only; applied directly under [[D-017_user-only-with-explicit-permission|D-017]] explicit permission (principal chose "apply it now" via multiple-choice). Proposal moved to `deities/guthix/proposals/archive/2026-05-30-render-the-cut-not-keystrokes.md`. Committed `cb01e32`.

## Outcome

**§R is fully closed** — all three findings from the Jebrim conversation are now built/landed. No other bankstanding work attempted this pass.

## Left for a full pass (whenever next cued)

The standard cross-cutting sweep was not run: Phase 0 alching of Jebrim/Zezima (both have recent activity), inbox triage, global drafts review, size-budget checks, rejected-pattern review. None urgent; surfaced here so the next full bankstand knows this pass didn't touch them.
