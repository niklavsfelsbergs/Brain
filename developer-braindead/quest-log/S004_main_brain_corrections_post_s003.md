# S004 — 2026-05-20 — Main brain corrections: lorebook redefined, three-modes axis added, audit iterated

Same calendar day as [[S003_main_brain_phase_1_scaffold]]; this session followed directly. Built the `gielinor-audit.html` Niklavs asked for at the end of [[S003_main_brain_phase_1_scaffold]], then he reviewed it and surfaced four corrections that needed to land before the brain runs on real work. The corrections were not nits — two were genuine design refinements made after the original prompt was sent that hadn't crossed into the build, and two were tightenings of existing content.

- **Built the audit tool.** `gielinor-audit.html` shipped at brain root: single self-contained HTML, dark/dense/monospace, collapsible tree, drift-flag callouts on every file where the builder made non-obvious judgment calls (44 flags at first pass), per-file purpose-vs-content view, in-page wiki-link and path resolution. Niklavs reviewed and asked for a second iteration: keep the structural audit, add a linear "File Contents — verbatim" section with every load-bearing file's literal contents in spec order. Done. Generator script lives at `developer-braindead/../build_audit.py` (project root), re-runnable as the brain changes.

- **Correction 1 — lorebook redefined as self-improvement log.** Niklavs flagged that the lorebook's purpose had narrowed between the original design prompt and now: it is the agent's self-improvement log, not a build log. Construction history belongs in the dev brain. Restructured `gielinor/lorebook/` from `{drafts, decisions/, assumptions.md, patch-notes.md, _about.md}` to the identity-layer pattern `{drafts, confirmed, archive, rejected, _about.md}`. Moved [[D-001_two_brain_split]] and [[D-002_folder_name]] (main-brain decisions), `patch-notes.md`, and `assumptions.md` out of `gielinor/lorebook/` and into `developer-braindead/bank/main-brain-construction/` with an `_about.md` explaining what's there and why it moved. Rewrote `gielinor/lorebook/_about.md` with the new framing — "decided by me about myself, distinct from construction history." Updated `meta/write-rules.md` (collapsed three lorebook rows into one identity-pattern row), `dwarf-write-boundary.py` hook (removed `lorebook/patch-notes.md` from the allow-list; lorebook is now principal-only entirely), and the master `CLAUDE.md` layer index.

- **Correction 2 — player invocation rule, gap in `players/_about.md`.** The address-based invocation rule already landed in [[S003_main_brain_phase_1_scaffold]] as main-brain [[D-002_player_invocation_by_address]] and was encoded in master `CLAUDE.md` and `spellbook/rituals/respawn.md`. The audit had surfaced one inconsistency: `players/_about.md` still described a preemptive "which player?" prompt at session start. Rewrote that section to match the address model. Also strengthened the cross-player dwarf invocation in master `CLAUDE.md` and `players/_about.md` to enumerate trigger phrases (`ask {name} to ...`, `have {name} ...`, `get {name} to ...`, `let {name} ...`, `{name} should ...`).

- **Correction 3 — bankstanding is its own session mode + three-modes axis.** Bankstanding was framed as a ritual but not as a mode. Niklavs wanted it explicit: bankstanding is a third session mode alongside player mode and unscoped mode, with cross-cutting reach into every layer. Substantially rewrote `meta/modes.md` to add a "Session modes — what kind of session is this?" section at the top with three modes (player, unscoped, bankstanding), declared orthogonal to the existing principal/dwarf role axis (renamed from "modes" to "roles" to free up the word). Rewrote `spellbook/rituals/bankstanding.md` to lead with "bankstanding is its own mode" and explicit reach; added a new step 3 (cross-player synthesis — promote recurring per-player patterns to the global layer); restructured the procedure into 8 steps; removed old steps 7-8 (update assumptions, append patch-notes) since those files no longer exist; replaced with "if anything in this round changed how the agent operates, log it in lorebook/drafts/."

- **Correction 4 — four tightenings.** (4a) Inbox 4-week age limit — already encoded in `players/inbox/_about.md` from [[S003_main_brain_phase_1_scaffold]], confirmed no change needed. (4b) Rejected drafts are data — added an explicit "Rejected drafts are data" section to global `examine/_about.md`, `niksis8/_about.md`, the new `lorebook/_about.md`, per-player `examine/_about.md` for both Zezima and Jebrim, and per-player `niksis8_character/_about.md` for both. Each section is scoped to that layer (system-level vs character-level vs Niklavs-model). (4c) Reconciliation prompt — refined the section in `respawn.md` to surface quest title + last completed turn + last `pending` action by name, with three explicit options (resume / abandon / reconcile externally first), and an explicit "do not auto-resume" rule. (4d) `ticks.md` metaphor — already encoded from [[S003_main_brain_phase_1_scaffold]], confirmed no change needed.

- **Audit regenerated.** `build_audit.py` updated: removed META entries for the four moved files, rewrote annotations for every changed file (lorebook, modes, respawn, bankstanding, players, CLAUDE.md, write-rules, the six identity-layer _abouts) to call out what changed in S004 and what to verify. CONTENT_GROUPS pruned. "Start auditing here" list refreshed. `gielinor-audit.html` regenerated: 99 files / 80 dirs / 42 drift flags.

**Cascade.** Dev-brain files written this session: this quest-log entry; new `developer-braindead/bank/main-brain-construction/` folder holding the four relocated files + an `_about.md`; `respawn.md` overwritten with new "where we are" + "next concrete step." `plan.md` left intact — §B remains `[x]` done; S004 corrections don't reopen the foundation, they refine it. No new D-NNN dev-brain decision recorded — the S004 corrections are user-side product decisions implemented by me in one session, not dev-brain meta-choices. They live in this quest-log entry and in git history.

**Main-brain changes.** Substantial. Files moved out: 4 ([[D-001_two_brain_split|D-001]], [[D-002_folder_name|D-002]], patch-notes.md, assumptions.md → now in dev brain). Files rewritten in place: `lorebook/_about.md`, `spellbook/rituals/bankstanding.md`. Files significantly edited: `meta/modes.md`, `meta/write-rules.md`, `spellbook/rituals/respawn.md`, `players/_about.md`, master `CLAUDE.md`, six identity-layer `_about.md` files, the `dwarf-write-boundary.py` hook. New scaffolding: `lorebook/{confirmed,archive,rejected}/.gitkeep`. Net main-brain delta from end of [[S003_main_brain_phase_1_scaffold]]: −4 files moved out, −1 directory removed (`lorebook/decisions/`), +3 directories added (the new lorebook subdirs). Audit count went from 56 → effectively 51 substantive files (the new `.gitkeep`s are placeholders, not content).

**Discipline observation.** This session validated the pattern Niklavs flagged in [[S003_main_brain_phase_1_scaffold]]: he reviewed the build and surfaced corrections within hours of it landing, before content accumulated. The audit tool earned its weight — drift flags led the eye directly to the spots that needed adjustment. The pattern to capture: **build the verification surface alongside the artifact, not after.** Worth a future `examine/` draft candidate.
