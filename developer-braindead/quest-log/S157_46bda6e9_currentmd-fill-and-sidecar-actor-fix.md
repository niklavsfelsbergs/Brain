# S157 — 2026-06-03 — §Y.2 current.md summaries fill + status-sidecar actor-misresolution fix

Dev-brain via "lets develop gielinor"; OPEN→CLOSING via `comms_append`; no live Braindead siblings ([[S156_c69fce82_agents-md-codex-entrypoint-sync|S156]] `c69fce82` closed). Built plan [[plan|§Y.2]] — the hollow half of [[S150_26ef6430_session-start-forced-read|X.4]]'s forced-read — and fixed the actor-resolution defect that blocked it mid-build.

- **§Y.2 — filled the three hollow load-bearing `current.md` stubs**, all curated from *existing confirmed material* (nothing invented), principal-approved as identity-shaped work (drafts shown for sign-off first, not a unilateral `confirmed/` write):
  - [[keepsake_current|gielinor/keepsake/current.md]] (global, 332 B→2.4 KB) — the surface X.4 force-injects every session: 3 cross-player operating reflexes (verify-the-thing · anchor-to-existing-state · grounding-precondition) + the "address Niklavs as you" register pin. Deliberately tight (it loads every session); pointer-pins into the examine entries.
  - [[examine_confirmed_current|gielinor/examine/confirmed/current.md]] (global, 345 B→2.8 KB) — the 4 graduated cross-player reflexes as a rolling self-model.
  - [[jebrim_examine_confirmed_current|gielinor/players/jebrim/examine/confirmed/current.md]] (278 B→6.5 KB) — his 38 confirmed examine entries clustered into 8 themes (empiricism · grounding · data-analysis · re-rating · search/source · parallel-git · deliverable form · calibrations), each one tight line + a wiki-link to its dated anchor (distilled via an Explore sub-agent, curated here).
  - Promoted the pending global draft `2026-06-01-glob-results-are-claims-not-ground-truth` → `confirmed/` (folded into the verify-the-thing parent). Committed **`496bad2`** (pathspec).
- **Caught + fixed the defect that blocked Y.2 — `status-sidecar.py` actor-misresolution.** The first `Write` to `confirmed/` was BLOCKED by `require-open-on-entry` demanding a *gielinor* OPEN, because [[S150_26ef6430_session-start-forced-read|resolve_actor]] resolved this dev session as **`jebrim`** — and the same wrong resolution made [[S150_26ef6430_session-start-forced-read|X.4]] force-inject *Jebrim's* keepsake into a dev-brain session. Root cause: `_detect_actor` falls straight to the `state-instances.json` heuristic ("most-recently-allocated actor") when no `braindead-<sid8>.txt` intent anchor exists yet, and never consults a dev-brain signal. I'd skipped writing the intent bubble this session, so there was no anchor → it guessed Jebrim.
- **The fix (Niklavs picked "fix the sidecar now"):** new `_is_dev_session(sid8)` — a per-session, **parallel-safe** signal (this sid8 posted a `braindead-<sid8>` entry to dev comms; a player never writes there, so it can't mis-tag a player closing in parallel — the trap a GLOBAL `active-mode.txt` discriminator falls into, the [[S155_a21a06af_close-gate-stop-hook|S155]] lesson). Checked in `_detect_actor` BEFORE the instance heuristic. Verified: dev-comms anchor → `braindead`; non-dev sid → not braindead; intent file still wins (priority-1 unchanged); `py_compile` clean. Committed **`e31b5be`** (pathspec). Sibling of the [[S149_c6985ff9_reading-preamble-line-and-import-trim-stage-d|S149]] `resolve_actor`-treats-"unknown"-as-truthy note (same actor-resolution-floor family, different mis-resolution path).
- **Immediate unblock (this session):** completed the missing entry-sequence step — wrote `.claude/intent/braindead-46bda6e9.txt` (the on-disk anchor) — and corrected this session's `~/.claude/status/46bda6e9.json` actor `jebrim`→`braindead`, so `resolve_actor` returned `braindead` and the gated writes landed under [[D-032_braindead_full_access|D-032]]. **Honest residual:** X.4's player-inject already fired once (Jebrim keepsake, sentinel spent) before the catch — this session ran partly mis-grounded; the fix prevents recurrence, can't un-fire this one.

**Cascade.** `developer-braindead/.claude/hooks/status-sidecar.py` (the `_is_dev_session` fix); `bank/plan.md` (§Y.2 `[x]` + sidecar-fix note); `respawn.md`; this quest-log; `bank/build-lessons.md`; `comms/active.md` (OPEN+CLOSING).
**Main-brain changes.** Y.2 identity content (committed `496bad2`): `gielinor/keepsake/current.md`, `gielinor/examine/confirmed/current.md` + the promoted `glob-results` entry, `gielinor/players/jebrim/examine/confirmed/current.md`. No rulebook/`meta/`/ritual source touched; the `@import` chain (Claude brain-weight) unchanged — the keepsake is force-injected at runtime, not `@import`ed.
