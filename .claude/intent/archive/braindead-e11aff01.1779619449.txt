Read the cockpit shell: 3-col .app-grid (board | console | feed), and main.js ALREADY polls /api/feed every 2s — the neuron firing stream is in hand. A top-right neuron widget is cheap. Best as a fixed overlay OUTSIDE .app-grid so it dodges the zoom:1.35 landmine and barely touches shared files. But flagging: two siblings are live in the cockpit right now, one reworking zoom/layout — collision risk.