# S043 — 2026-05-22 — Switchboard state visibility

- **Chip + tint per state.** Added ALL-CAPS state chip (`WORKING` / `WAITING` / `CLOSING` / `IDLE` / `ENDED` / `Pending...` for unresolved) next to age, plus row-background tint matching each state's palette. Grid widened from 3 to 4 columns; intent line spans across. Diagnosis from screenshot: prior encoding leaned on a 10px dot + 0.3 opacity differential between `working` and `idle`, which didn't read at a glance — all four rows looked like "Braindead doing a thing." See `experiments/visualizer/index.html:454` (grid), `:500` (`.sb-state` rule), `:514–578` (per-state palette).
- **`working → idle` downgrade removed.** `deriveState` (line 3683) was demoting any `working` record whose `last_event_ts` was older than 5min to `idle`. Principal flagged Braindead-4 reading `IDLE` at 17m while actually working. Root cause: S037's settings register only `UserPromptSubmit` / `Stop` / `SessionEnd` (no per-tool-call fires, budget shape), so a single long turn produces zero heartbeat hooks. The 5-min idle assumption was a `PreToolUse`-era invariant the budget rollback invalidated; the GC at the sidecar (1h) is the real liveness gate now.
- **`Pending...` placeholder for unresolved actors.** `actorLabel` (line 3759) returned `Unknown` when `record.actor` was empty or `'unknown'`. Replaced with `Pending...` — accurately describes the cascading-resolver waiting window (`_detect_actor` intent file → `state-instances.json` byId → unknown). Resolves within ~5-30s of any sibling sidecar fire via `_write_manifest`'s write-time refresh.
- **`CLOSING` state, intent-detected.** New synthetic state derived client-side from `record.intent` matching `/wrap(ping)?\s*up|clos(e|ing)\s*session|session\s*close/i` — matches the canonical wrap-up narrations from `gielinor/meta/communication-protocol.md`. Amber chip (`#c45a18`), slow opacity pulse on the dot (1.8s cycle, distinct from waiting's 1.1s scale-pulse). Ranks between `working` and `idle` so closing sessions surface near the top — they free up soonest. Sidecar has no closing state of its own; the agent's intent narration is the signal.
- **Self-test.** Wrote `braindead-66b43d6e.txt = "Wrapping up S043"` at the start of this very wrap-up — this session's switchboard row should be the first live CLOSING row.

**Cascade.** `developer-braindead/experiments/visualizer/index.html` (only file touched — CSS + two JS edits in the switchboard block).

**Main-brain changes.** none.
