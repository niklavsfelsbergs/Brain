# Resume — S060 shipping-agent training campaign

**As of:** 2026-05-24, sid8 `7cd31d19`. Quest: `quest-log/in-progress/S060_7cd31d19_shipping-agent-training-campaign.md`.

## Status

`in-progress` — deliverable shipped (campaign + 4 teachings implemented + live-validated). **One parked follow-up: commits, held for explicit principal go.**

## Where we are

Campaign complete (10 Qs / 3 tiers, every number ground-truth-verified, zero hallucination). All 4 resulting teachings applied to the out-of-tree shipping-agent (6 edits / 3 files) and the scope self-gate live-validated (fires spontaneously on a neutral prompt now). **Nothing committed** — principal wrapped without authorizing the commit.

## Next concrete step (blocked on principal — phrase as question)

Does the principal want to commit? Two independent commits, both awaiting his explicit go:
- **Shipping-agent** (out-of-tree): scoped to `how_to.md` + `skills/savings-investigation.md` + `skills/query-patterns.md`; message refs S060 + the 4 teachings; **no push** (repo settings deny it).
- **Brain**: scoped to jebrim S060 quest-log + this inventory + 3 harvest drafts + `gielinor/comms/active.md`. Held because the brain working tree is tangled with parallel-session changes — needs explicit go.

Then: triage the 3 harvest drafts (`/drafts` or next alching) — examine `2026-05-24-primed-probe-contaminates-spontaneity-test`, skill `stress-testing-an-agent-by-embodying-it`, memory already written.

## Files to read first

- `quest-log/in-progress/S060_7cd31d19_shipping-agent-training-campaign.md` — full campaign + grades + the 4 teachings.
- The 4 edited shipping-agent files (above) — to review the applied teachings before committing.

## What was implemented (for the commit message / review)

1. Scope/denominator self-gate → `savings-investigation.md` new item 4 + `how_to.md` rule 4.
2. "we/our" = all-lines default + state-scope + surface-the-fork-when-sign-flips → `how_to.md` rule 12.
3. Carrier values stored UPPERCASE (`= 'FedEx'` → 0 rows) → `how_to.md` rule 14 + `query-patterns.md`.
4. (folded into #1's rule-4 edit) sanity-check own shares/percentages.
