# [[S162_816950e9_domain_cue_force_inline|S162]] — 2026-06-05 — Domain-cue force-inline (name→inline gap for small in-tree homes)

Dev-brain via "lets develop gielinor"; OPEN→CLOSING via `comms_append`; no live Braindead siblings ([[S161_fb951652_cockpit-wake-resume-in-place-reconnect|S161]] `fb951652` closed/committed `70267ff`, a continuation of [[S160_fb951652_cockpit-clickable-paths-and-sleep-wake-resume|S160]]). Live parallel Jebrim WIP present (player work, no overlap).

- **The ask.** Niklavs: "could we make gielinor players, when given a task, mandatorily look up things in the brain which are relevant for context? Do we already do this?" Grounded the answer in the live mechanisms before answering (`keepsake-forced-read.py` §X.4, `domain-cue-reminder.py` + `cue_registry.py` [[S144_9b67aceb_domain_grounding_cue_registry|S144]]/§X.3, `grounding-cue` [[D-028_switchboard_cockpit_rebuild|D-028]], the `Reading:` line §X.10). **Verdict: we mostly already do this** — but the *domain* reflex stopped at *naming* the knowledge home, while the *keepsake* reflex *inlines contents*. That name→inline gap is exactly §X.4's own lesson ("a directive only NAMES; inlining FORCES") left unapplied to domain knowledge.
- **Built (the "do it" = the recommended actionable path; option 1/live-fire needs a real player session, can't run from dev).** New optional `inline_homes` field on a `cue_registry.py` domain row — small, in-tree knowledge files whose **contents** the hook now force-injects, not just names. `deploy-schema` uses it (its 3 jebrim deploy notes, ~7.5 KB); **shipping deliberately does not** (external repo / large reference → stays name-only + routes to the specialist). [[cue_registry|gielinor/.claude/hooks/cue_registry.py]] schema-doc + entry; [[domain-cue-reminder|gielinor/.claude/hooks/domain-cue-reminder.py]] `_maybe_inline` + `_read_gielinor` + match-loop wiring.
- **Guardrails, all mechanical (not by-convention):** once per session per domain (sentinel `~/.claude/status/<sid8>.dcue-<domain>`, mirrors the keepsake `.fread`); byte cap `INLINE_BYTE_CAP=12000` → over it, **fall back to naming AND don't burn the sentinel** (an oversized set can't go silent); sub-agents excluded (read their brief, like keepsake skips them); `inline` vs `nudge` logged distinctly (keeps the "fired 14×, obedience unmeasured" gap measurable).
- **Verification.** `py_compile` clean; NEW boundary harness [[test_domain_cue_inline|developer-braindead/verification/test_domain_cue_inline.py]] **26/26** (real note *contents* land; once-per-session; per-session re-inline; shipping stays name-only; sub-agent excluded; over-cap fallback + no-sentinel-burn; braindead skip; log distinction; exit-0/malformed). Existing [[test_domain_cue_registry|developer-braindead/verification/test_domain_cue_registry.py]] still **39/39** (isolated its `STATUS_DIR` to tmp so neither harness litters `~/.claude/status`). Real hook binary smoke: exit 0, correct braindead-skip, no crash. **`@import` chain flat for this build** — both files are runtime hooks, never imported, zero eager weight (the §X thesis: discipline→guarantee at zero always-on cost). Cost: ~2 K tokens injected once/session, only when a deploy topic actually comes up.
- **Regression set ([[knowledge-miss-regression-set|developer-braindead/verification/knowledge-miss-regression-set.md]]).** Tally **holds at 5/2/3** — no bucket sits above `caught`. Cases 8 + 9 annotated `caught ✓✓`: hardened from *caught-by-naming* (an ignorable nudge) to *caught-by-inlining* (contents in context, a guarantee on par with the `[FRD]` keepsake inject). Honest result recorded as a mechanism upgrade, not a count move (same shape as the draft-gate "moves nothing by design").
- **Self-caught slips (low signal, no principal correction):** a `MISSING:` file scare from omitting the `gielinor/` prefix in a stat (re-checked → all present); a sentinel-case assertion bug in the harness (`sid[:8].lower()` → fixed before the run). Both caught pre-commit.

**Leaving open:** LIVE-FIRE the inline from a real Jebrim deploy/schema prompt (hook skips braindead — watch `domain-cue:deploy-schema inline` in `switchboard/ritual-events.ndjson`); the standing live-fire debt (X.2 close-gate / X.3 draft-gate / X.4 forced-read / domain-cue obedience — all need a real player session, then re-run `adherence-rates.py`); ~6.6k deeper CORE-thinning call; Khaan item 9; 2 [[S128_b64229ad_comms-append-lock|S128]] ritual one-liners; cockpit rebuild (carries [[S160_fb951652_cockpit-clickable-paths-and-sleep-wake-resume|S160]]/[[S161_fb951652_cockpit-wake-resume-in-place-reconnect|S161]] features to fold in).

**Cascade.** New `developer-braindead/verification/test_domain_cue_inline.py`; edited `developer-braindead/verification/test_domain_cue_registry.py` (tmp `STATUS_DIR`), `developer-braindead/verification/knowledge-miss-regression-set.md` (domain-cue inline upgrade + cases 8/9 annotation), `bank/build-lessons.md` (+1), `respawn.md`, this quest-log; `comms/active.md` OPEN+CLOSING.
**Main-brain changes.** `gielinor/.claude/hooks/cue_registry.py` (+`inline_homes` field & deploy-schema entry) and `gielinor/.claude/hooks/domain-cue-reminder.py` (force-inline arm). Runtime hooks only — no `@import`/rulebook/identity/`confirmed/` surface touched; `@import` chain unchanged.
