# S149 — 2026-06-02 — The `Reading:` preamble line (§X.10) + @import-chain trim Stage D

Dev-brain via "lets develop gielinor"; OPEN→CLOSING via `tools/comms_append.py`; no live Braindead siblings. Continued the [[S145_543c6caf_knowledge_loading_and_rule_adherence_audit|S145]] anti-deterioration work — two items, both Niklavs-picked, all `meta/` edited directly under [[D-032_braindead_full_access|D-032]] (diffs shown before commit).

- **§X.10 — the `Reading:` preamble line (the immediate cheap win).** Added a tight two-paragraph `## The `Reading:` line` section to the CORE [[communication-protocol|gielinor/meta/communication-protocol.md]] right after the preamble rationale (Format → Reading → Compression). **Threshold-gated:** on substantive / ambiguous / knowledge-dependent turns, name the concrete files/layers grounded in *before* answering; omit on trivial turns (the compression rule, else list-theater). Framed **R3-honest** — visible + correctable, **not enforced**: stating a read doesn't force it, so it *complements, never replaces*, the X.4 forced-read hook. The high-value payoff is the one Niklavs flagged: it makes domain-/grounding-cue **obedience observable**, closing the "fired 14×, obedience unmeasured" gap across [[S144_9b67aceb_domain_grounding_cue_registry|S144]]/[[S145_543c6caf_knowledge_loading_and_rule_adherence_audit|S145]]/[[S147_79b03308_import_chain_trim_stage_b|S147]]. CORE 3,722→4,036 tok (+314, acceptable per the brief).
- **@import-chain trim Stage D (the cleanup tail — Phase-1 now COMPLETE A–D).** Split [[task-lists|gielinor/meta/task-lists.md]]: the load-bearing **threshold** (when to make a list + the durable-`inventory`-mirror trigger) stays always-on CORE (1,415→721 tok); the **mechanics** (two-homes table, lifecycle, anti-patterns, sub-agent scope) carved verbatim into a new JIT [[task-lists-mechanics|gielinor/meta/task-lists-mechanics.md]] (not @imported). Built the **R3 rule-index** in [[CLAUDE|gielinor/CLAUDE.md]] — a one-screen *situation → JIT-file* lookup with the R3-weakness flagged inline (reference only, never a gate).
- **Verified (the bracket held a fourth time).** `brain-weight.py` before/after: chain **11,899 → 11,886 tok** net across both changes (task-lists −694 offsets X.10's +314 and the rule-index's +~367) — *fractionally below session-start* while *adding* the `Reading:` rule and the navigation index. `knowledge-miss-regression-set.md` re-scored **2/3/5 unchanged** both (load-order, not retrieval — the honest R3 result: the line forces nothing so catches nothing new; its value is observability on the already-`caught` cases). Adherence untouched (threshold stayed CORE; no hooks).
- **Honest non-do:** did **not** chase the design's ~6.6k CORE target — reaching it needs trimming load-bearing CORE (`layer-routing.md`, the comms-protocol behavioral core, the CLAUDE.md body), which is high-blast-radius + underspecified and cuts against the Stage-B/C principle of keeping everyday summaries in CORE. Realized ~11.9k (~49% off the 23.3k peak) is the honest floor of "trim what a trigger re-fires"; 6.6k is a *different deliberate summary-thinning decision*. Flagged in plan §X.5. Also surfaced to Niklavs: the rule-index is partly navigational-redundant with the JIT bullets (kept — different function; cheap to cut if it reads as bloat).
- **Build-lessons harvested** (see [[build-lessons]]): (1) the `block-deletes` braindead-bypass silently fails to engage when the status sidecar reports `actor="unknown"` — non-empty, so it short-circuits `resolve_actor`'s intent-file fallback; a `mv`-out-of-tree is the clean non-delete workaround. (2) The dev-brain `active-mode.txt` marker lives at **repo-root** `.claude/`, not a nested `brain/.claude/` (a path slip I caught + cleaned mid-session).

**Cascade.** [[S149_c6985ff9_reading-preamble-line-and-import-trim-stage-d|quest-log/S149]]; [[plan|bank/plan.md]] §X.10 → `[x]` + §X.5 Stage D → DONE (Phase-1 complete A–D); [[knowledge-miss-regression-set|verification/knowledge-miss-regression-set.md]] re-score log (two entries); [[build-lessons|bank/build-lessons.md]] (2 lessons); [[respawn|respawn.md]]; `comms/active.md` OPEN+CLOSING.
**Main-brain changes.** `gielinor/meta/communication-protocol.md` (the `Reading:` section), `gielinor/meta/task-lists.md` (trimmed to CORE), `gielinor/meta/task-lists-mechanics.md` (NEW, JIT), `gielinor/CLAUDE.md` (JIT-list +1 + R3 rule-index). All rulebook edits under [[D-032_godly_proposal_flow_and_code_bearing_seam|D-032]]; no `confirmed/`, no player namespaces, no hooks touched.
