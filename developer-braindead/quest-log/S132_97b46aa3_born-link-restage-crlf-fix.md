# S132 — 2026-05-30 — Born-link pre-commit re-stage CRLF fix

- **Entered dev-brain mid-conversation** via "lets develop gielinor", pivoting from the [[B-012_2026-05-30_twelfth-bankstanding|B-012]] Guthix bankstanding (same session, sid `97b46aa3`). OPEN posted via `tools/comms_append.py`; 3 live Braindead siblings ([[S131_63750f50_construction-side-severity-audit|braindead-63750f50]] building the [[S131_63750f50_construction-side-severity-audit|S131]] audit fixes, `233cf954` cockpit-feed, `234a9b87` product-shape discussion) — zero overlap with the born-link hook.
- **The bug, found live during B-012.** The born-link pre-commit hook auto-wrapped 4 staged files to full-stem wikilinks but **failed to re-stage them** — the commit landed with the un-wrapped versions and the enrichments sat dirty, needing a second commit. Root cause: the hook's re-stage loop reads `FIX\t<rel>` lines from `born-link-lint.py`, but the linter prints under Windows text-mode stdout (CRLF), so `rel` kept a trailing `\r`; `git add "<vault>/<rel>\r"` then fails `did not match any files` (the `\r` renders as `?` — the observed `…md?` error). Every born-link auto-wrap on Windows therefore required a manual follow-up commit.
- **Fix** in [[born-link-pre-commit.sh]] (`developer-braindead/bank/research/born-link-pre-commit.sh`): one `| tr -d '\r'` in the re-stage pipeline, stripping CR from the linter's output stream before `read`. Reinstalled to `.git/hooks/pre-commit` (`cp` + `chmod +x`; no sibling mid-commit) — verified identical to source.
- **Verified empirically** (not just wired): synthetic CRLF `FIX` line through the OLD pipeline reproduces the exact `fatal: pathspec '…md?' did not match` failure; through the NEW pipeline (`tr -d '\r'`) `git add` succeeds on a clean path. End-to-end confirmed at this session's own close commit — auto-wrapped files re-staged cleanly, no dirty leftover (the B-012 symptom did not recur).
- **Wrapped both halves of the session:** the Guthix B-012 bankstanding (graduated its trace to `deities/guthix/quest-log/completed/`; gielinor changes already committed `dac7aa4`/`ea527b9`/`a3ad174`) and this dev close.

**Cascade.** [[born-link-pre-commit.sh]] (CRLF re-stage fix) + `.git/hooks/pre-commit` reinstall; this quest-log entry; `respawn.md` prepend; `comms/active.md` OPEN+CLOSING.

**Main-brain changes.** None from the hook fix (the hook source is dev-brain; `.git/hooks/` is not in either vault). The earlier B-012 bankstanding half of this session did land `gielinor/` changes — committed and traced under [[B-012_2026-05-30_twelfth-bankstanding|B-012]], not part of this dev cascade.
