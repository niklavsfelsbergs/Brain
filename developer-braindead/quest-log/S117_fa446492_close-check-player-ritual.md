# S117 — 2026-05-29 — close_check extended to the gielinor player close ritual

Continued the [[D-034_close_ritual_enforcement|D-034]] follow-on flagged in `respawn.md` ★ NEXT (the [[S116_caa72357_khaan-item-g-judgment-call|S116]] hand-off's best-ROI carry): the close-completeness ground-truth gate existed only for the dev-brain close ([[S115_a9fe4749_khaan-failure-receipts-and-hygiene|S115]] scoped it there); the gielinor *player* close-session ritual had no equivalent.

- **Decision (principal, multiple-choice): generalize, not a sibling file.** `developer-braindead/verification/close_check.py` gained `--ritual {dev,player}` (default `dev` → the wired dev `session-close.md` step-9 call is byte-unchanged). Shared harness — `BANNER`, `_git_clean`, the `_emit` print/exit loop — stays one copy; `run_dev` (5 checks, untouched) and `run_player` (4 new checks) are separate check-lists so neither forks the other with `if ritual==` arms.
- **Derived the player check-set from `close-session.md`'s load-bearing on-disk steps**, keyed by **sid8 across `players/*/`** (one session can touch >1 player; sid8 is the join key embedded in quest + resume filenames): **CLOSING posted** (`-<sid8> CLOSING` in `gielinor/comms/active.md`, skip if no OPEN), **quest-log present** (a `players/*/quest-log/{in-progress,completed}/*<sid8>*.md`, or `players/inbox/*<sid8>*.md` for unscoped), **inventory resume present** (every player with an in-progress quest for the sid8 has a `inventory/*-resume__<sid8>.md` — the step-9.1 inventory-empty enforcement clause mechanized), **core artifacts committed** (filesystem-glob + `_git_clean` per file, which also catches orphan-untracked quest files = step 9.2). Deliberately **omitted** the dev `respawn.md` arm (gielinor resume state is per-player inventory, not a rolling respawn) and the `active-mode.txt` arm (dev-brain-only visualizer marker). Held back brittle content-line asserts (no "pending" grep, no mandatory quest-body line) — the same false-trip restraint that held Khaan item G ([[S116_caa72357_khaan-item-g-judgment-call|S116]]).
- **Verified BOTH ways.** Clean-pass on real sid8 `3760e65b` (Jebrim S120 — OPEN+CLOSING+resume+committed → 4/4 PASS, exit 0, names the remaining `wrapped_up` marker). Banner+exit-1 on `fa446492` (this dev session — no gielinor trace → quest-log FAIL) and on `f41737e5` (Jebrim S118 — the resume-missing check fired precisely **and** caught an uncommitted quest file: a genuine ground-truth gap on a live Jebrim quest, left untouched as it's player WIP). Dev path re-confirmed: it FAILed only on this session's `active-mode=dev-brain`, proving its 5 checks still fire identically. `py_compile` clean.
- **Wired as step 10 of `gielinor/spellbook/rituals/close-session.md`** (the close-completeness gate, between Commit and State-the-close), with the forced 5 renumber cross-refs (old 10→11, old 11→12, header `7-11`→`7-12`, step-0 `1–10`→`1–11`, step-0 `step 11`→`step 12`). `spellbook/rituals/` is user-only — principal explicitly authorized the edit + renumber (write-rules "user-only with explicit permission"). Marked `bank/plan.md` §P.6 `[x]`.

**Cascade.** `developer-braindead/verification/close_check.py` (generalized), `developer-braindead/bank/plan.md` (§P.6), this quest-log entry, `respawn.md` (prepend), `comms/active.md` (OPEN+CLOSING).

**Main-brain changes.** `gielinor/spellbook/rituals/close-session.md` — new step 10 (close-completeness gate) + step renumber 10→11, 11→12 and the step-0/header cross-refs (principal-authorized user-only edit). No other gielinor writes; no player WIP touched.
