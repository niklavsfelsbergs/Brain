# S131 — Construction-side severity audit of gielinor (with Jebrim)

> Dev-brain via "lets develop gielinor", mid-conversation. OPEN posted (`braindead-63750f50`), no live Braindead siblings. Principal staged a **two-sided severity audit**: Braindead (construction memory) + the live `jebrim-0b0f2049` (runtime tenant) cross-examining on `gielinor/comms/active.md`, mirroring the [[S127_b2e3bea8_jebrim-conversation-and-read-comms-tts|S127]] pattern. Goal: exactly 10 improvements ranked by severity, the tenant's lived friction as discriminator.

## Method

- **Builder posts first** (Jebrim's call, so he doesn't anchor): 7 construction-side beams, each with failure-seen + the constraint blocking the fix.
- **Tenant holds each to lived runtime friction**, downgrades the toothless, reframes, names the blind spot.
- **Builder verifies with probes, not prose** (the brain's own *verify-don't-reguess* discipline): grepped the hook surface, checked render-the-cut's status, inspected in-progress debris, counted live litter.
- **Manufacturing guard honored** (Jebrim's own 00:42 warning): all candidates were already on the books — the job was *ranking + blind-spot*, not inventing beams. Zero manufactured S1s.

## Key cross-examination outcomes

- **The spine.** Findings 1/2/3 are one structural truth: *the brain gates its own writes, while the actual hazard surface — a shared git tree + N parallel sessions — is gated at zero.* #2 = can't see the sibling (detection), #1 = destroy their work (action), #3 = the work lives in task repos where no gate reaches. Index-clobber > invisibility because it destroys work even with perfect board visibility.
- **render-the-cut dropped** — probe found it LANDED (`f05ebae` B-011, `cb01e32`; section live in `communication-protocol.md`). Jebrim was reading stale respawn context.
- **Graduation downgraded S2→S3** — no post-D-029 player-quest leak proven; honest finding = "has auto-graduate fired clean? nobody checked." The probe surfaced a *different* S2 with teeth: orphaned **sub-agent traces** nobody owns (2 dwarf traces in in-progress now).
- **Debris-reaper** earned render-cut's freed slot — distinct *fix* (a reaping pass), evidenced (114 `.mode`, orphaned intent `.txt`, silted inventory).

## Probe evidence (this session)

- Hook grep (both dirs): **zero** hooks touch `git add`/`commit`/`index` → git tree confirmed ungated.
- `git status` = **124** dirty/untracked items live → a bare `git add -A` clobber-radius right now.
- in-progress: jebrim {[[S116_caa72357_khaan-item-g-judgment-call|S116]] genuine-open, S124, S131-this, **S125_dwarf orphan**}, zezima {**dwarf_tell-me orphan**}.
- litter: 114 `.claude/intent/archive/*.mode`, 2 orphaned root intent `.txt`, diag logs writing daily (`term-fit-diag.log` 1.29MB).

## The locked top-10 (rank / severity / owner)

1. **S1 — Git shared-index clobber across parallel sessions.** *Diagnosis:* one tree/one index/N sessions; no hook gates staging or commit; the six guarantees protect brain *content*, never the index that moves every deliverable. *Symptom (Jebrim):* hand-guards the index on every commit; one bare `git add` loses/misrepresents a sibling's work. *Fix:* PreToolUse Bash hook refusing bare `git add -A`/`git add .`/`git commit -a` (force explicit pathspec), `GIT_BARE_OK=1` escape like `COMMS_ROTATE`. *Acceptance:* on a two-session-dirty tree, `git add -A` blocks at exit 2 + reminder, no staging; explicit pathspec passes. *Surface:* new `gielinor/.claude/hooks/git-index-guard.py` + both settings.json. *Owner:* Braindead (hook) + Jebrim (validates discipline encoded). *Disagreement:* none.
2. **S1 — Actor-race fail-open: silent invisible writes (class open).** *Diagnosis:* hooks read actor from a lagging status file; require-open returned `''`→allow with zero telemetry (S124, verified-broken). Patched S125 for require-open only; the class (any actor-keyed hook) is open. *Symptom (Jebrim):* board-invisibility is the whole ballgame — an invisible session clobbers a live tree. *Fix:* factor S125's `_actor_from_intent` recovery into a shared helper every actor-keyed hook imports; emit `skip-noactor` on the unresolved branch. *Acceptance:* replay the S124 race against each actor-keyed hook — none allows silently. *Surface:* shared `gielinor/.claude/hooks/_actor.py`; refactor require-open + 4 boundary hooks. *Owner:* Braindead. *Disagreement:* none (spine #2).
3. **S1 — Task-repo blind spot: gates protect the brain, not the work.** *Diagnosis:* every hook scopes to brain paths; the heaviest work (bi-analytics/bi-etl/shipping-agent) is ungoverned and off-board. *Symptom (Jebrim):* "the gates protect the brain; they don't protect the WORK" — bigger than a missed OPEN. *Fix:* extend *coordination* outward — an OPEN convention that names task-repo targets so a sibling editing the same repo is visible; the #1 index-guard scoped to the whole tree covers the safety half. *Acceptance:* a shipping-agent edit posts a task-repo-target OPEN that a sibling's detection surfaces; index-guard fires on task-repo files. *Surface:* comms OPEN convention (docs) + #1 hook tree-wide. *Owner:* Braindead + Jebrim + Guthix (ritual note). *Disagreement:* none (Jebrim's reframe, upgraded to S1, spine #3).
4. **S2 — Hooks ship load-inert; guards unverified in the shipping session.** *Diagnosis:* harness loads hooks only at session start (no hot-reload); a mid-session hook is INERT, "verified" claimed on synthetic tests; [[S128_b64229ad_comms-append-lock|S128]]→[[S130_439e61bf_append-guard-verify-and-r2-open-dep|S130]] re-verify nearly drifted. The meta-pattern under half the list. *Symptom (Jebrim):* the guard meant to protect him doesn't, in the session that ships it. *Fix (Jebrim):* session-start hook-manifest self-test diffing settings.json vs hooks that actually loaded → surfaces inert ones as a board signal. *Acceptance:* an unloadable registered hook is flagged at next start; all-loaded passes silently. *Surface:* SessionStart/respawn arm + dev `verification/`. *Owner:* Braindead. *Disagreement:* I'd flagged S1-adjacent; resolved S2 (it enables S1s).
5. **S2 — Workflow writes possibly ungated (verify-first, not a build).** *Diagnosis:* §Q.2 — workflow subagents run acceptEdits (permission OFF) and may not populate `payload.agent_type`; if not, a workflow write to `confirmed/` hits no boundary hook. Unverified (S125 audit was read-only). *Symptom (Jebrim):* none lived — the finding IS "run the 10-min probe, don't let it loom." *Fix:* spawn `agent({agentType:'gnome'})` attempting a `confirmed/` write; close if gated, scope if not; gate brain-writing workflows until then. *Acceptance:* probe runs + recorded; if ungated, a follow-up makes the workflow write block at exit 2. *Surface:* throwaway Workflow script. *Owner:* Braindead. *Disagreement:* I listed looming-S1; Jebrim reframed verify-first S2 (agreed).
6. **S2 — Discipline caps don't self-hold.** *Diagnosis:* rotation+trim are manual WATCH; respawn regrew to ~14k tokens behind an 80-line count, both comms blew the 300-line trigger 2 days post-reset, A4 trim a 10-session no-op. Law: *what blocks holds, what nudges drifts.* *Symptom (Jebrim):* context-rot he eats every start. *Fix (Jebrim):* bake auto-rotate into `comms_append.py` at >300 lines (COMMS_ROTATE escape exists); mechanize respawn trim where possible. *Acceptance:* the 301st append rotates the bulk to archive atomically, nothing deleted. *Surface:* `tools/comms_append.py` + close_check backstop. *Owner:* Braindead. *Disagreement:* none.
7. **S2 — Verification debt behind a deferred relaunch + live diag-probe debris.** *Diagnosis:* ~10 sessions of RUNTIME-UNVERIFIED cockpit UI + the never-spawned shipping-agent chip behind one relaunch deferred since [[S110_144c0ca2_brain_full_audit|S110]]; 3 diag probes writing daily (`term-fit-diag.log` 1.29MB) flagged strip-once-verified since [[S093_f3239bdc_transcript-readability-and-term-fit|S093]]/94/112. The cockpit is the sibling-awareness window; misrender = the board lies. *Symptom (Jebrim):* trusts the board to not-clobber; 10 unverified sessions means it may misrender — and kill the diag debris now. *Fix:* (a) strip the 3 diag probes + gitignore NOW (no verify needed); (b) one batched relaunch clears the UI checklist + live-spawns shipping-agent. *Acceptance:* diag logs no longer written next session; relaunch walks each UNVERIFIED item to verified/bug. *Surface:* cockpit/switchboard code (a) + principal GUI relaunch (b). *Owner:* Braindead (probes) + principal/cockpit (relaunch). *Disagreement:* none (Jebrim split do-now from batch).
8. **S2 — No debris-reaping discipline; runtime litter accumulates unbounded.** *Diagnosis:* nothing reaps litter — 114 archived `.mode`, orphaned root intent `.txt`, 2 orphaned dwarf quest-traces (nobody owns a sub-agent's trace — the dwarf never runs close), ~30 silted inventory resumes. Every reap is deferred-manual → drifts (see #6); it's also the fuel #1 clobbers. *Symptom (Jebrim):* can't tell live state from sediment; orphaned traces make in-progress lie. *Fix:* a reaping pass tied to events that already fire — on graduation sweep matching `inventory/*-resume__<sid8>.md` + the session's dwarf traces to `archive/`; archive stale intent litter; archive never delete. *Acceptance:* after a graduating close, the session's resume + dwarf traces are in `archive/`, not `in-progress/`; nothing deleted. *Surface:* close-session reaping step + optional `tools/reap.py`. *Owner:* Braindead + Jebrim + Guthix (ritual, user-only). *Disagreement:* Jebrim pressure-tested overlap with #1/#10; accepted — the fix is distinct.
9. **S3 — Auto-graduate unverified post-D-029 (confirm, don't rebuild).** *Diagnosis:* [[D-029_switchbar_two_axis_states|D-029]] + the [[S130_439e61bf_append-guard-verify-and-r2-open-dep|S130]] open-dep marker fixed the discriminator; no proven post-fix player-quest leak (the 22-file B-010 backlog was PRE-fix); but "auto-graduate ships" is itself unverified-live (see #4). *Symptom (Jebrim):* none post-fix — he withdrew the S2 claim. *Fix:* inspect a recent graceful close where stale-done quests existed; record that auto-graduate fired clean. Do NOT build an engine ([[S121_03861733_ritual-analytics-item-11|S121]]: judgment can't be mechanized). *Acceptance:* a quest-log/git inspection shows >=1 post-D-029 silent auto-graduate; recorded. *Surface:* read-only inspection + a plan §R.2 note. *Owner:* Braindead/Jebrim. *Disagreement:* I ranked S2; Jebrim's S3 downgrade correct, conceded on evidence.
10. **S3 — Resume/reconciliation friction: three homes for "where am I."** *Diagnosis:* resume state lives in inventory resume + quest-log + harness task list with no single source of truth; the pending-before-execution crash marker is per-turn discipline that drops under speed. *Symptom (Jebrim):* three homes to check; the pending marker is exactly the discipline that drops fast. *Fix:* name one authoritative surface (the [[D-024_parallel_player_coordination|D-024]] suffixed inventory resume), make the [[S118_e0a88f49_khaan-item-6-freshness-header|S118]] freshness header the canonical "where am I" respawn reads first; don't add a fourth home. *Acceptance:* respawn reconciliation reads one file for current task + open deps + last action. *Surface:* respawn/close-session docs (user-only) + freshness header. *Owner:* Guthix (ritual) + Jebrim. *Disagreement:* none.

## Severity spread

3 S1 (1,2,3 — the parallel-session spine) / 5 S2 (4,5,6,7,8) / 2 S3 (9,10). No manufactured S1s.

## Build phase (principal directed "build them one by one")

7 of 10 BUILT + verified this session (general-purpose dev crew for code — NOT type-`dwarf`, which the dwarf-boundary would block; delicate ones by Braindead direct). **Nothing committed** (ask-before-commit); new hooks **LIVE-UNVERIFIED** (load next session — finding #4, confirmed by the new manifest tool flagging them INERT-THIS-SESSION). Per-item outcomes + verification in `bank/plan.md` §S Build-outcomes.

- BUILT: #1 git-index-guard hook · #2 shared `_actor.py` + require-open refactor (reframed: only require-open is actor-keyed) · #4 hook-manifest self-test · #6 comms auto-rotate · #7a diag-probe kill · #8 reaper tool + `.mode` gitignore (player half done by Guthix B-012) · #9 auto-graduate inspected (present, unexercised in git, don't rebuild).
- READY-NOT-RUN: #5 workflow-gate probe artifact (`verification/workflow-gate-probe.js`) — running needs workflow opt-in.
- PROPOSED (user-only → godly proposal): #3 task-repo OPEN convention (safety half covered by the tree-wide #1) · #10 single authoritative resume surface.

## Live coordination during the build (validated #1/#3 in real time)

Mid-build, siblings spun up: `braindead-233cf954` (cockpit FEED fixes — **co-editing `cockpit/backend.py` with my #7a**, flagged via comms), `braindead-234a9b87` (product-shape discussion), `guthix-97b46aa3` (ran bankstanding **B-012** — graduated the orphan traces = #8 player half + a lorebook draft). Two sessions uncommitted-editing one file with no loaded index-guard = finding #1 live; resolved by comms coordination + pathspec discipline. Exactly the hazard the audit named.

## Leaving open

**Commit decision (principal):** the working tree holds my 7 builds + sibling `233cf954`'s cockpit work + Guthix's bankstanding — a shared-index situation. Commits must be **pathspec-scoped per session** (never `git add -A`); my paths: `gielinor/.claude/hooks/{git-index-guard,_actor,require-open-on-entry}.py`, `.claude/settings.json`, `tools/{comms_append,reap}.py`, `cockpit/{backend.py,ptybridge.py,web/term.js}`, `.gitignore`, `developer-braindead/verification/{hook-manifest-check.py,test_git_index_guard.py,workflow-gate-probe.js}`, the dev-brain close artifacts. **`backend.py` is co-edited with 233cf954** — split hunks or coordinate. Two one-time untracks at commit: `git rm --cached switchboard/*-diag.log` + `.claude/intent/archive/*.mode`. Then a fresh session live-verifies the new hooks (#4). #3/#10 → next bankstanding godly proposal.
