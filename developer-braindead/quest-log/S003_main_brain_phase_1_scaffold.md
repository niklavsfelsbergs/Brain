# S003 — 2026-05-20 — Main brain Phase 1 scaffold built

- Reflected on the parallel main-brain design prompt and flagged four load-bearing uncertainties before drawing a tree: `spellbook/` global vs per-player contradiction, root folder name, `niksis8/` literal-vs-placeholder, `ticks.md` purpose. Niklavs resolved all four: spellbook is both, folder is `gielinor/`, `niksis8` is literal, `ticks.md` is the scheduling slot (dormant Phase 1, gets an `_about`-style header). No briefing went into the main brain's seed content — Niklavs held the line at "My name is Niklavs" and explicitly rejected front-loading facts the agent should be earning through observation.
- Proposed a concrete Phase 1 tree with eight surfaced ratification questions: `meta/` exists, `keepsake/` has `proposals/` not `drafts/`, `lorebook/` has `drafts/`, `quest-log/` is flat, `confirmed/current.md` is the identity-layer read target, hooks as `.ps1` (Niklavs flipped this to **portable** — landed as Python), no `README.md`, no `.gitkeep`s. All approved.
- Built the scaffold: 77 directories, 47 files. Body (`CLAUDE.md` + 3), `meta/` (rulebook, 6 files), global cognitive layers (5 layers with `_about.md` each), `spellbook/rituals/` (respawn + bankstanding), `players/` (system `_about` + `inbox/_about`), per-player content for Zezima and Jebrim (each with `_about`, `persona`, `CLAUDE.md`, plus 7 layer `_about.md` files), placeholder `current.md` and `assumptions.md` and `patch-notes.md` files with one-line headers, four portable Python hooks wired via `.claude/settings.json`. Seeded `niksis8/confirmed/current.md` with "My name is Niklavs."
- Recorded the birth in main-brain `lorebook/`: founding decision `D-001_phase-1-scaffold.md` + Day 0 patch-notes entry. Brain birthdate is 2026-05-20; counting starts here.
- Updated dev brain: closed [[Q-001]], [[Q-003]], [[Q-004]], [[Q-006]] with resolution notes pointing at the relevant main-brain files. Added [[D-007]] recording that the build happened today, with the meta-choices (gielinor naming, `niksis8/` literal, portable Python hooks, `meta/` as current-rulebook-distinct-from-lorebook, no seeding). [[D-007]] cross-refs main-brain [[D-001]] rather than duplicating its content.

**Cascade.** Main brain comes online at `Documents/GitHub/brain/gielinor/`. The dev brain's job shifts from "design the main brain" to "observe and refine through use." [[plan]] §B is fully resolved.

**Main-brain changes.** First creation. Full content lives in `gielinor/lorebook/patch-notes.md` Day 0 entry.

**Addendum — player invocation rule changed.** Same session, after the scaffold landed Niklavs replaced the preemptive "which player?" prompt with **address at message start**: `Hey Zezima, ...`, `Hey Jebrim, ...`, `Hey unscoped, ...`. Sticky once addressed. Strict matching (case-insensitive name, must be at message start, no fuzzy). Mid-sentence player mentions don't switch. First message with no address → start unscoped. Recorded as main-brain [[D-002]] which supersedes step 2 of [[D-001]]'s respawn ritual. Files changed in main brain: `CLAUDE.md` (new section *Player invocation by address*), `spellbook/rituals/respawn.md` (step 2 rewritten + mini-respawn section clarified), `lorebook/decisions/D-002_player_invocation_by_address.md`, `lorebook/patch-notes.md` (second entry of the day).

**Deferred (to real use, not to next session).** `/drafts` command shape, bankstanding triggers, crash-reconciliation prompt wording, `inventory/` mechanics, cross-player retrieval mechanism, hook for `meta/` and `rituals/` writes, size-budget tuning. None are blockers for Phase 1 operation; they emerge from real use.

**Discipline observation worth flagging.** Niklavs has been consistent across [[S001]], [[S002]], and now [[S003]] about a single pattern: **structure-first, content earns its way in.** Identity entries declined in [[S002]] for the dev brain, main-brain identity layers left empty in [[S003]], player biographies refused, Jebrim bank ingestion refused. Worth a candidate identity entry in `examine/` next bankstanding — currently this is structural memory only.
