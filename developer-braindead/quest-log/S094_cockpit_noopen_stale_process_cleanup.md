# S094 — 2026-05-26 — Cockpit no-open: stale-process / profile-lock cleanup

- Principal reported the cockpit (Switchboard) wouldn't open — *"the terminal opens, the cockpit window itself does not."* Entered dev-brain mid-conversation from a Jebrim session, which flagged the cockpit as construction territory and handed off (the [[S074]] stub in Jebrim's quest-log, resolved at close).
- Diagnosed: launch reliably hung inside `webview.start()` — no native window, no traceback, and **no cockpit-owned `msedgewebview2` child spawned** (0 of 41 WebView2 hosts on the machine referenced `cockpit/.webview`; the rest are Teams/Office/etc. — left untouched). `import webview` + `aiohttp` imported fine, so it was the GUI start, not imports and not `cockpit/` code (unchanged since [[S093]], principal-verified "works").
- Root mechanism (**inferred, not isolated under a stack trace**): multiple stacked cockpit launch attempts from the morning + my own leftover `backend.py` diagnostic probes squatting `:8770` → the single **persistent `.webview` profile** ([[S066]]'s `storage_path`) lock-contended, so each new `webview.start()` blocked. **Fix (empirically verified):** killed every `cockpit/.venv` python surgically by exe path (left the 41 unrelated WebView2 hosts alone), freed `:8770`; the next clean launch painted. Principal confirmed **"works."**
- Honest caveat: I created part of the confound — `backend.py` probes left running during the Jebrim-phase diagnosis squatted the port the cockpit reuses. Cleaned them up as part of the fix. The fix is verified; the precise mechanism (profile-lock vs port-squat vs stacked instances) was not definitively separated.
- No cockpit *code* changed — the failure was runtime state, not a regression. Surfaced a real fragility: the cockpit has **no single-instance guard and no stale-profile-lock recovery**, so one hang + a natural retry compounds into a self-inflicted lock pile-up. Logged as a next step + a carried lesson.

**Cascade.** New `developer-braindead/quest-log/S094_*.md`; `respawn.md` prepended (S094 block + cockpit-hardening next step); `bank/build-lessons.md` (+1 lesson); `comms/active.md` OPEN + CLOSING.
**Main-brain changes.** `gielinor/players/jebrim/quest-log/` — added then resolved the [[S074]] cockpit-diagnostic hand-off stub (in-progress → completed). No cockpit code changes; no other gielinor writes.
